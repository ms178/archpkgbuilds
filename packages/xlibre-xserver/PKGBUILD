# === GCC 2-stage-PGO REBUILD :: xlibre-xserver ================================
# Emergency GCC rebuild (clang 'llvm-ultimate' miscompiled the X stack: Xwayland
# SIGSEGV at 0x10 via ffi_call -> wl_display_dispatch). Toolchain pinned to
# gcc/g++ + GNU binutils; clang-only flags scrubbed. MANDATORY GCC 2-stage PGO
# (-fprofile-generate -> training -> -fprofile-use +correction). No GCC 2-stage PGO/BOLT
# (no GCC equivalent). Rename to 'PKGBUILD' before use.
# ====================================================================
# Maintainer: ms178 / Arena.ai tuned variant
# xlibre-xserver 25.1.8 - Raptor Lake / Vega optimized PGO + GCC 2-stage PGO + optional BOLT

pkgbase="xlibre-xserver"
pkgname=("${pkgbase}"{,-bootstrap,-common,-devel,-xvfb})
pkgver=25.1.8
pkgrel=3.1
arch=('aarch64' 'x86_64')
url="https://github.com/X11Libre/xserver"
license=('LicenseRef-Adobe-Display-PostScript' 'BSD-3-Clause' 'LicenseRef-DEC-3-Clause'
         'HPND' 'LicenseRef-HPND-sell-MIT-disclaimer-xserver' 'HPND-sell-variant'
         'ICU' 'ISC' 'MIT' 'MIT-open-group' 'NTP' 'SGI-B-2.0' 'SMLNJ' 'X11'
         'X11-distribute-modifications-variant')
makedepends=('libepoxy' 'libpciaccess' 'libunwind' 'libx11' 'libxaw' 'libxcvt' 'libxfont2' 'libxi' 'libxkbfile' 'libxmu' 'libxrender' 'libxres' 'libxshmfence>=1.1' 'libxtst' 'libxv' 'mesa' 'mesa-libgl' 'meson>=0.58' 'pixman-git' 'systemd>=209' 'xcb-util' 'xcb-util-image' 'xcb-util-keysyms' 'xcb-util-renderutil' 'xcb-util-wm' 'xorg-font-util' 'xorg-util-macros' 'xorg-xkbcomp' 'xorgproto-git' 'xtrans>=1.3.5' 'binutils' 'xorg-xdpyinfo' 'xorg-xprop' 'xorg-xrandr' 'xorg-xset' 'xorg-xwininfo' 'xorg-x11perf' 'xclip' 'mesa-demos' 'glmark2' 'vulkan-tools' 'meson' 'ninja' 'xorg-server-xvfb')
groups=('xlibre')
options=('!emptydirs')
_pkgsrc="xserver-xlibre-xserver-${pkgver}"
source=("${_pkgsrc}.tar.gz::${url}/archive/refs/tags/xlibre-xserver-${pkgver}.tar.gz"
        "xvfb-run"{,.1}
        ms178-1.patch
        ms178-2.patch)
b2sums=('3fb7299aeb965754b70e80a0d521993e0156d7d64ad02b4cf951fb8891df196c1658ea4ee774e58c69d8db3a8f554ea8a0988bc00f79f6a8361f7989e73bf476'
        '58c48ed893be841d14d3a09c9e1092a6da7bcb7fb773e1bf634c50a12e51ea3ad4aeba3843164a2834ee9f8ea95a7bca8b3ce8196a5328ce782724c082cb416f'
        '0e3738e099ee2b958df3e5a5adbdfcbd1150ad64645fdae70d74b50123c3f3d43f9f95f5e4bac82bf5c72b3deb978655b8c3177d37de0bd0a2a6e0e343863511'
        '018efb4dd3c5f51894aef07b726fc8b651adc03618f384608738b862d6f5011def781b20029ae76371a6ecaa4b948a869380b60f47a45ac9e713a29f0ef33af3'
        '101a65bfbabd882735edb61fcfa98004c96353cffea54b8b052f34504b28dbe0fd83a27a7a4ec93ff9c37f4313992a2b9467b5f2406fedd69aaa5aa6bb079c7a')

# Tool overrides are intentionally supported for using upstream LLVM 22.1.8 binaries:
#   CLANG=/path/to/clang LLVM_PROFDATA=/path/to/llvm-profdata LLVM_BOLT=/path/to/llvm-bolt makepkg

# High-fidelity instrumentation for better indirect-call/value profile coverage.

prepare() {
  rm -rf build-pgo build-cspgo build-final fakeinstall pgo-gen cspgo-gen bolt bolt_inst bolt_opt inst-pgo inst-cspgo

  cd "${_pkgsrc}"
  for srcfile in "${source[@]}"; do
    local s="${srcfile%%::*}"; s="${s##*/}"
    [[ $s = *.patch ]] || continue
    patch --forward --strip=1 < "${srcdir}/$s"
  done
}

# NOTE: original clang-only helper functions/arrays removed for the GCC
# rebuild; not used by the 2-stage GCC PGO build() below.

# ============================================================================
# GCC 2-stage PGO scaffold (shared preamble)  —  CachyOS xorg emergency GCC set
# ----------------------------------------------------------------------------
# WHY THIS EXISTS
#   /etc/makepkg.conf on this box points CC/CXX/*FLAGS at the clang
#   "llvm-ultimate" toolchain (clang 24 + ThinLTO/Polly/BOLT). That toolchain
#   miscompiled the X/Wayland stack (Xwayland SIGSEGV at 0x10 through
#   ffi_call -> wl_display_dispatch). These are the GCC emergency rebuilds.
#   They MUST pin GCC and MUST scrub clang-only flags, else the fix is void.
#
# WHAT IT PROVIDES
#   _gcc_pgo_init      : pin gcc/g++ + GNU binutils; sanitize inherited flags.
#   _gcc_pgo_xvfb_run  : run a training command on a private Xvfb (auto ports,
#                        robust wait, always tears the server down).
#   _gcc_pgo_merge_ok  : verify .gcda were produced (PGO is MANDATORY; the build
#                        aborts if training generated no profile).
#   Stage flags:  ${_PGO_GEN[@]}  (stage 1)   ${_PGO_USE[@]} (stage 2, needs dir)
#
#   GCC has no context-sensitive PGO and no BOLT; a clean, correct 2-stage PGO
#   (-fprofile-generate -> -fprofile-use, +correction +partial-training) is the
#   right, stable GCC equivalent. Per the GCC manual, -fprofile-correction and
#   -fprofile-partial-training make profile-use robust to multithreaded and
#   partially-covered training runs (both true here).
# ============================================================================

# Stage-1 instrumentation flags. The profile directory is passed EXPLICITLY via
# -fprofile-generate=<dir> so the .gcda live OUT of tree and survive the
# `make clean` / fresh meson dir between stage 1 and stage 2 (an in-tree default
# location would be wiped). -fprofile-update=atomic is REQUIRED: the X libraries
# and servers are multi-threaded (XCB i/o thread, input thread) and non-atomic
# counters corrupt under concurrent updates. -fprofile-generate at LINK time is
# needed so the instrumented runtime (-lgcov) is pulled in.
_gcc_pgo_gen() {  # $1 = profile dir -> stage-1 compile flags
  # -Wno-error: PGO instrumentation legitimately perturbs codegen and can trip
  # upstream -Werror=array-bounds / -Werror=maybe-uninitialized false positives
  # (e.g. libxcb's generated xinput.c). Instrumented objects are throwaway; we
  # must never let a warning promotion abort the profiling build.
  printf '%s' "-fprofile-generate=$1 -fprofile-update=atomic -Wno-error"
}
# Back-compat array (dir-less) kept for callers that set the dir themselves.
_PGO_GEN=(-fprofile-generate -fprofile-update=atomic)

_gcc_pgo_use() {  # $1 = profile dir  -> echoes stage-2 flags
  # -Wno-error for the same reason as the gen stage: -fprofile-use changes inlining
  # and block layout, which can surface new upstream -Werror= promotions on code
  # that is otherwise correct. The final objects are still fully optimized.
  printf '%s' "-fprofile-use=$1 -fprofile-correction -fprofile-partial-training -Wno-missing-profile -Wno-coverage-mismatch -Wno-error"
}

_gcc_pgo_init() {
  # These are GCC-specific rebuilds: the clang 'llvm-ultimate' toolchain
  # miscompiled the X stack (Xwayland SIGSEGV at 0x10 via ffi_call ->
  # wl_display_dispatch). Pin the GNU toolchain so the build is GCC regardless
  # of what CC/CXX /etc/makepkg.conf exports. gcc-ar/gcc-nm/gcc-ranlib are the
  # LTO-safe binutils wrappers (correct even if your makepkg.conf enables -flto).
  export CC=gcc CXX=g++
  export AR=gcc-ar NM=gcc-nm RANLIB=gcc-ranlib
  export CCACHE_DISABLE=1

  # HONOR /etc/makepkg.conf flags VERBATIM. This PKGBUILD injects ONLY the PGO
  # flags (see _gcc_pgo_gen / _gcc_pgo_use); it deliberately does NOT add
  # -march / -O / -fuse-ld / hardening / LTO — those come from your makepkg.conf.
  # NOTE: since the toolchain is pinned to GCC, your makepkg.conf CFLAGS/CXXFLAGS/
  # LDFLAGS must be GCC-compatible (no clang-only tokens such as -mllvm/-Xclang/
  # -flto=thin/-fuse-ld=lld-with-clang-plugins). That is the expected setup for a
  # GCC rebuild set.
  _ORIG_CFLAGS="${CFLAGS:-}"
  _ORIG_CXXFLAGS="${CXXFLAGS:-}"
  _ORIG_LDFLAGS="${LDFLAGS:-}"
}

# _gcc_pgo_xvfb_run <cmd...> : run cmd on a private Xvfb; returns cmd's status,
# 0 if no Xvfb/display is available (training is best-effort but profiles are
# still required — see _gcc_pgo_require).
_gcc_pgo_xvfb_run() {
  local d rc=0 xvfb_pid=""
  if ! command -v Xvfb >/dev/null 2>&1; then
    "$@"; return $?
  fi
  for d in 99 101 123 137 151; do
    [[ -e "/tmp/.X11-unix/X$d" ]] && continue
    Xvfb ":$d" -screen 0 1280x1024x24 -nolisten tcp >/dev/null 2>&1 &
    xvfb_pid=$!
    local ok=0 i
    for i in $(seq 1 80); do
      if command -v xdpyinfo >/dev/null 2>&1; then
        DISPLAY=":$d" xdpyinfo >/dev/null 2>&1 && { ok=1; break; }
      else
        kill -0 "$xvfb_pid" 2>/dev/null && { sleep 0.5; ok=1; break; }
      fi
      sleep 0.1
    done
    if (( ok )); then
      DISPLAY=":$d" "$@"; rc=$?
      kill "$xvfb_pid" 2>/dev/null; wait "$xvfb_pid" 2>/dev/null
      return $rc
    fi
    kill "$xvfb_pid" 2>/dev/null; wait "$xvfb_pid" 2>/dev/null
    xvfb_pid=""
  done
  # No Xvfb could start; run headless (some libs train without a display).
  "$@"; return $?
}

# _gcc_pgo_require <dir> : hard requirement that PGO training produced data.
_gcc_pgo_require() {
  local dir="$1" n
  n=$(find "$dir" -name '*.gcda' 2>/dev/null | wc -l)
  if (( n == 0 )); then
    echo "FATAL: PGO training produced no .gcda in $dir (PGO is mandatory)." >&2
    return 1
  fi
  echo "PGO: $n .gcda profile file(s) collected."
}

build() {
  _gcc_pgo_init
  cd "$srcdir"
  local _pgo="$srcdir/pgo-data"; rm -rf "$_pgo" "${_pkgsrc}/build-gen" "${_pkgsrc}/build-use"; mkdir -p "$_pgo"
  local _common=(
    -Dipv6=true -Dxvfb=true -Dxnest=false -Dxcsecurity=true -Dxorg=true
    -Dxephyr=false -Dglamor=true -Dudev=true -Ddtrace=false -Dseatd_libseat=false
    -Dsystemd_logind=true -Dsuid_wrapper=true -Dlibunwind=true
    -Dxkb_dir=/usr/share/X11/xkb -Dxkb_output_dir=/var/lib/xkb
    -Dwerror=false -Db_ndebug=true -Dpython.bytecompile=2
    -Dc_std=gnu2x -Dcpp_std=gnu++2a --buildtype=release --wrap-mode=nofallback
    -Db_pie=false
  )
  base64 -d > "$srcdir/train_xserver.sh" <<'__TRAIN_B64__'

__TRAIN_B64__
  chmod +x "$srcdir/train_xserver.sh"

  local _gen; _gen="$(_gcc_pgo_gen "$_pgo")"
  arch-meson "${_pkgsrc}" "${_pkgsrc}/build-gen" "${_common[@]}" -Db_lto=false \
    -Dc_args="$_ORIG_CFLAGS $_gen" -Dcpp_args="$_ORIG_CXXFLAGS $_gen" \
    -Dc_link_args="$_ORIG_LDFLAGS -fprofile-generate=$_pgo" \
    -Dcpp_link_args="$_ORIG_LDFLAGS -fprofile-generate=$_pgo"
  meson compile -C "${_pkgsrc}/build-gen"

  # Train the freshly-built Xvfb (present in this tree) with an X client corpus.
  local xvfb; xvfb="$(find "${_pkgsrc}/build-gen" -name 'Xvfb' -type f | head -1)"
  if [[ -n "$xvfb" ]]; then
    _gcc_pgo_xvfb_run bash "$srcdir/train_xserver.sh" "$xvfb" xvfb || true
  fi
  _gcc_pgo_require "$_pgo" || return 1

  local _use; _use="$(_gcc_pgo_use "$_pgo")"
  arch-meson "${_pkgsrc}" "${_pkgsrc}/build-use" "${_common[@]}" -Db_lto=true \
    -Dc_args="$_ORIG_CFLAGS $_use" -Dcpp_args="$_ORIG_CXXFLAGS $_use" \
    -Dc_link_args="$_ORIG_LDFLAGS" -Dcpp_link_args="$_ORIG_LDFLAGS"
  meson compile -C "${_pkgsrc}/build-use"
  # build-use is under ${_pkgsrc}; cwd here is $srcdir, so qualify the path.
  DESTDIR="$srcdir/fakeinstall" meson install -C "${_pkgsrc}/build-use"
}

package_xlibre-xserver() {
  pkgdesc="XLibre X server"
  depends=('dbus' 'glibc' 'libdrm' 'libepoxy' 'libgl' 'libpciaccess' 'libtirpc'
           'libunwind' 'libxau' 'libxcvt' 'libxdmcp' 'libxfont2' 'libbsd'
           'libxshmfence>=1.1' 'nettle' 'pixman-git' 'sh'
           'systemd-libs>=209' 'xlibre-xserver-common' 'xlibre-xf86-input-libinput'
           "${pkgbase}-common=${pkgver}-${pkgrel}")
  provides=('X-ABI-VIDEODRV_VERSION=28.0' 'X-ABI-XINPUT_VERSION=26.0'
            'X-ABI-EXTENSION_VERSION=11.0' 'x-server' 'xorg-server' 'xlibre-server')
  conflicts=('xorg-server' 'nvidia-utils<=331.20' 'glamor-egl' 'xf86-video-modesetting' 'xlibre-server')
  replaces=('xlibre-xserver-bootstrap' 'glamor-egl' 'xf86-video-modesetting')
  options=('emptydirs')
  install="${pkgname}.install"

  cp -a "${srcdir}/fakeinstall"/. "${pkgdir}"/
  install -vdm755 "${pkgdir}/etc/X11/xorg.conf.d"
  cd "${pkgdir}/usr"
  rm -rf include lib/pkgconfig share/aclocal
  rm -f lib/xorg/protocol.txt share/man/man1/Xserver.1
  find . -type f,l \( -name '*Xephyr*' -o -name '*Xnest*' -o -name '*Xvfb*' \) -delete
}

package_xlibre-xserver-bootstrap() {
  pkgdesc="XLibre X server (bootstrap)"
  depends=('dbus' 'glibc' 'libdrm' 'libepoxy' 'libgl' 'libpciaccess' 'libtirpc'
           'libunwind' 'libxau' 'libxcvt' 'libxdmcp' 'libxfont2' 'libmd'
           'libxshmfence>=1.1' 'nettle' 'pixman-git' 'sh' 'systemd-libs>=209'
           "${pkgbase}-common=${pkgver}-${pkgrel}")
  provides=("${pkgbase}" 'X-ABI-VIDEODRV_VERSION=28.0' 'X-ABI-XINPUT_VERSION=26.0'
            'X-ABI-EXTENSION_VERSION=11.0' 'x-server' 'xorg-server')
  conflicts=({xlibre,xorg}'-server' 'nvidia-utils<=331.20' 'glamor-egl' 'xf86-video-modesetting')
  options=('emptydirs')
  install="${pkgname}.install"

  cp -a "${srcdir}/fakeinstall"/. "${pkgdir}"/
  install -vdm755 "${pkgdir}/etc/X11/xorg.conf.d"
  cd "${pkgdir}/usr"
  rm -rf include lib/pkgconfig share/aclocal
  rm -f lib/xorg/protocol.txt share/man/man1/Xserver.1
  find . -type f,l \( -name '*Xephyr*' -o -name '*Xnest*' -o -name '*Xvfb*' \) -delete
}

package_xlibre-xserver-common() {
  pkgdesc="XLibre server common files"
  arch=('any')
  depends=('xkeyboard-config' 'xorg-setxkbmap' 'xorg-xkbcomp')
  provides=('xorg-server-common' 'xlibre-server-common')
  conflicts=('xorg-server-common' 'xlibre-server-common')

  install -vDm644 "${srcdir}/fakeinstall/usr/lib/xorg/protocol.txt" \
                  "${pkgdir}/usr/lib/xorg/protocol.txt"
  install -vDm644 "${srcdir}/fakeinstall/usr/share/man/man1/Xserver.1" \
                  "${pkgdir}/usr/share/man/man1/Xserver.1"
  install -vDm644 "${srcdir}/${_pkgsrc}/COPYING" \
                  "${pkgdir}/usr/share/licenses/${pkgbase}/COPYING"
  install -vDm644 "${srcdir}/${_pkgsrc}/xkb/README.compiled" \
                  "${pkgdir}/var/lib/xkb/README.compiled"
}

package_xlibre-xserver-devel() {
  pkgdesc="Development files for the XLibre X server"
  arch=('any')
  depends=('libpciaccess' 'mesa' 'pixman-git' 'xorgproto-git' 'xorg-util-macros')
  provides=('xorg-server-devel' 'xlibre-server-devel')
  conflicts=('xorg-server-devel' 'xlibre-server-devel')

  cp -a "${srcdir}/fakeinstall"/. "${pkgdir}"/
  find "${pkgdir}" -type f,l ! \( -name '*.h' -o -name '*.pc' -o -name '*.m4' \) -delete
}

package_xlibre-xserver-xvfb() {
  pkgdesc="Virtual framebuffer XLibre server"
  license=('MIT' 'GPL-2.0-only')
  depends=('glibc' 'libgl' 'libtirpc' 'libunwind' 'libxau' 'libxdmcp'
           'libxfont2' 'nettle' 'pixman-git' 'sh' 'systemd-libs>=209'
           "${pkgbase}-common=${pkgver}-${pkgrel}" 'xorg-xauth')
  provides=('xorg-server-xvfb' 'xlibre-server-xvfb')
  conflicts=('xorg-server-xvfb' 'xlibre-server-xvfb')

  cp -a "${srcdir}/fakeinstall"/. "${pkgdir}"/
  find "${pkgdir}" -type f,l ! -name '*Xvfb*' -delete
  install -vDm755 "${srcdir}/xvfb-run" "${pkgdir}/usr/bin/xvfb-run"
  install -vDm644 "${srcdir}/xvfb-run.1" "${pkgdir}/usr/share/man/man1/xvfb-run.1"
}

# =====================================================================
# PACKAGE NAME: xlibre-xserver
# Rename this file to 'PKGBUILD' inside the 'xlibre-xserver' package directory.
# =====================================================================
