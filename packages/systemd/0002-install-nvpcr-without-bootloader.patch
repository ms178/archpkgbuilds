--- a/src/tpm2-setup/meson.build
+++ b/src/tpm2-setup/meson.build
@@ -42,7 +42,9 @@
         },
 ]
 
-if conf.get('ENABLE_BOOTLOADER') == 1 and conf.get('HAVE_OPENSSL') == 1 and conf.get('HAVE_TPM2') == 1
+# These data files are also consumed by initramfs tooling when systemd-boot
+# and the bootloader-dependent TPM setup executables are disabled.
+if conf.get('HAVE_OPENSSL') == 1 and conf.get('HAVE_TPM2') == 1
         nvpcrs = [ 'cryptsetup',
                    'hardware',
                    'login',
