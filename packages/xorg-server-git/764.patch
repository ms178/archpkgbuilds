--- xorg-server-21.1.3.orig/dix/devices.c
+++ xorg-server-21.1.3/dix/devices.c
@@ -427,6 +427,9 @@ EnableDevice(DeviceIntPtr dev, BOOL send
     /* initialise an idle timer for this device*/
     dev->idle_counter = SyncInitDeviceIdleTime(dev);

+    if (dev->kbdfeed)
+	    (*dev->kbdfeed->CtrlProc) (dev, &dev->kbdfeed->ctrl);
+
     return TRUE;
 }
