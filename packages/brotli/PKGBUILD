pkgbase=brotli
pkgname=('brotli' 'python-brotli')
pkgver=1.2.0
pkgrel=4.4
pkgdesc='Generic-purpose lossless compression algorithm'
url='https://github.com/google/brotli'
arch=('x86_64')
license=('MIT')
makedepends=(
  git
  cmake
  ninja
  python-setuptools
  python-build
  python-installer
  python-wheel
  python-pkgconfig
  clang
  llvm
  llvm-libs
)
source=("${pkgbase}::git+${url}#tag=v${pkgver}")
sha512sums=('SKIP')

_pgo_instrument_flags=(-mllvm -vp-counters-per-site=64
                       -mllvm -runtime-counter-relocation
                       -mllvm -enable-value-profiling)

pkgver() {
  cd "${pkgbase}"
  git describe --tags --match 'v*' | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  mkdir -p "$srcdir/pgo-profiles/standard" "$srcdir/pgo-profiles/context-sensitive"

  cat > "$srcdir/training_workload.cpp" << 'WORKLOAD_EOF'
#include <brotli/decode.h>
#include <brotli/encode.h>
#include <algorithm>
#include <cstdint>
#include <cstring>
#include <iostream>
#include <random>
#include <vector>

static std::vector<uint8_t> generate_data(size_t size, int pattern) {
  std::vector<uint8_t> data(size);
  if (size == 0) return data;
  std::mt19937 gen(42);

  switch (pattern % 5) {
    case 0: {
      std::uniform_int_distribution<int> dis(0, 255);
      for (size_t i = 0; i < size; ++i) data[i] = static_cast<uint8_t>(dis(gen));
      break;
    }
    case 1: {
      const char* words[] = {"the ","quick ","brown ","fox ","jumps ",
                             "over ","lazy ","dog ","\n",", ",". "};
      std::uniform_int_distribution<int> dis(0, 10);
      size_t pos = 0;
      while (pos < size) {
        const char* w = words[dis(gen)];
        size_t n = std::strlen(w);
        for (size_t i = 0; i < n && pos < size; ++i) data[pos++] = static_cast<uint8_t>(w[i]);
      }
      break;
    }
    case 2: {
      std::uniform_int_distribution<int> val_dis(0, 255);
      std::uniform_int_distribution<int> len_dis(4, 256);
      size_t pos = 0;
      while (pos < size) {
        uint8_t v = static_cast<uint8_t>(val_dis(gen));
        size_t len = std::min(static_cast<size_t>(len_dis(gen)), size - pos);
        std::fill_n(data.begin() + pos, len, v);
        pos += len;
      }
      break;
    }
    case 3: {
      const char* tags[] = {"<div>","</div>","<p>","</p>","<span>","</span>",
                            "<a href=\"","\">","</a>","<b>","</b>","Hello ",
                            "World ","test ","content ","data "," "};
      std::uniform_int_distribution<int> dis(0, 16);
      size_t pos = 0;
      while (pos < size) {
        const char* w = tags[dis(gen)];
        size_t n = std::strlen(w);
        for (size_t i = 0; i < n && pos < size; ++i) data[pos++] = static_cast<uint8_t>(w[i]);
      }
      break;
    }
    default: {
      for (size_t i = 0; i < size; ++i) data[i] = static_cast<uint8_t>((i * 1315423911u) & 0xFFu);
      break;
    }
  }
  return data;
}

static bool roundtrip(const std::vector<uint8_t>& in, int q, int mode, int lgwin, bool oneshot) {
  std::vector<uint8_t> comp;
  std::vector<uint8_t> out;

  uint8_t dummy = 0;
  const uint8_t* in_ptr = in.empty() ? &dummy : in.data();

  if (oneshot) {
    size_t cap = BrotliEncoderMaxCompressedSize(in.size());
    comp.resize(cap);
    size_t comp_size = cap;
    if (BrotliEncoderCompress(q, lgwin, static_cast<BrotliEncoderMode>(mode),
                              in.size(), in_ptr, &comp_size, comp.data()) != BROTLI_TRUE) {
      return false;
    }
    comp.resize(comp_size);
  } else {
    BrotliEncoderState* st = BrotliEncoderCreateInstance(nullptr, nullptr, nullptr);
    if (!st) return false;
    BrotliEncoderSetParameter(st, BROTLI_PARAM_QUALITY, q);
    BrotliEncoderSetParameter(st, BROTLI_PARAM_MODE, mode);
    BrotliEncoderSetParameter(st, BROTLI_PARAM_LGWIN, lgwin);

    const uint8_t* next_in = in_ptr;
    size_t avail_in = in.size();
    uint8_t buf[65536];
    int guard = 8192;

    while (guard-- > 0) {
      BrotliEncoderOperation op = (avail_in == 0 ? BROTLI_OPERATION_FINISH : BROTLI_OPERATION_PROCESS);
      size_t avail_out = sizeof(buf);
      uint8_t* next_out = buf;
      BROTLI_BOOL ok = BrotliEncoderCompressStream(
          st, op, &avail_in, &next_in, &avail_out, &next_out, nullptr);
      if (!ok) {
        BrotliEncoderDestroyInstance(st);
        return false;
      }
      comp.insert(comp.end(), buf, buf + (sizeof(buf) - avail_out));
      if (BrotliEncoderIsFinished(st)) break;
    }
    bool done = BrotliEncoderIsFinished(st);
    BrotliEncoderDestroyInstance(st);
    if (!done) return false;
  }

  {
    BrotliDecoderState* st = BrotliDecoderCreateInstance(nullptr, nullptr, nullptr);
    if (!st) return false;
    const uint8_t* next_in = comp.empty() ? &dummy : comp.data();
    size_t avail_in = comp.size();
    uint8_t buf[65536];
    int guard = 8192;

    while (guard-- > 0) {
      size_t avail_out = sizeof(buf);
      uint8_t* next_out = buf;
      BrotliDecoderResult r = BrotliDecoderDecompressStream(
          st, &avail_in, &next_in, &avail_out, &next_out, nullptr);
      out.insert(out.end(), buf, buf + (sizeof(buf) - avail_out));
      if (r == BROTLI_DECODER_RESULT_SUCCESS) break;
      if (r == BROTLI_DECODER_RESULT_ERROR) {
        BrotliDecoderDestroyInstance(st);
        return false;
      }
    }
    bool ok = BrotliDecoderIsFinished(st);
    BrotliDecoderDestroyInstance(st);
    if (!ok) return false;
  }

  return (out.size() == in.size()) &&
         (std::memcmp(in.data(), out.data(), in.size()) == 0);
}

int main() {
  const size_t sizes[] = {0, 1, 64, 256, 1024, 4096, 16384, 65536, 262144};
  const int qualities[] = {1, 4, 6, 9, 11};
  const int modes[] = {BROTLI_MODE_GENERIC, BROTLI_MODE_TEXT, BROTLI_MODE_FONT};
  const int lgwins[] = {10, 16, 20, 22, 24};

  int total = 0;
  int passed = 0;

  for (int pat = 0; pat < 5; ++pat) {
    for (size_t sz : sizes) {
      auto data = generate_data(sz, pat);
      for (int q : qualities) {
        if (q == 11 && sz > 131072) continue;
        for (int m : modes) {
          for (int w : lgwins) {
            bool ok1 = roundtrip(data, q, m, w, true);
            bool ok2 = roundtrip(data, q, m, w, false);
            total += 2;
            if (ok1) passed++;
            if (ok2) passed++;
          }
        }
      }
    }
  }

  std::cout << "Training complete: " << passed << "/" << total << " tests passed\n";
  return (passed == total ? 0 : 1);
}
WORKLOAD_EOF
}

build() {
  cd "${pkgbase}"

  # ── Step 1: Standard PGO instrumentation build ──────────────────────────
  local instr_cflags="${CFLAGS}"
  for f in "${_pgo_instrument_flags[@]}"; do instr_cflags+=" $f"; done
  instr_cflags+=" -fprofile-generate=${srcdir}/pgo-profiles/standard"

  cmake -S . -B build-pgo-standard -G Ninja \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DBUILD_SHARED_LIBS=ON \
    -DCMAKE_C_COMPILER=clang \
    -DCMAKE_CXX_COMPILER=clang++ \
    -DCMAKE_C_FLAGS="${instr_cflags}" \
    -DCMAKE_EXE_LINKER_FLAGS="${LDFLAGS}" \
    -DCMAKE_BUILD_TYPE=Release \
    -Wno-dev
  cmake --build build-pgo-standard

  # ── Step 2: Compile & run training workload (standard PGO) ──────────────
  local train_cxxflags="${CXXFLAGS}"
  for f in "${_pgo_instrument_flags[@]}"; do train_cxxflags+=" $f"; done
  train_cxxflags+=" -fprofile-generate=${srcdir}/pgo-profiles/standard"

  clang++ -std=c++20 \
    ${train_cxxflags} \
    -I"${srcdir}/${pkgbase}/c/include" \
    "${srcdir}/training_workload.cpp" \
    -o "${srcdir}/training_workload_standard" \
    -L"${srcdir}/${pkgbase}/build-pgo-standard" \
    -lbrotlienc -lbrotlidec -lbrotlicommon

  export LLVM_PROFILE_FILE="${srcdir}/pgo-profiles/standard/profile_%m.profraw"
  LD_LIBRARY_PATH="${srcdir}/${pkgbase}/build-pgo-standard" \
    "${srcdir}/training_workload_standard"
  unset LLVM_PROFILE_FILE

  # ── Step 3: Merge standard PGO profiles ─────────────────────────────────
  llvm-profdata merge --sparse -output="${srcdir}/pgo-profiles/standard.profdata" \
    "${srcdir}/pgo-profiles/standard/"*.profraw

  # ── Step 4: Context-sensitive PGO instrumentation build ─────────────────
  local cs_cflags="${CFLAGS}"
  for f in "${_pgo_instrument_flags[@]}"; do cs_cflags+=" $f"; done
  cs_cflags+=" -fprofile-use=${srcdir}/pgo-profiles/standard.profdata"
  cs_cflags+=" -fcs-profile-generate=${srcdir}/pgo-profiles/context-sensitive"

  cmake -S . -B build-pgo-cs -G Ninja \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DBUILD_SHARED_LIBS=ON \
    -DCMAKE_C_COMPILER=clang \
    -DCMAKE_CXX_COMPILER=clang++ \
    -DCMAKE_C_FLAGS="${cs_cflags}" \
    -DCMAKE_EXE_LINKER_FLAGS="${LDFLAGS}" \
    -DCMAKE_BUILD_TYPE=Release \
    -Wno-dev
  cmake --build build-pgo-cs

  # ── Step 5: Compile & run training workload (context-sensitive PGO) ─────
  local cs_train_cxxflags="${CXXFLAGS}"
  for f in "${_pgo_instrument_flags[@]}"; do cs_train_cxxflags+=" $f"; done
  cs_train_cxxflags+=" -fcs-profile-generate=${srcdir}/pgo-profiles/context-sensitive"
  cs_train_cxxflags+=" -fprofile-use=${srcdir}/pgo-profiles/standard.profdata"

  clang++ -std=c++20 \
    ${cs_train_cxxflags} \
    -I"${srcdir}/${pkgbase}/c/include" \
    "${srcdir}/training_workload.cpp" \
    -o "${srcdir}/training_workload_cs" \
    -L"${srcdir}/${pkgbase}/build-pgo-cs" \
    -lbrotlienc -lbrotlidec -lbrotlicommon

  export LLVM_PROFILE_FILE="${srcdir}/pgo-profiles/context-sensitive/profile_%m.profraw"
  LD_LIBRARY_PATH="${srcdir}/${pkgbase}/build-pgo-cs" \
    "${srcdir}/training_workload_cs"
  unset LLVM_PROFILE_FILE

  # ── Step 6: Merge all PGO profiles ──────────────────────────────────────
  llvm-profdata merge --sparse -output="${srcdir}/pgo-profiles/combined.profdata" \
    "${srcdir}/pgo-profiles/standard.profdata" \
    "${srcdir}/pgo-profiles/context-sensitive/"*.profraw

  # ── Step 7: Final optimized build ───────────────────────────────────────
  cmake -S . -B build -G Ninja \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DBUILD_SHARED_LIBS=ON \
    -DCMAKE_C_COMPILER=clang \
    -DCMAKE_CXX_COMPILER=clang++ \
    -DCMAKE_C_FLAGS="${CFLAGS} -fprofile-use=${srcdir}/pgo-profiles/combined.profdata" \
    -DCMAKE_EXE_LINKER_FLAGS="${LDFLAGS}" \
    -DCMAKE_BUILD_TYPE=Release \
    -Wno-dev
  cmake --build build -v

  # ── Step 8: Python wheel ────────────────────────────────────────────────
  python -m build --wheel --no-isolation
}

check() {
  cd "${pkgbase}"
  local python_version
  python_version=$(python -c 'import sys; print("".join(map(str, sys.version_info[:2])))')
  PYTHONPATH="$PWD/bin/lib.linux-$CARCH-cpython-${python_version}" \
    python -m unittest discover python "*_test.py"
  cd build
  ctest --output-on-failure --stop-on-failure -j$(nproc)
}

package_brotli() {
  depends=('glibc')
  provides=(libbrotlicommon.so libbrotlidec.so libbrotlienc.so)

  cd "${pkgbase}"
  DESTDIR="$pkgdir" cmake --install build
  local man
  for man in docs/*.?; do
    install -Dm 644 "$man" "$pkgdir/usr/share/man/man${man##*.}/${man##*/}"
  done
  install -Dm 644 LICENSE -t "$pkgdir/usr/share/licenses/$pkgname"
}

package_python-brotli() {
  pkgdesc+=' - python library'
  depends=('python')

  cd "${pkgbase}"
  python -m installer --destdir="$pkgdir" dist/*.whl
  install -Dm 644 LICENSE -t "$pkgdir/usr/share/licenses/$pkgname"
}
