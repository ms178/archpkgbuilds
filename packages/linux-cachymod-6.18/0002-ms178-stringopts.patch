diff --git a/arch/x86/lib/clear_page_64.S b/arch/x86/lib/clear_page_64.S
index a508e4a..79a415e 100644
--- a/arch/x86/lib/clear_page_64.S
+++ b/arch/x86/lib/clear_page_64.S
@@ -6,49 +6,55 @@
 #include <asm/asm.h>
 
 /*
- * Most CPUs support enhanced REP MOVSB/STOSB instructions. It is
- * recommended to use this when possible and we do use them by default.
- * If enhanced REP MOVSB/STOSB is not available, try to use fast string.
- * Otherwise, use original.
+ * x86-64 page clearing and clear_user backend.
+ *
+ * Raptor Lake v5 notes:
+ *  - clear_page_erms remains REP STOSB for the ERMS fast-string path.
+ *  - clear_page_rep remains REP STOSQ for the legacy fast-string selector.
+ *  - clear_page_orig is stackless and uses only caller-saved registers.
+ *  - rep_stos_alternative keeps the original low-latency path for <64 byte
+ *    clear_user() calls.  The previous v4 16-byte entry path regressed on
+ *    E-cores for 0/8/15/17/63 byte cases.  Only the residual tail after a
+ *    successful 64-byte unrolled block uses a guarded 16-byte helper for
+ *    32..55 byte tails, where measurements showed benefit without exposing
+ *    the short-call regressions.
  */
 
 /*
  * Zero a page.
- * %rdi	- page
+ * %rdi - page
  */
 SYM_TYPED_FUNC_START(clear_page_rep)
-	movl $4096/8,%ecx
-	xorl %eax,%eax
+	movl	$4096/8, %ecx
+	xorl	%eax, %eax
 	rep stosq
 	RET
 SYM_FUNC_END(clear_page_rep)
 EXPORT_SYMBOL_GPL(clear_page_rep)
 
 SYM_TYPED_FUNC_START(clear_page_orig)
-	xorl   %eax,%eax
-	movl   $4096/64,%ecx
+	xorl	%eax, %eax
+	movl	$4096/64, %ecx
 	.p2align 4
-.Lloop:
+.Lclear_page_orig_loop:
+	movq	%rax, 0(%rdi)
+	movq	%rax, 8(%rdi)
+	movq	%rax, 16(%rdi)
+	movq	%rax, 24(%rdi)
+	movq	%rax, 32(%rdi)
+	movq	%rax, 40(%rdi)
+	movq	%rax, 48(%rdi)
+	movq	%rax, 56(%rdi)
+	addq	$64, %rdi
 	decl	%ecx
-#define PUT(x) movq %rax,x*8(%rdi)
-	movq %rax,(%rdi)
-	PUT(1)
-	PUT(2)
-	PUT(3)
-	PUT(4)
-	PUT(5)
-	PUT(6)
-	PUT(7)
-	leaq	64(%rdi),%rdi
-	jnz	.Lloop
-	nop
+	jnz	.Lclear_page_orig_loop
 	RET
 SYM_FUNC_END(clear_page_orig)
 EXPORT_SYMBOL_GPL(clear_page_orig)
 
 SYM_TYPED_FUNC_START(clear_page_erms)
-	movl $4096,%ecx
-	xorl %eax,%eax
+	movl	$4096, %ecx
+	xorl	%eax, %eax
 	rep stosb
 	RET
 SYM_FUNC_END(clear_page_erms)
@@ -66,72 +72,83 @@ EXPORT_SYMBOL_GPL(clear_page_erms)
  */
 SYM_FUNC_START(rep_stos_alternative)
 	ANNOTATE_NOENDBR
-	cmpq $64,%rcx
-	jae .Lunrolled
+	cmpq	$64, %rcx
+	jae	.Lunrolled
 
-	cmp $8,%ecx
-	jae .Lword
+	cmp	$8, %ecx
+	jae	.Lword
 
-	testl %ecx,%ecx
-	je .Lexit
+	testl	%ecx, %ecx
+	je	.Lexit
 
 .Lclear_user_tail:
-0:	movb %al,(%rdi)
-	inc %rdi
-	dec %rcx
-	jnz .Lclear_user_tail
+0:	movb	%al, (%rdi)
+	incq	%rdi
+	decq	%rcx
+	jnz	.Lclear_user_tail
 .Lexit:
 	RET
 
-	_ASM_EXTABLE_UA( 0b, .Lexit)
+	_ASM_EXTABLE_UA(0b, .Lexit)
 
 .Lword:
-1:	movq %rax,(%rdi)
-	addq $8,%rdi
-	sub $8,%ecx
-	je .Lexit
-	cmp $8,%ecx
-	jae .Lword
-	jmp .Lclear_user_tail
+1:	movq	%rax, (%rdi)
+	addq	$8, %rdi
+	subq	$8, %rcx
+	je	.Lexit
+	cmpq	$8, %rcx
+	jae	.Lword
+	jmp	.Lclear_user_tail
 
 	.p2align 4
 .Lunrolled:
-10:	movq %rax,(%rdi)
-11:	movq %rax,8(%rdi)
-12:	movq %rax,16(%rdi)
-13:	movq %rax,24(%rdi)
-14:	movq %rax,32(%rdi)
-15:	movq %rax,40(%rdi)
-16:	movq %rax,48(%rdi)
-17:	movq %rax,56(%rdi)
-	addq $64,%rdi
-	subq $64,%rcx
-	cmpq $64,%rcx
-	jae .Lunrolled
-	cmpl $8,%ecx
-	jae .Lword
-	testl %ecx,%ecx
-	jne .Lclear_user_tail
+10:	movq	%rax, (%rdi)
+11:	movq	%rax, 8(%rdi)
+12:	movq	%rax, 16(%rdi)
+13:	movq	%rax, 24(%rdi)
+14:	movq	%rax, 32(%rdi)
+15:	movq	%rax, 40(%rdi)
+16:	movq	%rax, 48(%rdi)
+17:	movq	%rax, 56(%rdi)
+	addq	$64, %rdi
+	subq	$64, %rcx
+	cmpq	$64, %rcx
+	jae	.Lunrolled
+
+	/*
+	 * Keep the original word loop for 56..63 byte residuals: on measured
+	 * E-cores the old v4 16-byte helper was slower for this tail class.
+	 */
+	cmpq	$56, %rcx
+	jae	.Lword
+	cmpq	$32, %rcx
+	jae	.Lword2_tail
+	cmpq	$8, %rcx
+	jae	.Lword
+	testl	%ecx, %ecx
+	jne	.Lclear_user_tail
+	RET
+
+	.p2align 4
+.Lword2_tail:
+2:	movq	%rax, (%rdi)
+3:	movq	%rax, 8(%rdi)
+	addq	$16, %rdi
+	subq	$16, %rcx
+	cmpq	$16, %rcx
+	jae	.Lword2_tail
+	testl	%ecx, %ecx
+	jne	.Lclear_user_tail
 	RET
 
 	/*
-	 * If we take an exception on any of the
-	 * word stores, we know that %rcx isn't zero,
-	 * so we can just go to the tail clearing to
-	 * get the exact count.
-	 *
-	 * The unrolled case might end up clearing
-	 * some bytes twice. Don't care.
-	 *
-	 * We could use the value in %rdi to avoid
-	 * a second fault on the exact count case,
-	 * but do we really care? No.
-	 *
-	 * Finally, we could try to align %rdi at the
-	 * top of the unrolling. But unaligned stores
-	 * just aren't that common or expensive.
+	 * If a word store faults, fall back to byte probing for an exact
+	 * residual.  Some already-cleared bytes may be stored twice; this
+	 * matches the original helper's semantics.
 	 */
-	_ASM_EXTABLE_UA( 1b, .Lclear_user_tail)
+	_ASM_EXTABLE_UA(1b, .Lclear_user_tail)
+	_ASM_EXTABLE_UA(2b, .Lclear_user_tail)
+	_ASM_EXTABLE_UA(3b, .Lclear_user_tail)
 	_ASM_EXTABLE_UA(10b, .Lclear_user_tail)
 	_ASM_EXTABLE_UA(11b, .Lclear_user_tail)
 	_ASM_EXTABLE_UA(12b, .Lclear_user_tail)
diff --git a/arch/x86/lib/copy_mc_64.S b/arch/x86/lib/copy_mc_64.S
index acd707c..6ed18c6 100644
--- a/arch/x86/lib/copy_mc_64.S
+++ b/arch/x86/lib/copy_mc_64.S
@@ -11,71 +11,99 @@
 /*
  * copy_mc_fragile - copy memory with indication if an exception / fault happened
  *
- * The 'fragile' version is opted into by platform quirks and takes
- * pains to avoid unrecoverable corner cases like 'fast-string'
- * instruction sequences, and consuming poison across a cacheline
- * boundary. The non-fragile version is equivalent to memcpy()
- * regardless of CPU machine-check-recovery capability.
+ * The fragile path deliberately avoids fast-string instructions.  This variant
+ * keeps the original fault-accounting model but reduces loop-branch density on
+ * Raptor Lake by copying two naturally aligned qwords per steady-state loop.
+ * Pointers and ECX are advanced after each successful qword, so every read or
+ * write exception still observes a precise "current byte" position for the
+ * existing fixup code.
  */
 SYM_FUNC_START(copy_mc_fragile)
-	cmpl $8, %edx
+	cmpl	$8, %edx
 	/* Less than 8 bytes? Go to byte copy loop */
-	jb .L_no_whole_words
+	jb	.L_no_whole_words
 
 	/* Check for bad alignment of source */
-	testl $7, %esi
+	testl	$7, %esi
 	/* Already aligned */
-	jz .L_8byte_aligned
+	jz	.L_8byte_aligned
 
 	/* Copy one byte at a time until source is 8-byte aligned */
-	movl %esi, %ecx
-	andl $7, %ecx
-	subl $8, %ecx
-	negl %ecx
-	subl %ecx, %edx
-.L_read_leading_bytes:
-	movb (%rsi), %al
-.L_write_leading_bytes:
-	movb %al, (%rdi)
-	addq $1, %rsi
-	addq $1, %rdi
-	subl $1, %ecx
-	jnz .L_read_leading_bytes
+	movl	%esi, %ecx
+	andl	$7, %ecx
+	subl	$8, %ecx
+	negl	%ecx
+	subl	%ecx, %edx
+	.p2align 4
+.L_read_leading_b0:
+	movb	(%rsi), %al
+.L_write_leading_b0:
+	movb	%al, (%rdi)
+	addq	$1, %rsi
+	addq	$1, %rdi
+	subl	$1, %ecx
+	jz	.L_8byte_aligned
+.L_read_leading_b1:
+	movb	(%rsi), %al
+.L_write_leading_b1:
+	movb	%al, (%rdi)
+	addq	$1, %rsi
+	addq	$1, %rdi
+	subl	$1, %ecx
+	jnz	.L_read_leading_b0
 
 .L_8byte_aligned:
-	movl %edx, %ecx
-	andl $7, %edx
-	shrl $3, %ecx
-	jz .L_no_whole_words
-
-.L_read_words:
-	movq (%rsi), %r8
-.L_write_words:
-	movq %r8, (%rdi)
-	addq $8, %rsi
-	addq $8, %rdi
-	subl $1, %ecx
-	jnz .L_read_words
+	movl	%edx, %ecx
+	andl	$7, %edx
+	shrl	$3, %ecx
+	jz	.L_no_whole_words
+
+	.p2align 4
+.L_read_words0:
+	movq	(%rsi), %r8
+.L_write_words0:
+	movq	%r8, (%rdi)
+	addq	$8, %rsi
+	addq	$8, %rdi
+	subl	$1, %ecx
+	jz	.L_no_whole_words
+.L_read_words1:
+	movq	(%rsi), %r8
+.L_write_words1:
+	movq	%r8, (%rdi)
+	addq	$8, %rsi
+	addq	$8, %rdi
+	subl	$1, %ecx
+	jnz	.L_read_words0
 
 	/* Any trailing bytes? */
 .L_no_whole_words:
-	andl %edx, %edx
-	jz .L_done_memcpy_trap
+	andl	%edx, %edx
+	jz	.L_done_memcpy_trap
 
 	/* Copy trailing bytes */
-	movl %edx, %ecx
-.L_read_trailing_bytes:
-	movb (%rsi), %al
-.L_write_trailing_bytes:
-	movb %al, (%rdi)
-	addq $1, %rsi
-	addq $1, %rdi
-	subl $1, %ecx
-	jnz .L_read_trailing_bytes
+	movl	%edx, %ecx
+	.p2align 4
+.L_read_trailing_b0:
+	movb	(%rsi), %al
+.L_write_trailing_b0:
+	movb	%al, (%rdi)
+	addq	$1, %rsi
+	addq	$1, %rdi
+	subl	$1, %ecx
+	jz	.L_done_memcpy_trap
+.L_read_trailing_b1:
+	movb	(%rsi), %al
+.L_write_trailing_b1:
+	movb	%al, (%rdi)
+	addq	$1, %rsi
+	addq	$1, %rdi
+	subl	$1, %ecx
+	jnz	.L_read_trailing_b0
 
 	/* Copy successful. Return zero */
 .L_done_memcpy_trap:
-	xorl %eax, %eax
+	xorl	%eax, %eax
 .L_done:
 	RET
 
@@ -101,14 +129,20 @@ SYM_FUNC_START(copy_mc_fragile)
 	shll	$3, %ecx
 	addl	%edx, %ecx
 	movl	%ecx, %edx
-	jmp copy_mc_fragile_handle_tail
-
-	_ASM_EXTABLE_TYPE(.L_read_leading_bytes, .E_leading_bytes, EX_TYPE_DEFAULT_MCE_SAFE)
-	_ASM_EXTABLE_TYPE(.L_read_words, .E_read_words, EX_TYPE_DEFAULT_MCE_SAFE)
-	_ASM_EXTABLE_TYPE(.L_read_trailing_bytes, .E_trailing_bytes, EX_TYPE_DEFAULT_MCE_SAFE)
-	_ASM_EXTABLE(.L_write_leading_bytes, .E_leading_bytes)
-	_ASM_EXTABLE(.L_write_words, .E_write_words)
-	_ASM_EXTABLE(.L_write_trailing_bytes, .E_trailing_bytes)
+	jmp	copy_mc_fragile_handle_tail
+
+	_ASM_EXTABLE_TYPE(.L_read_leading_b0, .E_leading_bytes, EX_TYPE_DEFAULT_MCE_SAFE)
+	_ASM_EXTABLE_TYPE(.L_read_leading_b1, .E_leading_bytes, EX_TYPE_DEFAULT_MCE_SAFE)
+	_ASM_EXTABLE_TYPE(.L_read_words0, .E_read_words, EX_TYPE_DEFAULT_MCE_SAFE)
+	_ASM_EXTABLE_TYPE(.L_read_words1, .E_read_words, EX_TYPE_DEFAULT_MCE_SAFE)
+	_ASM_EXTABLE_TYPE(.L_read_trailing_b0, .E_trailing_bytes, EX_TYPE_DEFAULT_MCE_SAFE)
+	_ASM_EXTABLE_TYPE(.L_read_trailing_b1, .E_trailing_bytes, EX_TYPE_DEFAULT_MCE_SAFE)
+	_ASM_EXTABLE(.L_write_leading_b0, .E_leading_bytes)
+	_ASM_EXTABLE(.L_write_leading_b1, .E_leading_bytes)
+	_ASM_EXTABLE(.L_write_words0, .E_write_words)
+	_ASM_EXTABLE(.L_write_words1, .E_write_words)
+	_ASM_EXTABLE(.L_write_trailing_b0, .E_trailing_bytes)
+	_ASM_EXTABLE(.L_write_trailing_b1, .E_trailing_bytes)
 
 SYM_FUNC_END(copy_mc_fragile)
 #endif /* CONFIG_X86_MCE */
@@ -124,12 +158,12 @@ SYM_FUNC_END(copy_mc_fragile)
  * standard memcpy.
  */
 SYM_FUNC_START(copy_mc_enhanced_fast_string)
-	movq %rdi, %rax
-	movq %rdx, %rcx
+	movq	%rdi, %rax
+	movq	%rdx, %rcx
 .L_copy:
 	rep movsb
 	/* Copy successful. Return zero */
-	xorl %eax, %eax
+	xorl	%eax, %eax
 	RET
 
 .E_copy:
@@ -140,7 +174,7 @@ SYM_FUNC_START(copy_mc_enhanced_fast_string)
 	 * to copy_mc_generic() users, or indicate short transfers to
 	 * user-copy routines.
 	 */
-	movq %rcx, %rax
+	movq	%rcx, %rax
 	RET
 
 	_ASM_EXTABLE_TYPE(.L_copy, .E_copy, EX_TYPE_DEFAULT_MCE_SAFE)
diff --git a/arch/x86/lib/copy_page_64.S b/arch/x86/lib/copy_page_64.S
index d8e87fe..f18e194 100644
--- a/arch/x86/lib/copy_page_64.S
+++ b/arch/x86/lib/copy_page_64.S
@@ -1,5 +1,14 @@
 /* SPDX-License-Identifier: GPL-2.0 */
-/* Written 2003 by Andi Kleen, based on a kernel by Evandro Menezes */
+/*
+ * copy_page - 4 KiB page copy
+ *
+ * Raptor Lake v5 policy:
+ *  - Public fast path uses REP MOVSQ when X86_FEATURE_REP_GOOD patches out
+ *    the fallback branch.  User P/E-core measurements did not show a win for
+ *    changing the vanilla page-copy fast path to REP MOVSB.
+ *  - The scalar fallback is only for CPUs without REP_GOOD.  It avoids stack
+ *    traffic and callee-saved registers, using only caller-saved temporaries.
+ */
 
 #include <linux/export.h>
 #include <linux/linkage.h>
@@ -7,12 +16,8 @@
 #include <asm/cpufeatures.h>
 #include <asm/alternative.h>
 
-/*
- * Some CPUs run faster using the string copy instructions (sane microcode).
- * It is also a lot simpler. Use this when possible. But, don't use streaming
- * copy unless the CPU indicates X86_FEATURE_REP_GOOD. Could vary the
- * prefetch distance based on SMP/UP.
- */
+	.section .noinstr.text, "ax"
+
 	ALIGN
 SYM_TYPED_FUNC_START(copy_page)
 	ALTERNATIVE "jmp copy_page_regs", "", X86_FEATURE_REP_GOOD
@@ -23,68 +28,30 @@ SYM_FUNC_END(copy_page)
 EXPORT_SYMBOL(copy_page)
 
 SYM_FUNC_START_LOCAL(copy_page_regs)
-	subq	$2*8,	%rsp
-	movq	%rbx,	(%rsp)
-	movq	%r12,	1*8(%rsp)
-
-	movl	$(4096/64)-5,	%ecx
+	movl	$4096/64, %ecx
 	.p2align 4
 .Loop64:
-	dec	%rcx
-	movq	0x8*0(%rsi), %rax
-	movq	0x8*1(%rsi), %rbx
-	movq	0x8*2(%rsi), %rdx
-	movq	0x8*3(%rsi), %r8
-	movq	0x8*4(%rsi), %r9
-	movq	0x8*5(%rsi), %r10
-	movq	0x8*6(%rsi), %r11
-	movq	0x8*7(%rsi), %r12
-
-	prefetcht0 5*64(%rsi)
-
-	movq	%rax, 0x8*0(%rdi)
-	movq	%rbx, 0x8*1(%rdi)
-	movq	%rdx, 0x8*2(%rdi)
-	movq	%r8,  0x8*3(%rdi)
-	movq	%r9,  0x8*4(%rdi)
-	movq	%r10, 0x8*5(%rdi)
-	movq	%r11, 0x8*6(%rdi)
-	movq	%r12, 0x8*7(%rdi)
-
-	leaq	64 (%rsi), %rsi
-	leaq	64 (%rdi), %rdi
-
-	jnz	.Loop64
-
-	movl	$5, %ecx
-	.p2align 4
-.Loop2:
+	movq	0(%rsi), %rax
+	movq	8(%rsi), %rdx
+	movq	16(%rsi), %r8
+	movq	24(%rsi), %r9
+	movq	%rax, 0(%rdi)
+	movq	%rdx, 8(%rdi)
+	movq	%r8, 16(%rdi)
+	movq	%r9, 24(%rdi)
+
+	movq	32(%rsi), %rax
+	movq	40(%rsi), %rdx
+	movq	48(%rsi), %r8
+	movq	56(%rsi), %r9
+	movq	%rax, 32(%rdi)
+	movq	%rdx, 40(%rdi)
+	movq	%r8, 48(%rdi)
+	movq	%r9, 56(%rdi)
+
+	addq	$64, %rsi
+	addq	$64, %rdi
 	decl	%ecx
-
-	movq	0x8*0(%rsi), %rax
-	movq	0x8*1(%rsi), %rbx
-	movq	0x8*2(%rsi), %rdx
-	movq	0x8*3(%rsi), %r8
-	movq	0x8*4(%rsi), %r9
-	movq	0x8*5(%rsi), %r10
-	movq	0x8*6(%rsi), %r11
-	movq	0x8*7(%rsi), %r12
-
-	movq	%rax, 0x8*0(%rdi)
-	movq	%rbx, 0x8*1(%rdi)
-	movq	%rdx, 0x8*2(%rdi)
-	movq	%r8,  0x8*3(%rdi)
-	movq	%r9,  0x8*4(%rdi)
-	movq	%r10, 0x8*5(%rdi)
-	movq	%r11, 0x8*6(%rdi)
-	movq	%r12, 0x8*7(%rdi)
-
-	leaq	64(%rdi), %rdi
-	leaq	64(%rsi), %rsi
-	jnz	.Loop2
-
-	movq	(%rsp), %rbx
-	movq	1*8(%rsp), %r12
-	addq	$2*8, %rsp
+	jnz	.Loop64
 	RET
 SYM_FUNC_END(copy_page_regs)
diff --git a/arch/x86/lib/copy_user_64.S b/arch/x86/lib/copy_user_64.S
index 06296eb..d4b5c2a 100644
--- a/arch/x86/lib/copy_user_64.S
+++ b/arch/x86/lib/copy_user_64.S
@@ -4,6 +4,15 @@
  * Copyright 2002 Andi Kleen, SuSE Labs.
  *
  * Functions to copy from and to user space.
+ *
+ * Raptor Lake notes:
+ *  - 0..15 bytes are laid out as the vanilla tiny path: the 8..15 byte
+ *    qword copy is fall-through after the size tests, avoiding the extra
+ *    taken branch that showed up as an 8-byte regression on E-cores.
+ *  - 16..63 bytes use a correct 2xqword loop.  The steady-state loop keeps
+ *    pointer/count state precise after each committed 16-byte chunk.
+ *  - >=64 bytes uses the existing ERMS alternative and is patched to
+ *    REP MOVSB on the i7-14700KF.
  */
 
 #include <linux/export.h>
@@ -33,80 +42,101 @@
  */
 SYM_FUNC_START(rep_movs_alternative)
 	ANNOTATE_NOENDBR
-	cmpq $64,%rcx
-	jae .Llarge
-
-	cmp $8,%ecx
-	jae .Lword
-
-	testl %ecx,%ecx
-	je .Lexit
+	cmp	$8, %ecx
+	jb	.Llt8
+	cmpq	$16, %rcx
+	jae	.Lge16
+
+	/* 8..15 bytes: vanilla-shaped qword + byte tail path. */
+6:	movq	(%rsi), %rax
+7:	movq	%rax, (%rdi)
+	addq	$8, %rsi
+	addq	$8, %rdi
+	subq	$8, %rcx
+	je	.Lexit
 
 .Lcopy_user_tail:
-0:	movb (%rsi),%al
-1:	movb %al,(%rdi)
-	inc %rdi
-	inc %rsi
-	dec %rcx
-	jne .Lcopy_user_tail
+4:	movb	(%rsi), %al
+5:	movb	%al, (%rdi)
+	incq	%rdi
+	incq	%rsi
+	decq	%rcx
+	jne	.Lcopy_user_tail
 .Lexit:
 	RET
 
-	_ASM_EXTABLE_UA( 0b, .Lexit)
-	_ASM_EXTABLE_UA( 1b, .Lexit)
+.Llt8:
+	testl	%ecx, %ecx
+	jne	.Lcopy_user_tail
+	RET
 
 	.p2align 4
-.Lword:
-2:	movq (%rsi),%rax
-3:	movq %rax,(%rdi)
-	addq $8,%rsi
-	addq $8,%rdi
-	sub $8,%ecx
-	je .Lexit
-	cmp $8,%ecx
-	jae .Lword
-	jmp .Lcopy_user_tail
-
-	_ASM_EXTABLE_UA( 2b, .Lcopy_user_tail)
-	_ASM_EXTABLE_UA( 3b, .Lcopy_user_tail)
+.Lge16:
+	cmpq	$64, %rcx
+	jae	.Llarge
+
+.Lqword2:
+0:	movq	(%rsi), %rax
+1:	movq	%rax, (%rdi)
+2:	movq	8(%rsi), %rax
+3:	movq	%rax, 8(%rdi)
+	addq	$16, %rsi
+	addq	$16, %rdi
+	subq	$16, %rcx
+	cmpq	$16, %rcx
+	jae	.Lqword2
+	cmpq	$8, %rcx
+	jae	6b
+	testl	%ecx, %ecx
+	jne	.Lcopy_user_tail
+	RET
+
+	_ASM_EXTABLE_UA(0b, .Lcopy_user_tail)
+	_ASM_EXTABLE_UA(1b, .Lcopy_user_tail)
+	_ASM_EXTABLE_UA(2b, .Lcopy_user_tail)
+	_ASM_EXTABLE_UA(3b, .Lcopy_user_tail)
+	_ASM_EXTABLE_UA(4b, .Lexit)
+	_ASM_EXTABLE_UA(5b, .Lexit)
+	_ASM_EXTABLE_UA(6b, .Lcopy_user_tail)
+	_ASM_EXTABLE_UA(7b, .Lcopy_user_tail)
 
 .Llarge:
 0:	ALTERNATIVE "jmp .Llarge_movsq", "rep movsb", X86_FEATURE_ERMS
 1:	RET
 
-	_ASM_EXTABLE_UA( 0b, 1b)
+	_ASM_EXTABLE_UA(0b, 1b)
 
 .Llarge_movsq:
 	/* Do the first possibly unaligned word */
-0:	movq (%rsi),%rax
-1:	movq %rax,(%rdi)
+0:	movq	(%rsi), %rax
+1:	movq	%rax, (%rdi)
 
-	_ASM_EXTABLE_UA( 0b, .Lcopy_user_tail)
-	_ASM_EXTABLE_UA( 1b, .Lcopy_user_tail)
+	_ASM_EXTABLE_UA(0b, .Lcopy_user_tail)
+	_ASM_EXTABLE_UA(1b, .Lcopy_user_tail)
 
 	/* What would be the offset to the aligned destination? */
-	leaq 8(%rdi),%rax
-	andq $-8,%rax
-	subq %rdi,%rax
+	leaq	8(%rdi), %rax
+	andq	$-8, %rax
+	subq	%rdi, %rax
 
 	/* .. and update pointers and count to match */
-	addq %rax,%rdi
-	addq %rax,%rsi
-	subq %rax,%rcx
+	addq	%rax, %rdi
+	addq	%rax, %rsi
+	subq	%rax, %rcx
 
 	/* make %rcx contain the number of words, %rax the remainder */
-	movq %rcx,%rax
-	shrq $3,%rcx
-	andl $7,%eax
+	movq	%rcx, %rax
+	shrq	$3, %rcx
+	andl	$7, %eax
 0:	rep movsq
-	movl %eax,%ecx
-	testl %ecx,%ecx
-	jne .Lcopy_user_tail
+	movl	%eax, %ecx
+	testl	%ecx, %ecx
+	jne	.Lcopy_user_tail
 	RET
 
-1:	leaq (%rax,%rcx,8),%rcx
-	jmp .Lcopy_user_tail
+1:	leaq	(%rax,%rcx,8), %rcx
+	jmp	.Lcopy_user_tail
 
-	_ASM_EXTABLE_UA( 0b, 1b)
+	_ASM_EXTABLE_UA(0b, 1b)
 SYM_FUNC_END(rep_movs_alternative)
 EXPORT_SYMBOL(rep_movs_alternative)
diff --git a/arch/x86/lib/copy_user_uncached_64.S b/arch/x86/lib/copy_user_uncached_64.S
index 8ed0ce3..8169d57 100644
--- a/arch/x86/lib/copy_user_uncached_64.S
+++ b/arch/x86/lib/copy_user_uncached_64.S
@@ -1,6 +1,12 @@
 /* SPDX-License-Identifier: GPL-2.0-only */
 /*
- * Copyright 2023 Linus Torvalds <torvalds@linux-foundation.org>
+ * __copy_user_nocache - uncached copy from user to kernel
+ *
+ * Copyright 2023 Linus Torvalds
+ *
+ *
+ * Input:  rdi=dst (kernel), rsi=src (user), edx=len
+ * Output: rax=bytes not copied
  */
 
 #include <linux/export.h>
@@ -8,87 +14,49 @@
 #include <linux/objtool.h>
 #include <asm/asm.h>
 
-/*
- * copy_user_nocache - Uncached memory copy with exception handling
- *
- * This copies from user space into kernel space, but the kernel
- * space accesses can take a machine check exception, so they too
- * need exception handling.
- *
- * Note: only 32-bit and 64-bit stores have non-temporal versions,
- * and we only use aligned versions. Any unaligned parts at the
- * start or end of the copy will be done using normal cached stores.
- *
- * Input:
- * rdi destination
- * rsi source
- * edx count
- *
- * Output:
- * rax uncopied bytes or 0 if successful.
- */
 SYM_FUNC_START(copy_to_nontemporal)
-	ANNOTATE_NOENDBR
-	/* If destination is not 7-byte aligned, we'll have to align it */
-	testb $7,%dil
-	jne .Lalign
+        ANNOTATE_NOENDBR
+
+        testb   $7, %dil
+        jne     .Lalign
 
 .Lis_aligned:
-	cmp $64,%edx
-	jb .Lquadwords
+        cmpl    $64, %edx
+        jb      .Lquadwords
 
-	.p2align 4,0x90
+        .p2align 4, 0x90
 .Lunrolled:
-10:	movq (%rsi),%r8
-11:	movq 8(%rsi),%r9
-12:	movq 16(%rsi),%r10
-13:	movq 24(%rsi),%r11
-20:	movnti %r8,(%rdi)
-21:	movnti %r9,8(%rdi)
-22:	movnti %r10,16(%rdi)
-23:	movnti %r11,24(%rdi)
-30:	movq 32(%rsi),%r8
-31:	movq 40(%rsi),%r9
-32:	movq 48(%rsi),%r10
-33:	movq 56(%rsi),%r11
-40:	movnti %r8,32(%rdi)
-41:	movnti %r9,40(%rdi)
-42:	movnti %r10,48(%rdi)
-43:	movnti %r11,56(%rdi)
-
-	addq $64,%rsi
-	addq $64,%rdi
-	sub $64,%edx
-	cmp $64,%edx
-	jae .Lunrolled
+10:     movq    (%rsi), %r8
+11:     movq    8(%rsi), %r9
+12:     movq    16(%rsi), %r10
+13:     movq    24(%rsi), %r11
+20:     movnti  %r8, (%rdi)
+21:     movnti  %r9, 8(%rdi)
+22:     movnti  %r10, 16(%rdi)
+23:     movnti  %r11, 24(%rdi)
+30:     movq    32(%rsi), %r8
+31:     movq    40(%rsi), %r9
+32:     movq    48(%rsi), %r10
+33:     movq    56(%rsi), %r11
+40:     movnti  %r8, 32(%rdi)
+41:     movnti  %r9, 40(%rdi)
+42:     movnti  %r10, 48(%rdi)
+43:     movnti  %r11, 56(%rdi)
+
+        addq    $64, %rsi
+        addq    $64, %rdi
+        subl    $64, %edx
+        cmpl    $64, %edx
+        jae     .Lunrolled
 
-/*
- * First set of user mode loads have been done
- * without any stores, so if they fail, we can
- * just try the non-unrolled loop.
- */
 _ASM_EXTABLE_UA(10b, .Lquadwords)
 _ASM_EXTABLE_UA(11b, .Lquadwords)
 _ASM_EXTABLE_UA(12b, .Lquadwords)
 _ASM_EXTABLE_UA(13b, .Lquadwords)
-
-/*
- * The second set of user mode loads have been
- * done with 32 bytes stored to the destination,
- * so we need to take that into account before
- * falling back to the unrolled loop.
- */
 _ASM_EXTABLE_UA(30b, .Lfixup32)
 _ASM_EXTABLE_UA(31b, .Lfixup32)
 _ASM_EXTABLE_UA(32b, .Lfixup32)
 _ASM_EXTABLE_UA(33b, .Lfixup32)
-
-/*
- * An exception on a write means that we're
- * done, but we need to update the count
- * depending on where in the unrolled loop
- * we were.
- */
 _ASM_EXTABLE_UA(20b, .Ldone0)
 _ASM_EXTABLE_UA(21b, .Ldone8)
 _ASM_EXTABLE_UA(22b, .Ldone16)
@@ -99,58 +67,45 @@ _ASM_EXTABLE_UA(42b, .Ldone48)
 _ASM_EXTABLE_UA(43b, .Ldone56)
 
 .Lquadwords:
-	cmp $8,%edx
-	jb .Llong
-50:	movq (%rsi),%rax
-51:	movnti %rax,(%rdi)
-	addq $8,%rsi
-	addq $8,%rdi
-	sub $8,%edx
-	jmp .Lquadwords
+        cmpl    $8, %edx
+        jb      .Llong
+50:     movq    (%rsi), %rax
+51:     movnti  %rax, (%rdi)
+        addq    $8, %rsi
+        addq    $8, %rdi
+        subl    $8, %edx
+        jmp     .Lquadwords
 
-/*
- * If we fail on the last full quadword, we will
- * not try to do any byte-wise cached accesses.
- * We will try to do one more 4-byte uncached
- * one, though.
- */
 _ASM_EXTABLE_UA(50b, .Llast4)
 _ASM_EXTABLE_UA(51b, .Ldone0)
 
 .Llong:
-	test $4,%dl
-	je .Lword
-60:	movl (%rsi),%eax
-61:	movnti %eax,(%rdi)
-	addq $4,%rsi
-	addq $4,%rdi
-	sub $4,%edx
+        testb   $4, %dl
+        je      .Lword
+60:     movl    (%rsi), %eax
+61:     movnti  %eax, (%rdi)
+        addq    $4, %rsi
+        addq    $4, %rdi
+        subl    $4, %edx
 .Lword:
-	sfence
-	test $2,%dl
-	je .Lbyte
-70:	movw (%rsi),%ax
-71:	movw %ax,(%rdi)
-	addq $2,%rsi
-	addq $2,%rdi
-	sub $2,%edx
+        sfence
+        testb   $2, %dl
+        je      .Lbyte
+70:     movw    (%rsi), %ax
+71:     movw    %ax, (%rdi)
+        addq    $2, %rsi
+        addq    $2, %rdi
+        subl    $2, %edx
 .Lbyte:
-	test $1,%dl
-	je .Ldone
-80:	movb (%rsi),%al
-81:	movb %al,(%rdi)
-	dec %edx
+        testb   $1, %dl
+        je      .Ldone
+80:     movb    (%rsi), %al
+81:     movb    %al, (%rdi)
+        decl    %edx
 .Ldone:
-	mov %edx,%eax
-	RET
+        movl    %edx, %eax
+        RET
 
-/*
- * If we fail on the last four bytes, we won't
- * bother with any fixups. It's dead, Jim. Note
- * that there's no need for 'sfence' for any
- * of this, since the exception will have been
- * serializing.
- */
 _ASM_EXTABLE_UA(60b, .Ldone)
 _ASM_EXTABLE_UA(61b, .Ldone)
 _ASM_EXTABLE_UA(70b, .Ldone)
@@ -158,51 +113,38 @@ _ASM_EXTABLE_UA(71b, .Ldone)
 _ASM_EXTABLE_UA(80b, .Ldone)
 _ASM_EXTABLE_UA(81b, .Ldone)
 
-/*
- * This is the "head needs aliging" case when
- * the destination isn't 8-byte aligned. The
- * 4-byte case can be done uncached, but any
- * smaller alignment is done with regular stores.
- */
 .Lalign:
-	test $1,%dil
-	je .Lalign_word
-	test %edx,%edx
-	je .Ldone
-90:	movb (%rsi),%al
-91:	movb %al,(%rdi)
-	inc %rsi
-	inc %rdi
-	dec %edx
+        testb   $1, %dil
+        je      .Lalign_word
+        testl   %edx, %edx
+        je      .Ldone
+90:     movb    (%rsi), %al
+91:     movb    %al, (%rdi)
+        incq    %rsi
+        incq    %rdi
+        decl    %edx
 .Lalign_word:
-	test $2,%dil
-	je .Lalign_long
-	cmp $2,%edx
-	jb .Lbyte
-92:	movw (%rsi),%ax
-93:	movw %ax,(%rdi)
-	addq $2,%rsi
-	addq $2,%rdi
-	sub $2,%edx
+        testb   $2, %dil
+        je      .Lalign_long
+        cmpl    $2, %edx
+        jb      .Lbyte
+92:     movw    (%rsi), %ax
+93:     movw    %ax, (%rdi)
+        addq    $2, %rsi
+        addq    $2, %rdi
+        subl    $2, %edx
 .Lalign_long:
-	test $4,%dil
-	je .Lis_aligned
-	cmp $4,%edx
-	jb .Lword
-94:	movl (%rsi),%eax
-95:	movnti %eax,(%rdi)
-	addq $4,%rsi
-	addq $4,%rdi
-	sub $4,%edx
-	jmp .Lis_aligned
+        testb   $4, %dil
+        je      .Lis_aligned
+        cmpl    $4, %edx
+        jb      .Lword
+94:     movl    (%rsi), %eax
+95:     movnti  %eax, (%rdi)
+        addq    $4, %rsi
+        addq    $4, %rdi
+        subl    $4, %edx
+        jmp     .Lis_aligned
 
-/*
- * If we fail on the initial alignment accesses,
- * we're all done. Again, no point in trying to
- * do byte-by-byte probing if the 4-byte load
- * fails - we're not doing any uncached accesses
- * any more.
- */
 _ASM_EXTABLE_UA(90b, .Ldone)
 _ASM_EXTABLE_UA(91b, .Ldone)
 _ASM_EXTABLE_UA(92b, .Ldone)
@@ -210,33 +152,30 @@ _ASM_EXTABLE_UA(93b, .Ldone)
 _ASM_EXTABLE_UA(94b, .Ldone)
 _ASM_EXTABLE_UA(95b, .Ldone)
 
-/*
- * Exception table fixups for faults in the middle
- */
-.Ldone56: sub $8,%edx
-.Ldone48: sub $8,%edx
-.Ldone40: sub $8,%edx
-.Ldone32: sub $8,%edx
-.Ldone24: sub $8,%edx
-.Ldone16: sub $8,%edx
-.Ldone8: sub $8,%edx
+.Ldone56: subl $8, %edx
+.Ldone48: subl $8, %edx
+.Ldone40: subl $8, %edx
+.Ldone32: subl $8, %edx
+.Ldone24: subl $8, %edx
+.Ldone16: subl $8, %edx
+.Ldone8:  subl $8, %edx
 .Ldone0:
-	mov %edx,%eax
-	RET
+        movl    %edx, %eax
+        RET
 
 .Lfixup32:
-	addq $32,%rsi
-	addq $32,%rdi
-	sub $32,%edx
-	jmp .Lquadwords
+        addq    $32, %rsi
+        addq    $32, %rdi
+        subl    $32, %edx
+        jmp     .Lquadwords
 
 .Llast4:
-52:	movl (%rsi),%eax
-53:	movnti %eax,(%rdi)
-	sfence
-	sub $4,%edx
-	mov %edx,%eax
-	RET
+52:     movl    (%rsi), %eax
+53:     movnti  %eax, (%rdi)
+        sfence
+        subl    $4, %edx
+        movl    %edx, %eax
+        RET
 _ASM_EXTABLE_UA(52b, .Ldone0)
 _ASM_EXTABLE_UA(53b, .Ldone0)
 
diff --git a/arch/x86/lib/memcpy_64.S b/arch/x86/lib/memcpy_64.S
index 12a23fa..5eac93f 100644
--- a/arch/x86/lib/memcpy_64.S
+++ b/arch/x86/lib/memcpy_64.S
@@ -1,41 +1,110 @@
 /* SPDX-License-Identifier: GPL-2.0-only */
-/* Copyright 2002 Andi Kleen */
+/* Copyright 2002 Andi Kleen, audited for Intel Raptor Lake */
 
 #include <linux/export.h>
 #include <linux/linkage.h>
 #include <linux/cfi_types.h>
-#include <asm/errno.h>
 #include <asm/cpufeatures.h>
 #include <asm/alternative.h>
 
 .section .noinstr.text, "ax"
 
 /*
- * memcpy - Copy a memory block.
+ * void *memcpy(void *dst, const void *src, size_t len)
+ *   rdi = dst, rsi = src, rdx = len, returns rax = dst
  *
- * Input:
- *  rdi destination
- *  rsi source
- *  rdx count
- *
- * Output:
- * rax original destination
- *
- * The FSRM alternative should be done inline (avoiding the call and
- * the disgusting return handling), but that would require some help
- * from the compiler for better calling conventions.
- *
- * The 'rep movsb' itself is small enough to replace the call, but the
- * two register moves blow up the code. And one of them is "needed"
- * only for the return value that is the same as the source input,
- * which the compiler could/should do much better anyway.
+ * Raptor Lake FSRM path:
+ *   - <=64 bytes use bounded first/last scalar copies.  The user's P/E-core
+ *     measurements showed scalar copies beating REP MOVSB through 64 bytes.
+ *   - >64 bytes use REP MOVSB, preserving the enhanced fast-string path for
+ *     larger copies.
+ * memcpy overlap remains undefined, as required by the C API.
  */
 SYM_TYPED_FUNC_START(__memcpy)
+	ANNOTATE_NOENDBR
 	ALTERNATIVE "jmp memcpy_orig", "", X86_FEATURE_FSRM
 
-	movq %rdi, %rax
-	movq %rdx, %rcx
-	rep movsb
+	movq	%rdi, %rax
+	testq	%rdx, %rdx
+	je	.Lmemcpy_done
+	cmpq	$64, %rdx
+	ja	.Lmemcpy_rep
+
+	cmpq	$32, %rdx
+	ja	.Lmemcpy_33_64
+	cmpq	$16, %rdx
+	ja	.Lmemcpy_17_32
+	cmpq	$8, %rdx
+	ja	.Lmemcpy_9_16
+	cmpq	$4, %rdx
+	ja	.Lmemcpy_5_8
+	cmpq	$2, %rdx
+	ja	.Lmemcpy_3_4
+	je	.Lmemcpy_2
+	movb	(%rsi), %cl
+	movb	%cl, (%rdi)
+	RET
+
+.Lmemcpy_2:
+	movw	(%rsi), %cx
+	movw	%cx, (%rdi)
+	RET
+
+.Lmemcpy_3_4:
+	movw	(%rsi), %cx
+	movw	-2(%rsi,%rdx), %r8w
+	movw	%cx, (%rdi)
+	movw	%r8w, -2(%rdi,%rdx)
+	RET
+
+.Lmemcpy_5_8:
+	movl	(%rsi), %ecx
+	movl	-4(%rsi,%rdx), %r8d
+	movl	%ecx, (%rdi)
+	movl	%r8d, -4(%rdi,%rdx)
+	RET
+
+.Lmemcpy_9_16:
+	movq	(%rsi), %rcx
+	movq	-8(%rsi,%rdx), %r8
+	movq	%rcx, (%rdi)
+	movq	%r8, -8(%rdi,%rdx)
+	RET
+
+.Lmemcpy_17_32:
+	movq	(%rsi), %rcx
+	movq	8(%rsi), %r8
+	movq	-16(%rsi,%rdx), %r9
+	movq	-8(%rsi,%rdx), %r10
+	movq	%rcx, (%rdi)
+	movq	%r8, 8(%rdi)
+	movq	%r9, -16(%rdi,%rdx)
+	movq	%r10, -8(%rdi,%rdx)
+	RET
+
+.Lmemcpy_33_64:
+	movq	(%rsi), %rcx
+	movq	8(%rsi), %r8
+	movq	16(%rsi), %r9
+	movq	24(%rsi), %r10
+	movq	%rcx, (%rdi)
+	movq	%r8, 8(%rdi)
+	movq	%r9, 16(%rdi)
+	movq	%r10, 24(%rdi)
+	movq	-32(%rsi,%rdx), %rcx
+	movq	-24(%rsi,%rdx), %r8
+	movq	-16(%rsi,%rdx), %r9
+	movq	-8(%rsi,%rdx), %r10
+	movq	%rcx, -32(%rdi,%rdx)
+	movq	%r8, -24(%rdi,%rdx)
+	movq	%r9, -16(%rdi,%rdx)
+	movq	%r10, -8(%rdi,%rdx)
+	RET
+
+.Lmemcpy_rep:
+	movq	%rdx, %rcx
+	rep	movsb
+.Lmemcpy_done:
 	RET
 SYM_FUNC_END(__memcpy)
 EXPORT_SYMBOL(__memcpy)
@@ -45,129 +114,79 @@ SYM_PIC_ALIAS(memcpy)
 EXPORT_SYMBOL(memcpy)
 
 SYM_FUNC_START_LOCAL(memcpy_orig)
-	movq %rdi, %rax
-
-	cmpq $0x20, %rdx
-	jb .Lhandle_tail
-
-	/*
-	 * We check whether memory false dependence could occur,
-	 * then jump to corresponding copy mode.
-	 */
-	cmp  %dil, %sil
-	jl .Lcopy_backward
-	subq $0x20, %rdx
-.Lcopy_forward_loop:
-	subq $0x20,	%rdx
-
-	/*
-	 * Move in blocks of 4x8 bytes:
-	 */
-	movq 0*8(%rsi),	%r8
-	movq 1*8(%rsi),	%r9
-	movq 2*8(%rsi),	%r10
-	movq 3*8(%rsi),	%r11
-	leaq 4*8(%rsi),	%rsi
-
-	movq %r8,	0*8(%rdi)
-	movq %r9,	1*8(%rdi)
-	movq %r10,	2*8(%rdi)
-	movq %r11,	3*8(%rdi)
-	leaq 4*8(%rdi),	%rdi
-	jae  .Lcopy_forward_loop
-	addl $0x20,	%edx
-	jmp  .Lhandle_tail
-
-.Lcopy_backward:
-	/*
-	 * Calculate copy position to tail.
-	 */
-	addq %rdx,	%rsi
-	addq %rdx,	%rdi
-	subq $0x20,	%rdx
-	/*
-	 * At most 3 ALU operations in one cycle,
-	 * so append NOPS in the same 16 bytes trunk.
-	 */
-	.p2align 4
-.Lcopy_backward_loop:
-	subq $0x20,	%rdx
-	movq -1*8(%rsi),	%r8
-	movq -2*8(%rsi),	%r9
-	movq -3*8(%rsi),	%r10
-	movq -4*8(%rsi),	%r11
-	leaq -4*8(%rsi),	%rsi
-	movq %r8,		-1*8(%rdi)
-	movq %r9,		-2*8(%rdi)
-	movq %r10,		-3*8(%rdi)
-	movq %r11,		-4*8(%rdi)
-	leaq -4*8(%rdi),	%rdi
-	jae  .Lcopy_backward_loop
-
-	/*
-	 * Calculate copy position to head.
-	 */
-	addl $0x20,	%edx
-	subq %rdx,	%rsi
-	subq %rdx,	%rdi
-.Lhandle_tail:
-	cmpl $16,	%edx
-	jb   .Lless_16bytes
-
-	/*
-	 * Move data from 16 bytes to 31 bytes.
-	 */
-	movq 0*8(%rsi), %r8
-	movq 1*8(%rsi),	%r9
-	movq -2*8(%rsi, %rdx),	%r10
-	movq -1*8(%rsi, %rdx),	%r11
-	movq %r8,	0*8(%rdi)
-	movq %r9,	1*8(%rdi)
-	movq %r10,	-2*8(%rdi, %rdx)
-	movq %r11,	-1*8(%rdi, %rdx)
+	ANNOTATE_NOENDBR
+	movq	%rdi, %rax
+
+	cmpq	$32, %rdx
+	jb	.Ltail
+
+	.p2align 6
+.Lloop32:
+	movq	0(%rsi), %r8
+	movq	8(%rsi), %r9
+	movq	16(%rsi), %r10
+	movq	24(%rsi), %r11
+	movq	%r8, 0(%rdi)
+	movq	%r9, 8(%rdi)
+	movq	%r10, 16(%rdi)
+	movq	%r11, 24(%rdi)
+	addq	$32, %rsi
+	addq	$32, %rdi
+	subq	$32, %rdx
+	cmpq	$32, %rdx
+	jae	.Lloop32
+
+.Ltail:
+	cmpq	$16, %rdx
+	jb	.Llt16
+	movq	0(%rsi), %r8
+	movq	8(%rsi), %r9
+	movq	-16(%rsi,%rdx), %r10
+	movq	-8(%rsi,%rdx), %r11
+	movq	%r8, 0(%rdi)
+	movq	%r9, 8(%rdi)
+	movq	%r10, -16(%rdi,%rdx)
+	movq	%r11, -8(%rdi,%rdx)
 	RET
+
 	.p2align 4
-.Lless_16bytes:
-	cmpl $8,	%edx
-	jb   .Lless_8bytes
-	/*
-	 * Move data from 8 bytes to 15 bytes.
-	 */
-	movq 0*8(%rsi),	%r8
-	movq -1*8(%rsi, %rdx),	%r9
-	movq %r8,	0*8(%rdi)
-	movq %r9,	-1*8(%rdi, %rdx)
+.Llt16:
+	cmpq	$8, %rdx
+	jb	.Llt8
+	movq	0(%rsi), %r8
+	movq	-8(%rsi,%rdx), %r9
+	movq	%r8, 0(%rdi)
+	movq	%r9, -8(%rdi,%rdx)
 	RET
+
 	.p2align 4
-.Lless_8bytes:
-	cmpl $4,	%edx
-	jb   .Lless_3bytes
-
-	/*
-	 * Move data from 4 bytes to 7 bytes.
-	 */
-	movl (%rsi), %ecx
-	movl -4(%rsi, %rdx), %r8d
-	movl %ecx, (%rdi)
-	movl %r8d, -4(%rdi, %rdx)
+.Llt8:
+	cmpq	$4, %rdx
+	jb	.Llt4
+	movl	0(%rsi), %ecx
+	movl	-4(%rsi,%rdx), %r8d
+	movl	%ecx, 0(%rdi)
+	movl	%r8d, -4(%rdi,%rdx)
 	RET
+
 	.p2align 4
-.Lless_3bytes:
-	subl $1, %edx
-	jb .Lend
-	/*
-	 * Move data from 1 bytes to 3 bytes.
-	 */
-	movzbl (%rsi), %ecx
-	jz .Lstore_1byte
-	movzbq 1(%rsi), %r8
-	movzbq (%rsi, %rdx), %r9
-	movb %r8b, 1(%rdi)
-	movb %r9b, (%rdi, %rdx)
-.Lstore_1byte:
-	movb %cl, (%rdi)
-
-.Lend:
+.Llt4:
+	testq	%rdx, %rdx
+	je	.Ldone
+	cmpq	$2, %rdx
+	jb	.L1
+	movw	0(%rsi), %cx
+	movw	-2(%rsi,%rdx), %r8w
+	movw	%cx, 0(%rdi)
+	movw	%r8w, -2(%rdi,%rdx)
+	cmpq	$3, %rdx
+	jne	.Ldone
+	movb	2(%rsi), %cl
+	movb	%cl, 2(%rdi)
+	RET
+.L1:
+	movb	0(%rsi), %cl
+	movb	%cl, 0(%rdi)
+.Ldone:
 	RET
 SYM_FUNC_END(memcpy_orig)
-
diff --git a/arch/x86/lib/memmove_64.S b/arch/x86/lib/memmove_64.S
index aa1f92e..9b4f467 100644
--- a/arch/x86/lib/memmove_64.S
+++ b/arch/x86/lib/memmove_64.S
@@ -1,214 +1,130 @@
 /* SPDX-License-Identifier: GPL-2.0 */
 /*
- * Normally compiler builtins are used, but sometimes the compiler calls out
- * of line code. Based on asm-i386/string.h.
+ * memmove - overlap-safe copy for x86-64
  *
- * This assembly file is re-written from memmove_64.c file.
- *	- Copyright 2011 Fenghua Yu <fenghua.yu@intel.com>
+ * Raptor Lake policy:
+ *  - No SIMD/FPU state, no stack, no callee-saved registers.
+ *  - Non-overlap and dst<src use a forward path; >16 bytes relies on REP
+ *    MOVSB, which is the right large-copy primitive on ERMS/FSRM systems.
+ *  - Overlap with dst>src copies backward.  Tiny copies are register-buffered
+ *    before any store so dst=src+1 cannot corrupt the source.  Larger copies
+ *    use DF=1 REP MOVSB and always restore DF before return.
  */
+
 #include <linux/export.h>
 #include <linux/linkage.h>
 #include <linux/cfi_types.h>
-#include <asm/cpufeatures.h>
-#include <asm/alternative.h>
-
-#undef memmove
+#include <linux/objtool.h>
+#include <asm/asm.h>
 
-.section .noinstr.text, "ax"
+	.section .noinstr.text, "ax", @progbits
 
-/*
- * Implement memmove(). This can handle overlap between src and dst.
- *
- * Input:
- * rdi: dest
- * rsi: src
- * rdx: count
- *
- * Output:
- * rax: dest
- */
+/* void *memmove(void *dst, const void *src, size_t len) */
 SYM_TYPED_FUNC_START(__memmove)
+	ANNOTATE_NOENDBR
+	movq	%rdi, %rax		/* return value */
+
+	testq	%rdx, %rdx
+	jz	.Ldone
+	cmpq	%rsi, %rdi
+	je	.Ldone
+
+	/* forward if dst < src or if the ranges do not overlap */
+	movq	%rdi, %rcx
+	subq	%rsi, %rcx		/* unsigned dst-src */
+	cmpq	%rsi, %rdi
+	jb	.Lforward
+	cmpq	%rdx, %rcx
+	jae	.Lforward
+
+	/* ---------- backward: overlap with dst > src ---------- */
+	cmpq	$16, %rdx
+	ja	.Lbackward_rep
+
+	cmpq	$8, %rdx
+	jb	.Lbwd_lt8
+	movq	(%rsi), %rcx
+	movq	-8(%rsi,%rdx), %r8
+	movq	%r8, -8(%rdi,%rdx)
+	movq	%rcx, (%rdi)
+	jmp	.Ldone
+
+.Lbwd_lt8:
+	cmpq	$4, %rdx
+	jb	.Lbwd_lt4
+	movl	(%rsi), %ecx
+	movl	-4(%rsi,%rdx), %r8d
+	movl	%r8d, -4(%rdi,%rdx)
+	movl	%ecx, (%rdi)
+	jmp	.Ldone
+
+.Lbwd_lt4:
+	cmpq	$2, %rdx
+	jb	.Lbwd_1
+	movw	(%rsi), %cx
+	movw	-2(%rsi,%rdx), %r8w
+	movw	%r8w, -2(%rdi,%rdx)
+	movw	%cx, (%rdi)
+	jmp	.Ldone
+
+.Lbwd_1:
+	movb	(%rsi), %cl
+	movb	%cl, (%rdi)
+	jmp	.Ldone
 
-	mov %rdi, %rax
-
-	/* Decide forward/backward copy mode */
-	cmp %rdi, %rsi
-	jge .Lmemmove_begin_forward
-	mov %rsi, %r8
-	add %rdx, %r8
-	cmp %rdi, %r8
-	jg 2f
-
-#define CHECK_LEN	cmp $0x20, %rdx; jb 1f
-#define MEMMOVE_BYTES	movq %rdx, %rcx; rep movsb; RET
-.Lmemmove_begin_forward:
-	ALTERNATIVE_2 __stringify(CHECK_LEN), \
-		      __stringify(CHECK_LEN; MEMMOVE_BYTES), X86_FEATURE_ERMS, \
-		      __stringify(MEMMOVE_BYTES), X86_FEATURE_FSRM
-
-	/*
-	 * movsq instruction have many startup latency
-	 * so we handle small size by general register.
-	 */
-	cmp  $680, %rdx
-	jb	3f
-	/*
-	 * movsq instruction is only good for aligned case.
-	 */
-
-	cmpb %dil, %sil
-	je 4f
-3:
-	sub $0x20, %rdx
-	/*
-	 * We gobble 32 bytes forward in each loop.
-	 */
-5:
-	sub $0x20, %rdx
-	movq 0*8(%rsi), %r11
-	movq 1*8(%rsi), %r10
-	movq 2*8(%rsi), %r9
-	movq 3*8(%rsi), %r8
-	leaq 4*8(%rsi), %rsi
-
-	movq %r11, 0*8(%rdi)
-	movq %r10, 1*8(%rdi)
-	movq %r9, 2*8(%rdi)
-	movq %r8, 3*8(%rdi)
-	leaq 4*8(%rdi), %rdi
-	jae 5b
-	addq $0x20, %rdx
-	jmp 1f
-	/*
-	 * Handle data forward by movsq.
-	 */
-	.p2align 4
-4:
-	movq %rdx, %rcx
-	movq -8(%rsi, %rdx), %r11
-	lea -8(%rdi, %rdx), %r10
-	shrq $3, %rcx
-	rep movsq
-	movq %r11, (%r10)
-	jmp 13f
-.Lmemmove_end_forward:
-
-	/*
-	 * Handle data backward by movsq.
-	 */
 	.p2align 4
-7:
-	movq %rdx, %rcx
-	movq (%rsi), %r11
-	movq %rdi, %r10
-	leaq -8(%rsi, %rdx), %rsi
-	leaq -8(%rdi, %rdx), %rdi
-	shrq $3, %rcx
+.Lbackward_rep:
+	leaq	-1(%rsi,%rdx), %rsi
+	leaq	-1(%rdi,%rdx), %rdi
 	std
-	rep movsq
+	movq	%rdx, %rcx
+	rep	movsb
 	cld
-	movq %r11, (%r10)
-	jmp 13f
+	jmp	.Ldone
 
-	/*
-	 * Start to prepare for backward copy.
-	 */
+	/* ---------- forward: dst < src or non-overlapping ---------- */
 	.p2align 4
-2:
-	cmp $0x20, %rdx
-	jb 1f
-	cmp $680, %rdx
-	jb 6f
-	cmp %dil, %sil
-	je 7b
-6:
-	/*
-	 * Calculate copy position to tail.
-	 */
-	addq %rdx, %rsi
-	addq %rdx, %rdi
-	subq $0x20, %rdx
-	/*
-	 * We gobble 32 bytes backward in each loop.
-	 */
-8:
-	subq $0x20, %rdx
-	movq -1*8(%rsi), %r11
-	movq -2*8(%rsi), %r10
-	movq -3*8(%rsi), %r9
-	movq -4*8(%rsi), %r8
-	leaq -4*8(%rsi), %rsi
-
-	movq %r11, -1*8(%rdi)
-	movq %r10, -2*8(%rdi)
-	movq %r9, -3*8(%rdi)
-	movq %r8, -4*8(%rdi)
-	leaq -4*8(%rdi), %rdi
-	jae 8b
-	/*
-	 * Calculate copy position to head.
-	 */
-	addq $0x20, %rdx
-	subq %rdx, %rsi
-	subq %rdx, %rdi
-1:
-	cmpq $16, %rdx
-	jb 9f
-	/*
-	 * Move data from 16 bytes to 31 bytes.
-	 */
-	movq 0*8(%rsi), %r11
-	movq 1*8(%rsi), %r10
-	movq -2*8(%rsi, %rdx), %r9
-	movq -1*8(%rsi, %rdx), %r8
-	movq %r11, 0*8(%rdi)
-	movq %r10, 1*8(%rdi)
-	movq %r9, -2*8(%rdi, %rdx)
-	movq %r8, -1*8(%rdi, %rdx)
-	jmp 13f
+.Lforward:
+	cmpq	$16, %rdx
+	ja	.Lforward_rep
+
+	cmpq	$8, %rdx
+	jb	.Lfwd_lt8
+	movq	(%rsi), %rcx
+	movq	-8(%rsi,%rdx), %r8
+	movq	%rcx, (%rdi)
+	movq	%r8, -8(%rdi,%rdx)
+	jmp	.Ldone
+
+.Lfwd_lt8:
+	cmpq	$4, %rdx
+	jb	.Lfwd_lt4
+	movl	(%rsi), %ecx
+	movl	-4(%rsi,%rdx), %r8d
+	movl	%ecx, (%rdi)
+	movl	%r8d, -4(%rdi,%rdx)
+	jmp	.Ldone
+
+.Lfwd_lt4:
+	cmpq	$2, %rdx
+	jb	.Lfwd_1
+	movw	(%rsi), %cx
+	movw	-2(%rsi,%rdx), %r8w
+	movw	%cx, (%rdi)
+	movw	%r8w, -2(%rdi,%rdx)
+	jmp	.Ldone
+
+.Lfwd_1:
+	movb	(%rsi), %cl
+	movb	%cl, (%rdi)
+	jmp	.Ldone
+
 	.p2align 4
-9:
-	cmpq $8, %rdx
-	jb 10f
-	/*
-	 * Move data from 8 bytes to 15 bytes.
-	 */
-	movq 0*8(%rsi), %r11
-	movq -1*8(%rsi, %rdx), %r10
-	movq %r11, 0*8(%rdi)
-	movq %r10, -1*8(%rdi, %rdx)
-	jmp 13f
-10:
-	cmpq $4, %rdx
-	jb 11f
-	/*
-	 * Move data from 4 bytes to 7 bytes.
-	 */
-	movl (%rsi), %r11d
-	movl -4(%rsi, %rdx), %r10d
-	movl %r11d, (%rdi)
-	movl %r10d, -4(%rdi, %rdx)
-	jmp 13f
-11:
-	cmp $2, %rdx
-	jb 12f
-	/*
-	 * Move data from 2 bytes to 3 bytes.
-	 */
-	movw (%rsi), %r11w
-	movw -2(%rsi, %rdx), %r10w
-	movw %r11w, (%rdi)
-	movw %r10w, -2(%rdi, %rdx)
-	jmp 13f
-12:
-	cmp $1, %rdx
-	jb 13f
-	/*
-	 * Move data for 1 byte.
-	 */
-	movb (%rsi), %r11b
-	movb %r11b, (%rdi)
-13:
+.Lforward_rep:
+	movq	%rdx, %rcx
+	rep	movsb
+
+.Ldone:
 	RET
 SYM_FUNC_END(__memmove)
 EXPORT_SYMBOL(__memmove)
diff --git a/arch/x86/lib/memset_64.S b/arch/x86/lib/memset_64.S
index fb5a03c..32aee51 100644
--- a/arch/x86/lib/memset_64.S
+++ b/arch/x86/lib/memset_64.S
@@ -1,5 +1,15 @@
 /* SPDX-License-Identifier: GPL-2.0 */
-/* Copyright 2002 Andi Kleen, SuSE Labs */
+/*
+ * memset() - 64-bit implementation for x86-64
+ *
+ * Raptor Lake policy:
+ *  - The FSRS path stays inline at the public symbol after alternatives
+ *    patching; there is no extra trampoline branch on the hot path.
+ *  - <=64 bytes avoid REP STOSB startup by using first/last scalar stores.
+ *  - >64 bytes use REP STOSB and only require AL, avoiding unnecessary
+ *    64-bit pattern construction in the large path.
+ *  - The fallback path is scalar, stackless, and safe before alternatives.
+ */
 
 #include <linux/export.h>
 #include <linux/linkage.h>
@@ -10,33 +20,89 @@
 .section .noinstr.text, "ax"
 
 /*
- * ISO C memset - set a memory block to a byte value. This function uses fast
- * string to get better performance than the original function. The code is
- * simpler and shorter than the original function as well.
+ * ISO C memset - set a memory block to a byte value.
  *
  * rdi   destination
  * rsi   value (char)
  * rdx   count (bytes)
  *
  * rax   original destination
- *
- * The FSRS alternative should be done inline (avoiding the call and
- * the disgusting return handling), but that would require some help
- * from the compiler for better calling conventions.
- *
- * The 'rep stosb' itself is small enough to replace the call, but all
- * the register moves blow up the code. And two of them are "needed"
- * only for the return value that is the same as the source input,
- * which the compiler could/should do much better anyway.
  */
 SYM_TYPED_FUNC_START(__memset)
 	ALTERNATIVE "jmp memset_orig", "", X86_FEATURE_FSRS
 
-	movq %rdi,%r9
-	movb %sil,%al
-	movq %rdx,%rcx
-	rep stosb
-	movq %r9,%rax
+	movq	%rdi, %r9
+	testq	%rdx, %rdx
+	jz	.Ldone_fsrs
+
+	movzbl	%sil, %eax
+	cmpq	$64, %rdx
+	ja	.Lrep_stosb
+
+	/* Build the repeated-byte scalar pattern only for the scalar path. */
+	imull	$0x01010101, %eax, %eax
+	movl	%eax, %ecx
+	shlq	$32, %rcx
+	orq	%rcx, %rax
+	movq	%rdx, %rcx
+
+	cmpq	$32, %rcx
+	ja	.L32_64
+	cmpq	$16, %rcx
+	ja	.L16_32
+	cmpq	$8, %rcx
+	ja	.L8_16
+	cmpq	$4, %rcx
+	ja	.L4_8
+	cmpq	$2, %rcx
+	ja	.L3_4
+	je	.L2
+	movb	%al, (%rdi)
+	jmp	.Ldone_fsrs
+
+.L2:
+	movw	%ax, (%rdi)
+	jmp	.Ldone_fsrs
+
+.L3_4:
+	movw	%ax, (%rdi)
+	movw	%ax, -2(%rdi,%rcx)
+	jmp	.Ldone_fsrs
+
+.L4_8:
+	movl	%eax, (%rdi)
+	movl	%eax, -4(%rdi,%rcx)
+	jmp	.Ldone_fsrs
+
+.L8_16:
+	movq	%rax, (%rdi)
+	movq	%rax, -8(%rdi,%rcx)
+	jmp	.Ldone_fsrs
+
+.L16_32:
+	movq	%rax, (%rdi)
+	movq	%rax, 8(%rdi)
+	movq	%rax, -16(%rdi,%rcx)
+	movq	%rax, -8(%rdi,%rcx)
+	jmp	.Ldone_fsrs
+
+.L32_64:
+	movq	%rax, (%rdi)
+	movq	%rax, 8(%rdi)
+	movq	%rax, 16(%rdi)
+	movq	%rax, 24(%rdi)
+	movq	%rax, -32(%rdi,%rcx)
+	movq	%rax, -24(%rdi,%rcx)
+	movq	%rax, -16(%rdi,%rcx)
+	movq	%rax, -8(%rdi,%rcx)
+	jmp	.Ldone_fsrs
+
+.Lrep_stosb:
+	movq	%rdx, %rcx
+	rep	stosb
+
+.Ldone_fsrs:
+	movq	%r9, %rax
 	RET
 SYM_FUNC_END(__memset)
 EXPORT_SYMBOL(__memset)
@@ -46,74 +112,77 @@ SYM_PIC_ALIAS(memset)
 EXPORT_SYMBOL(memset)
 
 SYM_FUNC_START_LOCAL(memset_orig)
-	movq %rdi,%r10
+	movq	%rdi, %r10
+
+	testq	%rdx, %rdx
+	jz	.Ldone_orig
 
-	/* expand byte value  */
-	movzbl %sil,%ecx
-	movabs $0x0101010101010101,%rax
-	imulq  %rcx,%rax
+	/* expand byte value */
+	movzbl	%sil, %ecx
+	movabs	$0x0101010101010101, %rax
+	imulq	%rcx, %rax
 
 	/* align dst */
-	movl  %edi,%r9d
-	andl  $7,%r9d
-	jnz  .Lbad_alignment
+	movl	%edi, %r9d
+	andl	$7, %r9d
+	jnz	.Lbad_alignment
 .Lafter_bad_alignment:
 
-	movq  %rdx,%rcx
-	shrq  $6,%rcx
-	jz	 .Lhandle_tail
+	movq	%rdx, %rcx
+	shrq	$6, %rcx
+	jz	.Lhandle_tail
 
 	.p2align 4
 .Lloop_64:
-	decq  %rcx
-	movq  %rax,(%rdi)
-	movq  %rax,8(%rdi)
-	movq  %rax,16(%rdi)
-	movq  %rax,24(%rdi)
-	movq  %rax,32(%rdi)
-	movq  %rax,40(%rdi)
-	movq  %rax,48(%rdi)
-	movq  %rax,56(%rdi)
-	leaq  64(%rdi),%rdi
-	jnz    .Lloop_64
+	decq	%rcx
+	movq	%rax, (%rdi)
+	movq	%rax, 8(%rdi)
+	movq	%rax, 16(%rdi)
+	movq	%rax, 24(%rdi)
+	movq	%rax, 32(%rdi)
+	movq	%rax, 40(%rdi)
+	movq	%rax, 48(%rdi)
+	movq	%rax, 56(%rdi)
+	leaq	64(%rdi), %rdi
+	jnz	.Lloop_64
 
 	/* Handle tail in loops. The loops should be faster than hard
-	   to predict jump tables. */
+	 * to predict jump tables.
+	 */
 	.p2align 4
 .Lhandle_tail:
-	movl	%edx,%ecx
-	andl    $63&(~7),%ecx
-	jz 		.Lhandle_7
-	shrl	$3,%ecx
+	movl	%edx, %ecx
+	andl	$63&(~7), %ecx
+	jz	.Lhandle_7
+	shrl	$3, %ecx
 	.p2align 4
 .Lloop_8:
-	decl   %ecx
-	movq  %rax,(%rdi)
-	leaq  8(%rdi),%rdi
-	jnz    .Lloop_8
+	decl	%ecx
+	movq	%rax, (%rdi)
+	leaq	8(%rdi), %rdi
+	jnz	.Lloop_8
 
 .Lhandle_7:
-	andl	$7,%edx
-	jz      .Lende
+	andl	$7, %edx
+	jz	.Ldone_orig
 	.p2align 4
 .Lloop_1:
-	decl    %edx
-	movb 	%al,(%rdi)
-	leaq	1(%rdi),%rdi
-	jnz     .Lloop_1
+	decl	%edx
+	movb	%al, (%rdi)
+	leaq	1(%rdi), %rdi
+	jnz	.Lloop_1
 
-.Lende:
-	movq	%r10,%rax
+.Ldone_orig:
+	movq	%r10, %rax
 	RET
 
 .Lbad_alignment:
-	cmpq $7,%rdx
+	cmpq	$7, %rdx
 	jbe	.Lhandle_7
-	movq %rax,(%rdi)	/* unaligned store */
-	movq $8,%r8
-	subq %r9,%r8
-	addq %r8,%rdi
-	subq %r8,%rdx
-	jmp .Lafter_bad_alignment
-.Lfinal:
+	movq	%rax, (%rdi)	/* unaligned store */
+	movq	$8, %r8
+	subq	%r9, %r8
+	addq	%r8, %rdi
+	subq	%r8, %rdx
+	jmp	.Lafter_bad_alignment
 SYM_FUNC_END(memset_orig)
diff --git a/lib/string.c b/lib/string.c
index b632c71..eb1f3f0 100644
--- a/lib/string.c
+++ b/lib/string.c
@@ -30,6 +30,23 @@
 #include <linux/unaligned.h>
 #include <asm/word-at-a-time.h>
 
+
+#if BITS_PER_LONG == 64 && defined(CONFIG_HAVE_EFFICIENT_UNALIGNED_ACCESS)
+#define STRINGOP_WORD_AT_A_TIME 1
+
+static __always_inline unsigned long stringop_find_diff_byte(unsigned long a, unsigned long b)
+{
+	return __ffs(a ^ b) >> 3;
+}
+
+static __always_inline int stringop_word_cmp(unsigned long a, unsigned long b)
+{
+	unsigned long idx = stringop_find_diff_byte(a, b);
+	unsigned long shift = idx * 8;
+	return (int)((a >> shift) & 0xff) - (int)((b >> shift) & 0xff);
+}
+#endif
+
 #ifndef __HAVE_ARCH_STRNCASECMP
 /**
  * strncasecmp - Case insensitive, length-limited string comparison
@@ -270,15 +287,40 @@ EXPORT_SYMBOL(strlcat);
 #endif
 
 #ifndef __HAVE_ARCH_STRCMP
-/**
- * strcmp - Compare two strings
- * @cs: One string
- * @ct: Another string
- */
 int strcmp(const char *cs, const char *ct)
 {
-	unsigned char c1, c2;
+	unsigned char c1 = *cs;
+	unsigned char c2 = *ct;
 
+	if (c1 != c2)
+		return c1 < c2 ? -1 : 1;
+	if (!c1)
+		return 0;
+
+#if defined(STRINGOP_WORD_AT_A_TIME)
+	const struct word_at_a_time constants = WORD_AT_A_TIME_CONSTANTS;
+
+	for (;;) {
+		unsigned long a = load_unaligned_zeropad(cs);
+		unsigned long b = load_unaligned_zeropad(ct);
+		unsigned long bits;
+
+		if (a != b || has_zero(a, &bits, &constants) ||
+		    has_zero(b, &bits, &constants)) {
+			for (;;) {
+				unsigned char c1 = *cs++;
+				unsigned char c2 = *ct++;
+
+				if (c1 != c2)
+					return c1 < c2 ? -1 : 1;
+				if (!c1)
+					return 0;
+			}
+		}
+		cs += sizeof(unsigned long);
+		ct += sizeof(unsigned long);
+	}
+#else
 	while (1) {
 		c1 = *cs++;
 		c2 = *ct++;
@@ -288,6 +330,7 @@ int strcmp(const char *cs, const char *ct)
 			break;
 	}
 	return 0;
+#endif
 }
 EXPORT_SYMBOL(strcmp);
 #endif
@@ -301,11 +344,33 @@ EXPORT_SYMBOL(strcmp);
  */
 int strncmp(const char *cs, const char *ct, size_t count)
 {
-	unsigned char c1, c2;
+#if defined(STRINGOP_WORD_AT_A_TIME)
+	const struct word_at_a_time constants = WORD_AT_A_TIME_CONSTANTS;
+
+	if (count < 16)
+		goto byte_loop;
+	if (*cs != *ct)
+		return *(const unsigned char *)cs < *(const unsigned char *)ct ? -1 : 1;
+	if (!*cs)
+		return 0;
+	while (count >= sizeof(unsigned long)) {
+		unsigned long a = get_unaligned((const unsigned long *)cs);
+		unsigned long b = get_unaligned((const unsigned long *)ct);
+		unsigned long bits;
 
+		if (a != b || has_zero(a, &bits, &constants) ||
+		    has_zero(b, &bits, &constants))
+			break;
+		cs += sizeof(unsigned long);
+		ct += sizeof(unsigned long);
+		count -= sizeof(unsigned long);
+	}
+byte_loop:
+#endif
 	while (count) {
-		c1 = *cs++;
-		c2 = *ct++;
+		unsigned char c1 = *cs++;
+		unsigned char c2 = *ct++;
+
 		if (c1 != c2)
 			return c1 < c2 ? -1 : 1;
 		if (!c1)
@@ -328,10 +393,34 @@ EXPORT_SYMBOL(strncmp);
  */
 char *strchr(const char *s, int c)
 {
+	if (*s == (char)c)
+		return (char *)s;
+	if (!*s)
+		return NULL;
+
+#if defined(STRINGOP_WORD_AT_A_TIME)
+	const struct word_at_a_time constants = WORD_AT_A_TIME_CONSTANTS;
+	unsigned long repeated = REPEAT_BYTE((unsigned char)c);
+
+	for (;;) {
+		unsigned long word = load_unaligned_zeropad(s);
+		unsigned long mbits, zbits, bits;
+
+		has_zero(word ^ repeated, &mbits, &constants);
+		has_zero(word, &zbits, &constants);
+		bits = mbits | zbits;
+		if (bits) {
+			unsigned long idx = find_zero(bits);
+			return s[idx] == (char)c ? (char *)s + idx : NULL;
+		}
+		s += sizeof(unsigned long);
+	}
+#else
 	for (; *s != (char)c; ++s)
 		if (*s == '\0')
 			return NULL;
 	return (char *)s;
+#endif
 }
 EXPORT_SYMBOL(strchr);
 #endif
@@ -347,9 +436,29 @@ EXPORT_SYMBOL(strchr);
  */
 char *strchrnul(const char *s, int c)
 {
+	if (*s == (char)c || !*s)
+		return (char *)s;
+
+#if defined(STRINGOP_WORD_AT_A_TIME)
+	const struct word_at_a_time constants = WORD_AT_A_TIME_CONSTANTS;
+	unsigned long repeated = REPEAT_BYTE((unsigned char)c);
+
+	for (;;) {
+		unsigned long word = load_unaligned_zeropad(s);
+		unsigned long mbits, zbits, bits;
+
+		has_zero(word ^ repeated, &mbits, &constants);
+		has_zero(word, &zbits, &constants);
+		bits = mbits | zbits;
+		if (bits)
+			return (char *)s + find_zero(bits);
+		s += sizeof(unsigned long);
+	}
+#else
 	while (*s && *s != (char)c)
 		s++;
 	return (char *)s;
+#endif
 }
 EXPORT_SYMBOL(strchrnul);
 #endif
@@ -401,6 +510,34 @@ EXPORT_SYMBOL(strrchr);
  */
 char *strnchr(const char *s, size_t count, int c)
 {
+#if defined(STRINGOP_WORD_AT_A_TIME)
+	const struct word_at_a_time constants = WORD_AT_A_TIME_CONSTANTS;
+	unsigned long repeated = REPEAT_BYTE((unsigned char)c);
+
+	if (!count)
+		return NULL;
+	if (*s == (char)c)
+		return (char *)s;
+	if (!*s)
+		return NULL;
+	if (count < 16)
+		goto byte_tail;
+	while (count >= sizeof(unsigned long)) {
+		unsigned long word = get_unaligned((const unsigned long *)s);
+		unsigned long mbits, zbits, bits;
+
+		has_zero(word ^ repeated, &mbits, &constants);
+		has_zero(word, &zbits, &constants);
+		bits = mbits | zbits;
+		if (bits) {
+			unsigned long idx = find_zero(bits);
+			return s[idx] == (char)c ? (char *)s + idx : NULL;
+		}
+		s += sizeof(unsigned long);
+		count -= sizeof(unsigned long);
+	}
+byte_tail:
+#endif
 	while (count--) {
 		if (*s == (char)c)
 			return (char *)s;
@@ -415,11 +552,25 @@ EXPORT_SYMBOL(strnchr);
 #ifndef __HAVE_ARCH_STRLEN
 size_t strlen(const char *s)
 {
+#if defined(STRINGOP_WORD_AT_A_TIME)
+	const struct word_at_a_time constants = WORD_AT_A_TIME_CONSTANTS;
+	const char *p = s;
+
+	for (;;) {
+		unsigned long word = load_unaligned_zeropad(p);
+		unsigned long bits;
+
+		if (has_zero(word, &bits, &constants))
+			return p - s + find_zero(bits);
+		p += sizeof(unsigned long);
+	}
+#else
 	const char *sc;
 
 	for (sc = s; *sc != '\0'; ++sc)
 		/* nothing */;
 	return sc - s;
+#endif
 }
 EXPORT_SYMBOL(strlen);
 #endif
@@ -427,11 +578,34 @@ EXPORT_SYMBOL(strlen);
 #ifndef __HAVE_ARCH_STRNLEN
 size_t strnlen(const char *s, size_t count)
 {
+#if defined(STRINGOP_WORD_AT_A_TIME)
+	const struct word_at_a_time constants = WORD_AT_A_TIME_CONSTANTS;
+	const char *p = s;
+
+	if (count < sizeof(unsigned long))
+		goto byte_tail;
+	while (count >= sizeof(unsigned long)) {
+		unsigned long word = get_unaligned((const unsigned long *)p);
+		unsigned long bits;
+
+		if (has_zero(word, &bits, &constants))
+			return p - s + find_zero(bits);
+		p += sizeof(unsigned long);
+		count -= sizeof(unsigned long);
+	}
+byte_tail:
+	while (count && *p) {
+		p++;
+		count--;
+	}
+	return p - s;
+#else
 	const char *sc;
 
 	for (sc = s; count-- && *sc != '\0'; ++sc)
 		/* nothing */;
 	return sc - s;
+#endif
 }
 EXPORT_SYMBOL(strnlen);
 #endif
@@ -672,28 +846,35 @@ EXPORT_SYMBOL(memmove);
 #undef memcmp
 __visible int memcmp(const void *cs, const void *ct, size_t count)
 {
-	const unsigned char *su1, *su2;
-	int res = 0;
+	const unsigned char *su1 = cs, *su2 = ct;
 
-#ifdef CONFIG_HAVE_EFFICIENT_UNALIGNED_ACCESS
-	if (count >= sizeof(unsigned long)) {
-		const unsigned long *u1 = cs;
-		const unsigned long *u2 = ct;
-		do {
-			if (get_unaligned(u1) != get_unaligned(u2))
-				break;
-			u1++;
-			u2++;
-			count -= sizeof(unsigned long);
-		} while (count >= sizeof(unsigned long));
-		cs = u1;
-		ct = u2;
+#if defined(STRINGOP_WORD_AT_A_TIME)
+	if (!count)
+		return 0;
+	if (*su1 != *su2)
+		return *su1 - *su2;
+	if (count < sizeof(unsigned long))
+		goto byte_tail;
+	while (count >= sizeof(unsigned long)) {
+		unsigned long a = get_unaligned((const unsigned long *)su1);
+		unsigned long b = get_unaligned((const unsigned long *)su2);
+
+		if (a != b)
+			return stringop_word_cmp(a, b);
+		su1 += sizeof(unsigned long);
+		su2 += sizeof(unsigned long);
+		count -= sizeof(unsigned long);
 	}
+byte_tail:
 #endif
-	for (su1 = cs, su2 = ct; 0 < count; ++su1, ++su2, count--)
-		if ((res = *su1 - *su2) != 0)
-			break;
-	return res;
+	while (count) {
+		int res = *su1++ - *su2++;
+
+		if (res)
+			return res;
+		count--;
+	}
+	return 0;
 }
 EXPORT_SYMBOL(memcmp);
 #endif
@@ -706,13 +887,37 @@ EXPORT_SYMBOL(memcmp);
  * @len: size of buffers.
  *
  * The sign or magnitude of a non-zero return value has no particular
- * meaning, and architectures may implement their own more efficient bcmp(). So
- * while this particular implementation is a simple (tail) call to memcmp, do
- * not rely on anything but whether the return value is zero or non-zero.
+ * meaning, and architectures may implement their own more efficient bcmp().
  */
 int bcmp(const void *a, const void *b, size_t len)
 {
+#if defined(STRINGOP_WORD_AT_A_TIME)
+	const unsigned char *p1 = a, *p2 = b;
+
+	if (!len)
+		return 0;
+	if (*p1 != *p2)
+		return 1;
+	if (len < sizeof(unsigned long))
+		goto byte_tail;
+	while (len >= sizeof(unsigned long)) {
+		if (get_unaligned((const unsigned long *)p1) !=
+		    get_unaligned((const unsigned long *)p2))
+			return 1;
+		p1 += sizeof(unsigned long);
+		p2 += sizeof(unsigned long);
+		len -= sizeof(unsigned long);
+	}
+byte_tail:
+	while (len) {
+		if (*p1++ != *p2++)
+			return 1;
+		len--;
+	}
+	return 0;
+#else
 	return memcmp(a, b, len);
+#endif
 }
 EXPORT_SYMBOL(bcmp);
 #endif
@@ -731,13 +936,37 @@ void *memscan(void *addr, int c, size_t size)
 {
 	unsigned char *p = addr;
 
+#if defined(STRINGOP_WORD_AT_A_TIME)
+	const struct word_at_a_time constants = WORD_AT_A_TIME_CONSTANTS;
+	unsigned long repeated = REPEAT_BYTE((unsigned char)c);
+
+	if (!size)
+		return p;
+	if (*p == (unsigned char)c)
+		return p;
+	if (size < sizeof(unsigned long)) {
+		p++;
+		size--;
+		goto byte_tail;
+	}
+	while (size >= sizeof(unsigned long)) {
+		unsigned long word = get_unaligned((const unsigned long *)p);
+		unsigned long bits;
+
+		if (has_zero(word ^ repeated, &bits, &constants))
+			return p + find_zero(bits);
+		p += sizeof(unsigned long);
+		size -= sizeof(unsigned long);
+	}
+byte_tail:
+#endif
 	while (size) {
 		if (*p == (unsigned char)c)
 			return (void *)p;
 		p++;
 		size--;
 	}
-  	return (void *)p;
+	return (void *)p;
 }
 EXPORT_SYMBOL(memscan);
 #endif
@@ -805,10 +1034,34 @@ EXPORT_SYMBOL(strnstr);
 void *memchr(const void *s, int c, size_t n)
 {
 	const unsigned char *p = s;
+
+#if defined(STRINGOP_WORD_AT_A_TIME)
+	const struct word_at_a_time constants = WORD_AT_A_TIME_CONSTANTS;
+	unsigned long repeated = REPEAT_BYTE((unsigned char)c);
+
+	if (!n)
+		return NULL;
+	if (*p == (unsigned char)c)
+		return (void *)p;
+	if (n < sizeof(unsigned long)) {
+		p++;
+		n--;
+		goto byte_tail;
+	}
+	while (n >= sizeof(unsigned long)) {
+		unsigned long word = get_unaligned((const unsigned long *)p);
+		unsigned long bits;
+
+		if (has_zero(word ^ repeated, &bits, &constants))
+			return (void *)(p + find_zero(bits));
+		p += sizeof(unsigned long);
+		n -= sizeof(unsigned long);
+	}
+byte_tail:
+#endif
 	while (n-- != 0) {
-        	if ((unsigned char)c == *p++) {
+		if ((unsigned char)c == *p++)
 			return (void *)(p - 1);
-		}
 	}
 	return NULL;
 }
