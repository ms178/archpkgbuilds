# Maintainer:

pkgname=libtiff-git
pkgver=4.4.0.r104.g37237150
pkgrel=1
pkgdesc="TIFF library and utilities"
arch=('i686' 'x86_64')
url="https://libtiff.gitlab.io/libtiff/"
license=('custom')
depends=('glibc' 'libjpeg' 'xz' 'zlib')
makedepends=('git' 'freeglut' 'glu' 'jbigkit' 'mesa')
optdepends=('freeglut: for using tiffgt')
provides=('libtiff' 'libtiff=4.4.0' 'libtiff.so.5' 'libtiff.so' 'libtiffxx.so')
conflicts=('libtiff')
options=('strip')
source=("git+https://gitlab.com/libtiff/libtiff.git")
sha256sums=('SKIP')


pkgver() {
  cd "libtiff"

  git describe --long --tags | sed 's/^Release-v//;s/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

build() {
  cd "libtiff"

  autoreconf -vfi
  ./configure \
    --prefix="/usr" \
    --disable-sphinx \
    --disable-docs \
    --disable-sphinx \
    --disable-tests \
    --enable-cxx \
    --disable-old-jpeg \
    --with-x
  make
}

package() {
  cd "libtiff"

  make DESTDIR="$pkgdir" install
}
