diff --git a/scheds/rust/scx_beerland/src/bpf/main.bpf.c b/scheds/rust/scx_beerland/src/bpf/main.bpf.c
index 5687c01e4aa1b6d46cb04206b059c3695a9f1010..71e3449017dbd62b6c30f8c45e340e7242575d48 100644
--- a/scheds/rust/scx_beerland/src/bpf/main.bpf.c
+++ b/scheds/rust/scx_beerland/src/bpf/main.bpf.c
@@ -153,7 +153,7 @@ struct task_ctx *try_lookup_task_ctx(const struct task_struct *p)
 /*
  * Return true if @p still wants to run, false otherwise.
  */
-static bool is_task_queued(const struct task_struct *p)
+static __always_inline bool is_task_queued(const struct task_struct *p)
 {
 	return p->scx.flags & SCX_TASK_QUEUED;
 }
@@ -209,7 +209,7 @@ static inline void mark_cpu_busy(s32 cpu, u64 now)
 /*
  * Return true if @p can only run on a single CPU, false otherwise.
  */
-static inline bool is_pcpu_task(const struct task_struct *p)
+static __always_inline bool is_pcpu_task(const struct task_struct *p)
 {
 	return p->nr_cpus_allowed == 1 || is_migration_disabled(p);
 }
@@ -371,7 +371,7 @@ static bool should_preempt_curr(const struct task_struct *curr, s32 curr_cpu,
  * Return true if @this_cpu and @that_cpu are in the same LLC, false
  * otherwise.
  */
-static inline bool cpus_share_cache(s32 this_cpu, s32 that_cpu)
+static __always_inline bool cpus_share_cache(s32 this_cpu, s32 that_cpu)
 {
         if (this_cpu == that_cpu)
                 return true;
@@ -382,7 +382,7 @@ static inline bool cpus_share_cache(s32 this_cpu, s32 that_cpu)
 /*
  * Return true if @this_cpu is faster than @that_cpu, false otherwise.
  */
-static inline bool is_cpu_faster(s32 this_cpu, s32 that_cpu)
+static __always_inline bool is_cpu_faster(s32 this_cpu, s32 that_cpu)
 {
         if (this_cpu == that_cpu)
                 return false;
@@ -600,7 +600,7 @@ int enable_primary_cpu(struct cpu_arg *input)
 /*
  * Return the tartget @cpu if it's usable by @p, or the first CPU usable.
  */
-static s32 task_cpu(const struct task_struct *p, s32 cpu)
+static __always_inline s32 task_cpu(const struct task_struct *p, s32 cpu)
 {
 	if (bpf_cpumask_test_cpu(cpu, p->cpus_ptr))
 		return cpu;
@@ -659,7 +659,7 @@ static s32 pick_idle_cpu(struct task_struct *p, s32 prev_cpu, s32 this_cpu, u64
 /*
  * Return true if @p can run on @cpu, false otherwise.
  */
-static bool is_cpu_allowed(const struct task_struct *p, s32 cpu)
+static __always_inline bool is_cpu_allowed(const struct task_struct *p, s32 cpu)
 {
 	return p->nr_cpus_allowed == nr_cpu_ids ||
 	       bpf_cpumask_test_cpu(cpu, p->cpus_ptr);
