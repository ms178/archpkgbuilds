diff --git a/src/alloc-posix.c b/src/alloc-posix.c
index c7d6e3b..edd116b 100644
--- a/src/alloc-posix.c
+++ b/src/alloc-posix.c
@@ -9,16 +9,31 @@ terms of the MIT license. A copy of the license can be found in the file
 // mi prefixed publi definitions of various Posix, Unix, and C++ functions
 // for convenience and used when overriding these functions.
 // ------------------------------------------------------------------------
+#if defined(__GNUC__) || defined(__clang__)
+// mimalloc's public headers are not clean under -Wundef/-Wsign-conversion (e.g. `#if _MSC_VER`
+// without a prior define, and ptrdiff-to-size_t conversions in inline helpers); scope the
+// diagnostic suppression to the mimalloc includes so the rest of this file stays warning-clean.
+#pragma GCC diagnostic push
+#pragma GCC diagnostic ignored "-Wundef"
+#pragma GCC diagnostic ignored "-Wsign-conversion"
+#endif
 #include "mimalloc.h"
 #include "mimalloc/internal.h"
+#if defined(__GNUC__) || defined(__clang__)
+#pragma GCC diagnostic pop
+#endif
+
 
 // ------------------------------------------------------
 // Posix & Unix functions definitions
 // ------------------------------------------------------
 
 #include <errno.h>
-#include <string.h>  // memset
+#include <stddef.h>  // ptrdiff_t, size_t
+#include <stdint.h>  // SIZE_MAX
 #include <stdlib.h>  // getenv
+#include <string.h>  // memset
+#include <wchar.h>   // wchar_t, wcslen
 
 #ifdef _MSC_VER
 #pragma warning(disable:4996)  // getenv _wgetenv
@@ -31,6 +46,7 @@ terms of the MIT license. A copy of the license can be found in the file
 #define ENOMEM 12
 #endif
 
+
 mi_decl_nodiscard size_t mi_malloc_good_size(size_t size) mi_attr_noexcept {
   return mi_good_size(size);
 }
@@ -45,9 +61,8 @@ int mi_posix_memalign(void** p, size_t alignment, size_t size) {  // mi_attr_noe
   // Note: The spec dictates we should not modify `*p` on an error. (issue#27)
   // <http://man7.org/linux/man-pages/man3/posix_memalign.3.html>
   if (p == NULL) return EINVAL;
-  // it is required that alignment is a power of 2 and a multiple of sizeof(void*)
-  if (alignment<sizeof(void*) || !_mi_is_power_of_two(alignment)) return EINVAL;  // not a power of 2
-  void* q = mi_malloc_aligned(size, alignment);
+  if (alignment < sizeof(void*) || !mi_alignment_is_valid(alignment)) return EINVAL;  // power of 2 and a multiple of sizeof(void*)
+  void* const q = mi_malloc_aligned(size, alignment);
   if (q==NULL && size != 0) return ENOMEM;
   mi_assert_internal(_mi_is_aligned(q,alignment));
   *p = q;
@@ -102,24 +117,27 @@ mi_decl_nodiscard int mi_reallocarr( void* ptrp, size_t count, size_t size ) mi_
   mi_assert(size != 0);
   mi_assert(ptrp != NULL);
   if (ptrp == NULL || size == 0) {
-    return (errno = EINVAL);
+    errno = EINVAL;
+    return EINVAL;
   }
-  size_t total;
+  size_t total = 0;
   if mi_unlikely(mi_count_size_overflow(count, size, &total)) {
-    return (errno = EOVERFLOW);
+    errno = EOVERFLOW;
+    return EOVERFLOW;
   }
-  void** op = (void**)ptrp;
+  void** const op = (void**)ptrp;
   if (total == 0) {
     mi_free(*op);
     *op = NULL;
     return 0;
   }
-  else {
-    void* newp = mi_realloc(*op,total);
-    if (newp == NULL) { return (errno = ENOMEM); }
-    *op = newp;
-    return 0;
+  void* const newp = mi_realloc(*op,total);
+  if (newp == NULL) {
+    errno = ENOMEM;
+    return ENOMEM;
   }
+  *op = newp;
+  return 0;
 }
 
 void* mi__expand(void* p, size_t newsize) mi_attr_noexcept {  // Microsoft
@@ -130,13 +148,18 @@ void* mi__expand(void* p, size_t newsize) mi_attr_noexcept {  // Microsoft
 
 mi_decl_nodiscard mi_decl_restrict wchar_t* mi_wcsdup(const wchar_t* s) mi_attr_noexcept {
   if (s==NULL) return NULL;
-  size_t wlen;
-  for(wlen = 0; s[wlen] != 0 && wlen < PTRDIFF_MAX; wlen++) { }  // prevent overflow on wlen+1
-  size_t size; 
-  if (mi_mul_overflow(wlen+1, sizeof(wchar_t), &size) || size > PTRDIFF_MAX) return NULL;
-  wchar_t* p = (wchar_t*)mi_malloc(size);
+  // Cap the scan by the allocator limit so `(wlen+1)*sizeof(wchar_t)` cannot overflow
+  const size_t wmax = (MI_MAX_ALLOC_SIZE / sizeof(wchar_t));
+  if (wmax == 0) return NULL;
+  size_t wlen = 0;
+  while (s[wlen] != 0) {
+    if mi_unlikely(wlen >= (wmax - 1)) return NULL;
+    wlen++;
+  }
+  const size_t bytes = (wlen + 1) * sizeof(wchar_t);
+  wchar_t* p = (wchar_t*)mi_malloc(bytes);
   if (p != NULL) {
-    _mi_memcpy(p,s,size);
+    _mi_memcpy(p,s,bytes);
   }
   return p;
 }
@@ -148,15 +171,15 @@ mi_decl_nodiscard mi_decl_restrict unsigned char* mi_mbsdup(const unsigned char*
 int mi_dupenv_s(char** buf, size_t* size, const char* name) mi_attr_noexcept {
   if (size != NULL) *size = 0;
   if (buf==NULL || name==NULL) return EINVAL;
-  char* p = getenv(name);
+  char* const p = getenv(name);
   if (p==NULL) {
     *buf = NULL;
+    return 0;
   }
-  else {
-    *buf = mi_strdup(p);
-    if (*buf==NULL) return ENOMEM;
-    if (size != NULL) { *size = _mi_strlen(p) + 1; }    // cannot overflow as mi_strdup is limited to PTRDIFF_MAX
-  }
+  char* const dup = mi_strdup(p);
+  if (dup==NULL) return ENOMEM;
+  *buf = dup;
+  if (size != NULL) { *size = _mi_strlen(p) + 1; }    // cannot overflow as mi_strdup is limited to PTRDIFF_MAX
   return 0;
 }
 
@@ -168,15 +191,15 @@ int mi_wdupenv_s(wchar_t** buf, size_t* size, const wchar_t* name) mi_attr_noexc
   *buf = NULL;
   return EINVAL;
 #else
-  wchar_t* p = (wchar_t*)_wgetenv(name);
+  wchar_t* const p = (wchar_t*)_wgetenv(name);
   if (p==NULL) {
     *buf = NULL;
+    return 0;
   }
-  else {
-    *buf = mi_wcsdup(p);
-    if (*buf==NULL) return ENOMEM;
-    if (size != NULL) { *size = wcslen(p) + 1; }  // cannot overflow as wcsdup is limited to PTRDIFF_MAX
-  }
+  wchar_t* const dup = mi_wcsdup(p);
+  if (dup==NULL) return ENOMEM;
+  *buf = dup;
+  if (size != NULL) { *size = wcslen(p) + 1; }  // cannot overflow as wcsdup is limited to PTRDIFF_MAX
   return 0;
 #endif
 }
diff --git a/src/alloc.c b/src/alloc.c
index 0c542af..09561e2 100644
--- a/src/alloc.c
+++ b/src/alloc.c
@@ -9,14 +9,50 @@ terms of the MIT license. A copy of the license can be found in the file
 #define _DEFAULT_SOURCE   // for realpath() on Linux
 #endif
 
+#if defined(__GNUC__) || defined(__clang__)
+// mimalloc's public headers are not clean under -Wundef/-Wsign-conversion (e.g. `#if _MSC_VER`
+// without a prior define, and ptrdiff-to-size_t conversions in inline helpers); scope the
+// diagnostic suppression to the mimalloc includes so the rest of this file stays warning-clean.
+#pragma GCC diagnostic push
+#pragma GCC diagnostic ignored "-Wundef"
+#pragma GCC diagnostic ignored "-Wsign-conversion"
+#endif
 #include "mimalloc.h"
 #include "mimalloc/internal.h"
+#ifndef MI_TSAN
+#define MI_TSAN 0
+#endif
+#ifndef MI_THEAP_INITASNULL
+#define MI_THEAP_INITASNULL 0
+#endif
+#ifndef MI_GUARDED
+#define MI_GUARDED 0
+#endif
+#ifndef MI_PADDING
+#define MI_PADDING 0
+#endif
+#ifndef MI_ENCODE_FREELIST
+#define MI_ENCODE_FREELIST 0
+#endif
+#ifndef MI_HUGE_PAGE_ABANDON
+#define MI_HUGE_PAGE_ABANDON 0
+#endif
 #include "mimalloc/atomic.h"
 #include "mimalloc/prim-tls.h"   // _mi_prim_thread_id()
+#if defined(__GNUC__) || defined(__clang__)
+#pragma GCC diagnostic pop
+#endif
+
 
 #include <string.h>      // memset, strlen (for mi_strdup)
 #include <stdlib.h>      // malloc, abort
 
+#if defined(__GNUC__) || defined(__clang__)
+#define MI_ALLOC_PREFETCH_READ(p) do { const void* mi__pf = (p); if (mi__pf != NULL) { __builtin_prefetch(mi__pf, 0, 3); } } while (0)
+#else
+#define MI_ALLOC_PREFETCH_READ(p) do { MI_UNUSED(p); } while (0)
+#endif
+
 #define MI_IN_ALLOC_C
 #include "alloc-override.c"
 #include "free.c"
@@ -43,10 +79,12 @@ static mi_decl_forceinline void* mi_page_malloc_zero(mi_theap_t* theap, mi_page_
     return _mi_malloc_generic(theap, size, (zero ? 1 : 0), usable);
   }
   mi_assert_internal(block != NULL && _mi_ptr_page(block) == page);
-  if (usable != NULL) { *usable = mi_page_usable_block_size(page); };
+  const size_t bsize = mi_page_usable_block_size(page);
+  if (usable != NULL) { *usable = bsize; }
 
   // pop from the free list
-  page->free = mi_block_next(page, block);
+  mi_block_t* const next = mi_block_next(page, block);
+  page->free = next;
   page->used++;
   mi_assert_internal(page->free == NULL || _mi_ptr_page(page->free) == page);
   mi_assert_internal(page->block_size < MI_MAX_ALIGN_SIZE || _mi_is_aligned(block, MI_MAX_ALIGN_SIZE));
@@ -60,7 +98,6 @@ static mi_decl_forceinline void* mi_page_malloc_zero(mi_theap_t* theap, mi_page_
   // allow use of the block internally
   // note: when tracking we need to avoid ever touching the MI_PADDING since
   // that is tracked by valgrind etc. as non-accessible (through the red-zone, see `mimalloc/track.h`)
-  const size_t bsize = mi_page_usable_block_size(page);
   mi_track_mem_undefined(block, bsize);
 
   #if (MI_STAT>0)
@@ -141,8 +178,9 @@ static mi_decl_forceinline mi_decl_restrict void* mi_theap_malloc_small_zero_non
   #endif
 
   // get page in constant time, and allocate from it
-  mi_page_t* page = _mi_theap_get_free_small_page(theap, size + MI_PADDING_SIZE);
-  void* const p = mi_page_malloc_zero(theap, page, size + MI_PADDING_SIZE, zero, usable);
+  const size_t req_size = size + MI_PADDING_SIZE;
+  mi_page_t* page = _mi_theap_get_free_small_page(theap, req_size);
+  void* const p = mi_page_malloc_zero(theap, page, req_size, zero, usable);
   mi_track_malloc(p,size,zero);
 
   #if MI_DEBUG>3
@@ -409,9 +447,13 @@ void* _mi_theap_realloc_zero(mi_theap_t* theap, void* p, size_t newsize, bool ze
     }
     if mi_likely(p != NULL) {
       const size_t copysize = (newsize > size ? size : newsize);
+      if (copysize >= 256) { // for large copies, prefetch both buffers to hide DRAM/L3 latency
+        MI_ALLOC_PREFETCH_READ(p);
+        MI_ALLOC_PREFETCH_READ(newp);
+      }
       mi_track_mem_defined(p,copysize);  // _mi_useable_size may be too large for byte precise memory tracking..
       _mi_memcpy(newp, p, copysize);
-      mi_free(p); // only free the original pointer if successful  // todo: optimize since page is known?
+      mi_free(p); // only free the original pointer if successful
     }
   }
   return newp;
@@ -593,10 +635,10 @@ static size_t mi_path_max(void) {
     #ifdef _PC_PATH_MAX
     m = pathconf("/",_PC_PATH_MAX);
     #endif
-    if (m <= 0) pmax = 4096;      // guess
-    else if (m < 256) pmax = 256; // at least 256
-    else if (m > 64*1024) pmax = 64*1024;  // at most 64 KiB
-    else pmax = m;
+    if (m <= 0) pmax = 4096;              // guess
+    else if (m < 256) pmax = 256;         // at least 256
+    else if (m > 64L*1024L) pmax = 64*1024;  // at most 64 KiB
+    else pmax = (size_t)m;
     size_t expected = 0;
     mi_atomic_cas_strong_acq_rel(&path_max, &expected, pmax);
   }
@@ -905,7 +947,7 @@ mi_decl_restrict void* _mi_theap_malloc_guarded(mi_theap_t* theap, size_t size,
   #endif
   #if MI_DEBUG>3
   if (zero) {
-    mi_assert_expensive(mi_mem_is_zero(p, size));
+    mi_assert_expensive(mi_mem_is_zero(p, obj_size));
   }
   #endif
   return p;
@@ -932,6 +974,7 @@ void* _mi_externs[] = {
   (void*)&mi_zalloc_small,
   (void*)&mi_heap_malloc,
   (void*)&mi_heap_malloc_small,
+  (void*)&mi_heap_zalloc_small,
   (void*)&mi_malloc_aligned
   // (void*)&mi_theap_alloc_new,
   // (void*)&mi_theap_alloc_new_n
diff --git a/src/bitmap.c b/src/bitmap.c
index aad7a55..5bca446 100644
--- a/src/bitmap.c
+++ b/src/bitmap.c
@@ -9,11 +9,41 @@ terms of the MIT license. A copy of the license can be found in the file
 Concurrent bitmap that can set/reset sequences of bits atomically
 ---------------------------------------------------------------------------- */
 
+#if defined(__GNUC__) || defined(__clang__)
+// mimalloc's public headers are not clean under -Wundef/-Wsign-conversion (e.g. `#if _MSC_VER`
+// without a prior define, and ptrdiff-to-size_t conversions in inline helpers); scope the
+// diagnostic suppression to the mimalloc includes so the rest of this file stays warning-clean.
+#pragma GCC diagnostic push
+#pragma GCC diagnostic ignored "-Wundef"
+#pragma GCC diagnostic ignored "-Wsign-conversion"
+#endif
 #include "mimalloc.h"
 #include "mimalloc/internal.h"
+#ifndef MI_TSAN
+#define MI_TSAN 0
+#endif
+#ifndef MI_THEAP_INITASNULL
+#define MI_THEAP_INITASNULL 0
+#endif
+#ifndef MI_GUARDED
+#define MI_GUARDED 0
+#endif
+#ifndef MI_PADDING
+#define MI_PADDING 0
+#endif
+#ifndef MI_ENCODE_FREELIST
+#define MI_ENCODE_FREELIST 0
+#endif
+#ifndef MI_HUGE_PAGE_ABANDON
+#define MI_HUGE_PAGE_ABANDON 0
+#endif
 #include "mimalloc/bits.h"
 #include "mimalloc/prim.h"  // _mi_prim_thread_yield
 #include "bitmap.h"
+#if defined(__GNUC__) || defined(__clang__)
+#pragma GCC diagnostic pop
+#endif
+
 
 #ifndef MI_OPT_SIMD
 #define MI_OPT_SIMD   0
@@ -133,8 +163,7 @@ static inline void mi_bfield_atomic_clear_once_set(mi_subproc_t* subproc, _Atomi
 // statistics correctly).
 static inline bool mi_bfield_atomic_set_mask(_Atomic(mi_bfield_t)*b, mi_bfield_t mask, size_t* already_set) {
   mi_assert_internal(mask != 0);
-  mi_bfield_t old = mi_atomic_load_relaxed(b);
-  while (!mi_atomic_cas_weak_acq_rel(b, &old, old|mask)) {};  // try to atomically set the mask bits until success
+  const mi_bfield_t old = mi_atomic_or_acq_rel(b, mask);  // single lock-prefixed instruction instead of a CAS loop
   if (already_set!=NULL) { *already_set = mi_bfield_popcount(old&mask); }
   return ((old&mask) == 0);
 }
@@ -143,8 +172,7 @@ static inline bool mi_bfield_atomic_set_mask(_Atomic(mi_bfield_t)*b, mi_bfield_t
 // `all_clear` is set to `true` if the new bfield became zero.
 static inline bool mi_bfield_atomic_clear_mask(_Atomic(mi_bfield_t)*b, mi_bfield_t mask, bool* all_clear) {
   mi_assert_internal(mask != 0);
-  mi_bfield_t old = mi_atomic_load_relaxed(b);
-  while (!mi_atomic_cas_weak_acq_rel(b, &old, old&~mask)) {};  // try to atomically clear the mask bits until success
+  const mi_bfield_t old = mi_atomic_and_acq_rel(b, ~mask);  // single lock-prefixed instruction instead of a CAS loop
   if (all_clear != NULL) { *all_clear = ((old&~mask)==0); }
   return ((old&mask) == mask);
 }
@@ -221,13 +249,13 @@ static inline bool mi_bfield_atomic_try_clearX(_Atomic(mi_bfield_t)*b, bool* all
 
 // Check if a bit is set
 static inline bool mi_bfield_atomic_is_set(const _Atomic(mi_bfield_t)*b, const size_t idx) {
-  const mi_bfield_t x = mi_atomic_load_acquire(b);
+  const mi_bfield_t x = mi_atomic_load_relaxed(b);
   return ((x & mi_bfield_mask(1,idx)) != 0);
 }
 
 // Check if a bit is clear
 static inline bool mi_bfield_atomic_is_clear(const _Atomic(mi_bfield_t)*b, const size_t idx) {
-  const mi_bfield_t x = mi_atomic_load_acquire(b);
+  const mi_bfield_t x = mi_atomic_load_relaxed(b);
   return ((x & mi_bfield_mask(1, idx)) == 0);
 }
 
@@ -240,14 +268,14 @@ static inline bool mi_bfield_atomic_is_xset(mi_xset_t set, const _Atomic(mi_bfie
 // Check if all bits corresponding to a mask are set.
 static inline bool mi_bfield_atomic_is_set_mask(const _Atomic(mi_bfield_t)* b, mi_bfield_t mask) {
   mi_assert_internal(mask != 0);
-  const mi_bfield_t x = mi_atomic_load_acquire(b);
+  const mi_bfield_t x = mi_atomic_load_relaxed(b);
   return ((x & mask) == mask);
 }
 
 // Check if all bits corresponding to a mask are clear.
 static inline bool mi_bfield_atomic_is_clear_mask(const _Atomic(mi_bfield_t)* b, mi_bfield_t mask) {
   mi_assert_internal(mask != 0);
-  const mi_bfield_t x = mi_atomic_load_acquire(b);
+  const mi_bfield_t x = mi_atomic_load_relaxed(b);
   return ((x & mask) == 0);
 }
 
@@ -260,7 +288,7 @@ static inline bool mi_bfield_atomic_is_xset_mask(mi_xset_t set, const _Atomic(mi
 
 // Count bits in a mask
 static inline size_t mi_bfield_atomic_popcount_mask(_Atomic(mi_bfield_t)*b, mi_bfield_t mask) {
-  const mi_bfield_t x = mi_atomic_load_acquire(b);
+  const mi_bfield_t x = mi_atomic_load_relaxed(b);
   return mi_bfield_popcount(x & mask);
 }
 
@@ -364,12 +392,32 @@ static inline bool mi_bchunk_clear(mi_bchunk_t* chunk, size_t cidx, bool* all_cl
   return mi_bfield_atomic_clear(&chunk->bfields[i], idx, all_clear);
 }
 
+// Clear `0 < n <= MI_BFIELD_BITS`; at most two bfields, using single-instruction atomic clears.
+static inline bool mi_bchunk_clearNX(mi_bchunk_t* chunk, size_t cidx, size_t n, bool* pmaybe_all_clear) {
+  mi_assert_internal(cidx < MI_BCHUNK_BITS);
+  mi_assert_internal(n > 0 && n <= MI_BFIELD_BITS);
+  const size_t i   = cidx / MI_BFIELD_BITS;
+  const size_t idx = cidx % MI_BFIELD_BITS;
+  if mi_likely(idx + n <= MI_BFIELD_BITS) {
+    return mi_bfield_atomic_clear_mask(&chunk->bfields[i], mi_bfield_mask(n, idx), pmaybe_all_clear);
+  }
+  else {
+    const size_t m = MI_BFIELD_BITS - idx;  // bits to clear in the first field
+    mi_assert_internal(m < n);
+    mi_assert_internal(i < MI_BCHUNK_FIELDS - 1);
+    bool field1_is_clear = false;
+    const bool all_set1 = mi_bfield_atomic_clear_mask(&chunk->bfields[i], mi_bfield_mask(m, idx), &field1_is_clear);
+    bool field2_is_clear = false;
+    const bool all_set2 = mi_bfield_atomic_clear_mask(&chunk->bfields[i + 1], mi_bfield_mask(n - m, 0), &field2_is_clear);
+    if (pmaybe_all_clear != NULL) { *pmaybe_all_clear = (field1_is_clear && field2_is_clear); }
+    return (all_set1 && all_set2);
+  }
+}
+
 static inline bool mi_bchunk_clearN(mi_bchunk_t* chunk, size_t cidx, size_t n, bool* maybe_all_clear) {
   mi_assert_internal(n>0 && n <= MI_BCHUNK_BITS);
   if (n==1) return mi_bchunk_clear(chunk, cidx, maybe_all_clear);
-  // if (n==8) return mi_bchunk_clear8(chunk, cidx, maybe_all_clear);
-  // if (n==MI_BFIELD_BITS) return mi_bchunk_clearX(chunk, cidx, maybe_all_clear);
-  // todo: implement mi_bchunk_xsetNX instead of setNX
+  if (n<=MI_BFIELD_BITS) return mi_bchunk_clearNX(chunk, cidx, n, maybe_all_clear);
   return mi_bchunk_xsetNC(MI_BIT_CLEAR, chunk, cidx, n, NULL, maybe_all_clear);
 }
 
@@ -681,7 +729,7 @@ static inline bool mi_bchunk_try_find_and_clear(mi_bchunk_t* chunk, size_t* pidx
     #endif
   }
   #else
-  for (int i = 0; i < MI_BCHUNK_FIELDS; i++) {
+  for (size_t i = 0; i < MI_BCHUNK_FIELDS; i++) {
     if (mi_bchunk_try_find_and_clear_at(chunk, i, pidx)) return true;
   }
   #endif
@@ -731,7 +779,7 @@ static mi_decl_noinline bool mi_bchunk_try_find_and_clear8(mi_bchunk_t* chunk, s
     const uint64_t mask = ((uint64_t)mask2 << 32) | mask1;
     // mask is inverted, so each bit is 0xFF iff the corresponding byte has a bit set (and thus can be cleared)
     if (mask==0) return false;
-    const size_t bidx = _tzcnt_u64(mask);          // byte-idx of the byte in the chunk
+    const size_t bidx = __builtin_ctzll(mask);    // byte-idx of the byte in the chunk
     const size_t chunk_idx = bidx / 8;
     const size_t idx = (bidx % 8)*8;
     mi_assert_internal(chunk_idx < MI_BCHUNK_FIELDS);
@@ -741,13 +789,17 @@ static mi_decl_noinline bool mi_bchunk_try_find_and_clear8(mi_bchunk_t* chunk, s
       return true;
     }
     // try again
-    // note: there must be an atomic release/acquire in between or otherwise the registers may not be reloaded  }
+    // note: there must be an atomic release/acquire in between or otherwise the registers may not be reloaded
+    // we add an explicit memory barrier as older gcc compilers do not reload the registers even with an atomic acquire (issue #1206)
+    #if defined(__GNUC__)
+    __asm __volatile ("" : : "g"(chunk) : "memory");
+    #endif
   }
   #else
-    for (int i = 0; i < MI_BCHUNK_FIELDS; i++) {
-      if (mi_bchunk_try_find_and_clear8_at(chunk, i, pidx)) return true;
-    }
-    return false;
+  for (size_t i = 0; i < MI_BCHUNK_FIELDS; i++) {
+    if (mi_bchunk_try_find_and_clear8_at(chunk, i, pidx)) return true;
+  }
+  return false;
   #endif
 }
 
@@ -765,7 +817,7 @@ mi_decl_noinline static bool mi_bchunk_try_find_and_clearNX(mi_bchunk_t* chunk,
   if (n == 0 || n > MI_BFIELD_BITS) return false;
   const mi_bfield_t mask = mi_bfield_mask(n, 0);
   // for all fields in the chunk
-  for (int i = 0; i < MI_BCHUNK_FIELDS; i++) {
+  for (size_t i = 0; i < MI_BCHUNK_FIELDS; i++) {
     mi_bfield_t b0 = mi_atomic_load_relaxed(&chunk->bfields[i]);
     mi_bfield_t b = b0;
     size_t idx;
@@ -779,6 +831,7 @@ mi_decl_noinline static bool mi_bchunk_try_find_and_clearNX(mi_bchunk_t* chunk,
       if ((b&bmask) == bmask) { // found a match with all bits set, try clearing atomically
         if mi_likely(mi_bfield_atomic_try_clear_mask_of(&chunk->bfields[i], bmask, b0, NULL)) {
           *pidx = (i*MI_BFIELD_BITS) + idx;
+
           mi_assert_internal(*pidx < MI_BCHUNK_BITS);
           mi_assert_internal(*pidx + n <= MI_BCHUNK_BITS);
           return true;
@@ -904,13 +957,17 @@ static inline void mi_bchunk_clear_once_set(mi_subproc_t* subproc, mi_bchunk_t*
 // are all bits in a bitmap chunk clear?
 static inline bool mi_bchunk_all_are_clear_relaxed(mi_bchunk_t* chunk) {
   #if MI_OPT_SIMD && defined(__AVX2__) && (MI_BCHUNK_BITS==256)
-  const __m256i vec = _mm256_load_si256((const __m256i*)chunk->bfields);
-  return mi_mm256_is_zero(vec);
-  #elif MI_OPT_SIMD &&  defined(__AVX2__) && (MI_BCHUNK_BITS==512)
-  // a 64b cache-line contains the entire chunk anyway so load both at once
-  const __m256i vec1 = _mm256_load_si256((const __m256i*)chunk->bfields);
-  const __m256i vec2 = _mm256_load_si256(((const __m256i*)chunk->bfields)+1);
-  return (mi_mm256_is_zero(_mm256_or_si256(vec1,vec2)));
+  if mi_likely(__builtin_cpu_supports("avx2")) {
+    const __m256i vec = _mm256_load_si256((const __m256i*)chunk->bfields);
+    return mi_mm256_is_zero(vec);
+  }
+  #elif MI_OPT_SIMD && defined(__AVX2__) && (MI_BCHUNK_BITS==512)
+  if mi_likely(__builtin_cpu_supports("avx2")) {
+    // a 64b cache-line contains the entire chunk anyway so load both at once
+    const __m256i vec1 = _mm256_load_si256((const __m256i*)chunk->bfields);
+    const __m256i vec2 = _mm256_load_si256(((const __m256i*)chunk->bfields)+1);
+    return (mi_mm256_is_zero(_mm256_or_si256(vec1,vec2)));
+  }
   #elif MI_OPT_SIMD && (MI_BCHUNK_BITS==512) && MI_ARCH_ARM64
   const uint64x2_t v0 = vld1q_u64((uint64_t*)chunk->bfields);
   const uint64x2_t v1 = vld1q_u64((uint64_t*)chunk->bfields + 2);
@@ -918,37 +975,39 @@ static inline bool mi_bchunk_all_are_clear_relaxed(mi_bchunk_t* chunk) {
   const uint64x2_t v3 = vld1q_u64((uint64_t*)chunk->bfields + 6);
   const uint64x2_t v  = vorrq_u64(vorrq_u64(v0,v1),vorrq_u64(v2,v3));
   return (vmaxvq_u32(vreinterpretq_u32_u64(v)) == 0);
-  #else
-  for (int i = 0; i < MI_BCHUNK_FIELDS; i++) {
+  #endif
+  for (size_t i = 0; i < MI_BCHUNK_FIELDS; i++) {
     if (mi_atomic_load_relaxed(&chunk->bfields[i]) != 0) return false;
   }
   return true;
-  #endif
 }
 
 // are all bits in a bitmap chunk set?
 static inline bool mi_bchunk_all_are_set_relaxed(mi_bchunk_t* chunk) {
-#if MI_OPT_SIMD && defined(__AVX2__) && (MI_BCHUNK_BITS==256)
-  const __m256i vec = _mm256_load_si256((const __m256i*)chunk->bfields);
-  return mi_mm256_is_ones(vec);
-#elif MI_OPT_SIMD &&  defined(__AVX2__) && (MI_BCHUNK_BITS==512)
-  // a 64b cache-line contains the entire chunk anyway so load both at once
-  const __m256i vec1 = _mm256_load_si256((const __m256i*)chunk->bfields);
-  const __m256i vec2 = _mm256_load_si256(((const __m256i*)chunk->bfields)+1);
-  return (mi_mm256_is_ones(_mm256_and_si256(vec1, vec2)));
-#elif MI_OPT_SIMD && (MI_BCHUNK_BITS==512) && MI_ARCH_ARM64
+  #if MI_OPT_SIMD && defined(__AVX2__) && (MI_BCHUNK_BITS==256)
+  if mi_likely(__builtin_cpu_supports("avx2")) {
+    const __m256i vec = _mm256_load_si256((const __m256i*)chunk->bfields);
+    return mi_mm256_is_ones(vec);
+  }
+  #elif MI_OPT_SIMD && defined(__AVX2__) && (MI_BCHUNK_BITS==512)
+  if mi_likely(__builtin_cpu_supports("avx2")) {
+    // a 64b cache-line contains the entire chunk anyway so load both at once
+    const __m256i vec1 = _mm256_load_si256((const __m256i*)chunk->bfields);
+    const __m256i vec2 = _mm256_load_si256(((const __m256i*)chunk->bfields)+1);
+    return (mi_mm256_is_ones(_mm256_and_si256(vec1, vec2)));
+  }
+  #elif MI_OPT_SIMD && (MI_BCHUNK_BITS==512) && MI_ARCH_ARM64
   const uint64x2_t v0 = vld1q_u64((uint64_t*)chunk->bfields);
   const uint64x2_t v1 = vld1q_u64((uint64_t*)chunk->bfields + 2);
   const uint64x2_t v2 = vld1q_u64((uint64_t*)chunk->bfields + 4);
   const uint64x2_t v3 = vld1q_u64((uint64_t*)chunk->bfields + 6);
   const uint64x2_t v  = vandq_u64(vandq_u64(v0,v1),vandq_u64(v2,v3));
   return (vminvq_u32(vreinterpretq_u32_u64(v)) == 0xFFFFFFFFUL);
-#else
-  for (int i = 0; i < MI_BCHUNK_FIELDS; i++) {
+  #endif
+  for (size_t i = 0; i < MI_BCHUNK_FIELDS; i++) {
     if (~mi_atomic_load_relaxed(&chunk->bfields[i]) != 0) return false;
   }
   return true;
-#endif
 }
 
 
@@ -965,7 +1024,52 @@ static bool mi_bchunk_bsr(mi_bchunk_t* chunk, size_t* pidx) {
   return false;
 }
 
-static bool mi_bchunk_bsr_inv(mi_bchunk_t* chunk, size_t* pidx) {  
+static bool mi_bchunk_bsr_inv(mi_bchunk_t* chunk, size_t* pidx) {
+  // scan for the highest 0 bit in the chunk; used to allocate slices from the top
+  #if MI_OPT_SIMD && defined(__AVX2__) && (MI_BCHUNK_BITS==512)
+  if mi_likely(__builtin_cpu_supports("avx2")) {
+    const __m256i ones = _mm256_set1_epi64x(~INT64_C(0));
+    const __m256i v1 = _mm256_load_si256((const __m256i*)chunk->bfields);
+    const __m256i v2 = _mm256_load_si256(((const __m256i*)chunk->bfields) + 1);
+    // lane == all-ones => FF bytes; invert movemask => bytes FF where lane has at least one clear bit
+    const uint32_t m1 = (uint32_t)~_mm256_movemask_epi8(_mm256_cmpeq_epi64(v1, ones));
+    const uint32_t m2 = (uint32_t)~_mm256_movemask_epi8(_mm256_cmpeq_epi64(v2, ones));
+    const uint64_t m = ((uint64_t)m2 << 32) | (uint64_t)m1;
+    if (m != 0) {
+      size_t hi;
+      (void)mi_bfield_find_highest_bit((mi_bfield_t)m, &hi);
+      const size_t field_hint = hi / 8;  // 8 mask-bits per 64-bit field
+      if (field_hint < MI_BCHUNK_FIELDS) {
+        const mi_bfield_t b = mi_atomic_load_relaxed(&chunk->bfields[field_hint]);
+        size_t idx;
+        if (mi_bfield_find_highest_bit(~b, &idx)) {
+          *pidx = (field_hint * MI_BFIELD_BITS) + idx;
+          return true;
+        }
+      }
+    }
+  }
+  #elif MI_OPT_SIMD && defined(__AVX2__) && (MI_BCHUNK_BITS==256)
+  if mi_likely(__builtin_cpu_supports("avx2")) {
+    const __m256i ones = _mm256_set1_epi64x(~INT64_C(0));
+    const __m256i v1 = _mm256_load_si256((const __m256i*)chunk->bfields);
+    const uint32_t m = (uint32_t)~_mm256_movemask_epi8(_mm256_cmpeq_epi64(v1, ones));
+    if (m != 0) {
+      size_t hi;
+      (void)mi_bfield_find_highest_bit((mi_bfield_t)m, &hi);
+      const size_t field_hint = hi / 8;
+      if (field_hint < MI_BCHUNK_FIELDS) {
+        const mi_bfield_t b = mi_atomic_load_relaxed(&chunk->bfields[field_hint]);
+        size_t idx;
+        if (mi_bfield_find_highest_bit(~b, &idx)) {
+          *pidx = (field_hint * MI_BFIELD_BITS) + idx;
+          return true;
+        }
+      }
+    }
+  }
+  #endif
+  // scalar fallback (race-safe: the caller validates the claim atomically)
   for (size_t i = MI_BCHUNK_FIELDS; i > 0; ) {
     i--;
     mi_bfield_t b = mi_atomic_load_relaxed(&chunk->bfields[i]);
@@ -1018,7 +1122,6 @@ static bool mi_bitmap_chunkmap_try_clear(mi_bitmap_t* bitmap, size_t chunk_idx)
 -------------------------------------------------------------------------------- */
 
 size_t mi_bitmap_size(size_t bit_count, size_t* pchunk_count) {
-  mi_assert_internal((bit_count % MI_BCHUNK_BITS) == 0);
   bit_count = _mi_align_up(bit_count, MI_BCHUNK_BITS);
   mi_assert_internal(bit_count <= MI_BITMAP_MAX_BIT_COUNT);
   mi_assert_internal(bit_count > 0);
@@ -1918,49 +2021,48 @@ rollback:
 // Todo: This scans directly through the chunks -- we might want to consult the cmap as well?
 bool mi_bbitmap_try_find_and_clearN_(mi_bbitmap_t* bbitmap, size_t tseq, size_t n, size_t* pidx) {
   MI_UNUSED(tseq);
-  mi_assert(n > 0); if (n==0) { return false; }
+  mi_assert(n > 0);
+  if (n == 0) return false;
 
   const size_t chunk_max = mi_bbitmap_chunk_count(bbitmap);
-  const size_t chunk_req = _mi_divide_up(n, MI_BCHUNK_BITS);  // minimal number of chunks needed
-  if (chunk_max < chunk_req) { return false; }
+  const size_t full_req = n / MI_BCHUNK_BITS;                    // number of fully free chunks needed
+  const size_t tail_req = n % MI_BCHUNK_BITS;                    // remaining bits in the tail chunk
+  const size_t chunk_req = full_req + (tail_req > 0 ? 1 : 0);    // minimal number of chunks needed
+  if (chunk_max < chunk_req) return false;
 
-  // iterate through the chunks
+  // iterate through the chunks: we first scan (non-atomically) for a range of free
+  // chunks and only then try to claim it atomically to avoid expensive failed clears
   size_t chunk_idx = 0;
-  while (chunk_idx <= chunk_max - chunk_req)
-  {
-    size_t count = 0;  // chunk count
-    do {
-      mi_assert_internal(chunk_idx + count < chunk_max);
+  while (chunk_idx <= chunk_max - chunk_req) {
+    size_t count = 0;  // number of fully free chunks found
+    while (count < full_req) {
       mi_bchunk_t* const chunk = &bbitmap->chunks[chunk_idx + count];
-      if (!mi_bchunk_all_are_set_relaxed(chunk)) {
-        break;
-      }
-      else {
-        count++;
-      }
+      if (!mi_bchunk_all_are_set_relaxed(chunk)) break;
+      count++;
     }
-    while (count < chunk_req);
-
-    // did we find a suitable range?
-    if (count == chunk_req) {
-      // now try to claim it!      
-      if (mi_bchunk_try_clearN_(bbitmap, chunk_idx, n)) {
-        *pidx = (chunk_idx * MI_BCHUNK_BITS);
-        for (size_t i = 0; i < count; i++) {
-          mi_bbitmap_set_chunk_bin(bbitmap, chunk_idx + i, MI_CBIN_HUGE);
-        }
-        mi_assert_internal(*pidx + n <= mi_bbitmap_max_bits(bbitmap));
-        return true;
+    if (count < full_req) {
+      chunk_idx += count + 1;
+      continue;
+    }
+    // the tail chunk must have its first `tail_req` bits set
+    if (tail_req > 0) {
+      mi_bchunk_t* const tail_chunk = &bbitmap->chunks[chunk_idx + full_req];
+      if (!mi_bchunk_is_xsetN(MI_BIT_SET, tail_chunk, 0, tail_req)) {
+        chunk_idx += full_req + 1;
+        continue;
       }
-      else {
-        // contended: we reset count to retry from the first 
-        // (we still skip the first chunk to guarantee progress)
-        count = 0;
+    }
+    // claim it atomically
+    if (mi_bchunk_try_clearN_(bbitmap, chunk_idx, n)) {
+      *pidx = (chunk_idx * MI_BCHUNK_BITS);
+      for (size_t i = 0; i < chunk_req; i++) {
+        mi_bbitmap_set_chunk_bin(bbitmap, chunk_idx + i, MI_CBIN_HUGE);
       }
+      mi_assert_internal(*pidx + n <= mi_bbitmap_max_bits(bbitmap));
+      return true;
     }
-
-    // keep searching but skip the scanned range
-    chunk_idx += count+1;
+    // contended: retry from the next chunk to guarantee progress
+    chunk_idx++;
   }
   return false;
 }
diff --git a/src/free.c b/src/free.c
index 0c830c6..e758f1e 100644
--- a/src/free.c
+++ b/src/free.c
@@ -7,9 +7,42 @@ terms of the MIT license. A copy of the license can be found in the file
 #if !defined(MI_IN_ALLOC_C)
 #error "this file should be included from 'alloc.c' (so aliases can work from alloc-override)"
 // add includes help an IDE
+#if defined(__GNUC__) || defined(__clang__)
+// mimalloc's public headers are not clean under -Wundef/-Wsign-conversion (e.g. `#if _MSC_VER`
+// without a prior define, and ptrdiff-to-size_t conversions in inline helpers); scope the
+// diagnostic suppression to the mimalloc includes so the rest of this file stays warning-clean.
+#pragma GCC diagnostic push
+#pragma GCC diagnostic ignored "-Wundef"
+#pragma GCC diagnostic ignored "-Wsign-conversion"
+#endif
 #include "mimalloc.h"
 #include "mimalloc/internal.h"
+#ifndef MI_TSAN
+#define MI_TSAN 0
+#endif
+#ifndef MI_THEAP_INITASNULL
+#define MI_THEAP_INITASNULL 0
+#endif
+#ifndef MI_GUARDED
+#define MI_GUARDED 0
+#endif
+#ifndef MI_PADDING
+#define MI_PADDING 0
+#endif
+#ifndef MI_ENCODE_FREELIST
+#define MI_ENCODE_FREELIST 0
+#endif
+#ifndef MI_CHECK_DOUBLE_FREE
+#define MI_CHECK_DOUBLE_FREE 0
+#endif
+#ifndef MI_HUGE_PAGE_ABANDON
+#define MI_HUGE_PAGE_ABANDON 0
+#endif
 #include "mimalloc/prim-tls.h"   // _mi_prim_thread_id()
+#if defined(__GNUC__) || defined(__clang__)
+#pragma GCC diagnostic pop
+#endif
+
 #endif
 
 // forward declarations
@@ -27,20 +60,29 @@ static void   mi_stat_free(const mi_page_t* page, const mi_block_t* block);
 // fast path written carefully to prevent spilling on the stack
 static inline void mi_free_block_local(mi_page_t* page, mi_block_t* block, bool was_guarded, bool track_stats, bool check_full)
 {
-  MI_UNUSED(was_guarded);
   // checks  
   if mi_unlikely(mi_check_is_double_free(page, block)) return;
   if (!was_guarded) { mi_check_padding(page, block); }
-  if (track_stats) { mi_stat_free(page, block); }
+  if (track_stats) {
+    mi_stat_free(page, block);
+    #if MI_TRACK_ENABLED
+    const size_t usize = mi_page_usable_size_of(page, block, was_guarded);
+    mi_track_free_size(block, usize);
+    #else
+    mi_track_free_size(block, 0);
+    #endif
+  }
   #if (MI_DEBUG>0) && !MI_TRACK_ENABLED  && !MI_TSAN
-  size_t dbgsize = mi_page_block_size(page);
-  if (dbgsize > 1*MI_MiB) { dbgsize = 1*MI_MiB; }
-  _mi_memset_aligned(block, MI_DEBUG_FREED, dbgsize);  
+  if (!was_guarded) { // never memset a guarded block: the guard page directly follows the block
+    size_t dbgsize = mi_page_block_size(page);
+    if (dbgsize > 1*MI_MiB) { dbgsize = 1*MI_MiB; }
+    _mi_memset_aligned(block, MI_DEBUG_FREED, dbgsize);
+  }
   #endif
-  if (track_stats) { mi_track_free_size(block, mi_page_usable_size_of(page, block, was_guarded)); } // faster then mi_usable_size as we already know the page and that p is unaligned
 
   // actual free: push on the local free list
-  mi_block_set_next(page, block, page->local_free);
+  mi_block_t* const local_free = page->local_free;
+  mi_block_set_next(page, block, local_free);
   page->local_free = block;
   #if defined(__clang__) && defined(__aarch64__)
   if mi_unlikely(page->used-- == 1)   // better code on arm64 than using `--page->used == 0`
@@ -105,7 +147,7 @@ static inline void mi_free_block_mt(mi_page_t* page, mi_block_t* block, bool was
 mi_block_t* _mi_page_ptr_unalign(const mi_page_t* page, const void* p) {
   mi_assert_internal(page!=NULL && p!=NULL);
 
-  const size_t diff = (uint8_t*)p - mi_page_start(page);
+  const size_t diff = (size_t)((uintptr_t)p - (uintptr_t)mi_page_start(page));
   const size_t block_size = mi_page_block_size(page);
   const size_t adjust = (_mi_is_power_of_two(block_size) ? diff & (block_size - 1) : diff % block_size);
   return (mi_block_t*)((uintptr_t)p - adjust);
@@ -422,7 +464,7 @@ static size_t mi_decl_noinline mi_page_usable_aligned_size_of(const mi_page_t* p
   const bool is_guarded = mi_block_ptr_is_guarded(block,p);
   const size_t size = mi_page_usable_size_of(page, block, is_guarded);
   mi_assert_internal((void*)p >= (void*)block);
-  const size_t adjust = (uint8_t*)p - (uint8_t*)block;
+  const size_t adjust = (size_t)((uintptr_t)p - (uintptr_t)block);
   mi_assert_internal(adjust <= size);
   const size_t aligned_size = (adjust <= size ? size - adjust : 0);  // size can be zero if the padding is corrupted
   return aligned_size;
diff --git a/src/page.c b/src/page.c
index 31a610b..c17679d 100644
--- a/src/page.c
+++ b/src/page.c
@@ -11,11 +11,41 @@ terms of the MIT license. A copy of the license can be found in the file
   exported is `mi_malloc_generic`.
 ----------------------------------------------------------- */
 
+#if defined(__GNUC__) || defined(__clang__)
+// mimalloc's public headers are not clean under -Wundef/-Wsign-conversion (e.g. `#if _MSC_VER`
+// without a prior define, and ptrdiff-to-size_t conversions in inline helpers); scope the
+// diagnostic suppression to the mimalloc includes so the rest of this file stays warning-clean.
+#pragma GCC diagnostic push
+#pragma GCC diagnostic ignored "-Wundef"
+#pragma GCC diagnostic ignored "-Wsign-conversion"
+#endif
 #include "mimalloc.h"
 #include "mimalloc/internal.h"
+#ifndef MI_TSAN
+#define MI_TSAN 0
+#endif
+#ifndef MI_THEAP_INITASNULL
+#define MI_THEAP_INITASNULL 0
+#endif
+#ifndef MI_GUARDED
+#define MI_GUARDED 0
+#endif
+#ifndef MI_PADDING
+#define MI_PADDING 0
+#endif
+#ifndef MI_ENCODE_FREELIST
+#define MI_ENCODE_FREELIST 0
+#endif
+#ifndef MI_HUGE_PAGE_ABANDON
+#define MI_HUGE_PAGE_ABANDON 0
+#endif
 #include "mimalloc/atomic.h"
 #include "mimalloc/prim.h"
 #include "mimalloc/prim-tls.h"
+#if defined(__GNUC__) || defined(__clang__)
+#pragma GCC diagnostic pop
+#endif
+
 
 /* -----------------------------------------------------------
   Definition of page queues for each block size
@@ -25,6 +55,16 @@ terms of the MIT license. A copy of the license can be found in the file
 #include "page-queue.c"
 #undef MI_IN_PAGE_C
 
+// software prefetch for linked structures: hide L2/L3 latency (~17-50 cycles on Raptor Lake)
+static mi_decl_forceinline void mi_prefetch_read(const void* p) {
+#if defined(__GNUC__) || defined(__clang__)
+  if (p != NULL) {
+    __builtin_prefetch(p, 0, 3);
+  }
+#else
+  MI_UNUSED(p);
+#endif
+}
 
 /* -----------------------------------------------------------
   Page helpers
@@ -179,7 +219,7 @@ static void mi_page_thread_collect_to_local(mi_page_t* page, mi_block_t* head)
   // update counts now
   mi_assert_internal(count <= UINT16_MAX);
   mi_assert_internal(page->used >= (uint16_t)count);
-  page->used = page->used - (uint16_t)count;
+  page->used = (uint16_t)(page->used - (uint16_t)count);
 }
 
 // Collect the local `thread_free` list using an atomic exchange.
@@ -201,7 +241,7 @@ static void mi_page_thread_free_collect(mi_page_t* page)
 }
 
 // returns `true` if after collection `mi_page_immediate_available` is true.
-static bool mi_page_free_quick_collect(mi_page_t* page) {
+static mi_decl_forceinline bool mi_page_free_quick_collect(mi_page_t* page) {
   if (page->free != NULL) return true;
   if (page->local_free == NULL) return false;
   // move local_free to free
@@ -443,9 +483,9 @@ void _mi_page_retire(mi_page_t* page) mi_attr_noexcept {
       #if MI_STAT>0
       mi_theap_stat_counter_increase(theap, pages_retire, 1);
       #endif
-      page->retire_expire = (bsize <= MI_SMALL_MAX_OBJ_SIZE ? MI_RETIRE_CYCLES : MI_RETIRE_CYCLES/4);
+      page->retire_expire = (uint8_t)(bsize <= MI_SMALL_MAX_OBJ_SIZE ? MI_RETIRE_CYCLES : MI_RETIRE_CYCLES/4);
       mi_assert_internal(pq >= theap->pages);
-      const size_t index = pq - theap->pages;
+      const size_t index = (size_t)(pq - theap->pages);
       mi_assert_internal(index < MI_BIN_FULL && index < MI_BIN_HUGE);
       if (index < theap->page_retired_min) theap->page_retired_min = index;
       if (index > theap->page_retired_max) theap->page_retired_max = index;
@@ -587,17 +627,17 @@ static mi_decl_noinline void mi_page_free_list_extend( mi_page_t* const page, co
   void* const page_area = mi_page_start(page);
 
   mi_block_t* const start = mi_page_block_at(page, page_area, bsize, page->capacity);
-
-  // initialize a sequential free list
-  mi_block_t* const last = mi_page_block_at(page, page_area, bsize, page->capacity + extend - 1);
   mi_block_t* block = start;
-  while(block <= last) {
-    mi_block_t* next = (mi_block_t*)((uint8_t*)block + bsize);
-    mi_block_set_next(page,block,next);
+
+  // initialize a sequential free list (count-based loop: avoids a pointer-comparison
+  // trip and lets the compiler use a simple induction variable)
+  for (size_t i = 1; i < extend; i++) {
+    mi_block_t* const next = (mi_block_t*)((uint8_t*)block + bsize);
+    mi_block_set_next(page, block, next);
     block = next;
   }
   // prepend to free list (usually `NULL`)
-  mi_block_set_next(page, last, page->free);
+  mi_block_set_next(page, block, page->free);
   page->free = start;
 }
 
@@ -686,7 +726,7 @@ static bool mi_page_extend_free(mi_theap_t* theap, mi_page_t* page) {
     mi_page_free_list_extend_secure(theap, page, bsize, extend);
   }
   // enable the new free list
-  page->capacity += (uint16_t)extend;
+  page->capacity = (uint16_t)(page->capacity + (uint16_t)extend);
   #if MI_STAT>0
   mi_theap_stat_increase(theap, page_committed, extend * bsize);
   #endif
@@ -753,13 +793,15 @@ static mi_decl_noinline mi_page_t* mi_page_queue_find_free_ex(mi_theap_t* theap,
   // search through the pages in "next fit" order
   size_t count = 0;
   long candidate_limit = 0;          // we reset this on the first candidate to limit the search
+  const long max_candidates = _mi_option_get_fast(mi_option_page_max_candidates);
   long page_full_retain = (pq->block_size > MI_SMALL_MAX_OBJ_SIZE ? 0 : theap->page_full_retain); // only retain small pages
   mi_page_t* page_candidate = NULL;  // a page with free space
   mi_page_t* page = pq->first;
 
   while (page != NULL)
   {
-    mi_page_t* next = page->next; // remember next (as this page can move to another queue)
+    mi_page_t* const next = page->next; // remember next (as this page can move to another queue)
+    mi_prefetch_read(next);             // prefetch the next page metadata (L2 ~17 cycles) while processing this page
     count++;
     candidate_limit--;
 
@@ -787,7 +829,7 @@ static mi_decl_noinline mi_page_t* mi_page_queue_find_free_ex(mi_theap_t* theap,
       // we prefer non-expandable pages with high usage as candidates (to reduce commit, and increase chances of free-ing up pages)
       if (page_candidate == NULL) {
         page_candidate = page;
-        candidate_limit = _mi_option_get_fast(mi_option_page_max_candidates);
+        candidate_limit = max_candidates;
       }
       else if (mi_page_all_free(page_candidate)) {
         _mi_page_free(page_candidate, pq);
@@ -870,6 +912,7 @@ static mi_page_t* mi_find_free_page(mi_theap_t* theap, mi_page_queue_t* pq) {
   // check the first page: we even do this with candidate search or otherwise we re-search every time
   mi_page_t* page = pq->first;
   if mi_likely(page != NULL && mi_page_free_quick_collect(page)) {
+    mi_prefetch_read(page->free); // prefetch the first free block so the pop in `mi_page_malloc_zero` hits L1
     #if (MI_SECURE>=2) // in secure mode, we extend half the time to increase randomness
     if (page->capacity < page->reserved && ((_mi_theap_random_next(theap) & 1) == 1)) {
       (void)mi_page_extend_free(theap, page);  // ok if this fails
@@ -955,8 +998,8 @@ static mi_page_t* mi_find_page(mi_theap_t* theap, size_t size, size_t huge_align
     return NULL;
   }
   mi_page_queue_t* pq = mi_page_queue(theap, (huge_alignment > 0 ? MI_LARGE_MAX_OBJ_SIZE+1 : size));
-  // huge allocation?
-  if mi_unlikely(mi_page_queue_is_huge(pq) || req_size > MI_MAX_ALLOC_SIZE) {
+  // huge allocation? (note: `req_size > MI_MAX_ALLOC_SIZE` was already checked above)
+  if mi_unlikely(mi_page_queue_is_huge(pq)) {
     return mi_huge_page_alloc(theap,size,huge_alignment,pq);
   }
   else {
@@ -978,7 +1021,7 @@ static mi_page_t* mi_find_page(mi_theap_t* theap, size_t size, size_t huge_align
 void* _mi_malloc_generic(mi_theap_t* theap, size_t size, size_t zero_huge_alignment, size_t* usable) mi_attr_noexcept
 {
   const bool zero = ((zero_huge_alignment & 1) != 0);
-  const size_t huge_alignment = (zero_huge_alignment & ~1);
+  const size_t huge_alignment = (zero_huge_alignment & ~(size_t)1);
 
   #if !MI_THEAP_INITASNULL
   mi_assert_internal(theap != NULL);
diff --git a/src/threadlocal.c b/src/threadlocal.c
index ca94307..c7246ef 100644
--- a/src/threadlocal.c
+++ b/src/threadlocal.c
@@ -11,9 +11,21 @@ Unlike most OS native implementations there is no limit on the number
 that can be allocated.
 -----------------------------------------------------------------------------*/
 
+#if defined(__GNUC__) || defined(__clang__)
+// mimalloc's public headers are not clean under -Wundef/-Wsign-conversion (e.g. `#if _MSC_VER`
+// without a prior define, and ptrdiff-to-size_t conversions in inline helpers); scope the
+// diagnostic suppression to the mimalloc includes so the rest of this file stays warning-clean.
+#pragma GCC diagnostic push
+#pragma GCC diagnostic ignored "-Wundef"
+#pragma GCC diagnostic ignored "-Wsign-conversion"
+#endif
 #include "mimalloc.h"
 #include "mimalloc/internal.h"
 #include "mimalloc/prim.h"
+#if defined(__GNUC__) || defined(__clang__)
+#pragma GCC diagnostic pop
+#endif
+
 
 /* -----------------------------------------------------------
   Each thread can have (a dynamically expanding) array of
@@ -79,6 +91,7 @@ mi_define_thread_local(void*, mi_slot_fast, NULL)
 #endif
 #define MI_TLS_IDX_MASK     ((MI_ZU(1)<<MI_TLS_IDX_BITS)-1)
 #define MI_TLS_IDX_MAX      MI_TLS_IDX_MASK
+#define MI_TLS_COUNT_MAX    (MI_TLS_IDX_MAX + 1)
 #define MI_TLS_VERSION_MAX  ((MI_ZU(1)<<(MI_SIZE_BITS - MI_TLS_IDX_BITS))-1)
 
 
@@ -99,26 +112,51 @@ static mi_thread_local_t mi_key_create( size_t index, size_t version ) {
 }
 
 
-// dynamically reallocate the thread local slots when needed
-static mi_thread_locals_t* mi_thread_locals_expand(size_t least_idx) {
-  mi_thread_locals_t* tls_old = mi_thread_locals_get();
-  const size_t count_old = tls_old->count;
+// compute the allocation size for `count` slots without overflow
+static bool mi_thread_locals_alloc_size(size_t count, size_t* size_out) {
+  const size_t base = offsetof(mi_thread_locals_t, slots);
+  if (count > ((SIZE_MAX - base) / sizeof(mi_tls_slot_t))) return false;
+  *size_out = base + (count * sizeof(mi_tls_slot_t));
+  return true;
+}
+
+// grow policy for the thread-local slots array (clamped and overflow-safe)
+static size_t mi_thread_locals_next_count(size_t count_old, size_t least_idx) {
+  mi_assert_internal(least_idx <= MI_TLS_IDX_MAX);
+  const size_t need = least_idx + 1;
   size_t count;
-  if (count_old==0) {
-    tls_old = NULL; // so we allocate fresh from mi_thread_locals_empty
+  if (count_old == 0) {
     count = 16;     // start with 16 slots
   }
   else if (count_old >= 1024) {
-    count = count_old + 1024;  // at some point increase linearly
+    const size_t add = 1024;
+    count = (count_old > (SIZE_MAX - add) ? SIZE_MAX : (count_old + add));  // linear growth
   }
   else {
-    count = 2*count_old;       // and double initially
+    count = (count_old > (SIZE_MAX / 2) ? SIZE_MAX : (2 * count_old));      // double initially
   }
-  if (count <= least_idx) {
-    count = least_idx + 1;
+  if (count < need) count = need;
+  if (count > MI_TLS_COUNT_MAX) count = MI_TLS_COUNT_MAX;
+  return count;
+}
+
+// dynamically reallocate the thread local slots when needed
+static mi_thread_locals_t* mi_thread_locals_expand(size_t least_idx) {
+  if mi_unlikely(least_idx > MI_TLS_IDX_MAX) return NULL;
+
+  mi_thread_locals_t* tls_old = mi_thread_locals_get();
+  const size_t count_old = tls_old->count;
+  if (count_old == 0) {
+    tls_old = NULL; // so we allocate fresh from mi_thread_locals_empty
   }
-  if (count > MI_TLS_IDX_MAX) { return NULL; }  // too large
-  mi_thread_locals_t* tls = (mi_thread_locals_t*)mi_rezalloc(tls_old, sizeof(mi_thread_locals_t) + count*sizeof(mi_tls_slot_t));
+
+  const size_t count = mi_thread_locals_next_count(count_old, least_idx);
+  if (count == 0) return NULL;  // defensive; cannot happen with the growth policy above
+
+  size_t alloc_size = 0;
+  if mi_unlikely(!mi_thread_locals_alloc_size(count, &alloc_size)) return NULL;
+
+  mi_thread_locals_t* tls = (mi_thread_locals_t*)mi_rezalloc(tls_old, alloc_size);
   if mi_unlikely(tls==NULL) return NULL;
   tls->count = count;
   mi_thread_locals_set(tls);
@@ -127,6 +165,7 @@ static mi_thread_locals_t* mi_thread_locals_expand(size_t least_idx) {
 
 static mi_decl_noinline bool mi_thread_local_set_expand( mi_thread_local_t key, void* val ) {
   if (val==NULL) return true;
+  if mi_unlikely(key == 0) return false;
   const size_t idx = mi_key_index(key);
   mi_thread_locals_t* tls = mi_thread_locals_expand(idx);
   if (tls==NULL) {
@@ -143,9 +182,9 @@ static mi_decl_noinline bool mi_thread_local_set_expand( mi_thread_local_t key,
 // set a tls slot; returns `true` if successful.
 // Can return `false` if we could not reallocate the slots array.
 static mi_decl_noinline bool mi_thread_local_set_regular( mi_thread_local_t key, void* val ) {
+  if mi_unlikely(key == 0) return false;
   mi_thread_locals_t* tls = mi_thread_locals_get();
   mi_assert_internal(tls!=NULL);
-  mi_assert_internal(key!=0);
   const size_t idx = mi_key_index(key);
   if mi_likely(idx < tls->count) {
     tls->slots[idx].value = val;
@@ -158,7 +197,7 @@ static mi_decl_noinline bool mi_thread_local_set_regular( mi_thread_local_t key,
 }
 
 bool _mi_thread_local_set( mi_thread_local_t key, void* val ) {
-  mi_assert_internal(key!=0);
+  if mi_unlikely(key == 0) return false;
   if (key == mi_thread_local_key_fast) {
     return mi_slot_fast_set(val);
   }
@@ -169,7 +208,7 @@ bool _mi_thread_local_set( mi_thread_local_t key, void* val ) {
 
 // get a tls slot value
 static mi_decl_noinline void* mi_thread_local_get_regular( mi_thread_local_t key ) {
-  mi_assert_internal(key!=0);
+  if mi_unlikely(key == 0) return NULL;
   const mi_thread_locals_t* const tls = mi_thread_locals_get();
   mi_assert_internal(tls!=NULL);
   const size_t idx = mi_key_index(key);
@@ -183,7 +222,7 @@ static mi_decl_noinline void* mi_thread_local_get_regular( mi_thread_local_t key
 
 // get a thread local value
 void* _mi_thread_local_get( mi_thread_local_t key ) {
-  mi_assert_internal(key!=0);
+  if mi_unlikely(key == 0) return NULL;
   if mi_likely(key == mi_thread_local_key_fast) {
     return mi_slot_fast_get();
   }
@@ -212,9 +251,12 @@ static mi_lock_t    mi_thread_locals_lock;    // we need a lock in order to re-a
 static mi_bitmap_t* mi_thread_locals_free;    // reuse an arena bitmap to track which slots were assigned (1=free, 0=in-use)
 static mi_memid_t   mi_thread_locals_memid;   // provenance of mi_thread_locals_free
 static size_t       mi_thread_locals_version; // version to be able to reuse slots safely
+static size_t       mi_thread_locals_search_start; // hint to reduce bitmap scans and spread threads over slots
 
 void _mi_thread_locals_init(void) {
   mi_lock_init(&mi_thread_locals_lock);
+  mi_thread_locals_version = 0;
+  mi_thread_locals_search_start = 0;
 }
 
 void _mi_thread_locals_done(void) {
@@ -224,7 +266,10 @@ void _mi_thread_locals_done(void) {
       const size_t slots_count = mi_bitmap_max_bits(slots);
       const size_t slots_size  = mi_bitmap_size(slots_count,NULL);
       _mi_meta_free(_mi_subproc_main(), slots,slots_size,mi_thread_locals_memid);
+      mi_thread_locals_free = NULL;
     }
+    mi_thread_locals_version = 0;
+    mi_thread_locals_search_start = 0;
   }
   mi_lock_done(&mi_thread_locals_lock);
   mi_thread_locals_delete();
@@ -242,24 +287,35 @@ static bool mi_thread_local_claim_fun(size_t _slice_index, mi_arena_t* _arena, b
 // (so if we reuse a slot it will be returning NULL initially when a thread tries to get it)
 static mi_thread_local_t mi_thread_local_claim(void) {
   size_t idx = 0;
-  if (mi_thread_locals_free != NULL && mi_bitmap_try_find_and_claim(mi_thread_locals_free,0,&idx,&mi_thread_local_claim_fun,NULL)) {
-    mi_thread_locals_version++;
-    if (mi_thread_locals_version >= MI_TLS_VERSION_MAX) { mi_thread_locals_version = 1; }  /* wrap around the version */
-    return mi_key_create( idx, mi_thread_locals_version);
-  }
-  else {
-    return 0;
+  mi_bitmap_t* const slots = mi_thread_locals_free;
+  if (slots == NULL) return 0;
+
+  // start the bitmap scan at the hint so that threads spread over the slots
+  // (reducing contention on a single bitmap field); fall back to scanning from 0
+  bool claimed = mi_bitmap_try_find_and_claim(slots, mi_thread_locals_search_start, &idx, &mi_thread_local_claim_fun, NULL);
+  if (!claimed && mi_thread_locals_search_start != 0) {
+    claimed = mi_bitmap_try_find_and_claim(slots, 0, &idx, &mi_thread_local_claim_fun, NULL);
   }
+  if (!claimed) return 0;
+
+  const size_t max_bits = mi_bitmap_max_bits(slots);
+  mi_thread_locals_search_start = (idx + 1 < max_bits ? idx + 1 : 0);
+
+  size_t version = mi_thread_locals_version + 1;
+  if (version == 0 || version > MI_TLS_VERSION_MAX) { version = 1; }  /* wrap around the version */
+  mi_thread_locals_version = version;
+  return mi_key_create( idx, version);
 }
 
 static bool mi_thread_local_create_expand(void) {
   mi_bitmap_t* const slots = mi_thread_locals_free;
-  // 1024 bits at a time
   const size_t oldcount = (slots==NULL ? 0 : mi_bitmap_max_bits(slots));
-  const size_t newcount = 1024 + oldcount;
-  if (newcount > MI_TLS_IDX_MAX) { return false; }
+  if (oldcount >= MI_TLS_COUNT_MAX) return false;
+  // 1024 bits at a time (clamped so we never exceed the index space)
+  const size_t grow = ((MI_TLS_COUNT_MAX - oldcount) >= 1024 ? 1024 : (MI_TLS_COUNT_MAX - oldcount));
+  if (grow == 0) return false;
+  const size_t newcount = oldcount + grow;
   const size_t newsize = mi_bitmap_size( newcount, NULL );
-  // mi_bitmap_t* newslots = (mi_bitmap_t*)mi_zalloc_aligned(newsize, MI_BCHUNK_SIZE);
   mi_memid_t memid;
   mi_bitmap_t* newslots = (mi_bitmap_t*)_mi_meta_zalloc(_mi_subproc_main(), newsize, &memid); // always allocate thread locals in the main subprocess
   mi_assert_internal(_mi_is_aligned(newslots,MI_BCHUNK_SIZE));
@@ -271,9 +327,12 @@ static bool mi_thread_local_create_expand(void) {
     _mi_meta_free(_mi_subproc_main(), slots,oldsize,mi_thread_locals_memid);
   }
   mi_bitmap_init(newslots, newcount, true /* pretend already zero'd so we do not zero out the copied old entries */);
-  mi_bitmap_unsafe_setN(newslots, oldcount, newcount - oldcount);  /* set the new expanded slots as available */
+  mi_bitmap_unsafe_setN(newslots, oldcount, grow);  /* set the new expanded slots as available */
   mi_thread_locals_free = newslots;
   mi_thread_locals_memid = memid;
+  if (mi_thread_locals_search_start >= newcount) {
+    mi_thread_locals_search_start = oldcount;
+  }
   return true;
 }
 
@@ -302,6 +361,10 @@ void _mi_thread_local_free(mi_thread_local_t key) {
     mi_bitmap_t* const slots = mi_thread_locals_free;
     if (slots!=NULL && idx < mi_bitmap_max_bits(slots)) {
       mi_bitmap_set(slots,idx);
+      // lower the search hint so the freed slot is reused before scanning far ahead
+      if (idx < mi_thread_locals_search_start) {
+        mi_thread_locals_search_start = idx;
+      }
     }
   }
 }
