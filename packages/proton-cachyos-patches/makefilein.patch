--- a/make/rules-common.mk	2026-05-10 17:00:29.926539655 +0200
+++ b/make/rules-common.mk	2026-05-10 17:05:25.631277253 +0200
@@ -3,6 +3,16 @@
 #   $(2): uppercase package name
 #   $(3): build target arch
 #   $(4): build target os
+
+# Prefer distro mingw toolchain over accidental PATH pollution (e.g. /opt/llvm-mingw).
+# For non-mingw tools, return the original name unchanged.
+resolve-mingw-tool = $(if $(findstring w64-mingw32,$(1)),$(if $(wildcard /usr/bin/$(1)),/usr/bin/$(1),$(1)),$(1))
+# Ensure host system toolchain precedence for prefixed tool lookups.
+# This prevents accidental pickup of /opt/llvm-mingw tools.
+SYSTEM_BIN := /usr/bin
+# Flags that are valid/beneficial for GCC builds but break or are noisy with clang.
+GCC_ONLY_CFLAGS = -fvect-cost-model=cheap -fipa-pta -Wl,--exclude-libs=libstdc++.a
+
 define create-rules-common
 ifneq ($$(findstring $(3)-$(4),$$(ARCHS)),)
 $(2)_$(3)_OBJ := $$(OBJ)/obj-$(1)-$(3)
@@ -61,19 +71,30 @@ $$(OBJ)/.$(1)-$(3)-dist:
 	cd $$($(2)_$(3)_LIBDIR) && find -type f -printf '$$(DST_LIBDIR)/%h\0' | sort -z | uniq -z | xargs $(--verbose?) -0 -r -P$$(J) mkdir -p
 	cd $$($(2)_$(3)_LIBDIR) && find -type l -printf '%p\0$$(DST_LIBDIR)/%p\0' | xargs $(--verbose?) -0 -r -P$$(J) -n2 cp -a
 ifneq ($(UNSTRIPPED_BUILD),)
-	cd $$($(2)_$(3)_LIBDIR) && find -type f -not '(' -iname '*.pc' -or -iname '*.cmake' -or -iname '*.a' -or -iname '*.la' -or -iname '*.def' -or -iname '*.h' ')' \
-	    -printf '--only-keep-debug\0%p\0$$(DST_LIBDIR)/%p.debug\0' | \
-	    xargs $(--verbose?) -0 -r -P$$(J) -n3 $(OBJCOPY) $(OBJCOPY_FLAGS)
-	cd $$($(2)_$(3)_LIBDIR) && find -type f -not '(' -iname '*.pc' -or -iname '*.cmake' -or -iname '*.a' -or -iname '*.la' -or -iname '*.def' -or -iname '*.h' ')' \
-	    -printf '--add-gnu-debuglink=$$(DST_LIBDIR)/%p.debug\0--strip-debug\0%p\0$$(DST_LIBDIR)/%p\0' | \
-	    xargs $(--verbose?) -0 -r -P$$(J) -n4 $(OBJCOPY) $(OBJCOPY_FLAGS) --set-section-flags .text=contents,alloc,load,readonly,code
+	cd $$($(2)_$(3)_LIBDIR) && find -type f -not '(' -iname '*.pc' -or -iname '*.cmake' -or -iname '*.a' -or -iname '*.la' -or -iname '*.def' -or -iname '*.h' ')' -print0 | \
+	    xargs $(--verbose?) -0 -r -P$$(J) -n1 sh -ec ' \
+		f="$$$$1"; \
+		dst="$$(DST_LIBDIR)/$$$$f"; \
+		if $(OBJCOPY) $(OBJCOPY_FLAGS) --only-keep-debug "$$$$f" "$$$$dst.debug" >/dev/null 2>&1 && \
+		   $(OBJCOPY) $(OBJCOPY_FLAGS) --add-gnu-debuglink="$$$$dst.debug" --strip-debug --set-section-flags .text=contents,alloc,load,readonly,code "$$$$f" "$$$$dst" >/dev/null 2>&1; then \
+			:; \
+		else \
+			cp -a "$$$$f" "$$$$dst"; \
+			rm -f "$$$$dst.debug"; \
+		fi' sh
 	touch $$@
 else
 	cd $$($(2)_$(3)_LIBDIR) && find -type f -not '(' -iname '*.pc' -or -iname '*.cmake' -or -iname '*.a' -or -iname '*.la' -or -iname '*.def' -or -iname '*.h' ')' \
 	    -printf '$$(DST_LIBDIR)/%p.debug\0' | xargs $(--verbose?) -0 -r -P$$(J) rm -f
-	cd $$($(2)_$(3)_LIBDIR) && find -type f -not '(' -iname '*.pc' -or -iname '*.cmake' -or -iname '*.a' -or -iname '*.la' -or -iname '*.def' -or -iname '*.h' ')' \
-	    -printf '--strip-debug\0%p\0$$(DST_LIBDIR)/%p\0' | \
-	    xargs $(--verbose?) -0 -r -P$$(J) -n3 $(OBJCOPY) $(OBJCOPY_FLAGS) --set-section-flags .text=contents,alloc,load,readonly,code
+	cd $$($(2)_$(3)_LIBDIR) && find -type f -not '(' -iname '*.pc' -or -iname '*.cmake' -or -iname '*.a' -or -iname '*.la' -or -iname '*.def' -or -iname '*.h' ')' -print0 | \
+	    xargs $(--verbose?) -0 -r -P$$(J) -n1 sh -ec ' \
+		f="$$$$1"; \
+		dst="$$(DST_LIBDIR)/$$$$f"; \
+		if $(OBJCOPY) $(OBJCOPY_FLAGS) --strip-debug --set-section-flags .text=contents,alloc,load,readonly,code "$$$$f" "$$$$dst" >/dev/null 2>&1; then \
+			:; \
+		else \
+			cp -a "$$$$f" "$$$$dst"; \
+		fi' sh
 	touch $$@
 endif
 
@@ -108,25 +129,27 @@ $(2)_$(3)-unix_LIBFLAGS = $$(foreach d,$
 # unix version doesn't exist.
 
 $(2)_$(3)_ENV = \
-    CARGO_TARGET_$$(call toupper,$$($(3)-$(4)_CARGO_TARGET))_LINKER="$$($(3)-$(4)_TARGET)-gcc" \
+    CARGO_TARGET_$$(call toupper,$$($(3)-$(4)_CARGO_TARGET))_LINKER="$$(call resolve-mingw-tool,$$($(3)-$(4)_TARGET)-gcc)" \
     CCACHE_BASEDIR="$$(CCACHE_BASEDIR)" \
     STRIP="$$(STRIP)" \
-    AR="$$($(3)-$(4)_TARGET)-ar" \
-    RANLIB="$$($(3)-$(4)_TARGET)-ranlib" \
-    CC="$$($(3)-$(4)_TARGET)-gcc" \
-    CXX="$$($(3)-$(4)_TARGET)-g++" \
-    LD="$$($(3)-$(4)_TARGET)-ld" \
-    RC="$$($(3)-windows_TARGET)-windres" \
-    WIDL="$$($(3)-windows_TARGET)-widl" \
+    AR="$$(call resolve-mingw-tool,$$($(3)-$(4)_TARGET)-ar)" \
+    RANLIB="$$(call resolve-mingw-tool,$$($(3)-$(4)_TARGET)-ranlib)" \
+    NM="$$(call resolve-mingw-tool,$$($(3)-$(4)_TARGET)-nm)" \
+    AS="$$(call resolve-mingw-tool,$$($(3)-$(4)_TARGET)-as)" \
+    LD="$$(call resolve-mingw-tool,$$($(3)-$(4)_TARGET)-ld)" \
+    CC="$$(call resolve-mingw-tool,$$($(3)-$(4)_TARGET)-gcc)" \
+    CXX="$$(call resolve-mingw-tool,$$($(3)-$(4)_TARGET)-g++)" \
+    RC="$$(call resolve-mingw-tool,$$($(3)-windows_TARGET)-windres)" \
+    WIDL="$$(call resolve-mingw-tool,$$($(3)-windows_TARGET)-widl)" \
     PKG_CONFIG="$$($$(HOST_ARCH)-unix_TARGET)-pkg-config" \
-    PATH="$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_HOST_DEPS),$$($$(d)_$$(HOST_ARCH)_BINDIR)),,:):$$$$PATH" \
+    PATH="$(SYSTEM_BIN):$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_HOST_DEPS),$$($$(d)_$$(HOST_ARCH)_BINDIR))):$$$$PATH" \
     LD_LIBRARY_PATH="$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_HOST_DEPS),$$($$(d)_$$(HOST_ARCH)_LIBDIR)/$$($$(HOST_ARCH)-unix_LIBDIR)),,:)$$$$LD_LIBRARY_PATH" \
     PKG_CONFIG_PATH="$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_DEPS),$$($$(d)_$(3)_LIBDIR)/$$($(3)-$(4)_LIBDIR)/pkgconfig)):$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_DEPS),$$($$(d)_$(3)_DST)/share/pkgconfig))" \
     PKG_CONFIG_LIBDIR="/usr/$$($(3)-$(4)_LIBDIR_ARCH)/pkgconfig:/usr/share/pkgconfig" \
     CMAKE_PREFIX_PATH="$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_DEPS),$$($$(d)_$(3)_DST)))" \
-    CFLAGS="$$($(2)_$(3)_INCFLAGS) $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS)" \
-    CPPFLAGS="$$($(2)_$(3)_INCFLAGS) $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS)" \
-    CXXFLAGS="$$($(2)_$(3)_INCFLAGS) -std=c++17 $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS)" \
+    CFLAGS="$$($(2)_$(3)_INCFLAGS) $$(if $$(filter 1 yes true,$$($(2)_USE_CLANG)),$$(filter-out $$(GCC_ONLY_CFLAGS),$$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS)),$$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS))" \
+    CPPFLAGS="$$($(2)_$(3)_INCFLAGS) $$(if $$(filter 1 yes true,$$($(2)_USE_CLANG)),$$(filter-out $$(GCC_ONLY_CFLAGS),$$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS)),$$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS))" \
+    CXXFLAGS="$$($(2)_$(3)_INCFLAGS) -std=c++17 $$(if $$(filter 1 yes true,$$($(2)_USE_CLANG)),$$(filter-out $$(GCC_ONLY_CFLAGS),$$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS)),$$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS))" \
     LDFLAGS="$$($(2)_$(3)-$(4)_LIBFLAGS) $$($(2)_$(3)_LIBFLAGS) $$($(2)_LDFLAGS) $$($(3)_LDFLAGS) $$(LDFLAGS)" \
     SOURCE_DATE_EPOCH="$$($(2)_$(3)_SOURCE_DATE_EPOCH)" \
 
@@ -142,21 +165,25 @@ ifeq ($(1),wine)
 $(2)_$(3)_ENV += \
     CROSSCFLAGS="$$($(2)_$(3)_INCFLAGS) $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS)" \
     CROSSLDFLAGS="$$($(2)_$(3)-windows_LIBFLAGS) $$($(2)_$(3)_LIBFLAGS) $$($(2)_LDFLAGS) $$($(3)_LDFLAGS) $$(LDFLAGS)" \
-    i386_AR="$$(i386-windows_TARGET)-ar" \
-    i386_RANLIB="$$(i386-windows_TARGET)-ranlib" \
-    i386_CC="$$(i386-windows_TARGET)-gcc" \
-    i386_CXX="$$(i386-windows_TARGET)-g++" \
-    i386_LD="$$(i386-windows_TARGET)-ld" \
+    i386_AR="$$(call resolve-mingw-tool,$$(i386-windows_TARGET)-ar)" \
+    i386_RANLIB="$$(call resolve-mingw-tool,$$(i386-windows_TARGET)-ranlib)" \
+    i386_NM="$$(call resolve-mingw-tool,$$(i386-windows_TARGET)-nm)" \
+    i386_AS="$$(call resolve-mingw-tool,$$(i386-windows_TARGET)-as)" \
+    i386_CC="$$(call resolve-mingw-tool,$$(i386-windows_TARGET)-gcc)" \
+    i386_CXX="$$(call resolve-mingw-tool,$$(i386-windows_TARGET)-g++)" \
+    i386_LD="$$(call resolve-mingw-tool,$$(i386-windows_TARGET)-ld)" \
     i386_CFLAGS="$$($(2)_i386_INCFLAGS) $$($(2)_CFLAGS) $$(i386_CFLAGS) $$(CFLAGS)" \
     i386_CPPFLAGS="$$($(2)_i386_INCFLAGS) $$($(2)_CFLAGS) $$(i386_CFLAGS) $$(CFLAGS)" \
     i386_CXXFLAGS="$$($(2)_i386_INCFLAGS) -std=c++17 $$($(2)_CFLAGS) $$(i386_CFLAGS) $$(CFLAGS)" \
     i386_LDFLAGS="$$($(2)_i386-windows_LIBFLAGS) $$($(2)_i386_LIBFLAGS) $$($(2)_LDFLAGS) $$(i386_LDFLAGS) $$(LDFLAGS)" \
     i386_PKG_CONFIG_LIBDIR="/usr/lib32/pkgconfig:/usr/share/pkgconfig" \
-    x86_64_AR="$$(x86_64-windows_TARGET)-ar" \
-    x86_64_RANLIB="$$(x86_64-windows_TARGET)-ranlib" \
-    x86_64_CC="$$(x86_64-windows_TARGET)-gcc" \
-    x86_64_CXX="$$(x86_64-windows_TARGET)-g++" \
-    x86_64_LD="$$(x86_64-windows_TARGET)-ld" \
+    x86_64_AR="$$(call resolve-mingw-tool,$$(x86_64-windows_TARGET)-ar)" \
+    x86_64_RANLIB="$$(call resolve-mingw-tool,$$(x86_64-windows_TARGET)-ranlib)" \
+    x86_64_NM="$$(call resolve-mingw-tool,$$(x86_64-windows_TARGET)-nm)" \
+    x86_64_AS="$$(call resolve-mingw-tool,$$(x86_64-windows_TARGET)-as)" \
+    x86_64_CC="$$(call resolve-mingw-tool,$$(x86_64-windows_TARGET)-gcc)" \
+    x86_64_CXX="$$(call resolve-mingw-tool,$$(x86_64-windows_TARGET)-g++)" \
+    x86_64_LD="$$(call resolve-mingw-tool,$$(x86_64-windows_TARGET)-ld)" \
     x86_64_CFLAGS="$$($(2)_x86_64_INCFLAGS) $$($(2)_CFLAGS) $$(x86_64_CFLAGS) $$(CFLAGS)" \
     x86_64_CPPFLAGS="$$($(2)_x86_64_INCFLAGS) $$($(2)_CFLAGS) $$(x86_64_CFLAGS) $$(CFLAGS)" \
     x86_64_CXXFLAGS="$$($(2)_x86_64_INCFLAGS) -std=c++17 $$($(2)_CFLAGS) $$(x86_64_CFLAGS) $$(CFLAGS)" \

--- a/Makefile.in	2026-05-10 16:19:19.178649179 +0200
+++ b/Makefile.in	2026-05-10 16:58:35.271741310 +0200
@@ -51,11 +51,11 @@ DIST_VERSION := $(DST_BASE)/version
 DEPLOY_DIR := ./deploy
 REDIST_DIR := ./redist
 
-HOST_CFLAGS    ?= -O2 -march=nocona -mtune=core-avx2
+HOST_CFLAGS    ?= -O3 -march=native -mtune=native
 CCOS_CFLAGS    := $(HOST_CFLAGS) -pipe -mno-avx512f
 i386_CFLAGS    := -mstackrealign $(CCOS_CFLAGS) -mfpmath=sse
 x86_64_CFLAGS  := -mcmodel=small $(CCOS_CFLAGS) -mfpmath=sse
-HOST_RUSTFLAGS   ?= -Copt-level=3 -Ctarget-cpu=nocona
+HOST_RUSTFLAGS   ?= -Copt-level=3 -Ctarget-cpu=native
 i386_RUSTFLAGS   := $(HOST_RUSTFLAGS)
 x86_64_RUSTFLAGS := $(HOST_RUSTFLAGS)
 aarch64_CFLAGS := -march=armv8.2-a -mtune=cortex-x3
@@ -64,7 +64,7 @@ aarch64_RUSTFLAGS := -Copt-level=3 -Ctar
 arm64ec_RUSTFLAGS := -Copt-level=3 -Ctarget-cpu=armv8.2-a
 
 CFLAGS  = -fwrapv -fno-strict-aliasing
-CFLAGS += -ffunction-sections -fdata-sections -fno-omit-frame-pointer
+CFLAGS += -ffunction-sections -fdata-sections
 CFLAGS += -ffile-prefix-map=$(CCACHE_BASEDIR)=.
 
 ifneq ($(SUPPRESS_WARNINGS),)
@@ -116,9 +116,9 @@ ifeq ($(shell cc --version|grep 'clang')
 	TOOLCHAIN_PREFIX :=
 	OBJCOPY_FLAGS  := -p
 	MINGW_TYPE := gcc
-	i386_CFLAGS += -s -mno-avx -mno-avx2
+	i386_CFLAGS += -s
 	i386_CFLAGS += -fvect-cost-model=cheap -fno-semantic-interposition -fipa-pta
-	x86_64_CFLAGS += -s -mno-avx -mno-avx2
+	x86_64_CFLAGS += -s
 	x86_64_CFLAGS += -fvect-cost-model=cheap -fno-semantic-interposition -fipa-pta
 	i386_SANITY_FLAGS := -mpreferred-stack-boundary=2
 else
@@ -164,6 +164,14 @@ include $(SRC)/make/rules-wine-tools.mk
 include $(SRC)/make/rules-cargo.mk
 include $(SRC)/make/rules-nvidia-libs.mk
 
+# Local policy: AMD build, no NVIDIA stack, external DXVK-family payloads.
+WITHOUT_NVIDIA_LIBS ?= 1
+WITHOUT_VKREFLEX ?= 1
+WITHOUT_NVAPI ?= 1
+USE_EXTERNAL_DXVK ?= 1
+USE_EXTERNAL_DXVK_LLASYNC ?= 1
+USE_EXTERNAL_DXVK_SAREK ?= 1
+USE_EXTERNAL_D7VK ?= 1
 
 ##
 ## patches
@@ -300,7 +308,10 @@ $(OBJ)/.nghttp2-post-source:
 LIBSOUP_MESON_ARGS = \
 	-Ddocs=disabled \
 	-Dautobahn=disabled \
-	-Dtls_check=false
+	-Dtls_check=false \
+	-Dtests=false \
+	-Dintrospection=disabled \
+	-Dvapi=disabled
 
 LIBSOUP_DEPENDS = nghttp2
 
@@ -827,6 +838,18 @@ $(eval $(call rules-cmake,spirv-headers,
 ## glslang
 ##
 
+GLSLANG_USE_CLANG = 1
+
+GLSLANG_CMAKE_ARGS = \
+	-DCMAKE_BUILD_TYPE=Release \
+	-DCMAKE_C_COMPILER=clang \
+	-DCMAKE_CXX_COMPILER=clang++ \
+	-DCMAKE_C_FLAGS_RELEASE='-DNDEBUG -O3 -march=native -mtune=native -std=gnu17 -fno-semantic-interposition -ffunction-sections -fdata-sections -flto -fprofile-use=/home/marcus/Downloads/glslang.profdata' \
+	-DCMAKE_CXX_FLAGS_RELEASE='-DNDEBUG -O3 -march=native -mtune=native -std=gnu++20 -fno-semantic-interposition -ffunction-sections -fdata-sections -flto -fprofile-use=/home/marcus/Downloads/glslang.profdata' \
+	-DCMAKE_EXE_LINKER_FLAGS_RELEASE='-fuse-ld=lld -Wl,-O2,--as-needed,--gc-sections,-z,max-page-size=0x200000 -fprofile-use=/home/marcus/Downloads/glslang.profdata' \
+	-DCMAKE_SHARED_LINKER_FLAGS_RELEASE='-fuse-ld=lld -Wl,-O2,--as-needed,--gc-sections,-z,max-page-size=0x200000 -fprofile-use=/home/marcus/Downloads/glslang.profdata' \
+	-DCMAKE_MODULE_LINKER_FLAGS_RELEASE='-fuse-ld=lld -Wl,-O2,--as-needed,--gc-sections,-z,max-page-size=0x200000 -fprofile-use=/home/marcus/Downloads/glslang.profdata'
+
 $(eval $(call rules-source,glslang,$(SRCDIR)/glslang))
 $(eval $(call rules-cmake,glslang,i386,unix))
 $(eval $(call rules-cmake,glslang,x86_64,unix))
@@ -1177,6 +1200,24 @@ $(eval $(call rules-makedep,vrclient,aar
 ## dxvk
 ##
 
+# Use externally built DXVK payloads and skip in-tree DXVK compilation.
+# This keeps dist layout valid so files can be swapped in manually afterwards.
+USE_EXTERNAL_DXVK ?= 1
+
+ifeq ($(USE_EXTERNAL_DXVK),1)
+
+.PHONY: dxvk
+dxvk: | $(DST_DIR)
+	mkdir -p $(DST_LIBDIR)/wine/dxvk/i386-windows
+	mkdir -p $(DST_LIBDIR)/wine/dxvk/x86_64-windows
+ifneq ($(findstring aarch64,$(unix_ARCHS)),)
+	mkdir -p $(DST_LIBDIR)/wine/dxvk/aarch64-windows
+endif
+	rm -rf $(DST_LIBDIR)/wine/dxvk/version
+	( git -C $(SRCDIR) submodule status -- dxvk 2>/dev/null || echo "external-dxvk" ) > $(DST_LIBDIR)/wine/dxvk/version
+
+else
+
 # wine builds DLLs with the same names, we need to differentiate the timestamps
 DXVK_i386_SOURCE_DATE_EPOCH := $(shell expr $(i386_SOURCE_DATE_EPOCH) - 1)
 DXVK_x86_64_SOURCE_DATE_EPOCH := $(shell expr $(x86_64_SOURCE_DATE_EPOCH) - 1)
@@ -1209,6 +1250,8 @@ $(OBJ)/.dxvk-post-source:
 	echo "$(shell git -C $(SRCDIR) submodule status -- dxvk)" > $(DST_LIBDIR)/wine/dxvk/version
 	touch $@
 
+endif
+
 
 ifneq ($(WITHOUT_LLASYNC),1)
 
@@ -1216,6 +1259,22 @@ ifneq ($(WITHOUT_LLASYNC),1)
 ## dxvk-llasync
 ##
 
+USE_EXTERNAL_DXVK_LLASYNC ?= 1
+
+ifeq ($(USE_EXTERNAL_DXVK_LLASYNC),1)
+
+.PHONY: dxvk-llasync
+dxvk-llasync: | $(DST_DIR)
+	mkdir -p $(DST_LIBDIR)/wine/dxvk-llasync/i386-windows
+	mkdir -p $(DST_LIBDIR)/wine/dxvk-llasync/x86_64-windows
+ifneq ($(findstring aarch64,$(unix_ARCHS)),)
+	mkdir -p $(DST_LIBDIR)/wine/dxvk-llasync/aarch64-windows
+endif
+	rm -rf $(DST_LIBDIR)/wine/dxvk-llasync/version
+	( git -C $(SRCDIR) submodule status -- dxvk-llasync 2>/dev/null || echo "external-dxvk-llasync" ) > $(DST_LIBDIR)/wine/dxvk-llasync/version
+
+else
+
 # wine builds DLLs with the same names, we need to differentiate the timestamps
 DXVK_LLASYNC_i386_SOURCE_DATE_EPOCH := $(shell expr $(i386_SOURCE_DATE_EPOCH) - 1)
 DXVK_LLASYNC_x86_64_SOURCE_DATE_EPOCH := $(shell expr $(x86_64_SOURCE_DATE_EPOCH) - 1)
@@ -1249,6 +1308,8 @@ $(OBJ)/.dxvk-llasync-post-source: patche
 	echo "$(shell git -C $(SRCDIR) submodule status -- dxvk-llasync)" > $(DST_LIBDIR)/wine/dxvk-llasync/version
 	touch $@
 
+endif
+
 default_pfx: dxvk-llasync
 
 endif # WITHOUT_LLASYNC
@@ -1260,6 +1321,22 @@ ifneq ($(WITHOUT_SAREK),1)
 ## dxvk-sarek
 ##
 
+USE_EXTERNAL_DXVK_SAREK ?= 1
+
+ifeq ($(USE_EXTERNAL_DXVK_SAREK),1)
+
+.PHONY: dxvk-sarek
+dxvk-sarek: | $(DST_DIR)
+	mkdir -p $(DST_LIBDIR)/wine/dxvk-sarek/i386-windows
+	mkdir -p $(DST_LIBDIR)/wine/dxvk-sarek/x86_64-windows
+ifneq ($(findstring aarch64,$(unix_ARCHS)),)
+	mkdir -p $(DST_LIBDIR)/wine/dxvk-sarek/aarch64-windows
+endif
+	rm -rf $(DST_LIBDIR)/wine/dxvk-sarek/version
+	( git -C $(SRCDIR) submodule status -- dxvk-sarek 2>/dev/null || echo "external-dxvk-sarek" ) > $(DST_LIBDIR)/wine/dxvk-sarek/version
+
+else
+
 # wine builds DLLs with the same names, we need to differentiate the timestamps
 DXVK_SAREK_i386_SOURCE_DATE_EPOCH := $(shell expr $(i386_SOURCE_DATE_EPOCH) - 1)
 DXVK_SAREK_x86_64_SOURCE_DATE_EPOCH := $(shell expr $(x86_64_SOURCE_DATE_EPOCH) - 1)
@@ -1292,6 +1369,8 @@ $(OBJ)/.dxvk-sarek-post-source:
 	echo "$(shell git -C $(SRCDIR) submodule status -- dxvk-sarek)" > $(DST_LIBDIR)/wine/dxvk-sarek/version
 	touch $@
 
+endif
+
 default_pfx: dxvk-sarek
 
 endif # WITHOUT_SAREK
@@ -1303,6 +1382,16 @@ ifneq ($(WITHOUT_D7VK),1)
 ## d7vk
 ##
 
+ifeq ($(USE_EXTERNAL_D7VK),1)
+
+.PHONY: d7vk
+d7vk: | $(DST_DIR)
+	mkdir -p $(DST_LIBDIR)/wine/d7vk/i386-windows
+	rm -rf $(DST_LIBDIR)/wine/d7vk/version
+	( git -C $(SRCDIR) submodule status -- d7vk 2>/dev/null || echo "external-d7vk" ) > $(DST_LIBDIR)/wine/d7vk/version
+
+else
+
 # wine builds DLLs with the same names, we need to differentiate the timestamps
 D7VK_i386_SOURCE_DATE_EPOCH := $(shell expr $(i386_SOURCE_DATE_EPOCH) - 1)
 
@@ -1314,7 +1403,7 @@ D7VK_MESON_ARGS += -Denable_dxgi=false -
 D7VK_i386_MESON_ARGS = --bindir=$(D7VK_i386_DST)/lib/wine/d7vk/i386-windows
 D7VK_HOST_DEPENDS = glslang
 
-D7VK_i386_CFLAGS = -O3 -mno-avx $(i386_SANITY_FLAGS)
+D7VK_i386_CFLAGS = -O3 $(i386_SANITY_FLAGS)
 D7VK_CPPFLAGS = -msse -msse2
 D7VK_LDFLAGS = -static -static-libgcc -static-libstdc++
 
@@ -1329,6 +1418,8 @@ $(OBJ)/.d7vk-post-source:
 	echo "$(shell git -C $(SRCDIR) submodule status -- d7vk)" > $(DST_LIBDIR)/wine/d7vk/version
 	touch $@
 
+endif
+
 default_pfx: d7vk
 
 endif # WITHOUT_D7VK
