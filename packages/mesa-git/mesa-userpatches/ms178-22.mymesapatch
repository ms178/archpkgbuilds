--- a/src/amd/vulkan/radv_sdma.c	2026-01-02 09:29:31.639612172 +0100
+++ b/src/amd/vulkan/radv_sdma.c	2026-03-10 10:04:29.326084384 +0100
@@ -66,6 +66,8 @@ static struct radv_sdma_chunked_copy_inf
 radv_sdma_get_chunked_copy_info(const struct radv_device *const device, const struct ac_sdma_surf *const img,
                                 const VkExtent3D extent)
 {
+   (void)device;
+
    const uint32_t blk_h = util_format_get_blockheight(img->img_format);
    const unsigned extent_horizontal_blocks = extent.width;
    const unsigned extent_vertical_blocks = extent.height;
@@ -234,11 +236,27 @@ void
 radv_sdma_fill_memory(const struct radv_device *device, struct radv_cmd_stream *cs, uint64_t va, uint64_t size,
                       const uint32_t value)
 {
+   if (size == 0)
+      return;
+
+   assert(util_is_aligned(va, 4));
+   assert(util_is_aligned(size, 4));
+
    const struct radv_physical_device *pdev = radv_device_physical(device);
+   const enum sdma_version ver = pdev->info.sdma_ip_version;
 
    while (size > 0) {
       radeon_check_space(device->ws, cs->b, 5);
-      uint64_t bytes_written = ac_emit_sdma_constant_fill(cs->b, pdev->info.sdma_ip_version, va, size, value);
+
+      const uint64_t bytes_written = ac_emit_sdma_constant_fill(cs->b, ver, va, size, value);
+
+      if (bytes_written == 0) {
+         assert(!"ac_emit_sdma_constant_fill returned 0 unexpectedly");
+         break;
+      }
+
+      assert(bytes_written <= size);
+      assert(util_is_aligned(bytes_written, 4));
 
       size -= bytes_written;
       va += bytes_written;
@@ -285,6 +303,9 @@ radv_sdma_copy_buffer_image(const struct
                             const struct ac_sdma_surf *buf, const struct ac_sdma_surf *img, const VkExtent3D extent,
                             bool to_image)
 {
+   if (extent.width == 0 || extent.height == 0 || extent.depth == 0)
+      return;
+
    if (img->surf->is_linear) {
       if (to_image)
          radv_sdma_emit_copy_linear_sub_window(device, cs, buf, img, extent);
@@ -316,6 +337,9 @@ radv_sdma_copy_buffer_image_unaligned(co
                                       const struct ac_sdma_surf *buf, const struct ac_sdma_surf *img_in,
                                       const VkExtent3D base_extent, struct radeon_winsys_bo *temp_bo, bool to_image)
 {
+   if (base_extent.width == 0 || base_extent.height == 0 || base_extent.depth == 0)
+      return;
+
    const struct radv_sdma_chunked_copy_info info = radv_sdma_get_chunked_copy_info(device, img_in, base_extent);
    struct ac_sdma_surf img = *img_in;
    struct ac_sdma_surf tmp = {
@@ -381,6 +405,9 @@ void
 radv_sdma_copy_image(const struct radv_device *device, struct radv_cmd_stream *cs, const struct ac_sdma_surf *src,
                      const struct ac_sdma_surf *dst, const VkExtent3D extent)
 {
+   if (extent.width == 0 || extent.height == 0 || extent.depth == 0)
+      return;
+
    if (src->surf->is_linear) {
       if (dst->surf->is_linear) {
          radv_sdma_emit_copy_linear_sub_window(device, cs, src, dst, extent);
@@ -409,6 +436,13 @@ radv_sdma_use_t2t_scanline_copy(const st
    /* SDMA can't do format conversion. */
    assert(src->bpp == dst->bpp);
 
+   /* Non-power-of-two or oversized bpp must fall back to scanline copy
+    * because the T2T alignment table is indexed by log2(bpp).
+    * Check before the device lookup to avoid an unnecessary pointer chase.
+    */
+   if (!util_is_power_of_two_nonzero(src->bpp) || src->bpp > 16)
+      return true;
+
    const struct radv_physical_device *pdev = radv_device_physical(device);
    const enum sdma_version ver = pdev->info.sdma_ip_version;
    if (ver < SDMA_5_0) {
@@ -471,6 +505,15 @@ radv_sdma_copy_image_t2t_scanline(const
                                   const struct ac_sdma_surf *src, const struct ac_sdma_surf *dst,
                                   const VkExtent3D extent, struct radeon_winsys_bo *temp_bo)
 {
+   assert(device);
+   assert(cs);
+   assert(src);
+   assert(dst);
+   assert(temp_bo);
+
+   if (extent.width == 0 || extent.height == 0 || extent.depth == 0)
+      return;
+
    const struct radv_sdma_chunked_copy_info info = radv_sdma_get_chunked_copy_info(device, src, extent);
    struct ac_sdma_surf t2l_src = *src;
    struct ac_sdma_surf t2l_dst = {

--- a/src/amd/vulkan/radv_cmd_buffer.h	2025-09-07 11:04:15.515661359 +0200
+++ b/src/amd/vulkan/radv_cmd_buffer.h	2026-03-28 11:09:14.952502874 +0200
@@ -84,6 +84,10 @@ enum radv_dynamic_state_bits {
    RADV_DYNAMIC_ALL = (1ull << 56) - 1,
 };
 
+/* Compile-time validation: ensure all dynamic state bits fit in uint64_t. */
+static_assert(RADV_DYNAMIC_SCISSOR_WITH_COUNT < (1ull << 63),
+              "Dynamic state bits must fit in 64-bit storage");
+
 enum radv_cmd_dirty_bits {
    RADV_CMD_DIRTY_PIPELINE = 1ull << 0,
    RADV_CMD_DIRTY_INDEX_BUFFER = 1ull << 1,
@@ -128,6 +132,10 @@ enum radv_cmd_dirty_bits {
    RADV_CMD_DIRTY_SHADER_QUERY = RADV_CMD_DIRTY_NGG_STATE | RADV_CMD_DIRTY_TASK_STATE,
 };
 
+/* Compile-time validation: ensure all dirty bits fit in uint64_t. */
+static_assert(RADV_CMD_DIRTY_GFX12_HIZ_WA_STATE < (1ull << 63),
+              "CMD dirty bits must fit in 64-bit storage");
+
 enum radv_cmd_flush_bits {
    /* Instruction cache. */
    RADV_CMD_FLAG_INV_ICACHE = 1 << 0,
@@ -167,6 +175,10 @@ enum radv_cmd_flush_bits {
                                  RADV_CMD_FLAG_INV_L2 | RADV_CMD_FLAG_WB_L2 | RADV_CMD_FLAG_CS_PARTIAL_FLUSH),
 };
 
+/* Compile-time validation: ensure all flush bits fit in enum (int). */
+static_assert(RADV_CMD_FLAG_VGT_STREAMOUT_SYNC < (1 << 30),
+              "Flush bits must fit in 32-bit enum");
+
 struct radv_vertex_binding {
    uint64_t addr;
    VkDeviceSize size;
@@ -178,17 +190,17 @@ struct radv_streamout_binding {
 };
 
 struct radv_streamout_state {
-   /* Mask of bound streamout buffers. */
-   uint8_t enabled_mask;
+   /* VA of the streamout state (GFX12+). */
+   uint64_t state_va;
 
    /* State of VGT_STRMOUT_BUFFER_(CONFIG|END) */
    uint32_t hw_enabled_mask;
 
+   /* Mask of bound streamout buffers. */
+   uint8_t enabled_mask;
+
    /* State of VGT_STRMOUT_(CONFIG|EN) */
    bool streamout_enabled;
-
-   /* VA of the streamout state (GFX12+). */
-   uint64_t state_va;
 };
 
 /**
@@ -625,7 +637,8 @@ radv_is_streamout_enabled(struct radv_cm
    struct radv_streamout_state *so = &cmd_buffer->state.streamout;
 
    /* Streamout must be enabled for the PRIMITIVES_GENERATED query to work. */
-   return (so->streamout_enabled || cmd_buffer->state.active_prims_gen_queries) && !cmd_buffer->state.suspend_streamout;
+   return (so->streamout_enabled || cmd_buffer->state.active_prims_gen_queries) &&
+          !cmd_buffer->state.suspend_streamout;
 }
 
 ALWAYS_INLINE static unsigned

--- a/src/amd/vulkan/radv_cmd_buffer.c
+++ b/src/amd/vulkan/radv_cmd_buffer.c
@@ -487,8 +487,13 @@ radv_cmd_set_blend_constants(struct radv
 {
    struct radv_cmd_state *state = &cmd_buffer->state;
 
-   memcpy(state->dynamic.vk.cb.blend_constants, blend_constants, sizeof(float) * 4);
+   if (unlikely(!blend_constants))
+      return;
+
+   if (__builtin_memcmp(state->dynamic.vk.cb.blend_constants, blend_constants, sizeof(float) * 4) == 0)
+      return;
 
+   __builtin_memcpy(state->dynamic.vk.cb.blend_constants, blend_constants, sizeof(float) * 4);
    state->dirty_dynamic |= RADV_DYNAMIC_BLEND_CONSTANTS;
 }
 
@@ -614,8 +619,22 @@ radv_cmd_set_viewport(struct radv_cmd_bu
 {
    struct radv_cmd_state *state = &cmd_buffer->state;
 
-   memcpy(state->dynamic.vk.vp.viewports + first, viewports, count * sizeof(*viewports));
-   memcpy(state->dynamic.vp_xform + first, vp_xform, count * sizeof(*vp_xform));
+   if (unlikely(first >= MAX_VIEWPORTS))
+      return;
+
+   count = MIN2(count, MAX_VIEWPORTS - first);
+   if (unlikely(count == 0 || !viewports || !vp_xform))
+      return;
+
+   const size_t vp_size = (size_t)count * sizeof(*viewports);
+   const size_t xform_size = (size_t)count * sizeof(*vp_xform);
+
+   if (__builtin_memcmp(state->dynamic.vk.vp.viewports + first, viewports, vp_size) == 0 &&
+       __builtin_memcmp(state->dynamic.vp_xform + first, vp_xform, xform_size) == 0)
+      return;
+
+   __builtin_memcpy(state->dynamic.vk.vp.viewports + first, viewports, vp_size);
+   __builtin_memcpy(state->dynamic.vp_xform + first, vp_xform, xform_size);
 
    state->dirty_dynamic |= RADV_DYNAMIC_VIEWPORT;
    state->dirty |= RADV_CMD_DIRTY_GUARDBAND;
@@ -636,7 +655,19 @@ radv_cmd_set_scissor(struct radv_cmd_buf
 {
    struct radv_cmd_state *state = &cmd_buffer->state;
 
-   memcpy(state->dynamic.vk.vp.scissors + first, scissors, count * sizeof(*scissors));
+   if (unlikely(first >= MAX_VIEWPORTS))
+      return;
+
+   count = MIN2(count, MAX_VIEWPORTS - first);
+   if (unlikely(count == 0 || !scissors))
+      return;
+
+   const size_t copy_size = (size_t)count * sizeof(*scissors);
+
+   if (__builtin_memcmp(state->dynamic.vk.vp.scissors + first, scissors, copy_size) == 0)
+      return;
+
+   __builtin_memcpy(state->dynamic.vk.vp.scissors + first, scissors, copy_size);
 
    state->dirty_dynamic |= RADV_DYNAMIC_SCISSOR;
 }
@@ -1125,9 +1156,24 @@ radv_write_data(struct radv_cmd_buffer *
 static void
 radv_emit_clear_data(struct radv_cmd_buffer *cmd_buffer, unsigned engine_sel, uint64_t va, unsigned size)
 {
-   uint32_t *zeroes = alloca(size);
-   memset(zeroes, 0, size);
-   radv_write_data(cmd_buffer, engine_sel, va, size / 4, zeroes, false);
+   /* 256-byte static zero buffer in .rodata - zero stack usage, Vega friendly */
+   constexpr uint32_t zeros[64] = {0};
+
+   assert(size > 0 && (size % 4) == 0);
+
+   if (likely(size <= sizeof(zeros))) {
+      radv_write_data(cmd_buffer, engine_sel, va, size / 4, zeros, false);
+      return;
+   }
+
+   /* Chunked for huge clears (never happens in games) */
+   unsigned remaining = size;
+   while (remaining) {
+      const unsigned chunk = MIN2(remaining, (unsigned)sizeof(zeros));
+      radv_write_data(cmd_buffer, engine_sel, va, chunk / 4, zeros, false);
+      va += chunk;
+      remaining -= chunk;
+   }
 }
 
 static void
@@ -2060,35 +2106,37 @@ radv_compute_sample_locs_pixel(uint32_t
 static uint64_t
 radv_compute_centroid_priority(struct radv_cmd_buffer *cmd_buffer, VkOffset2D *sample_locs, uint32_t num_samples)
 {
-   uint32_t *centroid_priorities = alloca(num_samples * sizeof(*centroid_priorities));
-   uint32_t sample_mask = num_samples - 1;
-   uint32_t *distances = alloca(num_samples * sizeof(*distances));
+   (void)cmd_buffer;
+   assert(sample_locs != NULL);
+
+   if (unlikely(num_samples != 1 && num_samples != 2 && num_samples != 4 && num_samples != 8))
+      return 0;
+
+   uint32_t centroid_priorities[8];
+   uint32_t distances[8];
+   const uint32_t sample_mask = num_samples - 1;
    uint64_t centroid_priority = 0;
 
-   /* Compute the distances from center for each sample. */
-   for (int i = 0; i < num_samples; i++) {
-      distances[i] = (sample_locs[i].x * sample_locs[i].x) + (sample_locs[i].y * sample_locs[i].y);
+   for (uint32_t i = 0; i < num_samples; i++) {
+      const int32_t x = sample_locs[i].x;
+      const int32_t y = sample_locs[i].y;
+      distances[i] = (uint32_t)(x * x + y * y);
    }
 
-   /* Compute the centroid priorities by looking at the distances array. */
-   for (int i = 0; i < num_samples; i++) {
+   for (uint32_t i = 0; i < num_samples; i++) {
       uint32_t min_idx = 0;
-
-      for (int j = 1; j < num_samples; j++) {
+      for (uint32_t j = 1; j < num_samples; j++) {
          if (distances[j] < distances[min_idx])
             min_idx = j;
       }
-
       centroid_priorities[i] = min_idx;
-      distances[min_idx] = 0xffffffff;
+      distances[min_idx] = UINT32_MAX;
    }
 
-   /* Compute the final centroid priority. */
-   for (int i = 0; i < 8; i++) {
-      centroid_priority |= centroid_priorities[i & sample_mask] << (i * 4);
-   }
+   for (uint32_t i = 0; i < 8; i++)
+      centroid_priority |= (uint64_t)centroid_priorities[i & sample_mask] << (i * 4);
 
-   return centroid_priority << 32 | centroid_priority;
+   return centroid_priority | (centroid_priority << 32);
 }
 
 /**
@@ -2683,13 +2731,21 @@ radv_emit_binning_state(struct radv_cmd_
 static void
 radv_emit_shader_prefetch(struct radv_cmd_buffer *cmd_buffer, struct radv_shader *shader)
 {
-   if (!shader)
+   /* Vega-64 critical: warm shader code in L2 as early as possible.
+    * Must guard bo because Dawn hits partial shader binds during
+    * pipeline creation / shader objects / secondary cmd buffers.
+    */
+   if (unlikely(!shader || !shader->bo))
       return;
 
-   struct radv_device *const device = radv_cmd_buffer_device(cmd_buffer);
+   struct radv_device *device = radv_cmd_buffer_device(cmd_buffer);
    struct radv_cmd_stream *cs = radv_get_pm4_cs(cmd_buffer);
    const uint64_t va = radv_shader_get_va(shader);
 
+   /* Must be in IB validation list or CP DMA faults on Vega */
+   radv_cs_add_buffer(device->ws, cs->b, shader->bo);
+
+   /* Non-blocking L2 prefetch - hides latency on first draw */
    radv_cs_cp_dma_prefetch(device, cs, va, shader->code_size, cmd_buffer->state.predicating);
 }
 
@@ -4133,27 +4189,75 @@ radv_emit_viewport_state(struct radv_cmd
    radeon_end();
 }
 
+ALWAYS_INLINE static int32_t
+radv_i64_to_i32_sat(int64_t v)
+{
+   if (v < (int64_t)INT32_MIN) return INT32_MIN;
+   if (v > (int64_t)INT32_MAX) return INT32_MAX;
+   return (int32_t)v;
+}
+
+ALWAYS_INLINE static int32_t
+radv_floorf_to_i32_sat(float v)
+{
+   const double d = __builtin_floor((double)v);
+   if (d <= (double)INT32_MIN) return INT32_MIN;
+   if (d >= (double)INT32_MAX) return INT32_MAX;
+   return (int32_t)d;
+}
+
+ALWAYS_INLINE static int32_t
+radv_ceilf_to_i32_sat(float v)
+{
+   const double d = __builtin_ceil((double)v);
+   if (d <= (double)INT32_MIN) return INT32_MIN;
+   if (d >= (double)INT32_MAX) return INT32_MAX;
+   return (int32_t)d;
+}
+
 static VkRect2D
 radv_scissor_from_viewport(const float scale[3], const float translate[3])
 {
-   VkRect2D rect;
+   const float abs_scale_x = __builtin_fabsf(scale[0]);
+   const float abs_scale_y = __builtin_fabsf(scale[1]);
 
-   rect.offset.x = translate[0] - fabsf(scale[0]);
-   rect.offset.y = translate[1] - fabsf(scale[1]);
-   rect.extent.width = ceilf(translate[0] + fabsf(scale[0])) - rect.offset.x;
-   rect.extent.height = ceilf(translate[1] + fabsf(scale[1])) - rect.offset.y;
-
-   return rect;
+   const int32_t min_x = radv_floorf_to_i32_sat(translate[0] - abs_scale_x);
+   const int32_t min_y = radv_floorf_to_i32_sat(translate[1] - abs_scale_y);
+   const int32_t max_x = radv_ceilf_to_i32_sat(translate[0] + abs_scale_x);
+   const int32_t max_y = radv_ceilf_to_i32_sat(translate[1] + abs_scale_y);
+
+   return (VkRect2D){
+      .offset = { .x = min_x, .y = min_y },
+      .extent = {
+         .width = (uint32_t)MAX2(0, max_x - min_x),
+         .height = (uint32_t)MAX2(0, max_y - min_y),
+      },
+   };
 }
 
 static VkRect2D
 radv_intersect_scissor(const VkRect2D *a, const VkRect2D *b)
 {
-   VkRect2D ret;
-   ret.offset.x = MAX2(a->offset.x, b->offset.x);
-   ret.offset.y = MAX2(a->offset.y, b->offset.y);
-   ret.extent.width = MIN2(a->offset.x + a->extent.width, b->offset.x + b->extent.width) - ret.offset.x;
-   ret.extent.height = MIN2(a->offset.y + a->extent.height, b->offset.y + b->extent.height) - ret.offset.y;
+   assert(a != NULL && b != NULL);
+
+   const int64_t a_br_x = (int64_t)a->offset.x + (int64_t)a->extent.width;
+   const int64_t a_br_y = (int64_t)a->offset.y + (int64_t)a->extent.height;
+   const int64_t b_br_x = (int64_t)b->offset.x + (int64_t)b->extent.width;
+   const int64_t b_br_y = (int64_t)b->offset.y + (int64_t)b->extent.height;
+
+   const int64_t tl_x = MAX2((int64_t)a->offset.x, (int64_t)b->offset.x);
+   const int64_t tl_y = MAX2((int64_t)a->offset.y, (int64_t)b->offset.y);
+   const int64_t br_x = MIN2(a_br_x, b_br_x);
+   const int64_t br_y = MIN2(a_br_y, b_br_y);
+
+   VkRect2D ret = {
+      .offset = { .x = radv_i64_to_i32_sat(tl_x), .y = radv_i64_to_i32_sat(tl_y) },
+      .extent = { .width = 0, .height = 0 },
+   };
+
+   if (br_x > tl_x) ret.extent.width = (uint32_t)(br_x - tl_x);
+   if (br_y > tl_y) ret.extent.height = (uint32_t)(br_y - tl_y);
+
    return ret;
 }
 
@@ -6986,18 +7090,21 @@ struct radv_prim_vertex_count {
 };
 
 static inline unsigned
-radv_prims_for_vertices(struct radv_prim_vertex_count *info, unsigned num)
+radv_prims_for_vertices(const struct radv_prim_vertex_count *info, unsigned num)
 {
-   if (num == 0)
+   if (unlikely(num < info->min || info->incr == 0))
       return 0;
 
-   if (info->incr == 0)
-      return 0;
+   const unsigned n = num - info->min;
 
-   if (num < info->min)
-      return 0;
-
-   return 1 + ((num - info->min) / info->incr);
+   switch (info->incr) {
+   case 1:  return 1 + n;
+   case 2:  return 1 + (n >> 1);
+   case 3:  return 1 + (unsigned)(((uint64_t)n * 0xAAAAAAABull) >> 33);
+   case 4:  return 1 + (n >> 2);
+   case 6:  return 1 + (unsigned)(((uint64_t)n * 0x2AAAAAABull) >> 32);
+   default: return 1 + (n / info->incr);
+   }
 }
 
 static const struct radv_prim_vertex_count prim_size_table[] = {
@@ -7425,16 +7532,30 @@ can_skip_buffer_l2_flushes(struct radv_d
  */
 
 enum radv_cmd_flush_bits
-radv_src_access_flush(struct radv_cmd_buffer *cmd_buffer, VkPipelineStageFlags2 src_stages, VkAccessFlags2 src_flags,
-                      VkAccessFlags3KHR src3_flags, const struct radv_image *image,
+radv_src_access_flush(struct radv_cmd_buffer *cmd_buffer, VkPipelineStageFlags2 src_stages,
+                      VkAccessFlags2 src_flags, VkAccessFlags3KHR src3_flags, const struct radv_image *image,
                       const VkImageSubresourceRange *range)
 {
-   const struct radv_device *device = radv_cmd_buffer_device(cmd_buffer);
+   (void)src3_flags; /* Unused, ABI compatibility */
+
+   struct radv_device *device = radv_cmd_buffer_device(cmd_buffer);
 
+   /* Expand stage-dependent access flags per Vulkan spec */
    src_flags = vk_expand_src_access_flags2(src_stages, src_flags);
 
-   bool has_CB_meta = true, has_DB_meta = true;
-   bool image_is_coherent = image ? radv_image_is_l2_coherent(device, image, range) : false;
+   /* Metadata tracking */
+   bool has_CB_meta = true;
+   bool has_DB_meta = true;
+
+   /* Determine L2 coherency.
+    * BUGFIX: The original code hardcoded 'false' for buffers here, causing massive
+    * redundant L2 cache evictions on every compute/transfer write on GFX9+!
+    * We now properly evaluate buffer coherency.
+    */
+   const bool resource_is_coherent =
+      image ? radv_image_is_l2_coherent(device, image, range)
+            : can_skip_buffer_l2_flushes(device);
+
    enum radv_cmd_flush_bits flush_bits = 0;
 
    if (image) {
@@ -7444,14 +7565,16 @@ radv_src_access_flush(struct radv_cmd_bu
          has_DB_meta = false;
    }
 
-   if (src_flags & VK_ACCESS_2_COMMAND_PREPROCESS_WRITE_BIT_EXT)
-      flush_bits |= RADV_CMD_FLAG_INV_L2;
+   /* Command Preprocess (e.g., NGG/DGC): Requires strict L2 consistency */
+   if (src_flags & VK_ACCESS_2_COMMAND_PREPROCESS_WRITE_BIT_EXT) {
+      flush_bits |= RADV_CMD_FLAG_WB_L2;
+   }
 
+   /* Shader Storage & AS Writes */
    if (src_flags & (VK_ACCESS_2_SHADER_STORAGE_WRITE_BIT | VK_ACCESS_2_ACCELERATION_STRUCTURE_WRITE_BIT_KHR)) {
-      /* since the STORAGE bit isn't set we know that this is a meta operation.
-       * on the dst flush side we skip CB/DB flushes without the STORAGE bit, so
-       * set it here. */
+
       if (image && !(image->vk.usage & VK_IMAGE_USAGE_STORAGE_BIT)) {
+         /* Handle meta operation writes via non-storage paths */
          if (vk_format_is_depth_or_stencil(image->vk.format)) {
             flush_bits |= RADV_CMD_FLAG_FLUSH_AND_INV_DB;
          } else {
@@ -7459,37 +7582,50 @@ radv_src_access_flush(struct radv_cmd_bu
          }
       }
 
-      if (!image_is_coherent)
-         flush_bits |= RADV_CMD_FLAG_INV_L2;
+      /* Write-back L2 if resource is not coherent to make writes available to memory.
+       * Use WB_L2 instead of INV_L2 to push dirty lines without discarding the cache.
+       */
+      if (!resource_is_coherent) {
+         flush_bits |= RADV_CMD_FLAG_WB_L2;
+      }
    }
 
-   if (src_flags &
-       (VK_ACCESS_2_TRANSFORM_FEEDBACK_WRITE_BIT_EXT | VK_ACCESS_2_TRANSFORM_FEEDBACK_COUNTER_WRITE_BIT_EXT)) {
-      if (!image_is_coherent)
+   /* Transform Feedback & Counters (Buffers) */
+   if (src_flags & (VK_ACCESS_2_TRANSFORM_FEEDBACK_WRITE_BIT_EXT | VK_ACCESS_2_TRANSFORM_FEEDBACK_COUNTER_WRITE_BIT_EXT)) {
+      if (!resource_is_coherent) {
          flush_bits |= RADV_CMD_FLAG_WB_L2;
+      }
    }
 
+   /* Color Attachment Writes */
    if (src_flags & VK_ACCESS_2_COLOR_ATTACHMENT_WRITE_BIT) {
       flush_bits |= RADV_CMD_FLAG_FLUSH_AND_INV_CB;
-      if (!image_is_coherent)
-         flush_bits |= RADV_CMD_FLAG_INV_L2;
+
+      if (!resource_is_coherent)
+         flush_bits |= RADV_CMD_FLAG_WB_L2;
+
       if (has_CB_meta)
          flush_bits |= RADV_CMD_FLAG_FLUSH_AND_INV_CB_META;
    }
 
+   /* Depth/Stencil Attachment Writes */
    if (src_flags & VK_ACCESS_2_DEPTH_STENCIL_ATTACHMENT_WRITE_BIT) {
       flush_bits |= RADV_CMD_FLAG_FLUSH_AND_INV_DB;
-      if (!image_is_coherent)
-         flush_bits |= RADV_CMD_FLAG_INV_L2;
+
+      if (!resource_is_coherent)
+         flush_bits |= RADV_CMD_FLAG_WB_L2;
+
       if (has_DB_meta)
          flush_bits |= RADV_CMD_FLAG_FLUSH_AND_INV_DB_META;
    }
 
+   /* Transfer Writes */
    if (src_flags & VK_ACCESS_2_TRANSFER_WRITE_BIT) {
       flush_bits |= RADV_CMD_FLAG_FLUSH_AND_INV_CB | RADV_CMD_FLAG_FLUSH_AND_INV_DB;
 
-      if (!image_is_coherent)
-         flush_bits |= RADV_CMD_FLAG_INV_L2;
+      if (!resource_is_coherent)
+         flush_bits |= RADV_CMD_FLAG_WB_L2;
+
       if (has_CB_meta)
          flush_bits |= RADV_CMD_FLAG_FLUSH_AND_INV_CB_META;
       if (has_DB_meta)
@@ -7952,34 +8088,34 @@ radv_CmdBindVertexBuffers3KHR(VkCommandB
    struct radv_vertex_binding *vb = cmd_buffer->vertex_bindings;
    struct radv_dynamic_state *d = &cmd_buffer->state.dynamic;
 
-   /* We have to defer setting up vertex buffer since we need the buffer
-    * stride from the pipeline. */
-
    assert(firstBinding + bindingCount <= MAX_VBS);
 
    uint32_t misaligned_mask_invalid = 0;
 
    for (uint32_t i = 0; i < bindingCount; i++) {
       const VkBindVertexBuffer3InfoKHR *binding_info = &pBindingInfos[i];
-      uint32_t idx = firstBinding + i;
+      const uint32_t idx = firstBinding + i;
+
+      const VkDeviceSize size = binding_info->addressRange.size;
+      const VkDeviceSize raw_stride = binding_info->setStride ? binding_info->addressRange.stride : 0;
+      const uint64_t addr = size ? binding_info->addressRange.address : 0;
 
-      VkDeviceSize size = binding_info->addressRange.size;
-      VkDeviceSize stride = binding_info->setStride ? binding_info->addressRange.stride : 0;
-      uint64_t addr = size ? binding_info->addressRange.address : 0;
+      assert(raw_stride <= UINT16_MAX);
+      const uint16_t stride = (uint16_t)MIN2(raw_stride, (VkDeviceSize)UINT16_MAX);
 
       if (!!vb[idx].addr != !!addr ||
-          (addr && ((vb[idx].addr & 0x3) != (addr & 0x3) || (d->vk.vi_binding_strides[idx] & 0x3) != (stride & 0x3)))) {
-         misaligned_mask_invalid |= d->vertex_input.bindings_match_attrib ? BITFIELD_BIT(idx) : 0xffffffff;
+          (addr && (((vb[idx].addr & 0x3u) != (addr & 0x3u)) ||
+                    ((uint64_t)d->vk.vi_binding_strides[idx] & 0x3u) != ((uint64_t)stride & 0x3u)))) {
+         misaligned_mask_invalid |= d->vertex_input.bindings_match_attrib ? BITFIELD_BIT(idx) : 0xffffffffu;
       }
 
       vb[idx].addr = addr;
       vb[idx].size = size;
 
-      /* If setStride=false, it shouldn't overwrite the strides specified by CmdSetVertexInputEXT */
       if (binding_info->setStride)
-         radv_cmd_set_vertex_binding_strides(cmd_buffer, idx, 1, (uint16_t *)&stride);
+         radv_cmd_set_vertex_binding_strides(cmd_buffer, idx, 1, &stride);
 
-      uint32_t bit = BITFIELD_BIT(idx);
+      const uint32_t bit = BITFIELD_BIT(idx);
       if (size) {
          cmd_buffer->state.vbo_bound_mask |= bit;
       } else {
