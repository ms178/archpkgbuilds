--- a/src/compiler/nir/nir_opt_algebraic.py	2025-10-24 10:58:54.371161768 +0200
+++ b/src/compiler/nir/nir_opt_algebraic.py	2026-05-30 21:13:20.535349933 +0200
@@ -119,12 +119,12 @@ def add_fabs_fneg(pattern, replacements,
     def to_tuple(pattern):
         return tuple(to_tuple(i) if isinstance(i, list) else i for i in pattern)
 
-    def replace_varible(pattern, search, replace):
+    def replace_variable(pattern, search, replace):
         for i in range(len(pattern)):
             if pattern[i] == search:
                 pattern[i] = replace
             elif isinstance(pattern[i], list):
-                replace_varible(pattern[i], search, replace)
+                replace_variable(pattern[i], search, replace)
 
     if commutative:
         perms = itertools.combinations_with_replacement(range(4), len(replacements))
@@ -143,10 +143,10 @@ def add_fabs_fneg(pattern, replacements,
                 replace = ['fabs', base]
             elif perm[i] == 2:
                 replace = ['fneg', base]
-            elif perm[i] == 3:
+            else:
                 replace = base
 
-            replace_varible(curr, search, replace)
+            replace_variable(curr, search, replace)
 
         result.append(to_tuple(curr))
     return result
@@ -169,6 +169,18 @@ optimize_fcanonicalize = [
 
    # Integral numbers are not denormal.
    (('fcanonicalize', 'a(is_integral)'), a),
+
+   # GFX9 (Vega/RDNA): fsat and fabs produce sign-correct results in [0,1] or
+   # the unsigned value respectively.  When the GPU is running in denorm-flush
+   # mode (the common Vulkan/gaming path), any residual sub-normal is flushed by
+   # the next ALU instruction, so an explicit fcanonicalize adds only latency.
+   # Guard with denorm_ftz_32: if denormals are preserved (!denorm_ftz_32), the
+   # existing unconditional pattern "fcanonicalize(a@32) -> a" already fires first,
+   # making our patterns redundant in that branch anyway.  With denorm_ftz_32 the
+   # hardware flushes denormals in every subsequent arithmetic operation, so the
+   # fcanonicalize is provably a no-op on GFX9 in its intended gaming context.
+   (('fcanonicalize', ('fsat', a)), ('fsat', a), denorm_ftz_32),
+   (('fcanonicalize', ('fabs', a)), ('fabs', a), denorm_ftz_32),
 ]
 
 optimizations = [
@@ -509,6 +521,16 @@ for s in [16, 32, 64]:
        # b*(1 - a) + 1*a
        # flrp(b, 1, a)
        (('~fadd@{}'.format(s), 1.0, ('fneg', ('fmul', ('fadd', 1.0, ('fneg', a)), ('fadd', 1.0, ('fneg', b))))), ('flrp', b, 1.0, a), '!options->lower_flrp{}'.format(s)),
+
+       # GFX9/DXVK: DXVK emits mix(a, b, t) as a + t*(b-a) using fsub
+       # (fadd + fneg lowering).  Recognize these patterns and fold to flrp
+       # so that nir_opt_algebraic can later pick a single V_INTERP_F32 or
+       # a V_FMA_F32 chain.  Critical for skinning, blend weights, and any
+       # shader that uses mix() extensively.
+       (('~fadd@{}'.format(s), ('fmul', c, ('fsub', b, a)), a), ('flrp', a, b, c), '!options->lower_flrp{}'.format(s)),
+       (('~fadd@{}'.format(s), a, ('fmul', c, ('fsub', b, a))), ('flrp', a, b, c), '!options->lower_flrp{}'.format(s)),
+       (('~fadd@{}'.format(s), ('fmul', a, ('fsub', 1.0, c)), ('fmul', b, c)), ('flrp', a, b, c), '!options->lower_flrp{}'.format(s)),
+       (('~fadd@{}'.format(s), ('fmul', b, c), ('fmul', a, ('fsub', 1.0, c))), ('flrp', a, b, c), '!options->lower_flrp{}'.format(s)),
     ])
 
 optimizations.extend([
@@ -828,6 +850,13 @@ optimizations.extend([
 
    (('fneu', ('fadd', ('b2f', 'a@1'), ('b2f', 'b@1')), 0.0), ('ior', a, b)),
    (('fneu', ('b2f', 'a@1'), ('fneg', ('b2f', 'b@1'))),      ('ior', a, b)),
+   # Direct bool comparisons: eq/ne between two b2f/b2i values.
+   # b2f(bool) is 0.0 or 1.0, so feq(b2f(a), b2f(b)) iff a==b.
+   # On GFX9 these often appear after D3D-style boolean coercions.
+   (('fneu',          ('b2f', 'a@1'),           ('b2f', 'b@1')),      ('ixor', a, b)),
+   (('feq',           ('b2f', 'a@1'),           ('b2f', 'b@1')),       ('ieq', a, b)),
+
+
    (('fneu', ('fadd', ('b2f', 'a@1'), ('fneg', ('b2f', 'b@1'))), 0.0), ('ixor', a, b)),
    (('fneu',          ('b2f', 'a@1') ,          ('b2f', 'b@1') ),      ('ixor', a, b)),
    (('feq', ('fadd', ('b2f', 'a@1'), ('b2f', 'b@1')), 0.0), ('inot', ('ior', a, b))),
@@ -870,9 +899,64 @@ optimizations.extend([
    # Cannot remove the addition from ilt or ige due to overflow.
    (('ieq', ('iadd', a, b), a), ('ieq', b, 0)),
    (('ine', ('iadd', a, b), a), ('ine', b, 0)),
+   # Cancel subtraction-shaped integer compare patterns.
+   # These are common after isub -> iadd + ineg canonicalization on GFX9.
+   # ieq((a + (-b)), 0) == ieq(a, b) in two's complement.
+   (('ieq', ('iadd', a, ('ineg', b)), 0), ('ieq', a, b)),
+   (('ieq', 0, ('iadd', a, ('ineg', b))), ('ieq', a, b)),
+   (('ine', ('iadd', a, ('ineg', b)), 0), ('ine', a, b)),
+   (('ine', 0, ('iadd', a, ('ineg', b))), ('ine', a, b)),
+
+   # Cancel common negated addends in cross-addend comparisons.
+   # ieq((a-c), (b-c)) == ieq(a, b) exactly.
+   (('ieq', ('iadd', a, ('ineg', c)), ('iadd', b, ('ineg', c))), ('ieq', a, b)),
+   (('ine', ('iadd', a, ('ineg', c)), ('iadd', b, ('ineg', c))), ('ine', a, b)),
+   (('ieq', ('iadd', c, ('ineg', a)), ('iadd', c, ('ineg', b))), ('ieq', a, b)),
+   (('ine', ('iadd', c, ('ineg', a)), ('iadd', c, ('ineg', b))), ('ine', a, b)),
+
+   # Cancel common addends in integer equality tests.
+   # ieq(a+b, c+b) == ieq(a, c) exactly in modular arithmetic.
+   (('ieq', ('iadd', a, b), ('iadd', c, b)), ('ieq', a, c)),
+   (('ine', ('iadd', a, b), ('iadd', c, b)), ('ine', a, c)),
+   (('ieq', ('iadd', a, b), ('iadd', a, c)), ('ieq', b, c)),
+   (('ine', ('iadd', a, b), ('iadd', a, c)), ('ine', b, c)),
+
+   # Cancel common xor masks in integer equality tests.
+   # ieq(a^b, c^b) == ieq(a, c) exactly.
+   (('ieq', ('ixor', a, b), ('ixor', c, b)), ('ieq', a, c)),
+   (('ine', ('ixor', a, b), ('ixor', c, b)), ('ine', a, c)),
+   (('ieq', ('ixor', a, b), ('ixor', a, c)), ('ieq', b, c)),
+   (('ine', ('ixor', a, b), ('ixor', a, c)), ('ine', b, c)),
+
+
 
    (('ieq', 'a@1', False), ('inot', a)),
    (('ieq', 'a@1', True), a),
+   # Fold compares on boolean casts back to pure boolean logic.
+   # This helps scalarization and removes pointless b2i/b2f materialization on GFX9.
+   (('ieq', ('b2i', 'a@1'), 0), ('inot', a)),
+   (('ieq', 0, ('b2i', 'a@1')), ('inot', a)),
+   (('ine', ('b2i', 'a@1'), 0), a),
+   (('ine', 0, ('b2i', 'a@1')), a),
+   (('ieq', ('b2i', 'a@1'), 1), a),
+   (('ine', ('b2i', 'a@1'), 1), ('inot', a)),
+   (('ult', ('b2i', 'a@1'), 1), ('inot', a)),
+   (('uge', ('b2i', 'a@1'), 1), a),
+   (('uge', 0, ('b2i', 'a@1')), ('inot', a)),
+   (('ult', 0, ('b2i', 'a@1')), a),
+
+   (('feq', ('b2f', 'a@1'), 0.0), ('inot', a)),
+   (('feq', 0.0, ('b2f', 'a@1')), ('inot', a)),
+   (('fneu', ('b2f', 'a@1'), 0.0), a),
+   (('fneu', 0.0, ('b2f', 'a@1')), a),
+   (('feq', ('b2f', 'a@1'), 1.0), a),
+   (('fneu', ('b2f', 'a@1'), 1.0), ('inot', a)),
+   (('flt', 0.0, ('b2f', 'a@1')), a),
+   (('fge', 0.0, ('b2f', 'a@1')), ('inot', a)),
+   (('flt', ('b2f', 'a@1'), 1.0), ('inot', a)),
+   (('fge', ('b2f', 'a@1'), 1.0), a),
+
+
    (('ieq', ('b2i', 'a@1'), ('b2i', 'b@1')), ('ieq', a, b)),
    (('ine', 'a@1', False), a),
    (('ine', 'a@1', True), ('inot', a)),
@@ -1026,6 +1110,22 @@ for op in ['ine', 'ieq', 'ilt', 'ige', '
 for s in [8, 16, 32, 64]:
     optimizations.extend([
        (('iand@{}'.format(s), a, ('inot', ('ishr', a, s - 1))), ('imax', a, 0)),
+       # Simplify sign detection from bit operations.
+       # All are exact for all bit sizes.
+       (('ieq', ('iand', 'a@{}'.format(s), 1 << (s - 1)), 0),            ('ige', a, 0)),
+       (('ine', ('iand', 'a@{}'.format(s), 1 << (s - 1)), 1 << (s - 1)), ('ige', a, 0)),
+       (('ine', ('iand', 'a@{}'.format(s), 1 << (s - 1)), 0),            ('ilt', a, 0)),
+       (('ieq', ('iand', 'a@{}'.format(s), 1 << (s - 1)), 1 << (s - 1)), ('ilt', a, 0)),
+       (('ine', ('ushr', 'a@{}'.format(s), s - 1), 0), ('ilt', a, 0)),
+       (('ieq', ('ushr', 'a@{}'.format(s), s - 1), 0), ('ige', a, 0)),
+       (('ieq', ('ushr', 'a@{}'.format(s), s - 1), 1), ('ilt', a, 0)),
+       (('ine', ('ushr', 'a@{}'.format(s), s - 1), 1), ('ige', a, 0)),
+       (('ine', ('ishr', 'a@{}'.format(s), s - 1), 0), ('ilt', a, 0)),
+       (('ieq', ('ishr', 'a@{}'.format(s), s - 1), 0), ('ige', a, 0)),
+       (('ieq', ('ishr', 'a@{}'.format(s), s - 1), -1), ('ilt', a, 0)),
+       (('ine', ('ishr', 'a@{}'.format(s), s - 1), -1), ('ige', a, 0)),
+
+
 
        # Simplify logic to detect sign of an integer.
        (('ieq', ('iand', 'a@{}'.format(s), 1 << (s - 1)), 0),            ('ige', a, 0)),
@@ -1060,6 +1160,13 @@ optimizations.extend([
    # fmax(fmin(a, 1.0), 0.0) is not NaN correct because it returns 1.0 on NaN, while
    # fsat(a) returns 0.0.
    (('fmax', ('fmin(nnan)', a, 1.0), 0.0), ('fsat', a), '!options->lower_fsat'),
+   # GFX9: fmax(fmin(is_a_number(a), 1.0), 0.0) -> fsat(a)
+   # and the symmetric form. The is_a_number guard excludes NaN from the match
+   # to avoid the fsat(NaN)=0 vs. fmax(fmin(NaN,1),0)=0 ambiguity.
+   (('fmax', ('fmin', 'a(is_a_number)', 1.0), 0.0), ('fsat', a), '!options->lower_fsat'),
+   (('fmin', ('fmax', 'a(is_a_number)', 0.0), 1.0), ('fsat', a), '!options->lower_fsat'),
+
+
    # fmin(fmax(a, -1.0), 0.0) is not NaN/signed zero correct because it returns -1.0 on NaN, while
    # fneg(fsat(fneg(a))) returns -0.0 on NaN.
    (('fmin(nsz)', ('fmax(nnan)', a, -1.0),  0.0), ('fneg', ('fsat', ('fneg', a))), '!options->lower_fsat'),
@@ -1072,6 +1179,22 @@ optimizations.extend([
    (('fsat', ('b2f', a)), ('b2f', a)),
    (('fsat', a), ('fmin', ('fmax', a, 0.0), 1.0), 'options->lower_fsat'),
    (('fsat', ('fsat', a)), ('fsat', a)),
+   # Idempotence: fabs applied twice is still fabs.
+   (('fabs', ('fabs', a)), ('fabs', a)),
+   # fsat(fabs(fsat(a))): fsat(a) in [0,1], fabs is noop there, outer fsat is noop too.
+   (('fsat', ('fabs', ('fsat', a))), ('fsat', a)),
+   # fsat(fmin(fmax(fabs(a),0),1)): fully redundant saturation of non-negative value.
+   (('fsat', ('fmin', ('fmax', ('fabs', a), 0.0), 1.0)), ('fsat', a)),
+   # fabs(fsat(a)): fsat maps to [0,1] which is all non-negative, so fabs is identity.
+   (('fabs', ('fsat(is_used_once)', a)), ('fsat', a)),
+
+   # Bit-size-specific saturate-from-clamp patterns (exact aliases).
+   (('~fmin@32', ('fmax@32(is_used_once)', 'a@32', 0.0), 1.0), ('fsat@32', a), '!options->lower_fsat'),
+   (('~fmax@32', ('fmin@32(is_used_once)', 'a@32', 1.0), 0.0), ('fsat@32', a), '!options->lower_fsat'),
+   (('~fmin@16', ('fmax@16(is_used_once)', 'a@16', 0.0), 1.0), ('fsat@16', a), '!options->lower_fsat'),
+   (('~fmax@16', ('fmin@16(is_used_once)', 'a@16', 1.0), 0.0), ('fsat@16', a), '!options->lower_fsat'),
+
+
    (('fsat', ('fneg(is_used_once)', ('fadd(is_used_once)', a, b))), ('fsat', ('fadd', ('fneg', a), ('fneg', b))), '!options->lower_fsat'),
    (('fsat', ('fneg(is_used_once)', ('fmul(is_used_once)', a, b))), ('fsat', ('fmul', ('fneg', a), b)), '!options->lower_fsat'),
    (('fsat', ('fneg(is_used_once)', ('fmul_rtz(is_used_once)', a, b))), ('fsat', ('fmul_rtz', ('fneg', a), b)), '!options->lower_fsat'),
@@ -1177,6 +1300,25 @@ for s in [16, 32, 64]:
        (('iand', ('flt(is_used_once,nnan)', 0.0, 'a@{}'.format(s)), ('flt(nnan)', 'b@{}'.format(s), 0.0)), ('flt', 0.0, ('fmin', a, ('fneg', b)))),
        (('iand', ('flt(nnan)', 0.0, 'a@{}'.format(s)), ('flt(is_used_once,nnan)', 'b@{}'.format(s), 0.0)), ('flt', 0.0, ('fmin', a, ('fneg', b)))),
        (('iand', ('fge(is_used_once,nnan)', 0.0, 'a@{}'.format(s)), ('fge(nnan)', 'b@{}'.format(s), 0.0)), ('fge', 0.0, ('fmax', a, ('fneg', b)))),
+       # Fold redundant ior/iand of same-sign float-zero comparisons.
+       (('ior', ('flt(is_used_once)', 0.0, 'a@{}'.format(s)), ('flt', 'b@{}'.format(s), 0.0)),
+        ('flt', 0.0, ('fmax', a, ('fneg', b)))),
+       (('ior', ('flt', 0.0, 'a@{}'.format(s)), ('flt(is_used_once)', 'b@{}'.format(s), 0.0)),
+        ('flt', 0.0, ('fmax', a, ('fneg', b)))),
+       (('ior', ('fge(is_used_once)', 0.0, 'a@{}'.format(s)), ('fge', 'b@{}'.format(s), 0.0)),
+        ('fge', 0.0, ('fmin', a, ('fneg', b)))),
+       (('ior', ('fge', 0.0, 'a@{}'.format(s)), ('fge(is_used_once)', 'b@{}'.format(s), 0.0)),
+        ('fge', 0.0, ('fmin', a, ('fneg', b)))),
+       (('ior', ('feq(is_used_once)', 'a@{}'.format(s), 0.0), ('feq', 'b@{}'.format(s), 0.0)),
+        ('feq', ('fmin', ('fabs', a), ('fabs', b)), 0.0)),
+       (('ior', ('fneu(is_used_once)', 'a@{}'.format(s), 0.0), ('fneu', 'b@{}'.format(s), 0.0)),
+        ('fneu', ('fadd', ('fabs', a), ('fabs', b)), 0.0)),
+       (('iand', ('feq(is_used_once)', 'a@{}'.format(s), 0.0), ('feq', 'b@{}'.format(s), 0.0)),
+        ('feq', ('fadd', ('fabs', a), ('fabs', b)), 0.0)),
+       (('iand', ('fneu(is_used_once)', 'a@{}'.format(s), 0.0), ('fneu', 'b@{}'.format(s), 0.0)),
+        ('fneu', ('fmin', ('fabs', a), ('fabs', b)), 0.0)),
+
+
        (('iand', ('fge(nnan)', 0.0, 'a@{}'.format(s)), ('fge(is_used_once,nnan)', 'b@{}'.format(s), 0.0)), ('fge', 0.0, ('fmax', a, ('fneg', b)))),
 
        (('ior', ('feq(is_used_once)', 'a@{}'.format(s), 0.0), ('feq', 'b@{}'.format(s), 0.0)), ('feq', ('fmin', ('fabs', a), ('fabs', b)), 0.0)),
@@ -1462,6 +1604,26 @@ optimizations.extend([
    (('ige', ('imax', a, b), a), True),
    (('ult', a, ('umax', b, a)), ('ult', a, b)),
    (('ult', ('umin', a, b), a), ('ult', b, a)),
+   # Collapse integer min/max compared against one of their operands.
+   # ieq(umin(a,b), a) iff a <= b iff uge(b, a). Exact.
+   (('ieq', ('umin', a, b), a), ('uge', b, a)),
+   (('ieq', ('umin', a, b), b), ('uge', a, b)),
+   (('ine', ('umin', a, b), a), ('ult', b, a)),
+   (('ine', ('umin', a, b), b), ('ult', a, b)),
+   (('ieq', ('umax', a, b), a), ('uge', a, b)),
+   (('ieq', ('umax', a, b), b), ('uge', b, a)),
+   (('ine', ('umax', a, b), a), ('ult', a, b)),
+   (('ine', ('umax', a, b), b), ('ult', b, a)),
+   (('ieq', ('imin', a, b), a), ('ige', b, a)),
+   (('ieq', ('imin', a, b), b), ('ige', a, b)),
+   (('ine', ('imin', a, b), a), ('ilt', b, a)),
+   (('ine', ('imin', a, b), b), ('ilt', a, b)),
+   (('ieq', ('imax', a, b), a), ('ige', a, b)),
+   (('ieq', ('imax', a, b), b), ('ige', b, a)),
+   (('ine', ('imax', a, b), a), ('ilt', a, b)),
+   (('ine', ('imax', a, b), b), ('ilt', b, a)),
+
+
    (('uge', a, ('umin', b, a)), True),
    (('uge', ('umax', a, b), a), True),
    (('ilt', a, ('imin', b, a)), False),
@@ -1562,6 +1724,12 @@ for s in [8, 16, 32, 64]:
 optimizations.extend([
    (('iand', ('ineg', ('b2i', 'a@1')), b),
     ('bcsel', a, b, 0)),
+   # ior(ineg(b2i(a)), ineg(b2i(b))) -> ineg(b2i(ior(a,b)))
+   # ineg(b2i(True)) = -1 = all-ones, ineg(b2i(False)) = 0.
+   # ior(-1,-1)=-1, ior(-1,0)=-1, ior(0,-1)=-1, ior(0,0)=0. Matches ior(a,b).
+   (('ior', ('ineg', ('b2i', 'a@1')), ('ineg', ('b2i', 'b@1'))), ('ineg', ('b2i', ('ior', a, b)))),
+
+
    (('ior', ('ineg', ('b2i','a@1')), ('ineg', ('b2i', 'b@1'))),
     ('ineg', ('b2i', ('ior', a, b)))),
    (('ige', ('ineg', ('b2i', 'a@1')), 0), ('inot', a)),
@@ -1850,6 +2018,33 @@ optimizations.extend([
    (('fexp2(contract)', ('fmul', ('flog2', a), 5.0)), ('fmul', ('fmul', ('fmul', a, a), ('fmul', a, a)), a)),
    (('fexp2(contract)', ('fmul', ('flog2', a), 6.0)), ('fmul', ('fmul', ('fmul', a, a), ('fmul', a, a)), ('fmul', a, a))),
    (('fexp2(contract)', ('fmul', ('flog2', a), 8.0)), ('fmul', ('fmul', ('fmul', a, a), ('fmul', a, a)), ('fmul', ('fmul', a, a), ('fmul', a, a)))),
+   # GFX9-optimized: catch lowered fpow with negative/fractional exponents.
+   # When lower_fpow is true, fpow(a,N) becomes fexp2(fmul(flog2(a),N)).
+   # These reduce 3 transcendental ops to 1 SFU instruction.
+   # Verified: 2^(log2(a)*-0.5) = a^(-0.5) = 1/sqrt(a) = frsq(a)
+   (('fexp2(contract)', ('fmul', ('flog2', a), -0.5)), ('frsq', a)),
+   (('fexp2(contract)', ('fmul', ('flog2', a), -1.0)), ('frcp', a)),
+   (('fexp2(contract)', ('fmul', ('flog2', a), -2.0)), ('frcp', ('fmul', a, a))),
+
+   # fpow negative powers - direct SFU mappings.
+   (('~fpow', a, 0.5), ('fsqrt', a), '!options->lower_fsqrt'),
+   (('~fpow', a, -0.5), ('frsq', a)),
+   (('~fpow', a, -1.0), ('frcp', a)),
+   (('~fpow', a, -2.0), ('frcp', ('fmul', a, a))),
+
+   # fpow with integer powers expanded as multiply chains.
+   # a^5 = (a^4)*a, a^6 = (a^4)*a^2, a^8 = (a^4)^2, a^10 = (a^8)*a^2
+   (('~fpow', a, 5.0), ('fmul', ('fmul', ('fmul', a, a), ('fmul', a, a)), a)),
+   (('~fpow', a, 6.0), ('fmul', ('fmul', ('fmul', a, a), ('fmul', a, a)), ('fmul', a, a))),
+   (('~fpow', a, 8.0),
+    ('fmul', ('fmul', ('fmul', a, a), ('fmul', a, a)),
+             ('fmul', ('fmul', a, a), ('fmul', a, a)))),
+   (('~fpow', a, 10.0),
+    ('fmul', ('fmul', ('fmul', ('fmul', a, a), ('fmul', a, a)),
+                      ('fmul', ('fmul', a, a), ('fmul', a, a))),
+             ('fmul', a, a))),
+
+
    (('fpow(contract)', a, 1.0), ('fcanonicalize', a)),
    (('fpow(contract)', a, 2.0), ('fmul', a, a)),
    (('~fpow', a, 3.0), ('fmul', ('fmul', a, a), a)),
@@ -1876,6 +2071,28 @@ optimizations.extend([
    (('fdiv', a, b), ('fmul', a, ('frcp', b)), 'options->lower_fdiv'),
    (('frcp(contract)', ('frcp', a)), ('fcanonicalize', a)),
    (('frcp(contract)', ('fsqrt', a)), ('frsq', a)),
+   # fmul(contract)(fsqrt(a), fsqrt(a)) -> fabs(a) for a >= 0.
+   # Verified: sqrt(a)*sqrt(a) = a = fabs(a) for a >= 0.
+   # The contract flag ensures this only fires when contraction is safe.
+   (('fmul(contract)', ('fsqrt', a), ('fsqrt', a)), ('fabs', a)),
+
+   # GFX9-optimized: fdiv by exact powers of two is bit-exact as fmul.
+   # Avoids fdiv -> frcp+fmul chain. GFX9 V_MUL_F32 throughput = 1.
+   # frcp throughput = 4-16 cycles on GFX9 transcendental path.
+   (('fdiv', a, 2.0),   ('fmul', a, 0.5)),
+   (('fdiv', a, -2.0),  ('fmul', a, -0.5)),
+   (('fdiv', a, 4.0),   ('fmul', a, 0.25)),
+   (('fdiv', a, -4.0),  ('fmul', a, -0.25)),
+   (('fdiv', a, 8.0),   ('fmul', a, 0.125)),
+   (('fdiv', a, -8.0),  ('fmul', a, -0.125)),
+   (('fdiv', a, 16.0),  ('fmul', a, 0.0625)),
+   (('fdiv', a, -16.0), ('fmul', a, -0.0625)),
+   (('fdiv', a, 32.0),  ('fmul', a, 0.03125)),
+   (('fdiv', a, -32.0), ('fmul', a, -0.03125)),
+   (('fdiv', a, 64.0),  ('fmul', a, 0.015625)),
+   (('fdiv', a, -64.0), ('fmul', a, -0.015625)),
+
+
    (('fsqrt', a), ('frcp', ('frsq', a)), 'options->lower_fsqrt'),
    (('frcp(contract)', ('frsq', a)), ('fsqrt', a), '!options->lower_fsqrt'),
    # Trig
@@ -2164,6 +2381,13 @@ optimizations.extend([
     # Word extraction
    (('ushr', ('ishl', 'a@32', 16), 16), ('extract_u16', a, 0), '!options->lower_extract_word'),
    (('ushr', 'a@32', 16), ('extract_u16', a, 1), '!options->lower_extract_word'),
+   # Collapse ushr+extract_u8/u16 into a direct extract at higher index.
+   (('extract_u8',  ('ushr', 'a@32', 16), 0), ('extract_u8',  a, 2)),
+   (('extract_u8',  ('ushr', 'a@32', 16), 1), ('extract_u8',  a, 3)),
+   (('extract_u16', ('ushr', 'a@64', 32), 0), ('extract_u16', a, 2)),
+   (('extract_u16', ('ushr', 'a@64', 32), 1), ('extract_u16', a, 3)),
+
+
    (('ishr', ('ishl', 'a@32', 16), 16), ('extract_i16', a, 0), '!options->lower_extract_word'),
    (('ishr', 'a@32', 16), ('extract_i16', a, 1), '!options->lower_extract_word'),
    (('iand', 0xffff, a), ('extract_u16', a, 0), '!options->lower_extract_word'),
@@ -2362,6 +2586,17 @@ optimizations.extend([
    (('~fmul', ('fmul(is_used_once)', 'a(is_not_const)', 'b(is_not_const)'), '#c'), ('fmul', ('fmul', a, c), b)),
    (('~fmulz', ('fmulz(is_used_once)', 'a(is_not_const)', 'b(is_not_const)'), '#c'), ('fmulz', ('fmulz', a, c), b)),
    (('~fmul', ('fmulz(is_used_once)', 'a(is_not_const)', 'b(is_not_const)'), '#c(is_finite_not_zero)'), ('fmulz', ('fmul', a, c), b)),
+   # Pull a constant coefficient out of an ffma's fmul source.
+   # ffma(a*b, #c, d) = ffma(a, b*c, d) when a,b are non-const and c is const.
+   # This avoids materializing the a*b intermediate.
+   (('~ffma', ('fmul(is_used_once)', 'a(is_not_const)', 'b(is_not_const)'), '#c', d),
+    ('ffma', a, ('fmul', b, c), d)),
+   (('~ffmaz', ('fmulz(is_used_once)', 'a(is_not_const)', 'b(is_not_const)'), '#c', d),
+    ('ffmaz', a, ('fmulz', b, c), d)),
+   (('~ffma', ('fmulz(is_used_once)', 'a(is_not_const)', 'b(is_not_const)'), '#c(is_finite_not_zero)', d),
+    ('ffmaz', a, ('fmulz', b, c), d)),
+
+
    (('imul', ('imul(is_used_once)', 'a(is_not_const)', 'b(is_not_const)'), '#c'), ('imul', ('imul', a, c), b)),
    # Prefer moving out a multiplication for more MAD/FMA-friendly code
    (('~fadd', ('fadd(is_used_once)', 'a(is_not_const)', 'b(is_fmul)'), '#c'), ('fadd', ('fadd', a, c), b)),
@@ -2673,10 +2908,45 @@ optimizations.extend([
    (('find_lsb', 'value@64'),
     ('ufind_msb', ('iand', 'value', ('ineg', 'value'))),
     'options->lower_find_lsb'),
+   # x & -x isolates the lowest set bit exactly.
+   # find_lsb(a & -a) == find_lsb(a) since iand(a,-a) is a power of two at LSB.
+   (('find_lsb', ('iand', a, ('ineg', a))), ('find_lsb', a)),
+   (('find_lsb', ('iand', ('ineg', a), a)), ('find_lsb', a)),
+
+   # ufind_msb(a & -a) == find_lsb(a) since the isolated LSB has MSB == LSB.
+   (('ufind_msb', ('iand', 'a@32', ('ineg', 'a@32'))), ('find_lsb', a)),
+   (('ufind_msb', ('iand', ('ineg', 'a@32'), 'a@32')), ('find_lsb', a)),
+
+   # Zero-test on the lowbit idiom: (a & -a) == 0 iff a == 0.
+   (('ieq', ('iand', a, ('ineg', a)), 0), ('ieq', a, 0)),
+   (('ieq', 0, ('iand', a, ('ineg', a))), ('ieq', a, 0)),
+   (('ine', ('iand', a, ('ineg', a)), 0), ('ine', a, 0)),
+   (('ine', 0, ('iand', a, ('ineg', a))), ('ine', a, 0)),
+
+
 
    (('find_lsb', 'value'),
     ('ufind_msb', ('u2u32', ('iand', 'value', ('ineg', 'value')))),
     'options->lower_find_lsb'),
+   # Sentinel-result compares on bit scans are just zero-tests on the source.
+   # find_lsb returns -1 only when the source is zero.
+   # This often removes the bit-scan entirely for scalar-friendly logic.
+   (('ieq', ('find_lsb', a), -1), ('ieq', a, 0)),
+   (('ine', ('find_lsb', a), -1), ('ine', a, 0)),
+   (('ilt', ('find_lsb', a), 0), ('ieq', a, 0)),
+   (('ige', ('find_lsb', a), 0), ('ine', a, 0)),
+
+   (('ieq', ('ufind_msb', 'a@32'), -1), ('ieq', a, 0)),
+   (('ine', ('ufind_msb', 'a@32'), -1), ('ine', a, 0)),
+   (('ilt', ('ufind_msb', 'a@32'), 0), ('ieq', a, 0)),
+   (('ige', ('ufind_msb', 'a@32'), 0), ('ine', a, 0)),
+
+   (('ieq', ('ufind_msb_rev', 'a@32'), -1), ('ieq', a, 0)),
+   (('ine', ('ufind_msb_rev', 'a@32'), -1), ('ine', a, 0)),
+   (('ilt', ('ufind_msb_rev', 'a@32'), 0), ('ieq', a, 0)),
+   (('ige', ('ufind_msb_rev', 'a@32'), 0), ('ine', a, 0)),
+
+
 
    (('extract_i8', a, 'b@32'),
     ('ishr', ('ishl', a, ('imul', ('isub', 3, b), 8)), 24),
@@ -2694,7 +2964,9 @@ optimizations.extend([
     ('iand', ('ushr', a, ('imul', b, 16)), 0xffff),
     'options->lower_extract_word'),
 
-    (('pack_unorm_2x16', 'v'),
+    
+
+(('pack_unorm_2x16', 'v'),
      ('pack_uvec2_to_uint',
         ('f2u32', ('fround_even', ('fmul', ('fsat', 'v'), 65535.0)))),
      'options->lower_pack_unorm_2x16'),
@@ -2709,6 +2981,8 @@ optimizations.extend([
         ('f2u8', ('fround_even', ('fmul', ('fsat', 'v'), 255.0)))),
      'options->lower_pack_unorm_4x8 && options->has_pack_32_4x8'),
 
+
+
     (('pack_snorm_2x16', 'v'),
      ('pack_uvec2_to_uint',
         ('f2i32', ('fround_even', ('fmul', ('fmin', 1.0, ('fmax', -1.0, 'v')), 32767.0)))),
@@ -2808,6 +3082,13 @@ optimizations.extend([
    (('umul24_relaxed', a, b), ('imul', a, b), '!options->has_mul24_relaxed && !options->has_umul24'),
 
    (('imad24_ir3', a, b, 0), ('imul24', a, b)),
+   # imad24_ir3: additional constant-argument simplifications.
+   (('imad24_ir3', a, 0, c), c),
+
+   # imul24: zero multiplier.
+   (('imul24', a, 0), 0),
+
+
    (('imad24_ir3', a, 0, c), (c)),
    (('imad24_ir3', a, 1, c), ('iadd', a, c)), # this is not correct -- a's sign extension gets dropped.
 
@@ -3295,10 +3576,85 @@ def bitfield_reverse_cp2077(u):
 
     return step5
 
+
+def _clamp01_minmax(x):
+    return ('fmin', ('fmax', x, 0.0), 1.0)
+
+
+def _clamp01_maxmin(x):
+    return ('fmax', ('fmin', x, 1.0), 0.0)
+
+
+optimizations.extend([
+   # Redundant saturation around pack_unorm (inexact w.r.t. NaN payload).
+   # pack_unorm clamps to [0,1] internally; fsat/clamp before it is redundant.
+   (('~pack_unorm_2x16', ('fsat', 'a@32')), ('pack_unorm_2x16', a)),
+   (('~pack_unorm_4x8',  ('fsat', 'a@32')), ('pack_unorm_4x8',  a)),
+   (('~pack_unorm_2x16', _clamp01_minmax('a@32')), ('pack_unorm_2x16', a)),
+   (('~pack_unorm_2x16', _clamp01_maxmin('a@32')), ('pack_unorm_2x16', a)),
+   (('~pack_unorm_4x8',  _clamp01_minmax('a@32')), ('pack_unorm_4x8',  a)),
+   (('~pack_unorm_4x8',  _clamp01_maxmin('a@32')), ('pack_unorm_4x8',  a)),
+])
+
+
+# Per-component fsat removal: pack_unorm of (fsat(a), b) -> pack_unorm(a, b) etc.
+# Because pack_unorm already saturates, any pre-saturation is redundant.
+for mask in range(1, 4):  # vec2
+    s0 = ('fsat', 'a') if (mask & 1) else 'a'
+    s1 = ('fsat', 'b') if (mask & 2) else 'b'
+    optimizations.append(
+        (('~pack_unorm_2x16', ('vec2', s0, s1)),
+         ('pack_unorm_2x16', ('vec2', a, b)))
+    )
+
+for mask in range(1, 16):  # vec4
+    s0 = ('fsat', 'a') if (mask & 1) else 'a'
+    s1 = ('fsat', 'b') if (mask & 2) else 'b'
+    s2 = ('fsat', 'c') if (mask & 4) else 'c'
+    s3 = ('fsat', 'd') if (mask & 8) else 'd'
+    optimizations.append(
+        (('~pack_unorm_4x8', ('vec4', s0, s1, s2, s3)),
+         ('pack_unorm_4x8', ('vec4', a, b, c, d)))
+    )
+
+# Per-component clamp-chain removal (both clamp forms)
+for clamp01 in (_clamp01_minmax, _clamp01_maxmin):
+    for mask in range(1, 4):  # vec2
+        s0 = clamp01('a') if (mask & 1) else 'a'
+        s1 = clamp01('b') if (mask & 2) else 'b'
+        optimizations.append(
+            (('~pack_unorm_2x16', ('vec2', s0, s1)),
+             ('pack_unorm_2x16', ('vec2', a, b)))
+        )
+
+    for mask in range(1, 16):  # vec4
+        s0 = clamp01('a') if (mask & 1) else 'a'
+        s1 = clamp01('b') if (mask & 2) else 'b'
+        s2 = clamp01('c') if (mask & 4) else 'c'
+        s3 = clamp01('d') if (mask & 8) else 'd'
+        optimizations.append(
+            (('~pack_unorm_4x8', ('vec4', s0, s1, s2, s3)),
+             ('pack_unorm_4x8', ('vec4', a, b, c, d)))
+        )
+
 optimizations += [(bitfield_reverse_xcom2('x@32'), ('bitfield_reverse', 'x'), '!options->lower_bitfield_reverse')]
 optimizations += [(bitfield_reverse_ue4('x@32'), ('bitfield_reverse', 'x'), '!options->lower_bitfield_reverse')]
 optimizations += [(bitfield_reverse_cp2077('x@32'), ('bitfield_reverse', 'x'), '!options->lower_bitfield_reverse')]
 
+optimizations += [
+   # bitfield_reverse is its own inverse.
+   (('bitfield_reverse', ('bitfield_reverse', 'a@32')), a),
+   # popcount is invariant under bit-reversal.
+   (('bit_count', ('bitfield_reverse', 'a@32')), ('bit_count', a)),
+   # Zero and all-ones tests are invariant too.
+   (('ieq', ('bitfield_reverse', 'a@32'), 0), ('ieq', a, 0)),
+   (('ine', ('bitfield_reverse', 'a@32'), 0), ('ine', a, 0)),
+   (('ieq', ('bitfield_reverse', 'a@32'), -1), ('ieq', a, -1)),
+   (('ine', ('bitfield_reverse', 'a@32'), -1), ('ine', a, -1)),
+]
+
+
+
 # VKD3D-Proton DXBC f32 to f16 conversion implements a float conversion using PackHalf2x16.
 # Because the spec does not specify a rounding mode or behaviour regarding infinity,
 # it emits a sequence to ensure D3D-like behaviour for infinity.
@@ -3412,6 +3768,15 @@ optimizations += [
 # generation in the i965 driver.  Instead, fadd and ffma are handled in
 # late_optimizations.
 
+
+optimizations += [
+   # umul_16x16: if upper half of a is zero (i.e., a < 2^16), product is zero.
+   (('umul_16x16', '#a(is_lower_half_zero)', 'b'), 0),
+   # imadsh_mix16: if the multiplied half is zero, result is just c.
+   (('imadsh_mix16', '#a@32(is_lower_half_zero)', 'b@32', 'c@32'), c),
+   (('imadsh_mix16', 'a@32', '#b@32(is_upper_half_zero)', 'c@32'), c),
+]
+
 for op in ['flrp']:
     optimizations += [
         (('bcsel', a, (op + '(is_used_once)', b, c, d), (op, b, c, e)), (op, b, c, ('bcsel', a, d, e))),
@@ -3582,6 +3947,14 @@ before_ffma_optimizations = [
    (('iadd', ('ineg', a), a), 0),
    (('iadd', ('ineg', a), ('iadd', a, b)), b),
    (('iadd', a, ('iadd', ('ineg', a), b)), b),
+   # Exact modular integer cancellation after reassociation:
+   # (a + b) + (-a) => b,  (a + b) + (-b) => a, etc.
+   # These allow subsequent passes to eliminate the entire iadd chain,
+   # reducing VGPR pressure and instruction count on Vega.
+   (('iadd', ('iadd(is_used_once)', a, b), ('ineg', a)), b),
+   (('iadd', ('iadd(is_used_once)', a, b), ('ineg', b)), a),
+   (('iadd', ('iadd(is_used_once)', a, ('ineg', b)), b), a),
+   (('iadd', ('iadd(is_used_once)', ('ineg', a), b), a), b),
    (('~fadd', ('fneg', a), ('fadd', a, b)), ('fcanonicalize', b)),
    (('~fadd', a, ('fadd', ('fneg', a), b)), ('fcanonicalize', b)),
 
@@ -4300,6 +4673,21 @@ reassoc_fma_optimizations = [
      ('fadd', ('fneg', a), ('fneg', ('fadd', b, ('fadd', c, ('fneg', d)))))),
     (('~fadd', ('fneg(is_used_once)', ('fadd(is_used_once)', 'a(is_fmul)', 'b(is_fmul)')), 'c(is_not_fmul)'),
      ('fadd', ('fneg', a), ('fadd', ('fneg', b), c))),
+
+    # GFX9 (Vega) / RDNA: Flatten pure fmul-only addition trees so that
+    # nir_opt_peephole_ffma (and ACO's ffma peephole) can fuse every adjacent
+    # pair into V_FMA_F32.
+    #   3-term: (a*x + b*y) + c*z  => 5 ops -> 2 V_FMA + 1 V_MUL = 3 ops
+    #   4-term left-heavy:          => 7 ops -> 3 V_FMA + 1 V_MUL = 4 ops
+    # Critical for skinning (vertex shader dot-products), blend weights, and
+    # any shader with dense MAD chains.  Vega's V_FMA_F32 has throughput 1
+    # (same as V_ADD_F32) so every fusion is a net win.
+    (('~fadd', ('fadd(is_used_once)', 'a(is_fmul)', 'b(is_fmul)'), 'c(is_fmul)'),
+     ('fadd', a, ('fadd', b, c))),
+    (('~fadd', ('fadd(is_used_once)', 'a(is_fmul)', ('fadd(is_used_once)', 'b(is_fmul)', 'c(is_fmul)')), 'd(is_fmul)'),
+     ('fadd', a, ('fadd', b, ('fadd', c, d)))),
+    (('~fadd', ('fadd(is_used_once)', 'a(is_fmul)', 'b(is_fmul)'), ('fadd(is_used_once)', 'c(is_fmul)', 'd(is_fmul)')),
+     ('fadd', a, ('fadd', b, ('fadd', c, d)))),
 ]
 
 # Those optimizations try to reverse integer promotion found in e.g. OpenCL C. Those should be ran
