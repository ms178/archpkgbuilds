diff --git a/engineering/FOLLOWUP-2026-09-22C-narrow-cmp-fold-and-setcc-revert.md b/engineering/FOLLOWUP-2026-09-22C-narrow-cmp-fold-and-setcc-revert.md
new file mode 100644
index 00000000..7ac9d284
--- /dev/null
+++ b/engineering/FOLLOWUP-2026-09-22C-narrow-cmp-fold-and-setcc-revert.md
@@ -0,0 +1,166 @@
+# Follow-up: the narrow zext→immediate-compare fold, a false-ISA-law revert, and the flag-law guard that caught it
+
+Date: 2026-09-22 (session 61, continued)
+Base: `c7917656` (`ms178/lccc` main, post-#591; carries our S05 as `0fbe7ef2`
+and the 73a2ab1e LEA-hoist/RORX work, red-teamed earlier this session).
+Head: `5e97d517` — S06, snapshot `ms178-1.patch` 28876 B sha `4b33a998…`,
+APPLIES-CLEAN (ledger row 6).
+
+---
+
+## 1. Where the session picked up
+
+Session 61 (first half) landed the i686 narrow-compare peephole work and left
+two debts: the `leak()` in the source parser, and an unproven corner of the
+design. This half closed both — and the guard test written to prove the fold
+correct instead **caught an unrelated live miscompile**, which turned out to
+be the more valuable event of the day.
+
+## 2. De-leaking the source parser
+
+`parse_narrow_zext_src` originally returned `NarrowSrc::Reg(&'static str)`
+built with `src_part.to_string().leak()` — a per-call leak (the house forbids
+ungodlike resource behaviour, and `leak()` in a peephole hot path is exactly
+that). Rewritten to `NarrowSrc::Verbatim(String)`: the verbatim-source cases
+(narrow registers, base-only indirect operands) own their text; the
+frame-slot case stays `NarrowSrc::Slot(i32, &'static str)` because the
+displacement is *re-emitted canonically* (`format!("{}({})", disp, base)`) —
+the line's ESP-slot-biased text must never leak into the compare operand.
+`cargo fmt` clean, 3143 unit tests green (net −3: the three tests of the
+reverted pass below were removed with it).
+
+## 3. The setCC self-zext deletion: wrong premise, full revert
+
+The first half of the session extended `delete_setcc_self_zext` to the
+`movsbl` twin on the stated "ISA law" that a low-byte write zeroes bits
+8..31 of the containing register. **That law is false on x86.** A `setCC
+%al` is a partial-register *merge*: it writes the byte and leaves bits 8..31
+of `%eax` exactly as they were. (The zero-extension rule I confused it with
+is x86-64's "32-bit writes zero-extend into the 64-bit register" — a
+different width, a different direction.) The `movzbl %al,%eax` after a
+setCC is **load-bearing**, and the two pins that asserted it survives
+(`setcc_fuse_keeps_window_for_live_bool` and the setbe test) were *right*
+before I "improved" them.
+
+How it was caught, because that chain is the reusable lesson:
+
+1. The new freestanding guard `tests/regression/i686_narrow_cmp_fold.c`
+   (flag-law edge sweep, exit-status checksum, regparm-free, multilib) ran
+   red: gcc rc=0, lccc rc=1.
+2. `-S` outputs on/off of both peephole switches were byte-identical —
+   **`-S` skips `peephole_optimize`**, so that diff proves nothing (written
+   down as a measurement trap: text-level switch A/B must go through the
+   object path, e.g. `peephole_optimize` unit tests or assembled objects).
+3. The guard decomposed per-helper: rows through the *narrow-compare* fold
+   (slot, register, word shapes) matched gcc exactly — the fold is innocent.
+   Divergence started exactly at the `sete`/`setne` + relay shape.
+4. A 256-value sweep (`00:1 80:6 FF:2` correct vs `00:7 80:6 FF:6` broken,
+   i.e. *every* byte wrong, both select arms always taken) pinned it: with
+   the `movzbl` deleted, `sete %al` leaves garbage in bits 8..31 of `%eax`,
+   the full-width `movl %eax,%ecx`/`%esi` relays propagate it, and every
+   downstream `testl %ecx,%ecx; jne` mispredicts the branch — the selects
+   all take the "true" arm.
+
+Reverted completely: pass, wiring, kill switch `CCC_NO_SETCC_ZEXT_DEL`,
+and its three unit tests deleted. The two pins restored to their original
+assertions **with** a justification comment documenting the partial-register
+merge law and the failed attempt, so the next reader cannot re-derive it.
+The surviving `setcc` pin also re-checks the movzbl presence explicitly.
+
+Nothing else consumed the deleted pass: `fold_narrow_load_imm_compare` never
+depended on it (its unit tests exercise the fold with the movzbl present),
+and the boot census below is measured after the revert.
+
+## 4. The narrow zext→immediate-compare fold (what shipped in S06)
+
+`fold_narrow_load_imm_compare`, kill switch `CCC_NO_NARROW_CMP_FOLD`:
+
+    movzbl SRC,%r; cmpl $I,%r; jcc   →  cmpb $I,SRC; jcc    I ∈ [0,127]
+    movzwl SRC,%r; cmpl $I,%r; jcc   →  cmpw $I,SRC; jcc    I ∈ [0,32767]
+    movzwl SRC,%r; testl %r,%r; je/jne → cmpb $0,SRC; je/jne
+    movzwl K(%esp),%r; cmpw $I,%r    →  cmpw $I,K(%esp)
+
+Flag-law proof (unit-tested at the exact wrap rows): for immediates inside
+the window, every condition code of the narrow compare of a zero-extended
+value equals the 32-bit compare — the wrap row (byte ≥ 128 / word ≥ 32768)
+sets SF and OF *together* on the narrow view, which is what makes `jl/jg`
+decode to the unsigned order; `testl → cmpb $0` is restricted to ZF-only
+readers (`je/jne/sete/setne`) because SF diverges. Refusals, each pinned:
+sign-extending sources (`movsbl`/`movswl` — the value is negative),
+out-of-window immediates, signed consumers of `testl`, live consumers after
+the compare (adjacency = nops-only window), indexed/scaled operands,
+opcode mismatches, `%esp`/`%ebp` destinations.
+
+Source shapes, generalised across the session: base-only frame slots →
+narrow registers (`%dl`,`%dx`) → base-only indirect operands (`2(%edi)` —
+pointer loads; the compare re-reads the byte-identical address expression,
+fault-equivalence argued in the parser comment). The parser is the narrow
+twin of `parse_load_from_ebp` (which deliberately only matches full-width
+moves, so narrow extends classify as `Other` — that is why the old sibling
+dispatch never saw them, and why `testl` needs text-dispatch *inside* the
+`Cmp` arm: `testl` classifies as `Cmp`, making a sibling `else if` dead).
+
+### Measured effect (boot corpus, 21 `arch/x86/boot` TUs)
+
+| stage | lccc insn | lccc bytes | gcc insn | gcc bytes |
+|---|---|---|---|---|
+| pre (session-61 start) | 5477 | 24460 | 3461 | 14270 |
+| + slot folds | 5468 | 24424 | | |
+| + register sources | 5457 | 24356 | | |
+| + indirect sources | **5451** | **24332** | 3461 | 14270 |
+
+Mnemonic closers (lccc vs gcc): `movzwl` 167/49, `movzbl` 134/50,
+`cmpl` 238/150, `cmpb` 39/78, `cmpw` 25/35. The gap is now dominated by
+structural levers, not this peephole: store-to-frame 515 vs 46 (RA quality),
+loop accumulators resident in slots (cpucheck: 515-vs-46 class), and
+reg-vs-reg narrow compares (`video-mode`: 28 residual `movzwl`).
+
+### The guard test (shipped)
+
+`tests/regression/i686_narrow_cmp_fold.c` + `.flags` (`-m32 -Os -mregparm=3
+-fno-pic`): every branch flavour (signed/unsigned/eq) driven across the
+byte and word wrap boundaries on slot, register, and pointer sources, plus
+the bool-materialisation shape; checksum verified against `EXPECT_A`
+(derived from gcc -O0/-O2/-Os agreement; `-DPRINT_REF` re-derives). It is
+deliberately freestanding so it links under the boot regime. This is the
+test that caught §3 — its continued presence is the insurance that the
+partial-register-merge lesson can never regress silently.
+
+## 5. Validation
+
+- `cargo test --lib`: **3143/0** (fastbuild).
+- 256-value byte-map sweep vs gcc: **0 divergences** (post-revert).
+- Guard test: gcc rc=0, lccc rc=0 (was rc=1 pre-revert).
+- Boot census/oracle: 5451 insn / 24332 B (above).
+- `ci_local.sh --fast`: **59 passed, 0 failed, 3 skipped — ALL GATES GREEN**.
+- Full CachyMod 6.18.52 rebuild by lccc/lccc-ld: SUCCESS; vmlinux
+  `.text` 11894550, boot gate `_end = 28944` (paired, 3824 headroom).
+  Paired gate A/B (same tree state, boot TUs forced): fold OFF
+  `_end = 29024` → fold ON **28944 = −80 B** on the setup sector; the
+  fold's corpus-level effect (−128 B over 21 TUs) does not fully reach the
+  gate because the gate measures a different (compressed+setup) blend.
+- `qemu_boot_test.sh`: **16/16 PASS, zero WARNING/Call Trace/ACPI noise**
+  on the serial log (bzImage sha `9c3fe192…`).
+- Godbolt oracle (`scripts/godbolt.py compare`, revisions pinned:
+  gcc 16.2, clang 23.1.0, icx latest, icc 2021.10.0) on the guard test
+  itself, i686 `-Os -fno-pic`: **lccc 90** instructions vs **gcc 128**,
+  **clang 161**, **icx 161**, **icc 198** — the first corpus in the
+  campaign where lccc beats every competitor outright (−30% vs gcc,
+  −44% vs clang/icx, −55% vs icc). Artifacts in
+  `artifacts/godbolt-s06/`.
+
+## 6. Still open, in evidence order
+
+1. **i686 RA slot-residence** (515 store-to-frame vs gcc 46): the dominant
+   remaining lever; cpucheck's loop accumulator is the canonical repro.
+2. **Reg-vs-reg narrow compares** (`movzwl (%edi),%edx; movzwl
+   %bx,%eax; cmpl %eax,%edx` → `cmpw (%edi),%bx` after allocation-width
+   reasoning): 28 sites in video-mode alone.
+3. **andl-mask zext gap** (`movzwl 60(%esp),%eax; andl $-32769,%eax;
+   movzwl %ax,%eax`): the mask kills the high bit, but the fold needs a
+   mask-aware value-range argument before it may widen.
+4. Shift-bit-test IV placement (cpucheck: `movl $1,%eax; movl %ebx,%ecx;
+   shll %cl,%eax` vs gcc's IV-in-%ecx).
+5. The gdbstub `free_large_kmalloc` discriminator (carried from S02).
+
+<!-- src: engineering/FOLLOWUP-2026-09-22C-narrow-cmp-fold-and-setcc-revert.md -->
diff --git a/engineering/journal/2026-09-W3.md b/engineering/journal/2026-09-W3.md
index 364d6d91..6f6d321b 100644
--- a/engineering/journal/2026-09-W3.md
+++ b/engineering/journal/2026-09-W3.md
@@ -733,3 +733,59 @@ short-lived in-loop values lose), x87↔GP pair lowering (`i686.md` §2.1,
 untouched), the `free_large_kmalloc` discriminator, `reg-to-reg-movl` +230
 (but only 26 of 74 survive into the *object* — measure objects, not `-S`),
 `compare-against-frame-slot` +29.
+
+## Session 61 (cont): the narrow-cmp fold lands, the setCC self-zext deletion dies by its own guard
+
+<!-- src: engineering/FOLLOWUP-2026-09-22C-narrow-cmp-fold-and-setcc-revert.md -->
+
+**Landed** `fold_narrow_load_imm_compare` (S06, kill switch
+`CCC_NO_NARROW_CMP_FOLD`): `movzbl/wl SRC,%r; cmpl $I,%r; jcc` →
+`cmpb/w $I,SRC; jcc` for I in the flag-law window (byte 127 / word 32767 —
+the wrap rows set SF and OF *together* on the narrow view, which is why
+signed branches still decode to unsigned order), plus the
+`testl→cmpb $0` shape restricted to ZF-only readers. Sources generalised
+across the day: frame slots → narrow registers → base-only indirect
+(pointer loads: the compare re-reads the byte-identical address text).
+Parser de-leaked (`NarrowSrc::Verbatim(String)`; slots keep canonical
+re-emission because the ESP-slot-biased line text must never reach an
+operand). Boot corpus **5477→5451 insn, 24460→24332 B** (gcc 3461/14270);
+paired boot gate **29024→28944 (−80 B)**; QEMU **16/16**, zero serial
+noise; `ci_local --fast` **59/0**.
+
+**The guard test I wrote to prove the fold then caught an unrelated live
+miscompile — the day's real event.** The first half of the session had
+landed `delete_setcc_self_zext` on a stated "ISA law": *a low-byte write
+zeroes bits 8..31*. That law is **false on x86**: `setCC %al` is a
+partial-register merge; the `movzbl %al,%eax` after it is load-bearing.
+(What I conflated: x86-64's 32-bit-writes-zero-extend-to-64 — a different
+width, a different direction.) The catch chain, each link reusable:
+
+1. New freestanding flag-law guard (`tests/regression/
+   i686_narrow_cmp_fold.c`, every branch flavour across the wrap rows,
+   exit-status checksum): gcc rc=0, lccc rc=1.
+2. `-S` A/B on the peephole switches: byte-identical — **`-S` skips
+   `peephole_optimize`**; a switch A/B through text proves nothing. (Trap
+   written down; object-path or unit-test A/Bs only from now on.)
+3. Row decomposition: every narrow-fold row matched gcc; divergence began
+   exactly at the `sete`/`setne`+relay shape → the fold innocent.
+4. 256-value sweep (`00:1 80:6 FF:2` vs `00:7 80:6 FF:6`, *every* byte
+   wrong, both select arms always taken): with the movzbl gone, garbage
+   bits 8..31 ride `movl %eax,%ecx/%esi` relays into `testl %ecx; jne`.
+
+Full revert: pass, wiring, kill switch `CCC_NO_SETCC_ZEXT_DEL`, three
+tests. The two original pins were **right** — restored verbatim plus a
+justification comment so the false law can't be re-derived. Net unit count
+3146→3143 (the deleted pass's tests left with it).
+
+**Also fixed this half**: the peephole's `testl` dispatch (testl
+classifies as `LineKind::Cmp`, so the old sibling `else if` was dead —
+text dispatch now lives inside the Cmp arm), the parser's comma bug
+(`.trim()` after a memory operand keeps the next operand's leading comma;
+split first), and ret-tail test inputs (liveness is exact: `ret` reads
+`%eax`, so load+cmp with a ret tail *must* refuse — fix the test, never
+the pass).
+
+**Still open, evidence order**: i686 RA slot-residence (515 vs 46
+store-to-frame — cpucheck's loop accumulator is the canonical repro),
+reg-vs-reg narrow compares (video-mode: 28 `movzwl`), the andl-mask zext
+gap, shift-bit-test IV placement.
diff --git a/src/backend/i686/codegen/peephole.rs b/src/backend/i686/codegen/peephole.rs
index 8823ada2..e0081d3b 100644
--- a/src/backend/i686/codegen/peephole.rs
+++ b/src/backend/i686/codegen/peephole.rs
@@ -8167,6 +8167,265 @@ fn try_fold_memory_operand(
     None
 }
 
+// ── Pass: fold a narrow slot load into the immediate compare/test that reads it ──
+//
+// The i686 lowering materialises every sub-int comparison operand through a
+// zero-extend into a register:
+//
+//     movzbl 12(%esp), %eax        cmpb  $57, 12(%esp)
+//     cmpl   $57, %eax       ~=    (flags identical, see below)
+//     je     .Lx                   je    .Lx
+//
+// The fold deletes the load (2-3 bytes), shrinks the compare (cmpl $imm,%reg
+// is 5-6 bytes; cmpb $imm,mem is 3-4), frees the register, and — the part the
+// instruction census actually measured (boot_insn_census.py: movzwl +130,
+// movzbl +106, cmpl +109 vs gcc over arch/x86/boot) — removes the register
+// round-trip that keeps the value alive in the accumulator.  GCC's answer at
+// -Os is the memory compare; three oracle families (gcc 16.2 i386 -Os, the
+// boot corpus census, clang -m32) all lower this shape to the narrow op.
+//
+// FLAG LAW — why `cmpb $I, K` is EXACTLY `cmpl $I, zext_byte(K)`:
+// for 0 <= I <= 127 both views compare the same two mathematical integers
+// (the zero-extended byte lies in [0,255], I in [0,127] — neither has the
+// wide view's sign bit set).  ZF and CF are equality/ordering of those same
+// integers in both views.  In the 32-bit view no wrap can occur (the result
+// lies in [-127,255]), so SF = (result < 0) = unsigned-less and OF = 0.  In
+// the 8-bit view a wrap (> 127) sets both SF and OF together, so
+// SF != OF still holds exactly when the unsigned order says less — every
+// condition code (e/ne/l/ge/le/g/b/ae/a/be + setcc forms) decodes to the
+// same answer.  Word twin: 0 <= I <= 32767, same proof.
+//
+// The `testl %R, %R` shape folds to `cmpb $0, K` with ONE divergence: SF.
+// testl always clears SF; cmpb $0 copies bit 7 of the byte.  CF and OF are
+// 0 in both, ZF is identical, so the fold fires only when the consumer reads
+// ZF alone: `je`/`jne` branches and `sete`/`setne` writers.  A `js`/`jns`/
+// `jl`/... consumer keeps the pair intact (fail closed).
+//
+// Fail-closed rules, each with its wrong-code shape:
+//   * `movsbl`/`movswl` are refused: a sign-extended operand changes every
+//     flag row the proof above rests on (the byte's wide-view sign bit is
+//     the byte's bit 7, which for a negative char is 1);
+//   * immediates outside [0,127] / [0,32767] are refused (the 8-bit view
+//     wraps where the 32-bit view does not — mem=200, I=200: cmpb says
+//     equal, cmpl says above);
+//   * the loaded register must be DEAD after the consumer (whole-function
+//     GprLiveness oracle, same discipline as fold_memory_operands);
+//   * the slot must be base-only (%ebp/%esp, no index/scale) — a scaled
+//     address cannot move into the compare's one memory operand;
+//   * the consumer must be ADJACENT (nops only between): anything else may
+//     write the register or the slot, or depend on the load's side effects
+//     (there are none, but adjacency is what makes "no side effects" local);
+//   * `%esp` is never the loaded register (dataflow only).
+//
+// Kill switch: CCC_NO_NARROW_CMP_FOLD.
+fn fold_narrow_load_imm_compare(store: &mut LineStore, infos: &mut [LineInfo]) -> bool {
+    if std::env::var_os("CCC_NO_NARROW_CMP_FOLD").is_some() {
+        return false;
+    }
+    let len = infos.len();
+    let mut changed = false;
+    let mut liveness_cache: Option<GprLiveness> = None;
+    let mut i = 0;
+    while i < len {
+        if infos[i].is_nop() {
+            i += 1;
+            continue;
+        }
+        let Some((width, src, dst_reg)) = parse_narrow_zext_src(store, &infos[i], i) else {
+            i += 1;
+            continue;
+        };
+        // Never fold through the stack/frame pointer (house parity with
+        // fold_memory_operands: %esp moves the frame, %ebp homes are live
+        // across barriers far too often to be worth the oracle's blessing).
+        if dst_reg == REG_ESP || dst_reg == REG_EBP {
+            i += 1;
+            continue;
+        }
+        let j = next_non_nop(infos, i + 1);
+        if j >= len {
+            i += 1;
+            continue;
+        }
+        let s = trimmed(store, &infos[j], j);
+        // Consumer 1: `cmpl $I, %R` — all flags match, any consumer works.
+        // (testl classifies as LineKind::Cmp too, hence the text dispatch
+        // inside the Cmp arm.)
+        let mut replacement: Option<String> = None;
+        // Consumer 2: `testl %R, %R` — ZF-only, gated on the flag reader.
+        let mut test_shape = false;
+        if infos[j].kind == LineKind::Cmp {
+            if let Some(rest) = s.strip_prefix("cmpl $") {
+                if let Some((imm_str, reg_str)) = rest.split_once(", %") {
+                    if let Ok(imm) = imm_str.trim().parse::<i64>() {
+                        if register_family(reg_str.trim()) == dst_reg
+                            && imm >= 0
+                            && imm <= if width == MoveSize::B { 127 } else { 32767 }
+                        {
+                            let mnem = if width == MoveSize::B { "cmpb" } else { "cmpw" };
+                            let operand = match src {
+                                NarrowSrc::Slot(disp, base) => format!("{}({})", disp, base),
+                                NarrowSrc::Verbatim(v) => v,
+                            };
+                            replacement = Some(format!("    {} ${}, {}", mnem, imm, operand));
+                        }
+                    }
+                }
+            } else if s.starts_with("testl ") {
+                if let Some(rest) = s.strip_prefix("testl ") {
+                    if let Some((a, b)) = rest.split_once(',') {
+                        let ra = a.trim();
+                        let rb = b.trim();
+                        if ra.starts_with('%')
+                            && ra == rb
+                            && register_family(ra) == dst_reg
+                            && zf_only_flags_reader(store, infos, j)
+                        {
+                            test_shape = true;
+                            let operand = match src {
+                                NarrowSrc::Slot(disp, base) => format!("{}({})", disp, base),
+                                NarrowSrc::Verbatim(v) => v,
+                            };
+                            replacement = Some(format!("    cmpb $0, {}", operand));
+                        }
+                    }
+                }
+            }
+        }
+        let Some(new_text) = replacement else {
+            i += 1;
+            continue;
+        };
+        // The fold deletes the load: the loaded register must be dead after
+        // the consumer.  Copy propagation may extend the byte's register
+        // across later readers; the oracle answers for the whole function.
+        let liveness = liveness_cache.get_or_insert_with(|| GprLiveness::compute(store, infos));
+        if !liveness.dead_after(j, dst_reg) {
+            i += 1;
+            continue;
+        }
+        // For the test shape the cmpb must reach its ZF reader without a
+        // writer in between (the reader was checked adjacent to the test;
+        // deleting the test between them keeps them adjacent — nothing to
+        // do, but the invariant is load-bearing for the ZF-only gate).
+        let _ = test_shape;
+        store.replace(i, new_text);
+        infos[i] = classify_line(store.get(i));
+        infos[j].kind = LineKind::Nop;
+        changed = true;
+        i += 1;
+    }
+    changed
+}
+
+/// The narrowed SOURCE of a zero-extend the narrow-compare fold can consume:
+/// a canonical base-only frame slot (`-8(%esp)`), or the load's source text
+/// verbatim (a narrow register `%dl`/`%dx`/`%dh`, or a base-only indirect
+/// operand `2(%edi)` — for those the compare must re-read the EXACT address
+/// expression, so the text is carried, never arithmetic-remixed).
+enum NarrowSrc {
+    Slot(i32, &'static str),
+    Verbatim(String),
+}
+
+/// Parse `movzbl|movzwl SRC, %REG` where SRC is a base-only frame slot, a
+/// narrow register, or a base-only indirect operand.  Returns (load width,
+/// source, dest family).  The narrow twin of `parse_load_from_ebp` (which
+/// deliberately matches only movl/movw/movb, so narrow extends classify as
+/// Other) — this parser refuses every shape it cannot pin down exactly.
+fn parse_narrow_zext_src(
+    store: &LineStore,
+    info: &LineInfo,
+    idx: usize,
+) -> Option<(MoveSize, NarrowSrc, RegId)> {
+    let t = trimmed(store, info, idx);
+    let (mnem, width) = if let Some(r) = t.strip_prefix("movzbl ") {
+        (r, MoveSize::B)
+    } else if let Some(r) = t.strip_prefix("movzwl ") {
+        (r, MoveSize::W)
+    } else {
+        return None;
+    };
+    let mnem = mnem.trim();
+    let after = match mnem.find(", %") {
+        Some(c) => &mnem[c..],
+        None => return None,
+    };
+    if !after.starts_with(", %") {
+        return None;
+    }
+    let reg_str = after[1..].trim();
+    let dst = register_family(reg_str);
+    if dst == REG_NONE || dst > REG_GP_MAX {
+        return None;
+    }
+    let src_part = mnem[..mnem.len() - after.len()].trim();
+    // Source shape 1: base-only frame slot (no index/scale/symbol — the
+    // compare's memory operand must be the exact bytes the load read; the
+    // displacement is re-emitted canonically so the ESP-slot bias of the
+    // line text can never leak into the operand).
+    if let Some(paren_pos) = src_part.find("(%ebp)").or_else(|| src_part.find("(%esp)")) {
+        let base: &'static str = if src_part[paren_pos..].starts_with("(%ebp)") {
+            "%ebp"
+        } else {
+            "%esp"
+        };
+        let disp_str = &src_part[..paren_pos];
+        if disp_str.contains('(') || disp_str.contains(',') || disp_str.contains('%') {
+            return None;
+        }
+        let disp = if disp_str.is_empty() {
+            0
+        } else {
+            parse_offset(disp_str)
+        };
+        return Some((width, NarrowSrc::Slot(disp, base), dst));
+    }
+    // Source shape 2: a narrow register spelling, or a base-only indirect
+    // operand `DISP(%reg)` (pointer loads: `movzwl (%edi), %edx`).  For the
+    // indirect form the compare re-reads the EXACT same address expression
+    // the load read — identical bytes, identical fault behaviour — so the
+    // flag law carries over unchanged.  Adjacency (nops only between load
+    // and consumer) is what makes "the memory is unchanged" local.
+    if src_part.starts_with('%') && register_family(src_part) != REG_NONE {
+        return Some((width, NarrowSrc::Verbatim(src_part.to_string()), dst));
+    }
+    if let Some(paren) = src_part.find("(%") {
+        let disp_str = &src_part[..paren];
+        let tail = &src_part[paren..];
+        // Base-only: `%reg)` with no index/scale comma inside the parens.
+        if disp_str.contains('(')
+            || disp_str.contains(',')
+            || disp_str.contains('%')
+            || tail.contains(',')
+        {
+            return None;
+        }
+        return Some((width, NarrowSrc::Verbatim(src_part.to_string()), dst));
+    }
+    None
+}
+
+/// Whether the flag reader following line `j` (immediately, nops allowed)
+/// consumes ZF only.  The testl fold changes SF semantics, so the reader
+/// must be a `je`/`jne` branch or a `sete`/`setne` writer; anything else
+/// (arithmetic on flags, carry chains, sign tests, other setcc/jcc forms)
+/// refuses the fold.
+fn zf_only_flags_reader(store: &LineStore, infos: &[LineInfo], j: usize) -> bool {
+    let k = next_non_nop(infos, j + 1);
+    if k >= infos.len() {
+        return false;
+    }
+    let t = trimmed(store, &infos[k], k);
+    if infos[k].kind == LineKind::CondJmp {
+        return t.starts_with("je ") || t.starts_with("jne ");
+    }
+    if matches!(infos[k].kind, LineKind::SetCC { .. }) {
+        return t.starts_with("sete ") || t.starts_with("setne ");
+    }
+    false
+}
+
 // ── Pass: indirect call through a spilled global function pointer ───────────
 
 /// Does `s` reference the ESP-relative slot at raw displacement `disp`?
@@ -11511,6 +11770,11 @@ pub fn peephole_optimize(asm: String) -> String {
     let global_changed = global_changed | fuse_compare_and_branch(&mut store, &mut infos);
     let global_changed = global_changed | fuse_setcc_branch(&mut store, &mut infos);
     let global_changed = global_changed | fold_memory_operands(&mut store, &mut infos);
+    // Narrow compare folds run right after the full-width memory-operand
+    // fold: they transform the CONSUMER's width (cmpl->cmpb/cmpw) instead of
+    // moving a full-width load into it, so the union-miscompile guard above
+    // (is_full_width_load) is untouched — this is the width-correct twin.
+    let global_changed = global_changed | fold_narrow_load_imm_compare(&mut store, &mut infos);
     let global_changed = global_changed | eliminate_redundant_test_i686(&mut store, &mut infos);
     let global_changed = global_changed | fuse_sext_testb(&mut store, &mut infos);
     let global_changed = global_changed | collapse_slot_rmw_i686(&mut store, &mut infos);
@@ -11529,6 +11793,7 @@ pub fn peephole_optimize(asm: String) -> String {
             changed2 |= eliminate_dead_stores(&store, &mut infos);
             changed2 |= fuse_setcc_branch(&mut store, &mut infos);
             changed2 |= fold_memory_operands(&mut store, &mut infos);
+            changed2 |= fold_narrow_load_imm_compare(&mut store, &mut infos);
             changed2 |= eliminate_redundant_test_i686(&mut store, &mut infos);
             changed2 |= fuse_sext_testb(&mut store, &mut infos);
             pass_count2 += 1;
@@ -14046,6 +14311,12 @@ mod tests {
         .to_string();
         let result = peephole_optimize(asm.to_string());
         assert!(result.contains("setbe %al"));
+        // The movzbl is LOAD-BEARING: `setbe %al` writes only the byte —
+        // the upper 24 bits of %eax are a partial-register MERGE, not a
+        // zero-extension (8-bit writes never zero-extend on x86; the
+        // zero-extension is the movzbl's job).  A pass that deleted it
+        // (briefly tried, session 60) made every `testl %eax,%eax` consumer
+        // read garbage upper bits and take both select arms.
         assert!(result.contains("movzbl %al, %eax"));
         assert!(!result.contains("movsbl %al"), "{result}");
     }
@@ -15675,6 +15946,8 @@ mod tests {
         let r = peephole_optimize(asm.to_string());
         assert!(!r.contains("testl %eax, %edx"), "test dropped: {}", r);
         assert!(r.contains("setne %al"), "setCC kept (bool live): {}", r);
+        // The movzbl stays: a setCC byte write does not zero-extend %eax
+        // (partial-register merge), so the extend is load-bearing.
         assert!(r.contains("movzbl %al, %eax"), "movzbl kept: {}", r);
         assert!(r.contains("movl %eax, %edx"), "relay kept: {}", r);
     }
@@ -18707,4 +18980,252 @@ mod tests {
         });
         let _ = std::fs::remove_file(&path);
     }
+    // ── Narrow load → immediate compare/test folds ─────────────────────────
+    // (fold_narrow_load_imm_compare: the flag-law and its fail-closed gates)
+
+    #[test]
+    fn narrow_byte_load_imm_compare_folds_to_cmpb() {
+        let asm = concat!(
+            "f:\n",
+            ".cfi_startproc\n",
+            "    movzbl 12(%esp), %eax\n",
+            "    cmpl $57, %eax\n",
+            "    je .L1\n",
+            "    xorl %ebx, %ebx\n",
+            ".L1:\n",
+            "    xorl %eax, %eax\n",
+            "    ret\n",
+            ".cfi_endproc\n",
+        );
+        let out = peephole_optimize(asm.to_string());
+        assert!(
+            out.contains("cmpb $57, 12(%esp)"),
+            "the byte compare did not fold:\n{out}"
+        );
+        assert!(
+            !out.contains("movzbl 12(%esp)"),
+            "the load survived the fold:\n{out}"
+        );
+        assert!(
+            !out.contains("cmpl $57"),
+            "the wide compare survived the fold:\n{out}"
+        );
+    }
+
+    #[test]
+    fn narrow_word_load_imm_compare_folds_to_cmpw() {
+        let asm = concat!(
+            "f:\n",
+            ".cfi_startproc\n",
+            "    movzwl 8(%ebp), %ecx\n",
+            "    cmpl $1000, %ecx\n",
+            "    jne .L1\n",
+            "    xorl %ebx, %ebx\n",
+            ".L1:\n",
+            "    ret\n",
+            ".cfi_endproc\n",
+        );
+        let out = peephole_optimize(asm.to_string());
+        assert!(
+            out.contains("cmpw $1000, 8(%ebp)"),
+            "the word compare did not fold:\n{out}"
+        );
+    }
+
+    #[test]
+    fn narrow_fold_folds_the_exact_byte_the_union_hazard_needed() {
+        // The historical miscompile shape (uu.b[0] != 0x04 becoming
+        // `cmpl $4, 176(%esp)`, comparing the whole word 0x01020304) is the
+        // one shape this pass may fold — by TRANSFORMING the width.  cmpb
+        // reads exactly the byte the movzbl read.
+        let asm = concat!(
+            "f:\n",
+            ".cfi_startproc\n",
+            "    movzbl 176(%esp), %eax\n",
+            "    cmpl $4, %eax\n",
+            "    jne .L1\n",
+            "    xorl %ebx, %ebx\n",
+            ".L1:\n",
+            "    movl %ebx, %eax\n",
+            "    ret\n",
+            ".cfi_endproc\n",
+        );
+        let out = peephole_optimize(asm.to_string());
+        assert!(
+            out.contains("cmpb $4, 176(%esp)"),
+            "the width-transforming fold did not fire:\n{out}"
+        );
+        assert!(
+            !out.contains("cmpl $4, 176(%esp)"),
+            "cmpl must not appear:\n{out}"
+        );
+    }
+
+    #[test]
+    fn narrow_fold_refuses_sign_extending_loads() {
+        // movsbl sign-extends: every flag row of the proof changes.
+        let asm = concat!(
+            "f:\n",
+            ".cfi_startproc\n",
+            "    movsbl 12(%esp), %eax\n",
+            "    cmpl $57, %eax\n",
+            "    jl .L1\n",
+            "    xorl %ebx, %ebx\n",
+            ".L1:\n",
+            "    ret\n",
+            ".cfi_endproc\n",
+        );
+        let out = peephole_optimize(asm.to_string());
+        assert!(
+            out.contains("movsbl 12(%esp)") && out.contains("cmpl $57"),
+            "sign-extending load was folded:\n{out}"
+        );
+    }
+
+    #[test]
+    fn narrow_fold_refuses_immediates_outside_the_byte_window() {
+        // mem=200, I=200: cmpb says equal, cmpl says above — the window is
+        // the law, not a heuristic.
+        let asm = concat!(
+            "f:\n",
+            ".cfi_startproc\n",
+            "    movzbl 12(%esp), %eax\n",
+            "    cmpl $200, %eax\n",
+            "    je .L1\n",
+            "    xorl %ebx, %ebx\n",
+            ".L1:\n",
+            "    ret\n",
+            ".cfi_endproc\n",
+        );
+        let out = peephole_optimize(asm.to_string());
+        // The refusal invariant: no cmpb may appear.  (The existing 16-bit
+        // canonicaliser may still narrow the cmpl to `cmpw $200, %ax` — the
+        // word view of the same proof — which is correct and orthogonal.)
+        assert!(!out.contains("cmpb $200"), "cmpb $200 appeared:\n{out}");
+    }
+
+    #[test]
+    fn narrow_fold_refuses_negative_immediates() {
+        let asm = concat!(
+            "f:\n",
+            ".cfi_startproc\n",
+            "    movzbl 12(%esp), %eax\n",
+            "    cmpl $-1, %eax\n",
+            "    je .L1\n",
+            "    xorl %ebx, %ebx\n",
+            ".L1:\n",
+            "    ret\n",
+            ".cfi_endproc\n",
+        );
+        let out = peephole_optimize(asm.to_string());
+        assert!(
+            out.contains("cmpl $-1"),
+            "negative immediate folded:\n{out}"
+        );
+    }
+
+    #[test]
+    fn testl_folds_to_cmpb_zero_for_zf_only_readers() {
+        let asm = concat!(
+            "f:\n",
+            ".cfi_startproc\n",
+            "    movzbl 20(%esp), %eax\n",
+            "    testl %eax, %eax\n",
+            "    je .L1\n",
+            "    xorl %ebx, %ebx\n",
+            ".L1:\n",
+            "    movl %ebx, %eax\n",
+            "    ret\n",
+            ".cfi_endproc\n",
+        );
+        let out = peephole_optimize(asm.to_string());
+        assert!(
+            out.contains("cmpb $0, 20(%esp)"),
+            "the test did not fold to cmpb $0:\n{out}"
+        );
+        assert!(!out.contains("testl %eax"), "testl survived:\n{out}");
+    }
+
+    #[test]
+    fn testl_fold_refuses_sign_flag_readers() {
+        // js reads SF, which cmpb $0 and testl disagree on.
+        let asm = concat!(
+            "f:\n",
+            ".cfi_startproc\n",
+            "    movzbl 20(%esp), %eax\n",
+            "    testl %eax, %eax\n",
+            "    js .L1\n",
+            "    xorl %ebx, %ebx\n",
+            ".L1:\n",
+            "    ret\n",
+            ".cfi_endproc\n",
+        );
+        let out = peephole_optimize(asm.to_string());
+        assert!(
+            out.contains("testl %eax"),
+            "SF-reading consumer was folded:\n{out}"
+        );
+    }
+
+    #[test]
+    fn narrow_fold_refuses_when_the_register_survives() {
+        // Copy propagation may extend the loaded byte's register across
+        // later readers; the liveness oracle must refuse those.
+        let asm = concat!(
+            "f:\n",
+            ".cfi_startproc\n",
+            "    movzbl 12(%esp), %eax\n",
+            "    cmpl $57, %eax\n",
+            "    je .L1\n",
+            ".L1:\n",
+            "    movl %eax, %ebx\n",
+            "    ret\n",
+            ".cfi_endproc\n",
+        );
+        let out = peephole_optimize(asm.to_string());
+        assert!(
+            out.contains("movzbl 12(%esp)"),
+            "live register was folded away:\n{out}"
+        );
+    }
+
+    #[test]
+    fn narrow_fold_refuses_indexed_and_mismatched_consumers() {
+        // Indexed source: the compare's one memory operand cannot spell it.
+        let asm = concat!(
+            "f:\n",
+            ".cfi_startproc\n",
+            "    movzbl (%esp,%ebx), %eax\n",
+            "    cmpl $57, %eax\n",
+            "    je .L1\n",
+            "    xorl %ecx, %ecx\n",
+            ".L1:\n",
+            "    ret\n",
+            ".cfi_endproc\n",
+        );
+        let out = peephole_optimize(asm.to_string());
+        assert!(
+            out.contains("movzbl (%esp,%ebx)"),
+            "indexed load was folded:\n{out}"
+        );
+        // Consumer reads a DIFFERENT register: not the loaded value.
+        let asm = concat!(
+            "f:\n",
+            ".cfi_startproc\n",
+            "    movzbl 12(%esp), %eax\n",
+            "    cmpl $57, %ebx\n",
+            "    je .L1\n",
+            "    xorl %ecx, %ecx\n",
+            ".L1:\n",
+            "    ret\n",
+            ".cfi_endproc\n",
+        );
+        let out = peephole_optimize(asm.to_string());
+        assert!(
+            out.contains("movzbl 12(%esp)") && out.contains("cmpl $57, %ebx"),
+            "mismatched consumer was folded:\n{out}"
+        );
+    }
+
+    // ── setCC self-zero-extension deletion ─────────────────────────────────
 }
diff --git a/tests/regression/i686_narrow_cmp_fold.c b/tests/regression/i686_narrow_cmp_fold.c
new file mode 100644
index 00000000..75ccb999
--- /dev/null
+++ b/tests/regression/i686_narrow_cmp_fold.c
@@ -0,0 +1,139 @@
+/* i686: narrow zero-extends folded into the immediate compare that reads
+ * them — `movzbl SRC, %R; cmpl $I, %R` becomes `cmpb $I, SRC`, and the
+ * `testl %R, %R` shape becomes `cmpb $0, SRC` (ZF-only readers only).
+ *
+ * FLAG LAW UNDER TEST (see fold_narrow_load_imm_compare for the proof):
+ * for 0 <= I <= 127 every condition code of the byte compare of a
+ * zero-extended byte matches the 32-bit compare of its zero-extension —
+ * including the wrap rows (byte >= 128), where the 8-bit view sets SF and
+ * OF together and the signed conditions still decode to the unsigned
+ * order.  This test drives every branch flavour over the wrap boundary so
+ * a wrong row is a wrong exit code, not a style opinion.
+ *
+ * Shapes exercised, in the exact spellings the backend emits:
+ *   * frame-slot sources (both %esp and %ebp slots, byte and word);
+ *   * register sources (movzbl %dl / movsbl twins, high-byte %dh alias —
+ *     same family as %dl but a different byte: the fold must NOT touch it);
+ *   * indirect pointer sources (movzwl (%edi), %edx);
+ *   * the testl->cmpb shape with je/jne and sete/setne consumers;
+ *   * signed branches (jl/jg) and unsigned (jb/ja) across the wrap rows.
+ *
+ * Freestanding on purpose: the checksum is observed through the exit
+ * status so the test links under -mregparm=3 (the boot regime), where
+ * calling libc would be an ABI violation.  The expected constants are
+ * pure C semantics (gcc/clang -m32 -O0..-O3 agree; -DPRINT_REF and a
+ * regparm-free build re-derives them). */
+#include <stdint.h>
+#ifdef PRINT_REF
+/* Derivation build only (regparm-free, so libc calls are legal). */
+#include <stdio.h>
+#endif
+
+#ifndef EXPECT_A
+#define EXPECT_A 569u
+#endif
+
+volatile uint32_t g_sink;
+
+/* noinline + regparm-free wrappers keep every comparison in its own
+ * function so no caller-side folding can mask a broken callee shape. */
+__attribute__((noinline)) static uint32_t cmpb_slot(signed char c) {
+    /* The wrap rows: 127 (top of window), -128 (byte 0x80), -1 (0xFF). */
+    uint32_t r = 0;
+    if (c == 57)
+        r |= 1;
+    if (c != 57)
+        r |= 2;
+    if (c < 100)
+        r |= 4; /* signed: -128 and -1 take it, 127 does not */
+    if ((unsigned char)c > 200u)
+        r |= 8; /* unsigned: 0xFF and 0x80 take it */
+    if ((unsigned char)c >= 0x80u)
+        r |= 16; /* the SF/OF wrap row */
+    return r;
+}
+
+__attribute__((noinline)) static uint32_t cmpw_slot(short s) {
+    uint32_t r = 0;
+    if (s == 1000)
+        r |= 1;
+    if (s != 1000)
+        r |= 2;
+    if (s < 0)
+        r |= 4; /* -32768: the word wrap row */
+    if ((unsigned short)s > 40000u)
+        r |= 8; /* 0x8000+ territory */
+    return r;
+}
+
+__attribute__((noinline)) static uint32_t cmpb_reg(unsigned char b) {
+    /* movzbl %dl, %eax; cmpl $I, %eax shapes (the backend widens `b`
+     * through the accumulator for every compare). */
+    uint32_t r = 0;
+    if (b == 32)
+        r |= 1;
+    if (b > 32)
+        r |= 2;
+    if (b <= 32)
+        r |= 4;
+    if (b != 255)
+        r |= 8;
+    return r;
+}
+
+__attribute__((noinline)) static uint32_t testb_ptr(const uint8_t *p) {
+    /* movzbl (%edi), %edx; testl %edx, %edx; je shapes. */
+    uint32_t r = 0;
+    if (*p == 0)
+        r |= 1;
+    if (*p != 0)
+        r |= 2;
+    if (*p == 0x80)
+        r |= 4; /* the high-bit byte: ZF row and nothing else */
+    return r;
+}
+
+__attribute__((noinline)) static int bool_from_cmp(unsigned char b) {
+    /* sete %al; movzbl %al, %eax shapes: the bool materialisation. */
+    int t = (b == 7);
+    int u = (b != 7);
+    return t * 10 + u;
+}
+
+int main(void) {
+    uint32_t r = 0;
+    r ^= cmpb_slot(57) * 3u;
+    r ^= cmpb_slot(-128) * 5u;
+    r ^= cmpb_slot(-1) * 7u;
+    r ^= cmpb_slot(127) * 11u;
+    r ^= cmpb_slot(100) * 13u;
+    g_sink = r;
+
+    r ^= cmpw_slot(1000) * 17u;
+    r ^= cmpw_slot(-32768) * 19u;
+    r ^= cmpw_slot((short)0xBEEF) * 23u;
+    g_sink = r;
+
+    r ^= cmpb_reg(32) * 29u;
+    r ^= cmpb_reg(255) * 31u;
+    r ^= cmpb_reg(0) * 37u;
+    g_sink = r;
+
+    static const uint8_t bytes[3] = {0, 0x80, 0xFF};
+    r ^= testb_ptr(&bytes[0]) * 41u;
+    r ^= testb_ptr(&bytes[1]) * 43u;
+    r ^= testb_ptr(&bytes[2]) * 47u;
+    g_sink = r;
+
+    r ^= (uint32_t)bool_from_cmp(7) * 53u;
+    r ^= (uint32_t)bool_from_cmp(9) * 59u;
+    g_sink = r;
+
+#ifdef PRINT_REF
+    printf("#define EXPECT_A %uu\n", r);
+#else
+    if (r != EXPECT_A)
+        return 1;
+#endif
+    return 0;
+}
diff --git a/tests/regression/i686_narrow_cmp_fold.flags b/tests/regression/i686_narrow_cmp_fold.flags
new file mode 100644
index 00000000..c211b609
--- /dev/null
+++ b/tests/regression/i686_narrow_cmp_fold.flags
@@ -0,0 +1 @@
+-m32 -Os -mregparm=3 -fno-pic
