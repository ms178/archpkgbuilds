diff --git a/.base_ref b/.base_ref
index 22b5ef40..37c882b0 100644
--- a/.base_ref
+++ b/.base_ref
@@ -1 +1 @@
-4be3a0a276336c6e86880c49be1a577ff162c0d4
+c23e3bd11ae21fb8566428eab7849662b34c7455
diff --git a/src/frontend/parser/declarators.rs b/src/frontend/parser/declarators.rs
index 11194c09..c6a01185 100644
--- a/src/frontend/parser/declarators.rs
+++ b/src/frontend/parser/declarators.rs
@@ -505,6 +505,8 @@ impl Parser {
                 // Array params: outermost dimension decays to pointer.
                 // Preserve the outermost dimension expression (if any) so side effects
                 // like `a++` in `int b[a++]` can be evaluated during IR lowering.
+                // (Inner dimensions are preserved in type_spec and evaluated by
+                // compute_vla_param_strides).
                 let mut vla_size_exprs = Vec::with_capacity(2);
                 if !array_dims.is_empty() {
                     if let Some(Some(expr)) = array_dims.first() {
diff --git a/src/ir/lowering/nested_functions.rs b/src/ir/lowering/nested_functions.rs
index 4440ff20..0335811f 100644
--- a/src/ir/lowering/nested_functions.rs
+++ b/src/ir/lowering/nested_functions.rs
@@ -938,9 +938,6 @@ impl Lowerer {
         let entry_label = self.fresh_label();
         self.start_block(entry_label);
         self.func_mut().sret_ptr = None;
-        self.allocate_function_params(&nf_clone, &param_info);
-        self.evaluate_vla_param_side_effects(&nf_clone);
-        self.handle_kr_float_promotion(&nf_clone);
 
         // 3e. Pre-register captured enclosing locals in the nested
         // function's locals map with chain-walk address values.
@@ -1024,6 +1021,12 @@ impl Lowerer {
             self.func_mut().locals.insert(name, info);
         }
 
+        // Allocate function params and evaluate VLA parameter expressions
+        // now that enclosing captured variables are in scope.
+        self.allocate_function_params(&nf_clone, &param_info);
+        self.evaluate_vla_param_side_effects(&nf_clone);
+        self.handle_kr_float_promotion(&nf_clone);
+
 
         // 3f. Non-local goto targets for the nested function, keyed by the
         // scope-qualified name that lower_goto_stmt resolves through the
diff --git a/src/passes/global_addr_cse.rs b/src/passes/global_addr_cse.rs
index 2d76b4c4..ae659ead 100644
--- a/src/passes/global_addr_cse.rs
+++ b/src/passes/global_addr_cse.rs
@@ -828,6 +828,8 @@ mod tests {
                         struct_arg_is_f128_sse: Vec::new(),
                         is_sret: false,
                         is_fastcall: false,
+                        is_pure: false,
+                        is_const: false,
                         ret_eightbyte_classes: vec![],
                         ret_is_f128_sse: false,
                     },
@@ -1282,6 +1284,8 @@ mod tests {
                         struct_arg_is_f128_sse: Vec::new(),
                         is_sret: false,
                         is_fastcall: false,
+                        is_pure: false,
+                        is_const: false,
                         ret_eightbyte_classes: vec![],
                         ret_is_f128_sse: false,
                     },
diff --git a/src/passes/gvn.rs b/src/passes/gvn.rs
index 624c6b0f..c83d5db6 100644
--- a/src/passes/gvn.rs
+++ b/src/passes/gvn.rs
@@ -2427,6 +2427,8 @@ mod tests {
                             struct_arg_is_f128_sse: Vec::new(),
                             is_sret: false,
                             is_fastcall: false,
+                            is_pure: false,
+                            is_const: false,
                             ret_eightbyte_classes: Vec::new(),
                             ret_is_f128_sse: false,
                         },
@@ -2688,6 +2690,8 @@ mod tests {
                             struct_arg_is_f128_sse: Vec::new(),
                             is_sret: false,
                             is_fastcall: false,
+                            is_pure: false,
+                            is_const: false,
                             ret_eightbyte_classes: Vec::new(),
                             ret_is_f128_sse: false,
                         },
@@ -2883,6 +2887,8 @@ mod tests {
                             struct_arg_is_f128_sse: Vec::new(),
                             is_sret: false,
                             is_fastcall: false,
+                            is_pure: false,
+                            is_const: false,
                             ret_eightbyte_classes: vec![],
                             ret_is_f128_sse: false,
                         },
diff --git a/src/passes/loop_unroll.rs b/src/passes/loop_unroll.rs
index 7a307c7c..58d594be 100644
--- a/src/passes/loop_unroll.rs
+++ b/src/passes/loop_unroll.rs
@@ -2346,6 +2346,8 @@ mod tests {
                 struct_arg_is_f128_sse: Vec::new(),
                 is_sret: false,
                 is_fastcall: false,
+                is_pure: false,
+                is_const: false,
                 ret_eightbyte_classes: vec![],
                 ret_is_f128_sse: false,
             },
diff --git a/src/passes/recursion_to_iter.rs b/src/passes/recursion_to_iter.rs
index 2aa6a2b9..7f717ff1 100644
--- a/src/passes/recursion_to_iter.rs
+++ b/src/passes/recursion_to_iter.rs
@@ -629,6 +629,8 @@ mod tests {
                         struct_arg_is_f128_sse: Vec::new(),
                         is_sret: false,
                         is_fastcall: false,
+                        is_pure: false,
+                        is_const: false,
                         ret_eightbyte_classes: Vec::new(),
                         ret_is_f128_sse: false,
                     },
@@ -662,6 +664,8 @@ mod tests {
                         struct_arg_is_f128_sse: Vec::new(),
                         is_sret: false,
                         is_fastcall: false,
+                        is_pure: false,
+                        is_const: false,
                         ret_eightbyte_classes: Vec::new(),
                         ret_is_f128_sse: false,
                     },
@@ -809,6 +813,8 @@ mod tests {
                     struct_arg_is_f128_sse: Vec::new(),
                     is_sret: false,
                     is_fastcall: false,
+                    is_pure: false,
+                    is_const: false,
                     ret_eightbyte_classes: Vec::new(),
                     ret_is_f128_sse: false,
                 },
diff --git a/src/passes/simplify.rs b/src/passes/simplify.rs
index 758bc752..84b2219a 100644
--- a/src/passes/simplify.rs
+++ b/src/passes/simplify.rs
@@ -2349,6 +2349,8 @@ mod tests {
                 struct_arg_is_f128_sse: Vec::new(),
                 is_sret: false,
                 is_fastcall: false,
+                is_pure: false,
+                is_const: false,
                 ret_eightbyte_classes: Vec::new(),
                 ret_is_f128_sse: false,
             },
diff --git a/src/passes/tail_call_elim.rs b/src/passes/tail_call_elim.rs
index 3c9025c3..68a340a1 100644
--- a/src/passes/tail_call_elim.rs
+++ b/src/passes/tail_call_elim.rs
@@ -1006,6 +1006,8 @@ mod tests {
                         struct_arg_is_f128_sse: Vec::new(),
                         is_sret: false,
                         is_fastcall: false,
+                        is_pure: false,
+                        is_const: false,
                         ret_eightbyte_classes: vec![],
                         ret_is_f128_sse: false,
                     },
@@ -1235,6 +1237,8 @@ mod tests {
                         struct_arg_is_f128_sse: Vec::new(),
                         is_sret: false,
                         is_fastcall: false,
+                        is_pure: false,
+                        is_const: false,
                         ret_eightbyte_classes: vec![],
                         ret_is_f128_sse: false,
                     },
