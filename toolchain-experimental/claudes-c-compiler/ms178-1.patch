diff --git a/src/backend/regalloc.rs b/src/backend/regalloc.rs
index 3821cb5..9dc322b 100644
--- a/src/backend/regalloc.rs
+++ b/src/backend/regalloc.rs
@@ -74,6 +74,7 @@ pub struct RegAllocConfig {
 
 /// Filter live intervals to only those eligible for register allocation,
 /// using the same whitelist + ineligibility rules as the three-phase allocator.
+#[inline]
 fn filter_eligible_intervals(
     liveness: &LivenessResult,
     eligible: &FxHashSet<u32>,
@@ -2017,6 +2018,7 @@ fn remove_ineligible_operands(
 
 /// Check whether a live interval spans any function call point.
 /// Uses binary search since call_points is sorted by program point.
+#[inline]
 fn spans_any_call(iv: &LiveInterval, call_points: &[u32]) -> bool {
     let start_idx = call_points.partition_point(|&cp| cp < iv.start);
     start_idx < call_points.len() && call_points[start_idx] <= iv.end
@@ -2391,6 +2393,7 @@ pub(crate) fn detect_phi_coalesce_groups(
 /// coalescing merge a pointer phi with its backedge increment while the phi
 /// was still live as an intrinsic operand (zlib-ng adler32_avx2 miscompile:
 /// the in-place `addq $32` clobbered the load address).
+#[inline]
 fn uses_value(inst: &Instruction, val_id: u32) -> bool {
     let mut found = false;
     for_each_operand_in_instruction(inst, |op| {
diff --git a/src/backend/stack_layout/copy_coalescing.rs b/src/backend/stack_layout/copy_coalescing.rs
index a5d6ce1..d90f29e 100644
--- a/src/backend/stack_layout/copy_coalescing.rs
+++ b/src/backend/stack_layout/copy_coalescing.rs
@@ -1366,7 +1366,7 @@ fn is_two_operand_binary(op: &crate::ir::intrinsics::IntrinsicOp) -> bool {
         | O::Pxor256 | O::Por256 | O::Pand256
         | O::AddF64x4 | O::MulF64x4 | O::AddI32x8
         | O::VecAddF64x4 | O::VecMulF64x4 | O::VecAddI32x8 | O::VecMulI32x8 | O::VecBroadcastI32x8
-        | O::VecAddF32x8 | O::VecMulF32x8
+        | O::VecBroadcastF32x8 | O::VecMulF32x8 | O::VecAddF32x8
         | O::VecAddF64x2 | O::VecMulF64x2 | O::VecAddI32x4
         | O::VecAddF32x4 | O::VecMulF32x4
     )
@@ -1543,9 +1543,10 @@ pub(super) fn compute_vector_defer_values(func: &IrFunction) -> FxHashSet<u32> {
                         | O::VecLoadF32x4 | O::VecLoadF32x8
                         | O::VecAddF64x2 | O::VecAddF64x4
                         | O::VecAddI32x4 | O::VecAddI32x8 | O::VecMulI32x8 | O::VecBroadcastI32x8
-                        | O::VecAddF32x4 | O::VecAddF32x8
+        | O::VecBroadcastF32x8 | O::VecMulF32x8 | O::VecAddF32x8
+                        | O::VecAddF32x4
                         | O::VecMulF64x2 | O::VecMulF64x4
-                        | O::VecMulF32x4 | O::VecMulF32x8
+                        | O::VecMulF32x4
                         | O::VecZeroF64x2 | O::VecZeroF64x4
                         | O::VecZeroI32x4 | O::VecZeroI32x8
                         | O::VecZeroF32x4 | O::VecZeroF32x8
diff --git a/src/backend/x86/codegen/emit.rs b/src/backend/x86/codegen/emit.rs
index 5a06b55..53c49d8 100644
--- a/src/backend/x86/codegen/emit.rs
+++ b/src/backend/x86/codegen/emit.rs
@@ -91,6 +91,7 @@ fn reg_name_to_32(name: &str) -> &'static str {
 
 /// Map a PhysReg index to its x86-64 register name.
 /// Handles both callee-saved (1-5) and caller-saved (10-15) registers.
+#[inline]
 pub(super) fn phys_reg_name(reg: PhysReg) -> &'static str {
     match reg.0 {
         1 => "rbx",
@@ -130,12 +131,14 @@ pub(super) fn phys_reg_name(reg: PhysReg) -> &'static str {
 }
 
 /// Check if a PhysReg is an XMM register (used for F64 values).
+#[inline]
 pub(super) fn is_xmm_reg(reg: PhysReg) -> bool {
     reg.0 >= 20 && reg.0 <= 33
 }
 
 /// Map a PhysReg index to its x86-64 32-bit sub-register name.
 /// Handles both callee-saved (1-5) and caller-saved (10-15) registers.
+#[inline]
 pub(super) fn phys_reg_name_32(reg: PhysReg) -> &'static str {
     match reg.0 {
         1 => "ebx",
diff --git a/src/backend/x86/codegen/intrinsics.rs b/src/backend/x86/codegen/intrinsics.rs
index 88068c4..07ff32b 100644
--- a/src/backend/x86/codegen/intrinsics.rs
+++ b/src/backend/x86/codegen/intrinsics.rs
@@ -202,6 +202,7 @@ impl X86Codegen {
     }
 
     /// Store an XMM register to a vector operand's home slot.
+    #[inline]
     pub(super) fn sse_store_dest(&mut self, dest_ptr: &Value, xmm: &'static str) {
         // CCC_ENABLE_VECREG redirect: keep the result in its allocated register.
         if let Some(&reg) = self.reg_assignments.get(&dest_ptr.0) {
@@ -335,6 +336,7 @@ impl X86Codegen {
         }
     }
 
+    #[inline]
     pub(super) fn emit_sse_binary_128(&mut self, dest_ptr: &Value, args: &[Operand], sse_inst: &str) {
         // Load operands into separate registers (direct slot addressing when
         // possible), perform the op, store the result to the destination slot.
@@ -2345,6 +2347,7 @@ impl X86Codegen {
                 if let Some(d) = dest {
                     self.state.vector_values.insert(d.0);
                     self.avx_store_dest(d);
+                    self.flush_pending_vec_store_impl();
                 }
             }
             IntrinsicOp::VecStoreI32x4 => {
@@ -2388,7 +2391,96 @@ impl X86Codegen {
                     self.state.emit("    vmovdqu %ymm0, (%rax)");
                 }
             }
-            IntrinsicOp::VecHorizontalAddF64x4 => {
+            
+            IntrinsicOp::VecBroadcastF32x8 => {
+                self.flush_pending_vec_store_impl();
+                self.state.invalidate_vec_peephole();
+                // scalar f32 in args[0] -> all 8 lanes via vbroadcastss.
+                // Prefer memory form; fall back to XMM then broadcast.
+                let mut done = false;
+                if let Operand::Value(v) = &args[0] {
+                    if let Some(addr) = self.state.resolve_slot_addr(v.0) {
+                        if let crate::backend::state::SlotAddr::Direct(slot) = addr {
+                            self.state.emit_fmt(format_args!(
+                                "    vbroadcastss {}, %ymm0", self.slot_ref(slot.0)));
+                            done = true;
+                        }
+                    }
+                    // Value may already live in an XMM home from ParamRef/float path.
+                    if !done {
+                        if let Some(&reg) = self.reg_assignments.get(&v.0) {
+                            if is_xmm_reg(reg) {
+                                let name = phys_reg_name(reg);
+                                self.state.emit_fmt(format_args!(
+                                    "    vbroadcastss %{}, %ymm0", name));
+                                done = true;
+                            }
+                        }
+                    }
+                }
+                if !done {
+                    // Stack/GPR materialisation of the f32 bits.
+                    self.operand_to_reg(&args[0], "rax");
+                    self.state.emit("    movd %eax, %xmm0");
+                    self.state.emit("    vbroadcastss %xmm0, %ymm0");
+                }
+                if let Some(d) = dest {
+                    self.state.vector_values.insert(d.0);
+                    self.avx_store_dest(d);
+                    // Consecutive broadcasts share %ymm0; materialise now.
+                    self.flush_pending_vec_store_impl();
+                }
+            }
+            IntrinsicOp::VecBroadcastF32x4 => {
+                self.flush_pending_vec_store_impl();
+                self.state.invalidate_vec_peephole();
+                self.operand_to_reg(&args[0], "rax");
+                self.state.emit("    movd %eax, %xmm0");
+                self.state.emit("    shufps $0x00, %xmm0, %xmm0");
+                if let Some(d) = dest {
+                    self.state.vector_values.insert(d.0);
+                    self.sse_store_dest(d, "xmm0");
+                }
+            }
+            IntrinsicOp::VecStoreF32x8 => {
+                let in_reg = matches!(&args[0], Operand::Value(v)
+                    if self.state.vec_last_store_reg && self.state.vec_last_store_val == Some(v.0));
+                if !in_reg {
+                    self.flush_pending_vec_store_impl();
+                    if let Operand::Value(v) = &args[0] {
+                        if let Some(addr) = self.state.resolve_slot_addr(v.0) {
+                            if let crate::backend::state::SlotAddr::Direct(slot) = addr {
+                                self.state.emit_fmt(format_args!("    vmovups {}, %ymm0", self.slot_ref(slot.0)));
+                            }
+                        }
+                    }
+                }
+                self.state.invalidate_vec_peephole();
+                if let Some(c_ptr) = dest_ptr {
+                    self.operand_to_reg(&Operand::Value(*c_ptr), "rax");
+                    self.state.emit("    vmovups %ymm0, (%rax)");
+                }
+            }
+            IntrinsicOp::VecStoreF32x4 => {
+                let in_reg = matches!(&args[0], Operand::Value(v)
+                    if self.state.sse_last_store_reg && self.state.sse_last_store_val == Some(v.0));
+                if !in_reg {
+                    self.flush_pending_vec_store_impl();
+                    if let Operand::Value(v) = &args[0] {
+                        if let Some(addr) = self.state.resolve_slot_addr(v.0) {
+                            if let crate::backend::state::SlotAddr::Direct(slot) = addr {
+                                self.state.emit_fmt(format_args!("    movups {}, %xmm0", self.slot_ref(slot.0)));
+                            }
+                        }
+                    }
+                }
+                self.state.invalidate_vec_peephole();
+                if let Some(c_ptr) = dest_ptr {
+                    self.operand_to_reg(&Operand::Value(*c_ptr), "rax");
+                    self.state.emit("    movups %xmm0, (%rax)");
+                }
+            }
+IntrinsicOp::VecHorizontalAddF64x4 => {
                 self.flush_pending_vec_store_impl();
                 self.state.invalidate_vec_peephole();
                 // %scalar = horizontal_add(%vec) - AVX2 4×F64 → F64
diff --git a/src/frontend/parser/declarators.rs b/src/frontend/parser/declarators.rs
index a155752..87e5dee 100644
--- a/src/frontend/parser/declarators.rs
+++ b/src/frontend/parser/declarators.rs
@@ -554,7 +554,7 @@ impl Parser {
             // These represent "array of pointers" and need to be propagated
             // to the caller so array-to-pointer decay is properly applied.
             // E.g. int (*a[]) = array of ptr-to-int = int **a (after decay)
-            let mut inner_array_dims = Vec::new();
+            let mut inner_array_dims = Vec::with_capacity(8);
             while matches!(self.peek(), TokenKind::LBracket) {
                 self.advance();
                 self.skip_array_qualifiers();
@@ -800,7 +800,7 @@ impl Parser {
         }
 
         // Parse array dimensions after pointer(s): (*[3][4])
-        let mut array_dims = Vec::new();
+        let mut array_dims = Vec::with_capacity(8);
         while matches!(self.peek(), TokenKind::LBracket) {
             self.advance();
             let size = if matches!(self.peek(), TokenKind::RBracket) {
diff --git a/src/frontend/parser/expressions.rs b/src/frontend/parser/expressions.rs
index 5084fcf..53b7330 100644
--- a/src/frontend/parser/expressions.rs
+++ b/src/frontend/parser/expressions.rs
@@ -389,7 +389,7 @@ impl Parser {
                     // Function call
                     let open = self.peek_span();
                     self.advance();
-                    let mut args = Vec::new();
+                    let mut args = Vec::with_capacity(8);
                     if !matches!(self.peek(), TokenKind::RParen) {
                         args.push(self.parse_assignment_expr());
                         while self.consume_if(&TokenKind::Comma) {
@@ -715,7 +715,7 @@ impl Parser {
         self.expect_context(&TokenKind::LParen, "after '_Generic'");
         let controlling = self.parse_assignment_expr();
         self.expect_context(&TokenKind::Comma, "after '_Generic' controlling expression");
-        let mut associations = Vec::new();
+        let mut associations = Vec::with_capacity(8);
         loop {
             if matches!(self.peek(), TokenKind::RParen) {
                 break;
diff --git a/src/frontend/parser/parse.rs b/src/frontend/parser/parse.rs
index 5ef1fda..74384be 100644
--- a/src/frontend/parser/parse.rs
+++ b/src/frontend/parser/parse.rs
@@ -411,7 +411,7 @@ impl Parser {
     }
 
     pub fn parse(&mut self) -> TranslationUnit {
-        let mut decls = Vec::new();
+        let mut decls = Vec::with_capacity(16);
         while !self.at_eof() {
             if let Some(decl) = self.parse_external_decl() {
                 decls.push(decl);
@@ -882,7 +882,7 @@ impl Parser {
     fn parse_string_attr_arg(&mut self) -> Option<String> {
         if !matches!(self.peek(), TokenKind::LParen) { return None; }
         self.advance(); // consume (
-        let mut result = String::new();
+        let mut result = String::with_capacity(64);
         while let TokenKind::StringLiteral(s) = self.peek() {
             result.push_str(s);
             self.advance();
diff --git a/src/frontend/parser/statements.rs b/src/frontend/parser/statements.rs
index 6df6792..9fadba3 100644
--- a/src/frontend/parser/statements.rs
+++ b/src/frontend/parser/statements.rs
@@ -12,8 +12,8 @@ impl Parser {
     pub(super) fn parse_compound_stmt(&mut self) -> CompoundStmt {
         let open_brace = self.peek_span();
         self.expect(&TokenKind::LBrace);
-        let mut items = Vec::new();
-        let mut local_labels = Vec::new();
+        let mut items = Vec::with_capacity(16);
+        let mut local_labels = Vec::with_capacity(8);
 
         // Save typedef shadowing state for this scope
         let saved_shadowed = self.shadowed_typedefs.clone();
@@ -319,9 +319,9 @@ impl Parser {
         let template = self.parse_asm_string();
 
         let mut outputs = Vec::new();
-        let mut inputs = Vec::new();
-        let mut clobbers = Vec::new();
-        let mut goto_labels = Vec::new();
+        let mut inputs = Vec::with_capacity(8);
+        let mut clobbers = Vec::with_capacity(8);
+        let mut goto_labels = Vec::with_capacity(8);
 
         // First colon: outputs
         if matches!(self.peek(), TokenKind::Colon) {
@@ -354,7 +354,7 @@ impl Parser {
     }
 
     fn parse_asm_string(&mut self) -> String {
-        let mut result = String::new();
+        let mut result = String::with_capacity(64);
         while let TokenKind::StringLiteral(ref s) = self.peek() {
             result.push_str(s);
             self.advance();
@@ -363,7 +363,7 @@ impl Parser {
     }
 
     fn parse_asm_operands(&mut self) -> Vec<AsmOperand> {
-        let mut operands = Vec::new();
+        let mut operands = Vec::with_capacity(8);
         if matches!(self.peek(), TokenKind::Colon | TokenKind::RParen) {
             return operands;
         }
@@ -418,7 +418,7 @@ impl Parser {
     }
 
     fn parse_asm_clobbers(&mut self) -> Vec<String> {
-        let mut clobbers = Vec::new();
+        let mut clobbers = Vec::with_capacity(8);
         if matches!(self.peek(), TokenKind::Colon | TokenKind::RParen) {
             return clobbers;
         }
diff --git a/src/frontend/parser/types.rs b/src/frontend/parser/types.rs
index 500b1a8..d8c3aca 100644
--- a/src/frontend/parser/types.rs
+++ b/src/frontend/parser/types.rs
@@ -812,7 +812,7 @@ impl Parser {
 
     /// Parse enum variant declarations inside braces.
     pub(super) fn parse_enum_variants(&mut self) -> Vec<EnumVariant> {
-        let mut variants = Vec::new();
+        let mut variants = Vec::with_capacity(8);
         let open = self.peek_span();
         self.expect_context(&TokenKind::LBrace, "for enum body");
         while !matches!(self.peek(), TokenKind::RBrace | TokenKind::Eof) {
diff --git a/src/frontend/preprocessor/expr_eval.rs b/src/frontend/preprocessor/expr_eval.rs
index 874f057..a74b7f2 100644
--- a/src/frontend/preprocessor/expr_eval.rs
+++ b/src/frontend/preprocessor/expr_eval.rs
@@ -14,7 +14,7 @@ impl Preprocessor {
     /// Replace remaining identifiers (not keywords) with 0 in a #if expression.
     /// Per C standard, after macro expansion, undefined identifiers in #if evaluate to 0.
     pub(super) fn replace_remaining_idents_with_zero(&self, expr: &str) -> String {
-        let mut result = String::new();
+        let mut result = String::with_capacity(64);
         let bytes = expr.as_bytes();
         let len = bytes.len();
         let mut i = 0;
@@ -113,7 +113,7 @@ impl Preprocessor {
         if !line.contains("__has_") {
             return line.to_string();
         }
-        let mut result = String::new();
+        let mut result = String::with_capacity(64);
         let bytes = line.as_bytes();
         let len = bytes.len();
         let mut i = 0;
@@ -169,7 +169,7 @@ impl Preprocessor {
     }
 
     pub(super) fn resolve_defined_in_expr(&mut self, expr: &str) -> String {
-        let mut result = String::new();
+        let mut result = String::with_capacity(64);
         let bytes = expr.as_bytes();
         let len = bytes.len();
         let mut i = 0;
diff --git a/src/frontend/preprocessor/macro_defs.rs b/src/frontend/preprocessor/macro_defs.rs
index f5fb320..eedc3a0 100644
--- a/src/frontend/preprocessor/macro_defs.rs
+++ b/src/frontend/preprocessor/macro_defs.rs
@@ -1261,7 +1261,7 @@ fn contains_standalone_ident(s: &str, ident: &str) -> bool {
 /// Operates on bytes to avoid Vec<char> allocation.
 fn stringify_arg(arg: &str) -> String {
     let trimmed = arg.trim();
-    let mut result = String::new();
+    let mut result = String::with_capacity(64);
     let bytes = trimmed.as_bytes();
     let len = bytes.len();
     let mut i = 0;
@@ -1385,7 +1385,7 @@ pub fn parse_define(line: &str) -> Option<MacroDef> {
     if i < len && bytes[i] == b'(' {
         // Function-like macro
         i += 1; // skip '('
-        let mut params = Vec::new();
+        let mut params = Vec::with_capacity(8);
         let mut is_variadic = false;
         let mut has_named_variadic = false;
 
diff --git a/src/frontend/preprocessor/pipeline.rs b/src/frontend/preprocessor/pipeline.rs
index b2c0b2d..3251a31 100644
--- a/src/frontend/preprocessor/pipeline.rs
+++ b/src/frontend/preprocessor/pipeline.rs
@@ -18,7 +18,7 @@ use super::text_processing::{strip_line_comment, split_first_word};
 /// Deduplicate a list of macro names, preserving order (first occurrence wins).
 /// Used to remove duplicate names from nested macro expansions.
 fn dedup_macro_names(names: Vec<String>) -> Vec<String> {
-    let mut unique = Vec::new();
+    let mut unique = Vec::with_capacity(8);
     for name in names {
         if !unique.contains(&name) {
             unique.push(name);
diff --git a/src/frontend/preprocessor/predefined_macros.rs b/src/frontend/preprocessor/predefined_macros.rs
index e71384a..0e991b1 100644
--- a/src/frontend/preprocessor/predefined_macros.rs
+++ b/src/frontend/preprocessor/predefined_macros.rs
@@ -330,7 +330,7 @@ impl Preprocessor {
 
     /// Get default system include paths (arch-neutral only).
     pub(super) fn default_system_include_paths() -> Vec<PathBuf> {
-        let mut paths = Vec::new();
+        let mut paths = Vec::with_capacity(8);
         // Bundled include directory takes priority over system GCC headers
         if let Some(bundled) = Self::bundled_include_dir() {
             paths.push(bundled);
diff --git a/src/frontend/sema/analysis.rs b/src/frontend/sema/analysis.rs
index 29623a0..8254e4f 100644
--- a/src/frontend/sema/analysis.rs
+++ b/src/frontend/sema/analysis.rs
@@ -756,7 +756,7 @@ impl SemanticAnalyzer {
                 // Store packed enum info for forward-reference lookups
                 if *is_packed {
                     if let Some(tag) = name {
-                        let mut variant_values = Vec::new();
+                        let mut variant_values = Vec::with_capacity(8);
                         let mut next_val: i64 = 0;
                         for v in variants {
                             if let Some(ref val_expr) = v.value {
@@ -1973,7 +1973,7 @@ impl type_builder::TypeConvertContext for SemanticAnalyzer {
         // done separately via process_enum_variants (requires &mut self).
         // We carry variant values so packed_size() can compute the correct size.
         let variant_values = if let Some(vars) = variants {
-            let mut result = Vec::new();
+            let mut result = Vec::with_capacity(8);
             let mut next_val: i64 = 0;
             for v in vars {
                 if let Some(ref val_expr) = v.value {
diff --git a/src/frontend/sema/const_eval.rs b/src/frontend/sema/const_eval.rs
index c560c37..9dfd228 100644
--- a/src/frontend/sema/const_eval.rs
+++ b/src/frontend/sema/const_eval.rs
@@ -763,7 +763,7 @@ impl<'a> SemaConstEval<'a> {
         }
         // Build from variant list
         let variant_values = if let Some(vars) = variants {
-            let mut result = Vec::new();
+            let mut result = Vec::with_capacity(8);
             let mut next_val: i64 = 0;
             for v in vars {
                 if let Some(ref val_expr) = v.value {
@@ -1161,7 +1161,7 @@ fn ctype_from_type_spec(spec: &TypeSpecifier, types: &TypeContext) -> CType {
             }
             // Inline definition: compute from variants
             if let Some(vars) = variants {
-                let mut variant_values = Vec::new();
+                let mut variant_values = Vec::with_capacity(8);
                 for (next_val, v) in (0_i64..).zip(vars.iter()) {
                     if let Some(ref _val_expr) = v.value {
                         // Can't easily eval here, but this path is rare
diff --git a/src/frontend/sema/type_checker.rs b/src/frontend/sema/type_checker.rs
index 544d331..017c818 100644
--- a/src/frontend/sema/type_checker.rs
+++ b/src/frontend/sema/type_checker.rs
@@ -607,7 +607,7 @@ impl<'a> ExprTypeChecker<'a> {
                 // GCC extension: determine underlying enum type based on value range.
                 // Uses progression: int -> unsigned int -> long long -> unsigned long long.
                 let values = if let Some(vars) = variants {
-                    let mut vals = Vec::new();
+                    let mut vals = Vec::with_capacity(8);
                     let mut next_val: i64 = 0;
                     for v in vars {
                         if let Some(ref val_expr) = v.value {
diff --git a/src/ir/intrinsics.rs b/src/ir/intrinsics.rs
index 4b13a43..0799590 100644
--- a/src/ir/intrinsics.rs
+++ b/src/ir/intrinsics.rs
@@ -238,6 +238,10 @@ pub enum IntrinsicOp {
     VecAddF32x4,
     /// Vector multiply: %dest_vec = %src1_vec * %src2_vec - AVX2 8×F32
     VecMulF32x8,
+    /// Broadcast scalar f32 to 8 lanes (vbroadcastss).
+    VecBroadcastF32x8,
+    /// Broadcast scalar f32 to 4 lanes.
+    VecBroadcastF32x4,
     /// Vector multiply: %dest_vec = %src1_vec * %src2_vec - SSE2 4×F32
     VecMulF32x4,
     /// Vector multiply: %dest_vec = %src1_vec * %src2_vec - 4×I32
@@ -252,6 +256,10 @@ pub enum IntrinsicOp {
     /// dest_ptr = destination pointer; args[0] = source vector value.
     VecStoreI32x4,
     VecStoreI32x8,
+    /// Store 8×F32 (vmovups ymm).
+    VecStoreF32x8,
+    /// Store 4×F32.
+    VecStoreF32x4,
     /// Load two signed I32 lanes and widen to two I64 lanes.
     VecLoadWidenI32ToI64x2,
     /// Load two I64 lanes (movdqu).
@@ -973,7 +981,7 @@ impl IntrinsicOp {
             | LoadF64x4 | LoadI32x8 | AddF64x4 | MulF64x4 | AddI32x8
             | VecLoadF64x4 | VecLoadI32x8 | VecAddF64x4 | VecMulF64x4
             | VecAddI32x8 | VecMulI32x8 | VecBroadcastI32x8 | VecZeroF64x4 | VecZeroI32x8
-            | VecLoadF32x8 | VecAddF32x8 | VecMulF32x8 | VecZeroF32x8
+            | VecLoadF32x8 | VecAddF32x8 | VecMulF32x8 | VecBroadcastF32x8 | VecZeroF32x8
             // Newly wired AVX/AVX2 ops (previously scalar header loops)
             | Pmulld256 | Psubd256 | Paddq256 | Psubq256 | Pandn256
             | Pcmpeqd256 | Pcmpeqq256 | Pcmpgtd256 | Pcmpgtq256
@@ -1010,7 +1018,7 @@ impl IntrinsicOp {
             | FmaF64x2 | LoadF64x2 | LoadI32x4 | AddF64x2 | MulF64x2
             | AddI32x4 | VecLoadF64x2 | VecLoadI32x4 | VecAddF64x2
             | VecMulF64x2 | VecAddI32x4 | VecZeroF64x2 | VecZeroI32x4
-            | VecLoadF32x4 | VecAddF32x4 | VecMulF32x4 | VecZeroF32x4
+            | VecLoadF32x4 | VecAddF32x4 | VecMulF32x4 | VecBroadcastF32x4 | VecZeroF32x4
             | VecLoadWidenI32ToI64x2 | VecLoadI64x2 | VecAddI64x2 | VecMulI64x2 | VecZeroI64x2
             | VecMulI32x4 | VecBroadcastI32x4
             | Paddusb128 | Paddsb128 | Paddusw128 | Paddsw128 | Psubsw128
diff --git a/src/passes/aggregate_copy_forward.rs b/src/passes/aggregate_copy_forward.rs
index 24c73c4..37b5530 100644
--- a/src/passes/aggregate_copy_forward.rs
+++ b/src/passes/aggregate_copy_forward.rs
@@ -339,7 +339,7 @@ fn forward_store_only_temporaries(func: &mut IrFunction) -> usize {
     let mut invalid = FxHashSet::default();
     for (bi, block) in func.blocks.iter().enumerate() {
         for (ii, inst) in block.instructions.iter().enumerate() {
-            let mut used = Vec::new(); inst.for_each_used_value(|v| used.push(v));
+            let mut used = Vec::with_capacity(16); inst.for_each_used_value(|v| used.push(v));
             for value in used {
                 let Some((root, _)) = paths.get(&value) else { continue };
                 let Some((copy_b, copy_i, _)) = copies.get(root) else { continue };
@@ -529,7 +529,7 @@ pub(crate) fn run(func: &mut IrFunction) -> usize {
     let mut invalid = FxHashSet::default();
     for (bi, block) in func.blocks.iter().enumerate() {
         for (ii, inst) in block.instructions.iter().enumerate() {
-            let mut used = Vec::new();
+            let mut used = Vec::with_capacity(16);
             inst.for_each_used_value(|v| used.push(v));
             for value in used {
                 let Some((root, _)) = paths.get(&value) else { continue };
@@ -613,7 +613,7 @@ pub(crate) fn run(func: &mut IrFunction) -> usize {
         // Same 1:1 length requirement as above (mismatch => drop spans, never index OOB).
         let has_spans = old_spans.len() == old.len();
         let mut out = Vec::with_capacity(old.len());
-        let mut spans = Vec::new();
+        let mut spans = Vec::with_capacity(16);
         for (ii, mut inst) in old.into_iter().enumerate() {
             if let Instruction::Memcpy { dest, .. } = &inst {
                 if candidates.get(&dest.0).is_some_and(|c| c.block == bi && c.inst == ii) {
diff --git a/src/passes/cfg_simplify.rs b/src/passes/cfg_simplify.rs
index 1af98c4..78b9161 100644
--- a/src/passes/cfg_simplify.rs
+++ b/src/passes/cfg_simplify.rs
@@ -355,7 +355,7 @@ fn fold_constant_switches(func: &mut IrFunction, label_to_idx: &FxHashMap<BlockI
                         .unwrap_or(*default);
 
                     // Collect unique not-taken targets.
-                    let mut not_taken = Vec::new();
+                    let mut not_taken = Vec::with_capacity(16);
                     if *default != taken && !not_taken.contains(default) {
                         not_taken.push(*default);
                     }
@@ -506,10 +506,10 @@ fn collect_thread_redirections(
         });
     }
 
-    let mut redirections = Vec::new();
+    let mut redirections = Vec::with_capacity(16);
 
     for block_idx in 0..func.blocks.len() {
-        let mut edge_changes = Vec::new();
+        let mut edge_changes = Vec::with_capacity(16);
         let block_label = func.blocks[block_idx].label;
 
         match &func.blocks[block_idx].terminator {
@@ -878,7 +878,7 @@ fn find_fusion_candidates(
     asm_goto_targets: &FxHashSet<BlockId>,
 ) -> Vec<(usize, usize)> {
     let entry_label = func.blocks[0].label;
-    let mut fusions = Vec::new();
+    let mut fusions = Vec::with_capacity(16);
     let mut absorbed: FxHashSet<usize> = FxHashSet::default();
     let mut absorbers: FxHashSet<usize> = FxHashSet::default();
 
diff --git a/src/passes/copy_prop.rs b/src/passes/copy_prop.rs
index 122a5b5..d0ea2cb 100644
--- a/src/passes/copy_prop.rs
+++ b/src/passes/copy_prop.rs
@@ -140,7 +140,7 @@ pub(crate) fn forward_large_memcpy_loads(module: &mut IrModule) -> usize {
                     { continue; }
 
                     let dest_derived = collect_derived_pointers(func, dest);
-                    let mut loads = Vec::new();
+                    let mut loads = Vec::with_capacity(16);
                     let mut safe = true;
                     for (bi, block) in func.blocks.iter().enumerate() {
                         for (ii, inst) in block.instructions.iter().enumerate() {
@@ -923,7 +923,7 @@ fn one_round_post_phi(func: &mut IrFunction) -> usize {
 
 /// A tiny single-entry copy map used by the post-phi substitution.
 fn copy_single_map(dest: Value, src: Operand) -> Vec<Option<Operand>> {
-    let mut m = Vec::new();
+    let mut m = Vec::with_capacity(16);
     let cap = dest.0 as usize + 1;
     m.resize(cap, None);
     m[dest.0 as usize] = Some(src);
diff --git a/src/passes/dead_statics.rs b/src/passes/dead_statics.rs
index 880f082..0ffa4fe 100644
--- a/src/passes/dead_statics.rs
+++ b/src/passes/dead_statics.rs
@@ -193,7 +193,7 @@ fn build_refs_and_address_taken(
             func_refs.push(Vec::new());
             continue;
         }
-        let mut refs = Vec::new();
+        let mut refs = Vec::with_capacity(16);
         for block in &func.blocks {
             for inst in &block.instructions {
                 for_each_instruction_symbol(inst, |name, takes_address| {
@@ -210,7 +210,7 @@ fn build_refs_and_address_taken(
 
     let mut global_refs = Vec::with_capacity(module.globals.len());
     for global in &module.globals {
-        let mut refs = Vec::new();
+        let mut refs = Vec::with_capacity(16);
         global.init.for_each_ref(&mut |name| {
             push_named_refs(name, name_to_ids, &mut refs);
             // An initializer that names a symbol takes its address
diff --git a/src/passes/div_by_const.rs b/src/passes/div_by_const.rs
index 97e9ec3..218d963 100644
--- a/src/passes/div_by_const.rs
+++ b/src/passes/div_by_const.rs
@@ -429,7 +429,7 @@ fn expand_udiv32(
     }
 
     let (magic, shift, needs_add) = compute_unsigned_magic_32(d)?;
-    let mut insts = Vec::new();
+    let mut insts = Vec::with_capacity(16);
 
     let x64 = fresh_value(next_id);
     insts.push(Instruction::Cast {
@@ -593,7 +593,7 @@ fn expand_sdiv32(
 
     if d > 0 && (d as u32).is_power_of_two() {
         let k = d.trailing_zeros();
-        let mut insts = Vec::new();
+        let mut insts = Vec::with_capacity(16);
 
         let sign = fresh_value(next_id);
         insts.push(Instruction::BinOp {
@@ -634,7 +634,7 @@ fn expand_sdiv32(
     }
 
     let (magic, shift) = compute_signed_magic_32(d);
-    let mut insts = Vec::new();
+    let mut insts = Vec::with_capacity(16);
 
     let x64 = fresh_value(next_id);
     insts.push(Instruction::Cast {
@@ -825,7 +825,7 @@ fn expand_udiv64(
     }
 
     let (magic, shift, needs_add) = compute_unsigned_magic_64(d)?;
-    let mut insts = Vec::new();
+    let mut insts = Vec::with_capacity(16);
 
     let x_u64 = if ty == IrType::U64 {
         *x
@@ -996,7 +996,7 @@ fn expand_sdiv64(
 
     if d > 0 && (d as u64).is_power_of_two() {
         let k = d.trailing_zeros();
-        let mut insts = Vec::new();
+        let mut insts = Vec::with_capacity(16);
 
         let sign = fresh_value(next_id);
         insts.push(Instruction::BinOp {
@@ -1037,7 +1037,7 @@ fn expand_sdiv64(
     }
 
     let (magic, shift) = compute_signed_magic_64(d);
-    let mut insts = Vec::new();
+    let mut insts = Vec::with_capacity(16);
 
     let x128 = fresh_value(next_id);
     insts.push(Instruction::Cast {
@@ -1223,7 +1223,7 @@ fn expand_udiv32_in_i64(
         return None;
     }
 
-    let mut insts = Vec::new();
+    let mut insts = Vec::with_capacity(16);
 
     // Explicitly zero-extend low 32 bits to protect against dirty upper bits from ABI register passing
     let x_masked = fresh_value(next_id);
@@ -1334,7 +1334,7 @@ fn expand_sdiv32_in_i64(
         return None;
     }
 
-    let mut insts = Vec::new();
+    let mut insts = Vec::with_capacity(16);
 
     // Explicitly sign-extend low 32 bits into 64-bit signed integer
     let x_shl = fresh_value(next_id);
diff --git a/src/passes/if_convert.rs b/src/passes/if_convert.rs
index 46cfe39..40ded7f 100644
--- a/src/passes/if_convert.rs
+++ b/src/passes/if_convert.rs
@@ -599,7 +599,7 @@ fn detect_diamond(
     // The merge block must have Phi nodes that reference both arms.
     // Collect phi nodes we can convert.
     let merge_block = &func.blocks[merge_idx];
-    let mut phi_selects = Vec::new();
+    let mut phi_selects = Vec::with_capacity(16);
 
     for inst in &merge_block.instructions {
         if let Instruction::Phi { dest, ty, incoming } = inst {
@@ -784,7 +784,7 @@ fn detect_triangle(
 
     // Collect phi nodes from merge block
     let merge_block = &func.blocks[merge_idx];
-    let mut phi_selects = Vec::new();
+    let mut phi_selects = Vec::with_capacity(16);
 
     for inst in &merge_block.instructions {
         if let Instruction::Phi { dest, ty, incoming } = inst {
diff --git a/src/passes/inline.rs b/src/passes/inline.rs
index 3d54490..f19abb4 100644
--- a/src/passes/inline.rs
+++ b/src/passes/inline.rs
@@ -618,7 +618,7 @@ fn inline_run(module: &mut IrModule, small_only: bool) -> usize {
                     crate::pgo::profile::derive_block_counts(caller_fn, fp);
                 }
                 // For each call site, check if PGO says to force inline or deny
-                let mut filtered = Vec::new();
+                let mut filtered = Vec::with_capacity(16);
                 for site in call_sites {
                     if let Some(callee) = callee_map.get(&site.callee_name) {
                         // Get callee name
@@ -864,7 +864,7 @@ fn inline_run(module: &mut IrModule, small_only: bool) -> usize {
                     crate::pgo::profile::derive_block_counts(caller_fn, fp);
                 }
                 // For each call site, check if PGO says to force inline or deny
-                let mut filtered = Vec::new();
+                let mut filtered = Vec::with_capacity(16);
                 for site in call_sites {
                     if let Some(callee) = callee_map.get(&site.callee_name) {
                         // Get callee name
@@ -1379,7 +1379,7 @@ fn validate_function_values(func: &IrFunction, last_inlined_callee: &str) {
     }
 
     // Check all used values
-    let mut errors = Vec::new();
+    let mut errors = Vec::with_capacity(16);
     for (block_idx, block) in func.blocks.iter().enumerate() {
         for (inst_idx, inst) in block.instructions.iter().enumerate() {
             for v in inst.used_values() {
@@ -1959,7 +1959,7 @@ fn find_inline_call_sites(
     skip_list: &[String],
     caller_has_section: bool,
 ) -> Vec<InlineCallSite> {
-    let mut sites = Vec::new();
+    let mut sites = Vec::with_capacity(16);
 
     for (block_idx, block) in func.blocks.iter().enumerate() {
         for (inst_idx, inst) in block.instructions.iter().enumerate() {
@@ -2187,7 +2187,7 @@ fn inline_call_site(
     // (zlib-ng deflateSetParamPre *out=param wrote a stale %rbx). Constants
     // are materialized at the use site, so they have no register interaction.
     let orig_homes: Vec<Value> = {
-        let mut v = Vec::new();
+        let mut v = Vec::with_capacity(16);
         for inst in &callee.blocks[0].instructions {
             if let Instruction::Alloca { dest, .. } = inst {
                 v.push(*dest);
@@ -2426,7 +2426,7 @@ fn inline_call_site(
             block.source_spans.len() == block.instructions.len() && !block.source_spans.is_empty();
         let old_spans = std::mem::take(&mut block.source_spans);
         let mut new_insts = Vec::with_capacity(block.instructions.len());
-        let mut new_spans = Vec::new();
+        let mut new_spans = Vec::with_capacity(16);
         let mut paramref_dests: crate::common::fx_hash::FxHashSet<u32> = crate::common::fx_hash::FxHashSet::default();
         for (idx, inst) in block.instructions.drain(..).enumerate() {
             if let Instruction::ParamRef { dest, .. } = &inst {
@@ -2574,7 +2574,7 @@ fn inline_call_site(
     // Create the merge block with the remaining instructions and original terminator.
     // If the callee had a non-void return, insert a Phi (or Copy for single-predecessor)
     // at the start of the merge block to define the call's result value.
-    let mut merge_instructions = Vec::new();
+    let mut merge_instructions = Vec::with_capacity(16);
     let mut merge_spans: Vec<crate::common::source::Span> = Vec::new();
     if let Some(call_dest) = site.dest {
         if phi_incoming.len() == 1 {
diff --git a/src/passes/ipcp.rs b/src/passes/ipcp.rs
index 9571941..dd72e0b 100644
--- a/src/passes/ipcp.rs
+++ b/src/passes/ipcp.rs
@@ -92,7 +92,7 @@ pub fn run(module: &mut IrModule) -> usize {
             for block in &mut func.blocks {
                 let has_spans = !block.source_spans.is_empty();
                 let mut new_insts = Vec::with_capacity(block.instructions.len());
-                let mut new_spans = Vec::new();
+                let mut new_spans = Vec::with_capacity(16);
                 for (idx, inst) in block.instructions.drain(..).enumerate() {
                     let is_dead = match &inst {
                         Instruction::Call { func, .. } => dead_calls.contains(func.as_str()),
@@ -715,7 +715,7 @@ fn propagate_constant_arguments(module: &mut IrModule) -> usize {
     // parameters that have a uniform constant across all call sites.
     let mut specializations: FxHashMap<String, Vec<(usize, IrConst)>> = FxHashMap::default();
     for (name, param_states) in &func_param_consts {
-        let mut specs = Vec::new();
+        let mut specs = Vec::with_capacity(16);
         for (i, state) in param_states.iter().enumerate() {
             if let ParamState::Const(c) = state {
                 specs.push((i, *c));
diff --git a/src/passes/iv_strength_reduce.rs b/src/passes/iv_strength_reduce.rs
index dab3e3d..89a6b71 100644
--- a/src/passes/iv_strength_reduce.rs
+++ b/src/passes/iv_strength_reduce.rs
@@ -431,7 +431,7 @@ fn find_basic_ivs(
     preheader: usize,
     back_blocks: &[usize],
 ) -> Vec<BasicIV> {
-    let mut ivs = Vec::new();
+    let mut ivs = Vec::with_capacity(16);
 
     // Build a map from value to its defining instruction within the loop
     let mut loop_defs: FxHashMap<u32, &Instruction> = FxHashMap::default();
@@ -533,7 +533,7 @@ fn find_derived_exprs(
     basic_ivs: &[BasicIV],
     loop_body: &FxHashSet<usize>,
 ) -> Vec<DerivedExpr> {
-    let mut derived = Vec::new();
+    let mut derived = Vec::with_capacity(16);
 
     // Build a set of IV phi values for quick lookup
     let mut iv_values: FxHashMap<u32, usize> = FxHashMap::default();
@@ -545,7 +545,7 @@ fn find_derived_exprs(
     // Copy(Cast(iv))). Multiple passes to handle chains.
     let mut iv_derived: FxHashMap<u32, usize> = FxHashMap::default();
     for _ in 0..3 {
-        let mut new_entries = Vec::new();
+        let mut new_entries = Vec::with_capacity(16);
         for &bi in loop_body {
             if bi >= func.blocks.len() {
                 continue;
@@ -702,7 +702,7 @@ fn find_derived_exprs(
 
             // Find GEPs that use this multiply/shift result as their offset
             let mul_dest_id = mul_dest.0;
-            let mut gep_uses = Vec::new();
+            let mut gep_uses = Vec::with_capacity(16);
 
             for &gbi in loop_body {
                 if gbi >= func.blocks.len() {
diff --git a/src/passes/loop_analysis.rs b/src/passes/loop_analysis.rs
index 772f2eb..352143a 100644
--- a/src/passes/loop_analysis.rs
+++ b/src/passes/loop_analysis.rs
@@ -308,7 +308,7 @@ impl NaturalLoop {
 
     /// Find all exiting blocks (blocks inside the loop that have at least one successor outside).
     pub fn exiting_blocks(&self, succs: &analysis::FlatAdj) -> Vec<usize> {
-        let mut exiting = Vec::new();
+        let mut exiting = Vec::with_capacity(16);
         for &block in &self.body {
             for &succ in succs.row(block) {
                 if !self.body.contains(&(succ as usize)) {
@@ -339,7 +339,7 @@ impl NaturalLoop {
 
     /// Find all exit edges `(from, to)` where `from` is inside the loop and `to` is outside.
     pub fn exit_edges(&self, succs: &analysis::FlatAdj) -> Vec<(usize, usize)> {
-        let mut edges = Vec::new();
+        let mut edges = Vec::with_capacity(16);
         for &block in &self.body {
             for &succ in succs.row(block) {
                 let s = succ as usize;
@@ -463,7 +463,7 @@ pub fn find_natural_loops(
     }
 
     let dom = DominanceChecker::new(num_blocks, idom);
-    let mut loops = Vec::new();
+    let mut loops = Vec::with_capacity(16);
 
     // Find back edges: an edge (tail -> header) where header dominates tail in O(1)
     for tail in 0..num_blocks {
diff --git a/src/passes/outline_switch.rs b/src/passes/outline_switch.rs
index ce3eab6..a061f9e 100644
--- a/src/passes/outline_switch.rs
+++ b/src/passes/outline_switch.rs
@@ -115,7 +115,7 @@ struct OutlinableCase {
 
 /// Find the Switch terminators in a function that are worth outlining.
 fn find_outline_candidates(func: &IrFunction) -> Vec<SwitchInfo> {
-    let mut candidates = Vec::new();
+    let mut candidates = Vec::with_capacity(16);
 
     for block in func.blocks.iter() {
         if let Terminator::Switch { cases, default, .. } = &block.terminator {
@@ -181,7 +181,7 @@ fn collect_case_blocks(
     // Collect blocks starting from entry_idx until we hit another case entry.
     // Don't stop at end_block — it may appear within the case (e.g., when the
     // end_block detection picks a block that's inside a case body).
-    let mut case_blocks = Vec::new();
+    let mut case_blocks = Vec::with_capacity(16);
     for idx in entry_idx..func.blocks.len() {
         let bid = func.blocks[idx].label;
 
@@ -748,7 +748,7 @@ fn build_outlined_function(
     let entry_label = BlockId(*global_max_block_id + 1);
     *global_max_block_id += 1;
 
-    let mut entry_instructions = Vec::new();
+    let mut entry_instructions = Vec::with_capacity(16);
     for (i, (_val, _ty)) in case.external_values.iter().enumerate() {
         entry_instructions.push(Instruction::ParamRef {
             dest: Value(param_value_base + i as u32),
diff --git a/src/passes/range_check.rs b/src/passes/range_check.rs
index f3da624..a529b3c 100644
--- a/src/passes/range_check.rs
+++ b/src/passes/range_check.rs
@@ -117,7 +117,7 @@ struct Range {
 /// their root AND their full cast chain match (an i8→i32 zero-extension is
 /// not the same operand as an i16→i32 sign-extension).
 fn follow_casts(mut v: Value, cast_defs: &[Option<(Operand, IrType, IrType)>]) -> (Value, Vec<(IrType, IrType)>) {
-    let mut chain = Vec::new();
+    let mut chain = Vec::with_capacity(16);
     loop {
         let idx = v.0 as usize;
         match cast_defs.get(idx).and_then(|x| x.as_ref()) {
diff --git a/src/passes/univsr.rs b/src/passes/univsr.rs
index 81cce59..3fc1815 100644
--- a/src/passes/univsr.rs
+++ b/src/passes/univsr.rs
@@ -112,7 +112,7 @@ pub(crate) fn run_univsr(func: &mut IrFunction) -> usize {
 
 /// Detect IVSR-created pointer IVs in the function.
 fn detect_ivsr_pointer_ivs(func: &IrFunction) -> Vec<IvsrPointerIV> {
-    let mut result = Vec::new();
+    let mut result = Vec::with_capacity(16);
 
     for block in &func.blocks {
         for inst in &block.instructions {
@@ -480,7 +480,7 @@ fn validate_transformation_safety(func: &IrFunction, ptr_iv: &IvsrPointerIV) ->
 
 /// Find all transitive Load/Store uses of the pointer phi, tracking accumulated constant offsets.
 fn find_transitive_ptr_uses(func: &IrFunction, ptr_iv: &IvsrPointerIV) -> Vec<PtrUse> {
-    let mut results = Vec::new();
+    let mut results = Vec::with_capacity(16);
     let mut visited_values = FxHashSet::default();
 
     // Queue of (Value, accumulated_offset)
diff --git a/src/passes/vectorize.rs b/src/passes/vectorize.rs
index 7654ee1..6b17fb4 100644
--- a/src/passes/vectorize.rs
+++ b/src/passes/vectorize.rs
@@ -1808,9 +1808,12 @@ fn analyze_map_pattern(
     // order), with loop-invariant scale and offset.
     let body = &func.blocks[body_idx];
     let add_inst = find_inst_by_dest(body, store_val)?;
-    let Instruction::BinOp { op: IrBinOp::Add, lhs: add_lhs, rhs: add_rhs, ty: IrType::I32, .. } = add_inst else {
+    let Instruction::BinOp { op: IrBinOp::Add, lhs: add_lhs, rhs: add_rhs, ty: add_ty, .. } = add_inst else {
         return None;
     };
+    if !matches!(add_ty, IrType::I32 | IrType::F32) {
+        return None;
+    }
     let (mul_val, offset) = match (add_lhs, add_rhs) {
         (Operand::Value(v), _) if is_invariant(add_rhs) => (*v, add_rhs.clone()),
         (_, Operand::Value(v)) if is_invariant(add_lhs) => (*v, add_lhs.clone()),
@@ -1818,9 +1821,12 @@ fn analyze_map_pattern(
     };
 
     let mul_inst = find_inst_by_dest(body, mul_val)?;
-    let Instruction::BinOp { op: IrBinOp::Mul, lhs: mul_lhs, rhs: mul_rhs, ty: IrType::I32, .. } = mul_inst else {
+    let Instruction::BinOp { op: IrBinOp::Mul, lhs: mul_lhs, rhs: mul_rhs, ty: mul_ty, .. } = mul_inst else {
         return None;
     };
+    if !matches!(mul_ty, IrType::I32 | IrType::F32) || mul_ty != add_ty {
+        return None;
+    }
     let scale = match (mul_lhs, mul_rhs) {
         (Operand::Value(v), _) if *v == load_dest && is_invariant(mul_rhs) => mul_rhs.clone(),
         (_, Operand::Value(v)) if *v == load_dest && is_invariant(mul_lhs) => mul_lhs.clone(),
@@ -2998,7 +3004,7 @@ fn transform_to_fma_f64x2(func: &mut IrFunction, pattern: &VectorizablePattern)
             }
 
             // Collect GEPs that need to be modified (two-pass to avoid borrow issues)
-            let mut geps_to_modify = Vec::new();
+            let mut geps_to_modify = Vec::with_capacity(16);
             for &block_idx in &innermost_blocks {
                 let block = &func.blocks[block_idx];
                 for (inst_idx, inst) in block.instructions.iter().enumerate() {
@@ -3696,7 +3702,7 @@ fn insert_reduction_remainder_loop(
         } else {
             (pattern.accumulator_phi, Vec::new())
         };
-        let mut instructions = Vec::new();
+        let mut instructions = Vec::with_capacity(16);
         instructions.append(&mut prefix);
         instructions.extend_from_slice(&[
             // Horizontal reduction: scalar_sum = reduce(vec_accumulator)
@@ -5465,18 +5471,12 @@ fn transform_map_vector(func: &mut IrFunction, pattern: &MapPattern, avx2: bool)
     let debug = std::env::var("LCCC_DEBUG_VECTORIZE").is_ok();
     let mut changes = 0;
     let vec_width: u64 = if avx2 { 8 } else { 4 };
+    // Detect element type from the body store (I32 or F32).
+    let mut elem_is_f32 = false;
     for inst in &func.blocks[pattern.body_idx].instructions {
-        if let Instruction::Store { ty: IrType::F32, .. } = inst {
-            if debug { eprintln!("[VEC-MAP] F32 map not implemented"); }
-            return 0;
-        }
-    }
-    // F32 map needs VecBroadcastF32x4 / VecStoreF32x4 — not yet on x86.
-    // Reject here so analysis can still report the pattern while staying correct.
-    for inst in &func.blocks[pattern.body_idx].instructions {
-        if let Instruction::Store { ty: IrType::F32, .. } = inst {
-            if debug { eprintln!("[VEC-MAP]   F32 map transform not yet implemented"); }
-            return 0;
+        if let Instruction::Store { ty, .. } = inst {
+            elem_is_f32 = matches!(ty, IrType::F32);
+            break;
         }
     }
 
@@ -5602,7 +5602,12 @@ fn transform_map_vector(func: &mut IrFunction, pattern: &MapPattern, avx2: bool)
     next_val_id += 1;
     {
         let preheader = &mut func.blocks[preheader_idx];
-        let bcast = if avx2 { IntrinsicOp::VecBroadcastI32x8 } else { IntrinsicOp::VecBroadcastI32x4 };
+        let bcast = match (elem_is_f32, avx2) {
+            (true, true) => IntrinsicOp::VecBroadcastF32x8,
+            (true, false) => IntrinsicOp::VecBroadcastF32x4,
+            (false, true) => IntrinsicOp::VecBroadcastI32x8,
+            (false, false) => IntrinsicOp::VecBroadcastI32x4,
+        };
         preheader.instructions.push(Instruction::Intrinsic {
             dest: Some(scale_vec),
             op: bcast,
@@ -5636,12 +5641,19 @@ fn transform_map_vector(func: &mut IrFunction, pattern: &MapPattern, avx2: bool)
             if debug { eprintln!("[VEC-MAP]   Store not found at transform time"); }
             return changes;
         };
-        let (load_op, mul_op, add_op, store_op) = if avx2 {
-            (IntrinsicOp::VecLoadI32x8, IntrinsicOp::VecMulI32x8,
-             IntrinsicOp::VecAddI32x8, IntrinsicOp::VecStoreI32x8)
-        } else {
-            (IntrinsicOp::VecLoadI32x4, IntrinsicOp::VecMulI32x4,
-             IntrinsicOp::VecAddI32x4, IntrinsicOp::VecStoreI32x4)
+        let (load_op, mul_op, add_op, store_op) = match (elem_is_f32, avx2) {
+            (true, true) => (
+                IntrinsicOp::VecLoadF32x8, IntrinsicOp::VecMulF32x8,
+                IntrinsicOp::VecAddF32x8, IntrinsicOp::VecStoreF32x8),
+            (true, false) => (
+                IntrinsicOp::VecLoadF32x4, IntrinsicOp::VecMulF32x4,
+                IntrinsicOp::VecAddF32x4, IntrinsicOp::VecStoreF32x4),
+            (false, true) => (
+                IntrinsicOp::VecLoadI32x8, IntrinsicOp::VecMulI32x8,
+                IntrinsicOp::VecAddI32x8, IntrinsicOp::VecStoreI32x8),
+            (false, false) => (
+                IntrinsicOp::VecLoadI32x4, IntrinsicOp::VecMulI32x4,
+                IntrinsicOp::VecAddI32x4, IntrinsicOp::VecStoreI32x4),
         };
         let vec_insts = [
             Instruction::Intrinsic {
@@ -5677,7 +5689,7 @@ fn transform_map_vector(func: &mut IrFunction, pattern: &MapPattern, avx2: bool)
     }
 
     // Step 5: Remainder loop for N % 4.
-    changes += insert_map_remainder_loop(func, pattern, vec_width, &mut next_val_id, &mut next_label);
+    changes += insert_map_remainder_loop(func, pattern, vec_width, elem_is_f32, &mut next_val_id, &mut next_label);
 
     func.next_value_id = next_val_id;
     func.next_label = next_label;
@@ -5695,9 +5707,11 @@ fn insert_map_remainder_loop(
     func: &mut IrFunction,
     pattern: &MapPattern,
     vec_width: u64,
+    elem_is_f32: bool,
     next_val_id: &mut u32,
     next_label: &mut u32,
 ) -> usize {
+    let elem_ty = if elem_is_f32 { IrType::F32 } else { IrType::I32 };
     // Extract GEP base pointers for the scalar body.
     let mut src_base = None;
     let mut dst_base = None;
@@ -5823,7 +5837,7 @@ fn insert_map_remainder_loop(
             Instruction::Load {
                 dest: load_v,
                 ptr: gep_src,
-                ty: IrType::I32,
+                ty: elem_ty,
                 seg_override: AddressSpace::Default,
             },
             Instruction::BinOp {
@@ -5831,19 +5845,19 @@ fn insert_map_remainder_loop(
                 op: IrBinOp::Mul,
                 lhs: Operand::Value(load_v),
                 rhs: pattern.scale.clone(),
-                ty: IrType::I32,
+                ty: elem_ty,
             },
             Instruction::BinOp {
                 dest: add_v,
                 op: IrBinOp::Add,
                 lhs: Operand::Value(mul_v),
                 rhs: pattern.offset.clone(),
-                ty: IrType::I32,
+                ty: elem_ty,
             },
             Instruction::Store {
                 val: Operand::Value(add_v),
                 ptr: gep_dst,
-                ty: IrType::I32,
+                ty: elem_ty,
                 seg_override: AddressSpace::Default,
             },
         ],
diff --git a/tests/regression/regalloc_five_int_args.c b/tests/regression/regalloc_five_int_args.c
new file mode 100644
index 0000000..3526194
--- /dev/null
+++ b/tests/regression/regalloc_five_int_args.c
@@ -0,0 +1,9 @@
+
+int add5(int a, int b, int c, int d, int e) {
+    return a + b + c + d + e;
+}
+int main(void) {
+    if (add5(1,2,3,4,5) != 15) return 1;
+    if (add5(-1,-2,-3,-4,-5) != -15) return 2;
+    return 0;
+}
diff --git a/tests/regression/regalloc_multi_ptr_args.c b/tests/regression/regalloc_multi_ptr_args.c
new file mode 100644
index 0000000..355773d
--- /dev/null
+++ b/tests/regression/regalloc_multi_ptr_args.c
@@ -0,0 +1,15 @@
+
+/* Multiple pointer + integer args must not clobber each other via ParamRef homes */
+long long mix(long long *a, long long *b, long long *c, int n, int k) {
+    long long s = 0;
+    for (int i = 0; i < n; i++)
+        s += a[i] * b[i] + c[i] * k;
+    return s;
+}
+int main(void) {
+    long long a[4] = {1,2,3,4}, b[4] = {5,6,7,8}, c[4] = {9,10,11,12};
+    long long s = mix(a, b, c, 4, 3);
+    long long expect = 0;
+    for (int i = 0; i < 4; i++) expect += a[i]*b[i] + c[i]*3;
+    return s == expect ? 0 : 1;
+}
diff --git a/tests/regression/vectorize_map_alias_globals.c b/tests/regression/vectorize_map_alias_globals.c
new file mode 100644
index 0000000..cb9f0d8
--- /dev/null
+++ b/tests/regression/vectorize_map_alias_globals.c
@@ -0,0 +1,14 @@
+
+static int Gsrc[16];
+static int Gdst[16];
+void scale_global(int scale, int offset, int n) {
+    for (int i = 0; i < n; i++)
+        Gdst[i] = Gsrc[i] * scale + offset;
+}
+int main(void) {
+    for (int i = 0; i < 16; i++) Gsrc[i] = i;
+    scale_global(4, 1, 16);
+    for (int i = 0; i < 16; i++)
+        if (Gdst[i] != i * 4 + 1) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n11.c b/tests/regression/vectorize_map_f32_n11.c
new file mode 100644
index 0000000..daa88df
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n11.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[11], dst[11];
+    for (int i = 0; i < 11; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 11);
+    for (int i = 0; i < 11; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n13.c b/tests/regression/vectorize_map_f32_n13.c
new file mode 100644
index 0000000..6a7afbd
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n13.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[13], dst[13];
+    for (int i = 0; i < 13; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 13);
+    for (int i = 0; i < 13; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n15.c b/tests/regression/vectorize_map_f32_n15.c
new file mode 100644
index 0000000..6bd0a1b
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n15.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[15], dst[15];
+    for (int i = 0; i < 15; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 15);
+    for (int i = 0; i < 15; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n17.c b/tests/regression/vectorize_map_f32_n17.c
new file mode 100644
index 0000000..31f0474
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n17.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[17], dst[17];
+    for (int i = 0; i < 17; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 17);
+    for (int i = 0; i < 17; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n19.c b/tests/regression/vectorize_map_f32_n19.c
new file mode 100644
index 0000000..0d11bc3
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n19.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[19], dst[19];
+    for (int i = 0; i < 19; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 19);
+    for (int i = 0; i < 19; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n23.c b/tests/regression/vectorize_map_f32_n23.c
new file mode 100644
index 0000000..fd789eb
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n23.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[23], dst[23];
+    for (int i = 0; i < 23; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 23);
+    for (int i = 0; i < 23; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n25.c b/tests/regression/vectorize_map_f32_n25.c
new file mode 100644
index 0000000..6c01168
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n25.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[25], dst[25];
+    for (int i = 0; i < 25; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 25);
+    for (int i = 0; i < 25; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n27.c b/tests/regression/vectorize_map_f32_n27.c
new file mode 100644
index 0000000..30336f4
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n27.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[27], dst[27];
+    for (int i = 0; i < 27; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 27);
+    for (int i = 0; i < 27; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n29.c b/tests/regression/vectorize_map_f32_n29.c
new file mode 100644
index 0000000..17da7fe
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n29.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[29], dst[29];
+    for (int i = 0; i < 29; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 29);
+    for (int i = 0; i < 29; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n3.c b/tests/regression/vectorize_map_f32_n3.c
new file mode 100644
index 0000000..af0cb0e
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n3.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[3], dst[3];
+    for (int i = 0; i < 3; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 3);
+    for (int i = 0; i < 3; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n32.c b/tests/regression/vectorize_map_f32_n32.c
new file mode 100644
index 0000000..05ccf3c
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n32.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[32], dst[32];
+    for (int i = 0; i < 32; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 32);
+    for (int i = 0; i < 32; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n5.c b/tests/regression/vectorize_map_f32_n5.c
new file mode 100644
index 0000000..113c237
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n5.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[5], dst[5];
+    for (int i = 0; i < 5; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 5);
+    for (int i = 0; i < 5; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n7.c b/tests/regression/vectorize_map_f32_n7.c
new file mode 100644
index 0000000..f70db06
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n7.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[7], dst[7];
+    for (int i = 0; i < 7; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 7);
+    for (int i = 0; i < 7; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_n9.c b/tests/regression/vectorize_map_f32_n9.c
new file mode 100644
index 0000000..3d35db4
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_n9.c
@@ -0,0 +1,14 @@
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[9], dst[9];
+    for (int i = 0; i < 9; i++) src[i] = (float)(i - 2);
+    scale_add_f32(dst, src, 1.25f, -0.25f, 9);
+    for (int i = 0; i < 9; i++) {
+        float e = (float)(i - 2) * 1.25f - 0.25f;
+        float d = dst[i] - e;
+        if (d < -0.05f || d > 0.05f) return 1 + i;
+    }
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_f32_scale_add.c b/tests/regression/vectorize_map_f32_scale_add.c
new file mode 100644
index 0000000..0fd9c24
--- /dev/null
+++ b/tests/regression/vectorize_map_f32_scale_add.c
@@ -0,0 +1,24 @@
+/* F32 map: dst[i] = src[i] * scale + offset (AVX2 8-wide + remainder) */
+void scale_add_f32(float *restrict dst, const float *restrict src, float scale, float offset, int n) {
+    for (int i = 0; i < n; i++)
+        dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    float src[17], dst[17];
+    int i;
+    for (i = 0; i < 17; i++) src[i] = (float)(i - 3);
+    scale_add_f32(dst, src, 5.0f, 7.0f, 17);
+    for (i = 0; i < 17; i++) {
+        float expect = (float)(i - 3) * 5.0f + 7.0f;
+        float d = dst[i] - expect;
+        if (d < -0.01f || d > 0.01f) return 1 + i;
+    }
+    /* small n (remainder-only path) */
+    {
+        float s0 = -3.0f, d0 = 0.0f;
+        scale_add_f32(&d0, &s0, 2.0f, 3.0f, 1);
+        if (d0 < -3.01f || d0 > -2.99f) return 100;
+    }
+    scale_add_f32(dst, src, 1.0f, 0.0f, 0);
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_large.c b/tests/regression/vectorize_map_i32_large.c
new file mode 100644
index 0000000..24b9e04
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_large.c
@@ -0,0 +1,14 @@
+
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++)
+        dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    enum { N = 64 };
+    int src[N], dst[N];
+    for (int i = 0; i < N; i++) src[i] = i * 3 - 10;
+    scale_add(dst, src, 7, -2, N);
+    for (int i = 0; i < N; i++)
+        if (dst[i] != src[i] * 7 - 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n1.c b/tests/regression/vectorize_map_i32_n1.c
new file mode 100644
index 0000000..ee464c9
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n1.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[1], dst[1];
+    for (int i = 0; i < 1; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 1);
+    for (int i = 0; i < 1; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n10.c b/tests/regression/vectorize_map_i32_n10.c
new file mode 100644
index 0000000..abcffb2
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n10.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[10], dst[10];
+    for (int i = 0; i < 10; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 10);
+    for (int i = 0; i < 10; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n11.c b/tests/regression/vectorize_map_i32_n11.c
new file mode 100644
index 0000000..daf918e
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n11.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[11], dst[11];
+    for (int i = 0; i < 11; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 11);
+    for (int i = 0; i < 11; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n12.c b/tests/regression/vectorize_map_i32_n12.c
new file mode 100644
index 0000000..67c788b
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n12.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[12], dst[12];
+    for (int i = 0; i < 12; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 12);
+    for (int i = 0; i < 12; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n128.c b/tests/regression/vectorize_map_i32_n128.c
new file mode 100644
index 0000000..6561006
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n128.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[128], dst[128];
+    for (int i = 0; i < 128; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 128);
+    for (int i = 0; i < 128; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n13.c b/tests/regression/vectorize_map_i32_n13.c
new file mode 100644
index 0000000..3dcf685
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n13.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[13], dst[13];
+    for (int i = 0; i < 13; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 13);
+    for (int i = 0; i < 13; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n14.c b/tests/regression/vectorize_map_i32_n14.c
new file mode 100644
index 0000000..d0bf0b6
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n14.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[14], dst[14];
+    for (int i = 0; i < 14; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 14);
+    for (int i = 0; i < 14; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n15.c b/tests/regression/vectorize_map_i32_n15.c
new file mode 100644
index 0000000..d3e5623
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n15.c
@@ -0,0 +1,13 @@
+
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++)
+        dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[15], dst[15];
+    for (int i = 0; i < 15; i++) src[i] = i * i - 3;
+    scale_add(dst, src, 11, -5, 15);
+    for (int i = 0; i < 15; i++)
+        if (dst[i] != src[i] * 11 - 5) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n16.c b/tests/regression/vectorize_map_i32_n16.c
new file mode 100644
index 0000000..973c946
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n16.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[16], dst[16];
+    for (int i = 0; i < 16; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 16);
+    for (int i = 0; i < 16; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n17.c b/tests/regression/vectorize_map_i32_n17.c
new file mode 100644
index 0000000..ecba07f
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n17.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[17], dst[17];
+    for (int i = 0; i < 17; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 17);
+    for (int i = 0; i < 17; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n18.c b/tests/regression/vectorize_map_i32_n18.c
new file mode 100644
index 0000000..4a30fe1
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n18.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[18], dst[18];
+    for (int i = 0; i < 18; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 18);
+    for (int i = 0; i < 18; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n19.c b/tests/regression/vectorize_map_i32_n19.c
new file mode 100644
index 0000000..854981b
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n19.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[19], dst[19];
+    for (int i = 0; i < 19; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 19);
+    for (int i = 0; i < 19; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n2.c b/tests/regression/vectorize_map_i32_n2.c
new file mode 100644
index 0000000..1290a5f
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n2.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[2], dst[2];
+    for (int i = 0; i < 2; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 2);
+    for (int i = 0; i < 2; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n20.c b/tests/regression/vectorize_map_i32_n20.c
new file mode 100644
index 0000000..2a3e5c9
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n20.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[20], dst[20];
+    for (int i = 0; i < 20; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 20);
+    for (int i = 0; i < 20; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n21.c b/tests/regression/vectorize_map_i32_n21.c
new file mode 100644
index 0000000..d98e6a7
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n21.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[21], dst[21];
+    for (int i = 0; i < 21; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 21);
+    for (int i = 0; i < 21; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n22.c b/tests/regression/vectorize_map_i32_n22.c
new file mode 100644
index 0000000..4e7bfcf
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n22.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[22], dst[22];
+    for (int i = 0; i < 22; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 22);
+    for (int i = 0; i < 22; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n23.c b/tests/regression/vectorize_map_i32_n23.c
new file mode 100644
index 0000000..5a4d78b
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n23.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[23], dst[23];
+    for (int i = 0; i < 23; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 23);
+    for (int i = 0; i < 23; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n24.c b/tests/regression/vectorize_map_i32_n24.c
new file mode 100644
index 0000000..9767a29
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n24.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[24], dst[24];
+    for (int i = 0; i < 24; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 24);
+    for (int i = 0; i < 24; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n25.c b/tests/regression/vectorize_map_i32_n25.c
new file mode 100644
index 0000000..53e7c22
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n25.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[25], dst[25];
+    for (int i = 0; i < 25; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 25);
+    for (int i = 0; i < 25; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n26.c b/tests/regression/vectorize_map_i32_n26.c
new file mode 100644
index 0000000..fac1435
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n26.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[26], dst[26];
+    for (int i = 0; i < 26; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 26);
+    for (int i = 0; i < 26; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n27.c b/tests/regression/vectorize_map_i32_n27.c
new file mode 100644
index 0000000..5f47a21
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n27.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[27], dst[27];
+    for (int i = 0; i < 27; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 27);
+    for (int i = 0; i < 27; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n28.c b/tests/regression/vectorize_map_i32_n28.c
new file mode 100644
index 0000000..cf7fc8d
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n28.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[28], dst[28];
+    for (int i = 0; i < 28; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 28);
+    for (int i = 0; i < 28; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n29.c b/tests/regression/vectorize_map_i32_n29.c
new file mode 100644
index 0000000..ce0032b
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n29.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[29], dst[29];
+    for (int i = 0; i < 29; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 29);
+    for (int i = 0; i < 29; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n3.c b/tests/regression/vectorize_map_i32_n3.c
new file mode 100644
index 0000000..8329bdc
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n3.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[3], dst[3];
+    for (int i = 0; i < 3; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 3);
+    for (int i = 0; i < 3; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n31.c b/tests/regression/vectorize_map_i32_n31.c
new file mode 100644
index 0000000..85ac38f
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n31.c
@@ -0,0 +1,13 @@
+
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++)
+        dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[31], dst[31];
+    for (int i = 0; i < 31; i++) src[i] = i * i - 3;
+    scale_add(dst, src, 11, -5, 31);
+    for (int i = 0; i < 31; i++)
+        if (dst[i] != src[i] * 11 - 5) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n33.c b/tests/regression/vectorize_map_i32_n33.c
new file mode 100644
index 0000000..ebaf54b
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n33.c
@@ -0,0 +1,13 @@
+
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++)
+        dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[33], dst[33];
+    for (int i = 0; i < 33; i++) src[i] = i * i - 3;
+    scale_add(dst, src, 11, -5, 33);
+    for (int i = 0; i < 33; i++)
+        if (dst[i] != src[i] * 11 - 5) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n36.c b/tests/regression/vectorize_map_i32_n36.c
new file mode 100644
index 0000000..7ca4d1d
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n36.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[36], dst[36];
+    for (int i = 0; i < 36; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 36);
+    for (int i = 0; i < 36; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n4.c b/tests/regression/vectorize_map_i32_n4.c
new file mode 100644
index 0000000..981162b
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n4.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[4], dst[4];
+    for (int i = 0; i < 4; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 4);
+    for (int i = 0; i < 4; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n40.c b/tests/regression/vectorize_map_i32_n40.c
new file mode 100644
index 0000000..24c1c86
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n40.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[40], dst[40];
+    for (int i = 0; i < 40; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 40);
+    for (int i = 0; i < 40; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n44.c b/tests/regression/vectorize_map_i32_n44.c
new file mode 100644
index 0000000..d0e01c8
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n44.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[44], dst[44];
+    for (int i = 0; i < 44; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 44);
+    for (int i = 0; i < 44; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n48.c b/tests/regression/vectorize_map_i32_n48.c
new file mode 100644
index 0000000..e234010
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n48.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[48], dst[48];
+    for (int i = 0; i < 48; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 48);
+    for (int i = 0; i < 48; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n5.c b/tests/regression/vectorize_map_i32_n5.c
new file mode 100644
index 0000000..3d1b9ac
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n5.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[5], dst[5];
+    for (int i = 0; i < 5; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 5);
+    for (int i = 0; i < 5; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n52.c b/tests/regression/vectorize_map_i32_n52.c
new file mode 100644
index 0000000..5d22ea0
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n52.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[52], dst[52];
+    for (int i = 0; i < 52; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 52);
+    for (int i = 0; i < 52; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n56.c b/tests/regression/vectorize_map_i32_n56.c
new file mode 100644
index 0000000..ecd6524
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n56.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[56], dst[56];
+    for (int i = 0; i < 56; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 56);
+    for (int i = 0; i < 56; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n6.c b/tests/regression/vectorize_map_i32_n6.c
new file mode 100644
index 0000000..11ef371
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n6.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[6], dst[6];
+    for (int i = 0; i < 6; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 6);
+    for (int i = 0; i < 6; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n60.c b/tests/regression/vectorize_map_i32_n60.c
new file mode 100644
index 0000000..c4e6d57
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n60.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[60], dst[60];
+    for (int i = 0; i < 60; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 60);
+    for (int i = 0; i < 60; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n63.c b/tests/regression/vectorize_map_i32_n63.c
new file mode 100644
index 0000000..e7fc933
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n63.c
@@ -0,0 +1,13 @@
+
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++)
+        dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[63], dst[63];
+    for (int i = 0; i < 63; i++) src[i] = i * i - 3;
+    scale_add(dst, src, 11, -5, 63);
+    for (int i = 0; i < 63; i++)
+        if (dst[i] != src[i] * 11 - 5) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n64.c b/tests/regression/vectorize_map_i32_n64.c
new file mode 100644
index 0000000..01d792e
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n64.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[64], dst[64];
+    for (int i = 0; i < 64; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 64);
+    for (int i = 0; i < 64; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n7.c b/tests/regression/vectorize_map_i32_n7.c
new file mode 100644
index 0000000..e36608a
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n7.c
@@ -0,0 +1,13 @@
+
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++)
+        dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[7], dst[7];
+    for (int i = 0; i < 7; i++) src[i] = i * i - 3;
+    scale_add(dst, src, 11, -5, 7);
+    for (int i = 0; i < 7; i++)
+        if (dst[i] != src[i] * 11 - 5) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n72.c b/tests/regression/vectorize_map_i32_n72.c
new file mode 100644
index 0000000..70bb25a
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n72.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[72], dst[72];
+    for (int i = 0; i < 72; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 72);
+    for (int i = 0; i < 72; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n8.c b/tests/regression/vectorize_map_i32_n8.c
new file mode 100644
index 0000000..e8a1818
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n8.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[8], dst[8];
+    for (int i = 0; i < 8; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 8);
+    for (int i = 0; i < 8; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n80.c b/tests/regression/vectorize_map_i32_n80.c
new file mode 100644
index 0000000..59cb557
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n80.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[80], dst[80];
+    for (int i = 0; i < 80; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 80);
+    for (int i = 0; i < 80; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n9.c b/tests/regression/vectorize_map_i32_n9.c
new file mode 100644
index 0000000..7ba214c
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n9.c
@@ -0,0 +1,13 @@
+
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++)
+        dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[9], dst[9];
+    for (int i = 0; i < 9; i++) src[i] = i * i - 3;
+    scale_add(dst, src, 11, -5, 9);
+    for (int i = 0; i < 9; i++)
+        if (dst[i] != src[i] * 11 - 5) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_n96.c b/tests/regression/vectorize_map_i32_n96.c
new file mode 100644
index 0000000..f973e83
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_n96.c
@@ -0,0 +1,11 @@
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++) dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[96], dst[96];
+    for (int i = 0; i < 96; i++) src[i] = i * 3 - 1;
+    scale_add(dst, src, 5, 2, 96);
+    for (int i = 0; i < 96; i++)
+        if (dst[i] != src[i] * 5 + 2) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_map_i32_negative.c b/tests/regression/vectorize_map_i32_negative.c
new file mode 100644
index 0000000..8183194
--- /dev/null
+++ b/tests/regression/vectorize_map_i32_negative.c
@@ -0,0 +1,13 @@
+
+void scale_add(int *restrict dst, const int *restrict src, int scale, int offset, int n) {
+    for (int i = 0; i < n; i++)
+        dst[i] = src[i] * scale + offset;
+}
+int main(void) {
+    int src[8] = {-1000000, -1, 0, 1, 2, 100, -50, 999};
+    int dst[8];
+    scale_add(dst, src, -3, 10, 8);
+    for (int i = 0; i < 8; i++)
+        if (dst[i] != src[i] * -3 + 10) return 1 + i;
+    return 0;
+}
diff --git a/tests/regression/vectorize_sum_f32.c b/tests/regression/vectorize_sum_f32.c
new file mode 100644
index 0000000..bfe798e
--- /dev/null
+++ b/tests/regression/vectorize_sum_f32.c
@@ -0,0 +1,15 @@
+
+float sum_f32(const float *restrict a, int n) {
+    float s = 0.0f;
+    for (int i = 0; i < n; i++) s += a[i];
+    return s;
+}
+int main(void) {
+    float a[16];
+    float expect = 0.0f;
+    for (int i = 0; i < 16; i++) { a[i] = (float)(i + 1); expect += a[i]; }
+    float s = sum_f32(a, 16);
+    if (s < expect - 0.1f || s > expect + 0.1f) return 1;
+    if (sum_f32(a, 0) != 0.0f) return 2;
+    return 0;
+}
diff --git a/tests/regression/vectorize_sum_i32_large.c b/tests/regression/vectorize_sum_i32_large.c
new file mode 100644
index 0000000..941efc8
--- /dev/null
+++ b/tests/regression/vectorize_sum_i32_large.c
@@ -0,0 +1,16 @@
+
+int sum_i32(const int *restrict a, int n) {
+    int s = 0;
+    for (int i = 0; i < n; i++) s += a[i];
+    return s;
+}
+int main(void) {
+    enum { N = 128 };
+    int a[N];
+    int expect = 0;
+    for (int i = 0; i < N; i++) { a[i] = i - 64; expect += a[i]; }
+    if (sum_i32(a, N) != expect) return 1;
+    if (sum_i32(a, 1) != a[0]) return 2;
+    if (sum_i32(a, 0) != 0) return 3;
+    return 0;
+}
diff --git a/tests/regression/vectorize_sum_i64_n16.c b/tests/regression/vectorize_sum_i64_n16.c
new file mode 100644
index 0000000..5edec9f
--- /dev/null
+++ b/tests/regression/vectorize_sum_i64_n16.c
@@ -0,0 +1,13 @@
+
+long long sum_i64(const long long *restrict a, int n) {
+    long long s = 0;
+    for (int i = 0; i < n; i++) s += a[i];
+    return s;
+}
+int main(void) {
+    long long a[16];
+    long long expect = 0;
+    for (int i = 0; i < 16; i++) { a[i] = i * 1000000007LL - 3; expect += a[i]; }
+    if (sum_i64(a, 16) != expect) return 1;
+    return 0;
+}
diff --git a/tests/regression/vectorize_sum_i64_n24.c b/tests/regression/vectorize_sum_i64_n24.c
new file mode 100644
index 0000000..65668dc
--- /dev/null
+++ b/tests/regression/vectorize_sum_i64_n24.c
@@ -0,0 +1,13 @@
+
+long long sum_i64(const long long *restrict a, int n) {
+    long long s = 0;
+    for (int i = 0; i < n; i++) s += a[i];
+    return s;
+}
+int main(void) {
+    long long a[24];
+    long long expect = 0;
+    for (int i = 0; i < 24; i++) { a[i] = i * 1000000007LL - 3; expect += a[i]; }
+    if (sum_i64(a, 24) != expect) return 1;
+    return 0;
+}
diff --git a/tests/regression/vectorize_sum_i64_n32.c b/tests/regression/vectorize_sum_i64_n32.c
new file mode 100644
index 0000000..958a884
--- /dev/null
+++ b/tests/regression/vectorize_sum_i64_n32.c
@@ -0,0 +1,13 @@
+
+long long sum_i64(const long long *restrict a, int n) {
+    long long s = 0;
+    for (int i = 0; i < n; i++) s += a[i];
+    return s;
+}
+int main(void) {
+    long long a[32];
+    long long expect = 0;
+    for (int i = 0; i < 32; i++) { a[i] = i * 1000000007LL - 3; expect += a[i]; }
+    if (sum_i64(a, 32) != expect) return 1;
+    return 0;
+}
diff --git a/tests/regression/vectorize_sum_i64_n8.c b/tests/regression/vectorize_sum_i64_n8.c
new file mode 100644
index 0000000..6b84d3d
--- /dev/null
+++ b/tests/regression/vectorize_sum_i64_n8.c
@@ -0,0 +1,13 @@
+
+long long sum_i64(const long long *restrict a, int n) {
+    long long s = 0;
+    for (int i = 0; i < n; i++) s += a[i];
+    return s;
+}
+int main(void) {
+    long long a[8];
+    long long expect = 0;
+    for (int i = 0; i < 8; i++) { a[i] = i * 1000000007LL - 3; expect += a[i]; }
+    if (sum_i64(a, 8) != expect) return 1;
+    return 0;
+}
