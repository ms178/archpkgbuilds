diff --git a/src/backend/elf/numeric_labels.rs b/src/backend/elf/numeric_labels.rs
index 577ed269..6aba6115 100644
--- a/src/backend/elf/numeric_labels.rs
+++ b/src/backend/elf/numeric_labels.rs
@@ -163,6 +163,7 @@ fn resolve_numeric_operand(
                     scale: mem.scale,
                     mask: mem.mask.clone(),
                     zeroing: mem.zeroing,
+                    z_first: mem.z_first,
                     broadcast: mem.broadcast,
                 })
             } else {
diff --git a/src/backend/i686/assembler/encoder/core.rs b/src/backend/i686/assembler/encoder/core.rs
index 48ce6a41..2a3fd350 100644
--- a/src/backend/i686/assembler/encoder/core.rs
+++ b/src/backend/i686/assembler/encoder/core.rs
@@ -1003,8 +1003,7 @@ mod i686_encoding_helper_tests {
             name: name.to_owned(),
             mask: None,
             zeroing: false,
-            sae: false,
-            rounding: None,
+            z_first: false,
             broadcast: None,
         }
     }
@@ -1024,6 +1023,7 @@ mod i686_encoding_helper_tests {
             scale,
             mask: None,
             zeroing: false,
+            z_first: false,
             broadcast: None,
         }
     }
diff --git a/src/backend/i686/assembler/encoder/gp_integer.rs b/src/backend/i686/assembler/encoder/gp_integer.rs
index c5c224e0..950f6526 100644
--- a/src/backend/i686/assembler/encoder/gp_integer.rs
+++ b/src/backend/i686/assembler/encoder/gp_integer.rs
@@ -1175,6 +1175,7 @@ impl super::InstructionEncoder {
                     scale: None,
                     mask: None,
                     zeroing: false,
+                    z_first: false,
                     broadcast: None,
                 };
                 self.encode_test(
diff --git a/src/backend/i686/assembler/encoder/mod.rs b/src/backend/i686/assembler/encoder/mod.rs
index ca24cf35..e0495059 100644
--- a/src/backend/i686/assembler/encoder/mod.rs
+++ b/src/backend/i686/assembler/encoder/mod.rs
@@ -2036,8 +2036,7 @@ mod stack_width_tests {
             name: name.to_owned(),
             mask: None,
             zeroing: false,
-            sae: false,
-            rounding: None,
+            z_first: false,
             broadcast: None,
         })
     }
diff --git a/src/backend/x86/assembler/encoder/avx.rs b/src/backend/x86/assembler/encoder/avx.rs
index f4b5f971..fa01bdb7 100644
--- a/src/backend/x86/assembler/encoder/avx.rs
+++ b/src/backend/x86/assembler/encoder/avx.rs
@@ -565,36 +565,6 @@ impl super::InstructionEncoder {
         }
     }
 
-    /// Validate a *suffixed* `{sae}`/`{r*-sae}` (on a vector register)
-    /// against the emitted instruction's SAE class. Returns the
-    /// offending token (`{sae}` / `{rn-sae}` / ...) when GAS would say
-    /// `unknown vector operation`, `None` when absent or legitimate.
-    /// (`sae` = bare suffix present; `rounding` = `{r*-sae}` rc if any.)
-    pub(crate) fn check_evex_sae_token(
-        map: u8,
-        opcode: u8,
-        sae: bool,
-        rounding: Option<u8>,
-    ) -> Option<String> {
-        if !sae && rounding.is_none() {
-            return None;
-        }
-        let tok = match rounding {
-            None => "{sae}".to_string(),
-            Some(0) => "{rn-sae}".to_string(),
-            Some(1) => "{rd-sae}".to_string(),
-            Some(2) => "{ru-sae}".to_string(),
-            Some(3) => "{rz-sae}".to_string(),
-            Some(rc) => format!("{{r{rc}-sae}}"),
-        };
-        let bad = match Self::evex_sae_class(map, opcode) {
-            EvexSae::None => true,
-            EvexSae::Er => rounding.is_none(),
-            EvexSae::Sae => rounding.is_some(),
-        };
-        bad.then_some(tok)
-    }
-
     /// EVEX vector length from operands: 00=128(xmm), 01=256(ymm), 10=512(zmm).
     /// EVEX LL from the WIDEST vector register in the operand list. Max, not
     /// first-found: `vinserti32x8 $imm, %ymm28, %zmm29, %zmm30` carries its
@@ -626,15 +596,6 @@ impl super::InstructionEncoder {
                     return (&ops[1..], Some(tok));
                 }
             }
-            Some(Operand::Register(r))
-                if (r.sae || r.rounding.is_some())
-                    && !is_xmm(&r.name)
-                    && !is_ymm(&r.name)
-                    && !is_zmm(&r.name)
-                    && !is_kreg(&r.name) =>
-            {
-                return (&ops[1..], Some(r.rounding));
-            }
             _ => {}
         }
         (ops, None)
@@ -659,6 +620,39 @@ impl super::InstructionEncoder {
         }
     }
 
+    /// EVEX scalar (Tuple1) disp8-N rule (GAS 2.47, measured byte-exact).
+    /// Scalar single/double (ss/sd) address memory with N = element size
+    /// (4/8), NOT N = VL: `vaddss 508(%rdx),...` takes disp8 = 127 (N=4)
+    /// where the packed path would emit disp32. (Scalar leaves EVEX.b
+    /// alone: b stays 0 except on the SAE/ER forms that set it through
+    /// `apply_evex_sae` — the V' bit beside it is what reads as 1.)
+    /// Detection is purely (map, pp, W, opcode) — no mnemonic needed:
+    /// - map 1 (0F) + pp F3/F2 (2/3) + scalar opcodes. No PACKED caller
+    ///   passes pp >= 2 in map 1 (F3/F2 is scalar-only space there; the
+    ///   VNNI pp-tricks live in map 2).
+    /// - map 2 (0F38) + pp 66 (1) + the odd scalar-FMA opcodes (packed
+    ///   FMA uses the even opcodes; the odd vfmsubadd 97/A7/B7 stay
+    ///   packed — hence the explicit set, never a range).
+    /// Returns `Some(N)` for scalar, `None` for packed.
+    pub(crate) fn evex_scalar_tuple_n(map: u8, pp: u8, w: u8, opcode: u8) -> Option<u32> {
+        if map == 1 && (pp == 2 || pp == 3) {
+            return match opcode {
+                0x51 | 0x58 | 0x59 | 0x5A | 0x5C | 0x5D | 0x5E | 0x5F | 0xC2 => {
+                    Some(if pp == 2 { 4 } else { 8 })
+                }
+                _ => None,
+            };
+        }
+        if map == 2 && pp == 1 {
+            return match opcode {
+                0x99 | 0x9B | 0x9D | 0x9F | 0xA9 | 0xAB | 0xAD | 0xAF | 0xB9 | 0xBB
+                | 0xBD | 0xBF => Some(if w == 0 { 4 } else { 8 }),
+                _ => None,
+            };
+        }
+        None
+    }
+
     /// EVEX 2-source binary op, AT&T (src, vvvv, dst) with optional mask on dst.
     /// Encodings verified byte-for-byte against GNU as 2.42 (see LCCC_ENGINEERING
     /// PLAN_PART3 WP-A; reference bytes captured from `as` on 2026-08-12).
@@ -679,6 +673,11 @@ impl super::InstructionEncoder {
         let vl_ll = Self::evex_ll(ops);
         let (sae_bcst, ll) = Self::apply_evex_sae(sae, Self::evex_sae_class(map, opcode), vl_ll)?;
         let mut bcst = sae_bcst;
+        // Scalar Tuple1 disp8-N is the element size. Broadcast on scalar
+        // memory is impossible (the post-encode check rejects it with the
+        // GAS text); the tuple is still well-defined, so encoding
+        // continues to the N below.
+        let scalar_n = Self::evex_scalar_tuple_n(map, pp, w, opcode);
         let (aaa, z) = Self::evex_mask_info(&ops[2]);
         match (&ops[0], &ops[1], &ops[2]) {
             (Operand::Register(src), Operand::Register(vvvv), Operand::Register(dst)) => {
@@ -699,7 +698,10 @@ impl super::InstructionEncoder {
                 Ok(())
             }
             (Operand::Memory(mem), Operand::Register(vvvv), Operand::Register(dst)) => {
-                let (mem_bcst, scale_n) = Self::evex_mem_scale(mem, vl_ll, 1);
+                let (mem_bcst, scale_n) = match scalar_n {
+                    Some(n) => (false, n),
+                    None => Self::evex_mem_scale(mem, vl_ll, 1),
+                };
                 bcst |= mem_bcst;
                 let dst_num = self.emit_evex_memop(
                     &dst.name,
@@ -1335,6 +1337,8 @@ impl super::InstructionEncoder {
         let vl_ll = Self::evex_ll(rest);
         let (sae_bcst, ll) = Self::apply_evex_sae(sae, Self::evex_sae_class(map, opcode), vl_ll)?;
         let mut bcst = sae_bcst;
+        // Scalar Tuple1 disp8-N is the element size (compare included).
+        let scalar_n = Self::evex_scalar_tuple_n(map, pp, w, opcode);
         match (head, &rest[0], &rest[1], &rest[2]) {
             (
                 Operand::Immediate(ImmediateValue::Integer(imm)),
@@ -1372,7 +1376,10 @@ impl super::InstructionEncoder {
                 Operand::Register(src1),
                 Operand::Register(kdst),
             ) => {
-                let (mem_bcst, scale_n) = Self::evex_mem_scale(mem, vl_ll, 1);
+                let (mem_bcst, scale_n) = match scalar_n {
+                    Some(n) => (false, n),
+                    None => Self::evex_mem_scale(mem, vl_ll, 1),
+                };
                 bcst |= mem_bcst;
                 let (aaa, z) = Self::evex_mask_info(&rest[2]);
                 if z {
diff --git a/src/backend/x86/assembler/encoder/gp_integer.rs b/src/backend/x86/assembler/encoder/gp_integer.rs
index b5b4c2a0..317d5cb1 100644
--- a/src/backend/x86/assembler/encoder/gp_integer.rs
+++ b/src/backend/x86/assembler/encoder/gp_integer.rs
@@ -66,6 +66,7 @@ impl super::InstructionEncoder {
                     scale: None,
                     mask: None,
                     zeroing: false,
+                    z_first: false,
                     broadcast: None,
                 };
                 self.encode_mov_mem_reg(&mem, dst, size)
@@ -80,6 +81,7 @@ impl super::InstructionEncoder {
                     scale: None,
                     mask: None,
                     zeroing: false,
+                    z_first: false,
                     broadcast: None,
                 };
                 self.encode_mov_reg_mem(src, &mem, size)
@@ -535,6 +537,7 @@ impl super::InstructionEncoder {
                     scale: None,
                     mask: None,
                     zeroing: false,
+                    z_first: false,
                     broadcast: None,
                 };
                 let dst_num = reg_num(&dst.name).ok_or("bad dst register")?;
@@ -831,6 +834,7 @@ impl super::InstructionEncoder {
                     scale: None,
                     mask: None,
                     zeroing: false,
+                    z_first: false,
                     broadcast: None,
                 };
                 if size == 2 {
@@ -861,6 +865,7 @@ impl super::InstructionEncoder {
                     scale: None,
                     mask: None,
                     zeroing: false,
+                    z_first: false,
                     broadcast: None,
                 };
                 if size == 2 {
diff --git a/src/backend/x86/assembler/encoder/mod.rs b/src/backend/x86/assembler/encoder/mod.rs
index c72a3721..982ed355 100644
--- a/src/backend/x86/assembler/encoder/mod.rs
+++ b/src/backend/x86/assembler/encoder/mod.rs
@@ -329,9 +329,7 @@ fn unwrap_indirect(op: &Operand) -> &Operand {
 fn op_decorated(op: &Operand) -> bool {
     let op = unwrap_indirect(op);
     match op {
-        Operand::Register(r) => {
-            r.mask.is_some() || r.zeroing || r.broadcast.is_some() || r.sae || r.rounding.is_some()
-        }
+        Operand::Register(r) => r.mask.is_some() || r.zeroing || r.broadcast.is_some(),
         Operand::Memory(m) => m.mask.is_some() || m.zeroing || m.broadcast.is_some(),
         Operand::Label(s) => registers::is_evex_sae_token(s),
         _ => false,
@@ -355,6 +353,7 @@ fn normalize_bare_label(op: &mut Operand) {
             scale: None,
             mask: None,
             zeroing: false,
+            z_first: false,
             broadcast: None,
         });
     }
@@ -746,35 +745,39 @@ impl InstructionEncoder {
             }
             return Ok(());
         }
-        // EVEX selected. First the suffixed-`{sae}` kind check (a
-        // decorator-value error, so it precedes placement): the class
-        // comes from the emitted (map, opcode), so no mnemonic table.
-        // (If the body FAILED (arity/shape), that error wins instead;
-        // GAS reports `unknown vector operation' even then, but the
-        // class is unknowable without emitted bytes. Verdict-identical.)
-        if self.bytes.len() >= pi + 5 {
-            let map = self.bytes[pi + 1] & 7;
-            let opcode = self.bytes[pi + 4];
-            for op in ops {
-                if let Operand::Register(r) = unwrap_indirect(op) {
-                    if (is_xmm(&r.name) || is_ymm(&r.name) || is_zmm(&r.name))
-                        && let Some(tok) =
-                            Self::check_evex_sae_token(map, opcode, r.sae, r.rounding)
-                    {
-                        return Err(format!("unknown vector operation: `{tok}'"));
-                    }
-                }
-            }
-        }
+        // EVEX selected. (Suffixed `{sae}` never reaches here: the
+        // parser rejects it with `unknown vector operation` on every
+        // operand kind.)
         // Broadcast on a register is never valid, and a write mask
         // anywhere but the destination (last operand) is misplaced.
-        // Memory broadcast the encoder ignored (`vmovaps` has no
-        // broadcast form) and per-instruction N validity are each EVEX
+        // Scalar-Tuple1 memory broadcast is tuple-impossible (no scalar
+        // insn has a {1toN} form), so it is rejected here, centrally,
+        // from the emitted (map, pp, W, opcode): every present and
+        // future scalar caller is covered with no per-arm chaining.
+        // Other ignored memory broadcast (`vmovaps` has no broadcast
+        // form) and per-instruction N validity stay each EVEX
         // encoder's job (`evex_forbid_*` audit follow-up).
+        let scalar_tuple = self.bytes.len() >= pi + 5
+            && Self::evex_scalar_tuple_n(
+                self.bytes[pi + 1] & 7,
+                self.bytes[pi + 2] & 3,
+                (self.bytes[pi + 2] >> 7) & 1,
+                self.bytes[pi + 4],
+            )
+            .is_some();
         for op in ops {
             let (_, _, bcst, _) = op_decor(op);
-            if bcst && matches!(unwrap_indirect(op), Operand::Register(_)) {
-                return Err(format!("unsupported broadcast for `{mnemonic}'"));
+            if !bcst {
+                continue;
+            }
+            match unwrap_indirect(op) {
+                Operand::Register(_) => {
+                    return Err(format!("unsupported broadcast for `{mnemonic}'"));
+                }
+                Operand::Memory(_) if scalar_tuple => {
+                    return Err(format!("unsupported broadcast for `{stem}'"));
+                }
+                _ => {}
             }
         }
         if let Some(last) = ops.len().checked_sub(1) {
@@ -4673,6 +4676,20 @@ mod apx_tests {
             }
         })
     }
+    /// Exact rejection text (stronger than `fails`): panics if `line`
+    /// parses+encodes, else returns the encoder's error string.
+    pub(super) fn fail_msg(line: &str) -> String {
+        let items = parse_asm(line).unwrap_or_else(|e| panic!("parse `{line}`: {e}"));
+        let mut enc = InstructionEncoder::new();
+        for it in items {
+            if let AsmItem::Instruction(i) = it {
+                if let Err(e) = enc.encode(&i) {
+                    return e;
+                }
+            }
+        }
+        panic!("expected `{line}` to fail encoding")
+    }
 
     fn hex_relocs(line: &str) -> (String, Vec<u32>) {
         let items = parse_asm(line).unwrap_or_else(|e| panic!("parse `{line}`: {e}"));
@@ -5076,7 +5093,7 @@ mod apx_tests {
 
 #[cfg(test)]
 mod encoding_opt_tests {
-    use super::apx_tests::{fails, hex};
+    use super::apx_tests::{fail_msg, fails, hex};
     use super::*;
 
     #[test]
@@ -5537,4 +5554,74 @@ mod encoding_opt_tests {
         assert!(fails("{evex} movq %rcx, %rax"));
         assert!(fails("{nf}{rex2} addq %rcx, %rax"));
     }
+
+    /// Scalar-Tuple1 table pins (map, pp, W, opcode) -> N. Packed
+    /// neighbors that must NOT match: map-1 pp0/1, the even packed-FMA
+    /// opcodes, the odd (but packed) vfmsubadd 97/A7/B7, and the VNNI
+    /// pp2/3 opcodes in map 2.
+    #[test]
+    fn evex_scalar_tuple_n_table() {
+        use super::InstructionEncoder as E;
+        let n = E::evex_scalar_tuple_n;
+        assert_eq!(n(1, 2, 0, 0x58), Some(4));
+        assert_eq!(n(1, 3, 1, 0x58), Some(8));
+        assert_eq!(n(1, 2, 0, 0x51), Some(4));
+        assert_eq!(n(1, 3, 1, 0x5A), Some(8));
+        assert_eq!(n(1, 2, 0, 0xC2), Some(4));
+        assert_eq!(n(1, 0, 0, 0x58), None);
+        assert_eq!(n(1, 1, 1, 0x58), None);
+        assert_eq!(n(2, 1, 0, 0x99), Some(4));
+        assert_eq!(n(2, 1, 1, 0x99), Some(8));
+        assert_eq!(n(2, 1, 1, 0xBF), Some(8));
+        assert_eq!(n(2, 1, 0, 0x98), None);
+        assert_eq!(n(2, 1, 1, 0xBE), None);
+        assert_eq!(n(2, 1, 0, 0x97), None);
+        assert_eq!(n(2, 1, 1, 0xA7), None);
+        assert_eq!(n(2, 3, 0, 0x50), None);
+        assert_eq!(n(2, 2, 0, 0x51), None);
+    }
+
+    /// Scalar Tuple1 disp8-N + forced EVEX.b (GAS 2.47 refs). disp8
+    /// covers N*127 (ss: 508, sd: 1016); N+1... and misaligned
+    /// displacements fall back to disp32 exactly like GAS.
+    #[test]
+    fn evex_scalar_tuple1() {
+        // disp8 at the N-boundary (ss N=4, sd N=8).
+        assert_eq!(
+            hex("vaddss 508(%rdx), %xmm5, %xmm6{%k7}"),
+            "62 f1 56 0f 58 72 7f"
+        );
+        assert_eq!(
+            hex("vaddsd 1016(%rdx), %xmm5, %xmm6{%k7}"),
+            "62 f1 d7 0f 58 72 7f"
+        );
+        // Past disp8 (512/4 = 128) and misaligned (508 % 8 != 0): disp32.
+        assert_eq!(
+            hex("vaddss 512(%rdx), %xmm5, %xmm6{%k7}"),
+            "62 f1 56 0f 58 b2 00 02 00 00"
+        );
+        assert_eq!(
+            hex("vaddsd 508(%rdx), %xmm5, %xmm6{%k7}"),
+            "62 f1 d7 0f 58 b2 fc 01 00 00"
+        );
+        // EVEX.b stays 0 on scalar (P2 0x0f = V' + aaa, not b).
+        assert_eq!(
+            hex("vaddss %xmm1, %xmm5, %xmm6{%k7}"),
+            "62 f1 56 0f 58 f1"
+        );
+        assert_eq!(
+            hex("vaddsd %xmm1, %xmm5, %xmm6{%k7}"),
+            "62 f1 d7 0f 58 f1"
+        );
+        // Scalar {1toN} is tuple-impossible: GAS text, centrally.
+        assert_eq!(
+            fail_msg("vaddss 8(%rax){1to4}, %xmm1, %xmm2"),
+            "unsupported broadcast for `vaddss'"
+        );
+        assert_eq!(
+            fail_msg("vaddsd 8(%rax){1to2}, %xmm1, %xmm2{%k1}"),
+            "unsupported broadcast for `vaddsd'"
+        );
+        assert!(fails("vmaxsd 8(%rax){1to2}, %xmm1, %xmm2"));
+    }
 }
diff --git a/src/backend/x86/assembler/encoder/registers.rs b/src/backend/x86/assembler/encoder/registers.rs
index 7acdbe4f..0eba7612 100644
--- a/src/backend/x86/assembler/encoder/registers.rs
+++ b/src/backend/x86/assembler/encoder/registers.rs
@@ -4,24 +4,24 @@ use super::super::parser::*;
 pub(crate) fn reg_num(name: &str) -> Option<u8> {
     match name {
         "al" | "ax" | "eax" | "rax" | "xmm0" | "st" | "st(0)" | "mm0" | "es" | "ymm0" | "zmm0"
-        | "k0" => Some(0),
-        "cl" | "cx" | "ecx" | "rcx" | "xmm1" | "st(1)" | "mm1" | "cs" | "ymm1" | "zmm1" | "k1" => {
+        | "k0" | "K0" => Some(0),
+        "cl" | "cx" | "ecx" | "rcx" | "xmm1" | "st(1)" | "mm1" | "cs" | "ymm1" | "zmm1" | "k1" | "K1" => {
             Some(1)
         }
-        "dl" | "dx" | "edx" | "rdx" | "xmm2" | "st(2)" | "mm2" | "ss" | "ymm2" | "zmm2" | "k2" => {
+        "dl" | "dx" | "edx" | "rdx" | "xmm2" | "st(2)" | "mm2" | "ss" | "ymm2" | "zmm2" | "k2" | "K2" => {
             Some(2)
         }
-        "bl" | "bx" | "ebx" | "rbx" | "xmm3" | "st(3)" | "mm3" | "ds" | "ymm3" | "zmm3" | "k3" => {
+        "bl" | "bx" | "ebx" | "rbx" | "xmm3" | "st(3)" | "mm3" | "ds" | "ymm3" | "zmm3" | "k3" | "K3" => {
             Some(3)
         }
         "ah" | "spl" | "sp" | "esp" | "rsp" | "xmm4" | "st(4)" | "mm4" | "fs" | "ymm4" | "zmm4"
-        | "k4" => Some(4),
+        | "k4" | "K4" => Some(4),
         "ch" | "bpl" | "bp" | "ebp" | "rbp" | "xmm5" | "st(5)" | "mm5" | "gs" | "ymm5" | "zmm5"
-        | "k5" => Some(5),
-        "dh" | "sil" | "si" | "esi" | "rsi" | "xmm6" | "st(6)" | "mm6" | "ymm6" | "zmm6" | "k6" => {
+        | "k5" | "K5" => Some(5),
+        "dh" | "sil" | "si" | "esi" | "rsi" | "xmm6" | "st(6)" | "mm6" | "ymm6" | "zmm6" | "k6" | "K6" => {
             Some(6)
         }
-        "bh" | "dil" | "di" | "edi" | "rdi" | "xmm7" | "st(7)" | "mm7" | "ymm7" | "zmm7" | "k7" => {
+        "bh" | "dil" | "di" | "edi" | "rdi" | "xmm7" | "st(7)" | "mm7" | "ymm7" | "zmm7" | "k7" | "K7" => {
             Some(7)
         }
         "r8b" | "r8w" | "r8d" | "r8" | "xmm8" | "ymm8" | "zmm8" => Some(0),
@@ -253,8 +253,6 @@ pub(crate) fn operand_needs_evex(op: &Operand) -> bool {
                 || needs_evex_rprime(&r.name)
                 || r.mask.is_some()
                 || r.zeroing
-                || r.sae
-                || r.rounding.is_some()
         }
         Operand::Memory(m) => m.mask.is_some() || m.zeroing || m.broadcast.is_some(),
         Operand::Label(s) => is_evex_sae_token(s),
diff --git a/src/backend/x86/assembler/encoder/system.rs b/src/backend/x86/assembler/encoder/system.rs
index 86c255c4..7f5060ac 100644
--- a/src/backend/x86/assembler/encoder/system.rs
+++ b/src/backend/x86/assembler/encoder/system.rs
@@ -253,6 +253,7 @@ impl super::InstructionEncoder {
                     scale: None,
                     mask: None,
                     zeroing: false,
+                    z_first: false,
                     broadcast: None,
                 };
                 self.bytes.extend_from_slice(&[0x0F, 0x01]);
diff --git a/src/backend/x86/assembler/encoder/x87_misc.rs b/src/backend/x86/assembler/encoder/x87_misc.rs
index 1d9233b3..ebd75a3a 100644
--- a/src/backend/x86/assembler/encoder/x87_misc.rs
+++ b/src/backend/x86/assembler/encoder/x87_misc.rs
@@ -70,6 +70,7 @@ impl super::InstructionEncoder {
                     scale: None,
                     mask: None,
                     zeroing: false,
+                    z_first: false,
                     broadcast: None,
                 };
                 self.emit_rex_rm(size, "", &mem);
diff --git a/src/backend/x86/assembler/parser.rs b/src/backend/x86/assembler/parser.rs
index fb0193f8..ba8c206b 100644
--- a/src/backend/x86/assembler/parser.rs
+++ b/src/backend/x86/assembler/parser.rs
@@ -256,10 +256,10 @@ pub struct Register {
     pub mask: Option<String>,
     /// EVEX zeroing semantics on the destination: `%zmm0{%k1}{z}`.
     pub zeroing: bool,
-    /// EVEX suppress-all-exceptions: `{sae}` attached to this operand.
-    pub sae: bool,
-    /// EVEX embedded rounding: 0=rn, 1=rd, 2=ru, 3=rz (`{r*-sae}`).
-    pub rounding: Option<u8>,
+    /// `{z}` came before the mask in this operand's suffix chain
+    /// (`%zmm0{z}{%k1}` encodes, but with a mask on an earlier operand
+    /// it is `invalid zeroing-masking `{z}{%kN}'`).
+    pub z_first: bool,
     /// Erroneous `{1toN}` on a register (`%zmm1{1to16}`); broadcast is
     /// memory-only, so the encoder rejects it (`unsupported broadcast`).
     /// Stored (not parsed away) so the diagnostic names the mnemonic.
@@ -285,8 +285,7 @@ impl Register {
             name: n.to_string(),
             mask: None,
             zeroing: false,
-            sae: false,
-            rounding: None,
+            z_first: false,
             broadcast: None,
         }
     }
@@ -331,6 +330,8 @@ pub struct MemoryOperand {
     pub mask: Option<String>,
     /// EVEX zeroing semantics on the destination.
     pub zeroing: bool,
+    /// True if the `{z}` decorator textually preceded the `{%kN}` mask.
+    pub z_first: bool,
     /// EVEX embedded broadcast: `(%rdi){1to16}`. The integer is N in `{1toN}`.
     pub broadcast: Option<u8>,
 }
@@ -1424,12 +1425,52 @@ fn split_mnemonic_operands(line: &str) -> (&str, &str) {
 fn parse_operands(s: &str) -> Result<Vec<Operand>, String> {
     let parts = split_operands(s);
     let mut operands = Vec::new();
-    for part in &parts {
-        operands.push(parse_operand(part.trim())?);
+    for (i, part) in parts.iter().enumerate() {
+        let op = parse_operand(part.trim())?;
+        // Standalone `{...}` validation (GAS 2.47): `{sae}`/`{r*-sae}`
+        // normalize (spacing is tolerated); anything else is `Missing
+        // '}'`, `Junk after '}'`, or `invalid char '{' beginning operand
+        // N` (N is 1-based), echoing the offending text.
+        if let Operand::Label(t) = &op {
+            if t.starts_with('{') {
+                let norm = check_standalone_braces(t, i + 1)?;
+                operands.push(Operand::Label(norm));
+                continue;
+            }
+        }
+        operands.push(op);
     }
     Ok(operands)
 }
 
+/// Normalize or reject a standalone `{...}` operand (see `parse_operands`).
+/// Returns the canonical label text (`{sae}` / `{r*-sae}`).
+fn check_standalone_braces(t: &str, idx: usize) -> Result<String, String> {
+    let raw = t.trim();
+    let Some(close) = raw.find('}') else {
+        return Err(format!("\0raw:Missing '}}': '{raw}'"));
+    };
+    let inner = raw[1..close].trim();
+    let rest = &raw[close + 1..];
+    let is_sae = matches!(
+        inner,
+        "sae" | "rn-sae" | "rd-sae" | "ru-sae" | "rz-sae"
+    );
+    if is_sae {
+        if rest.trim().is_empty() {
+            return Ok(format!("{{{inner}}}"));
+        }
+        // `{sae}` followed by junk: echo the token plus the
+        // whitespace-stripped remainder, exactly as GAS does.
+        let tail: String = rest.chars().filter(|c| !c.is_whitespace()).collect();
+        return Err(format!("\0raw:Junk after '}}': '{{{inner}}}{tail}'"));
+    }
+    let stripped: String = raw.chars().filter(|c| !c.is_whitespace()).collect();
+    Err(format!(
+        "\0raw:invalid char '{{' beginning operand {idx} `{stripped}'"
+    ))
+}
+
 /// Split operand string by commas, respecting parentheses.
 fn split_operands(s: &str) -> Vec<String> {
     let mut parts = Vec::new();
@@ -1524,7 +1565,7 @@ fn parse_operand(s: &str) -> Result<Operand, String> {
     // `foo{%k1}`) is stripped and validated first; the remainder then
     // parses as an absolute integer or label as usual.
     let suf = if s.contains('{') && !s.starts_with('{') {
-        Some(parse_evex_mask_suffix(s, false)?)
+        Some(parse_evex_mask_suffix(s)?)
     } else {
         None
     };
@@ -1535,10 +1576,10 @@ fn parse_operand(s: &str) -> Result<Operand, String> {
         && !is_numeric_label
     {
         if let Ok(val) = crate::backend::asm_expr::parse_integer_expr(s) {
-            let (mask, zeroing, broadcast) = suf
+            let (mask, zeroing, z_first, broadcast) = suf
                 .as_ref()
-                .map(|suf| (suf.mask.clone(), suf.zeroing, suf.broadcast))
-                .unwrap_or((None, false, None));
+                .map(|suf| (suf.mask.clone(), suf.zeroing, suf.z_first, suf.broadcast))
+                .unwrap_or((None, false, false, None));
             return Ok(Operand::Memory(MemoryOperand {
                 segment: None,
                 displacement: Displacement::Integer(val),
@@ -1547,19 +1588,18 @@ fn parse_operand(s: &str) -> Result<Operand, String> {
                 scale: None,
                 mask,
                 zeroing,
+                z_first,
                 broadcast,
             }));
         }
     }
 
     // Standalone EVEX decorator: `{sae}` / `{r*-sae}` as their own AT&T
-    // operand (`vaddps {rn-sae}, %zmm1, %zmm2, %zmm3`). Anything else in
-    // standalone position is `unknown vector operation`.
-    if s.starts_with('{') && s.ends_with('}') {
-        let inner = s[1..s.len() - 1].trim();
-        if !matches!(inner, "sae" | "rn-sae" | "rd-sae" | "ru-sae" | "rz-sae") {
-            return Err(format!("\0raw:unknown vector operation: `{s}'"));
-        }
+    // operand (`vaddps {rn-sae}, %zmm1, %zmm2, %zmm3`). Validation needs
+    // the 1-based operand index (`invalid char '{' beginning operand N`
+    // echoes it), which only `parse_operands` knows, so the raw text is
+    // returned here and normalized/checked there.
+    if s.starts_with('{') {
         return Ok(Operand::Label(s.to_string()));
     }
 
@@ -1574,6 +1614,7 @@ fn parse_operand(s: &str) -> Result<Operand, String> {
             scale: None,
             mask: suf.mask.clone(),
             zeroing: suf.zeroing,
+            z_first: suf.z_first,
             broadcast: suf.broadcast,
         }));
     }
@@ -1583,22 +1624,24 @@ fn parse_operand(s: &str) -> Result<Operand, String> {
     Ok(Operand::Label(s.to_string()))
 }
 
-/// Parsed `{k1}` / `{z}` / `{sae}` / `{rn-sae}` / `{1toN}` suffixes.
+/// Parsed `{%kN}` / `{z}` / `{1toN}` suffixes. A suffixed `{sae}` /
+/// `{r*-sae}` never parses (GAS 2.47 rejects it with `unknown vector
+/// operation` on every operand kind); standalone `{sae}` operands are
+/// labels, handled by `check_standalone_braces`.
 struct EvexSuffix {
     rest: String,
     mask: Option<String>,
     zeroing: bool,
-    sae: bool,
-    rounding: Option<u8>,
-    /// The `{sae}`/`{r*-sae}` token as written (for the misplaced echo).
-    sae_tok: Option<String>,
+    z_first: bool,
     broadcast: Option<u8>,
 }
 
-/// True for a bare `{kN}` / `{%kN}` write-mask number (1-7; `{%k0}` is
-/// a dedicated error, bare `{k0}` is unknown).
+/// True for a `{%kN}` write-mask number (1-7). The `k` matches
+/// case-insensitively (register names are; `{%K1}` encodes) but the
+/// token is returned as written (dup echoes preserve case). `{%k0}`
+/// is a dedicated error handled by the caller.
 fn evex_mask_num(inner: &str) -> Option<&str> {
-    let n = inner.strip_prefix('k')?;
+    let n = inner.strip_prefix('k').or_else(|| inner.strip_prefix('K'))?;
     if matches!(n, "1" | "2" | "3" | "4" | "5" | "6" | "7") {
         Some(inner)
     } else {
@@ -1606,21 +1649,24 @@ fn evex_mask_num(inner: &str) -> Option<&str> {
     }
 }
 
-/// Split EVEX suffixes: `%zmm0{%k1}{z}` / `(%rdi){1to16}` / `{rn-sae}`.
-/// Accepts both `{%k1}` and `{k1}` (GNU as tolerates the bare form).
+/// Split EVEX suffixes: `%zmm0{%k1}{z}` / `(%rdi){1to16}`.
 /// Decorator *values* are validated here with GNU as diagnostics;
 /// placement (mask-on-destination, broadcast support) is the encoder's
-/// job — it needs the mnemonic and the emitted prefix. `sae_allowed`
-/// is false for memory/integer/label operands: `{sae}` there is
-/// `unknown vector operation` even for SAE-capable instructions.
-fn parse_evex_mask_suffix(s: &str, sae_allowed: bool) -> Result<EvexSuffix, String> {
+/// job — it needs the mnemonic and the emitted prefix.
+///
+/// GAS 2.47 echo rule (probed): every suffix error echoes the offending
+/// token plus the whitespace-stripped remainder of the suffix chain
+/// (`` `{sae}{%k1}' ``, `` `{1to1}{%k1}' ``, `` `{foo}{%k1}' ``) —
+/// except `{%k0}`, whose text is fixed. Stripped left-to-right so the
+/// remainder is well-defined.
+fn parse_evex_mask_suffix(s: &str) -> Result<EvexSuffix, String> {
     let mut name = s.to_string();
-    let mut mask = None;
+    let mut mask: Option<String> = None;
     let mut zeroing = false;
-    let mut sae = false;
-    let mut rounding = None;
-    let mut sae_tok = None;
+    let mut z_first = false;
+    let mut z_seen = false;
     let mut broadcast = None;
+    let mut bcast_seen = false;
     loop {
         if let Some(pos) = name.find('{') {
             let close = name[pos..].find('}');
@@ -1629,47 +1675,53 @@ fn parse_evex_mask_suffix(s: &str, sae_allowed: bool) -> Result<EvexSuffix, Stri
             let before = name[..pos].to_string();
             let after = name[pos + close + 1..].to_string();
             name = before + &after;
-            // Echo with braces, as GAS does (`{1toX}`, `{%k9}`).
+            // Echo with braces, as GAS does, plus the stripped remainder.
             let tok = format!("{{{inner}}}");
+            let tail: String = after.chars().filter(|c| !c.is_whitespace()).collect();
+            let echo = format!("{tok}{tail}");
             if inner == "z" {
-                zeroing = true;
-            } else if inner == "sae"
-                || inner == "rn-sae"
-                || inner == "rd-sae"
-                || inner == "ru-sae"
-                || inner == "rz-sae"
-            {
-                if !sae_allowed {
-                    return Err(format!("\0raw:unknown vector operation: `{tok}'"));
+                if zeroing {
+                    return Err(format!("\0raw:duplicated `{echo}'"));
                 }
-                sae = true;
-                sae_tok = Some(tok);
-                rounding = match inner.as_str() {
-                    "rn-sae" => Some(0),
-                    "rd-sae" => Some(1),
-                    "ru-sae" => Some(2),
-                    "rz-sae" => Some(3),
-                    _ => None,
-                };
+                zeroing = true;
+                z_seen = true;
+            } else if matches!(
+                inner.as_str(),
+                "sae" | "rn-sae" | "rd-sae" | "ru-sae" | "rz-sae"
+            ) {
+                return Err(format!("\0raw:unknown vector operation: `{echo}'"));
             } else if let Some(rest) = inner.strip_prefix("1to") {
                 if !matches!(rest, "2" | "4" | "8" | "16" | "32") {
-                    return Err(format!("\0raw:Unsupported broadcast: `{tok}'"));
+                    return Err(format!("\0raw:Unsupported broadcast: `{echo}'"));
+                }
+                if bcast_seen {
+                    return Err(format!("\0raw:duplicated `{echo}'"));
                 }
+                bcast_seen = true;
                 broadcast = Some(rest.parse::<u8>().unwrap_or(0));
-            } else if let Some(rest) = inner.strip_prefix('%') {
-                let rest = rest.trim();
-                if rest == "k0" {
+            } else if let Some(pct) = inner.strip_prefix('%') {
+                let pct = pct.trim();
+                if pct.eq_ignore_ascii_case("k0") {
                     return Err("\0raw:`%k0' can't be used for write mask".to_string());
                 }
-                if let Some(k) = evex_mask_num(rest) {
-                    mask = Some(k.to_string());
-                } else {
-                    return Err(format!("\0raw:unknown vector operation: `{tok}'"));
+                if evex_mask_num(pct).is_none() {
+                    return Err(format!("\0raw:unknown vector operation: `{echo}'"));
+                }
+                if mask.is_some() {
+                    return Err(format!("\0raw:duplicated `{echo}'"));
                 }
-            } else if let Some(k) = evex_mask_num(&inner) {
-                mask = Some(k.to_string());
+                if z_seen {
+                    z_first = true;
+                }
+                // Preserve the written case (`{%K2}` echoes back
+                // upper-case in `duplicated `{%K2}'`); consumers read
+                // only the digit (see `reg_num`'s `K0`-`K7` arms).
+                mask = Some(pct.to_string());
             } else {
-                return Err(format!("\0raw:unknown vector operation: `{tok}'"));
+                // Bare `{kN}` (including `{k0}`), `{Z}`, `{SAE}`, `{1TO4}`
+                // (the `1to` match is lowercase-only), `{}` and anything
+                // else: `unknown vector operation`.
+                return Err(format!("\0raw:unknown vector operation: `{echo}'"));
             }
         } else {
             break;
@@ -1679,9 +1731,7 @@ fn parse_evex_mask_suffix(s: &str, sae_allowed: bool) -> Result<EvexSuffix, Stri
         rest: name,
         mask,
         zeroing,
-        sae,
-        rounding,
-        sae_tok,
+        z_first,
         broadcast,
     })
 }
@@ -1690,19 +1740,9 @@ fn parse_evex_mask_suffix(s: &str, sae_allowed: bool) -> Result<EvexSuffix, Stri
 fn parse_register_operand(s: &str) -> Result<Operand, String> {
     // GNU as tolerates whitespace between '%' and the register name
     // (glibc emits `mov % r13, ...` from `% " R13_LP "` macro splicing).
-    let suf = parse_evex_mask_suffix(s, true)?;
+    let suf = parse_evex_mask_suffix(s)?;
     let s = suf.rest.as_str();
     let name = s[1..].trim_start(); // strip % and any following spaces
-    // `{sae}` only exists on vector registers; on `%eax`, `%k1`, `%mm0`
-    // (anything else) it is `unknown vector operation`.
-    if suf.sae || suf.rounding.is_some() {
-        let vn = name.to_ascii_lowercase();
-        let vec = vn.starts_with("xmm") || vn.starts_with("ymm") || vn.starts_with("zmm");
-        if !vec {
-            let tok = suf.sae_tok.as_deref().unwrap_or("{sae}");
-            return Err(format!("\0raw:unknown vector operation: `{tok}'"));
-        }
-    }
 
     // Handle %st(N)
     if name.starts_with("st(") && name.ends_with(')') {
@@ -1714,14 +1754,11 @@ fn parse_register_operand(s: &str) -> Result<Operand, String> {
         let seg = &name[..colon_pos];
         let rest = &s[1 + colon_pos + 1..]; // after the colon
         if is_segment_name(seg) {
-            if suf.sae || suf.rounding.is_some() {
-                let tok = suf.sae_tok.as_deref().unwrap_or("{sae}");
-                return Err(format!("\0raw:unknown vector operation: `{tok}'"));
-            }
             let mut mem = parse_memory_inner(rest)?;
             mem.segment = Some(seg.to_string());
             mem.mask = suf.mask;
             mem.zeroing = suf.zeroing;
+            mem.z_first = suf.z_first;
             mem.broadcast = suf.broadcast;
             return Ok(Operand::Memory(mem));
         }
@@ -1730,8 +1767,7 @@ fn parse_register_operand(s: &str) -> Result<Operand, String> {
     let mut reg = Register::new(name);
     reg.mask = suf.mask;
     reg.zeroing = suf.zeroing;
-    reg.sae = suf.sae;
-    reg.rounding = suf.rounding;
+    reg.z_first = suf.z_first;
     reg.broadcast = suf.broadcast;
     Ok(Operand::Register(reg))
 }
@@ -1888,7 +1924,7 @@ fn parse_memory_inner(s: &str) -> Result<MemoryOperand, String> {
 
     // EVEX mask/zeroing suffix on a memory operand: `(%rax){%k1}`.
     if s.contains('{') {
-        let suf = parse_evex_mask_suffix(s, false)?;
+        let suf = parse_evex_mask_suffix(s)?;
         let mut mem = parse_memory_inner(&suf.rest)?;
         mem.mask = suf.mask;
         mem.zeroing = suf.zeroing;
@@ -1948,6 +1984,7 @@ fn parse_memory_inner(s: &str) -> Result<MemoryOperand, String> {
                     scale: None,
                     mask: None,
                     zeroing: false,
+                    z_first: false,
                     broadcast: None,
                 });
             }
@@ -2037,6 +2074,7 @@ fn parse_memory_inner(s: &str) -> Result<MemoryOperand, String> {
             scale,
             mask: None,
             zeroing: false,
+            z_first: false,
             broadcast: None,
         })
     } else {
@@ -2050,6 +2088,7 @@ fn parse_memory_inner(s: &str) -> Result<MemoryOperand, String> {
             scale: None,
             mask: None,
             zeroing: false,
+            z_first: false,
             broadcast: None,
         })
     }
