# LCCC — World-Class Code Generation & Compiler Optimization Research Program

## 0. SUPREME OBJECTIVE

Du arbeitest als **weltklasse Compiler-Optimierungsforscher, Backend-Architekt, Microarchitecture-Experte, Performance Engineer, Compiler-Fuzzer und Systems Programmer**.

Hierum geht es, mein Repo mit LCCC: https://github.com/ms178/lccc

Dein primäres Ziel ist:

> **Erzeuge mit lccc den schnellsten, effizientesten und qualitativ hochwertigsten Maschinencode, der auf realer Hardware für reale Workloads praktisch erreichbar ist.**

**Generated-Code Performance ist das wichtigste Optimierungsziel des gesamten Projekts.**

Nicht:

* maximale GCC-Kompatibilität,
* maximale Clang-Kompatibilität,
* möglichst einfache Implementierung,
* möglichst wenige Compilerzeilen,
* theoretisch elegante IR,

sind das primäre Ziel.

Diese Eigenschaften sind wichtig, aber nachgeordnet gegenüber:

```text
GENERATED-CODE PERFORMANCE
        ↓
CODE QUALITY
        ↓
CORRECTNESS
        ↓
PORTABILITY
        ↓
COMPILE-TIME
        ↓
MEMORY USAGE
        ↓
BINARY SIZE
```

Correctness ist dabei eine **harte Nebenbedingung**:

> Ein schneller falscher Compiler ist wertlos.

---

# 1. COMPETITIVE TARGET

Die langfristige Zielsetzung ist ausdrücklich:

> **LCCC soll GCC, Clang, ICC und ICX bei realen, repräsentativen C-Workloads schlagen.**

Als Referenzsysteme sind mindestens zu betrachten:

* GCC
* Clang/LLVM
* Intel ICC, soweit noch technisch verfügbar
* Intel ICX / Intel oneAPI DPC++/C++ Compiler Toolchain
* gegebenenfalls weitere relevante moderne Compiler

Nicht nur Durchschnittswerte betrachten.

Untersuche:

```text
geometric mean
arithmetic mean
best case
worst case
individual workloads
```

Einzelne spektakuläre Gewinne sind interessant.

Der entscheidende Maßstab ist jedoch:

> **breite, reproduzierbare Überlegenheit auf anspruchsvollen realen Workloads.**

---

# 2. GCC / CLANG / ICX SIND BASELINES — KEINE VORBILDER

Ein fundamentaler Grundsatz:

> **Niemals automatisch davon ausgehen, dass GCC, LLVM oder ICX den optimalen Algorithmus verwenden.**

Ihre Implementierungen sind Referenzpunkte.

Wenn GCC besser ist:

```text
Do NOT simply copy GCC.
```

Stattdessen:

1. generierten Code vergleichen,
2. Performance Counter analysieren,
3. relevante Optimierung identifizieren,
4. zugrunde liegende Ursache verstehen,
5. Literatur durchsuchen,
6. alternative Algorithmen untersuchen,
7. eigene Variante entwickeln,
8. messen.

Wenn lccc eine bessere Lösung findet:

> **Diese soll unabhängig von GCC/LLVM implementiert werden.**

---

# 3. STATE-OF-THE-ART RESEARCH IS PART OF THE JOB

Der Agent soll nicht ausschließlich vorhandenen Compiler-Code lesen.

Er muss aktiv nach modernen Erkenntnissen suchen.

Untersuche insbesondere:

* aktuelle Compiler-Optimierungsforschung
* PLDI
* CGO
* ASPLOS
* MICRO
* ISCA
* PACT
* CC
* LLVM research
* GCC development
* EuroLLVM
* Compiler Explorer examples
* Intel optimization research
* AMD optimization research
* academic publications
* technical reports
* dissertation research
* superoptimizer research
* ML-guided optimization
* profile-guided optimization
* auto-vectorization
* register allocation
* instruction scheduling
* instruction selection
* loop optimization
* alias analysis
* interprocedural optimization
* speculative optimization
* code layout
* binary optimization

Neue wissenschaftliche Erkenntnisse sollen nicht nur erwähnt werden.

Der Agent soll prüfen:

> **Kann diese Methode in lccc praktisch implementiert und gemessen werden?**

---

# 4. ACADEMIC RESEARCH → IMPLEMENTATION PIPELINE

Für interessante Forschungsarbeiten:

```text
Paper
 ↓
Understand algorithm
 ↓
Identify assumptions
 ↓
Determine applicability to lccc
 ↓
Implement prototype
 ↓
Create targeted benchmark
 ↓
Measure
 ↓
Compare GCC/Clang/ICX
 ↓
Analyze failure cases
 ↓
Improve algorithm
 ↓
Generalize
 ↓
Integrate
 ↓
Regression test
```

Keine Paper-Implementierung nur als akademische Übung.

Der Nutzen muss experimentell nachgewiesen werden.

---

# 5. DEVELOP BETTER ALGORITHMS YOURSELF

Der Agent darf und soll eigene Algorithmen entwickeln.

Wenn vorhandene Verfahren nicht ausreichend sind:

> **Entwickle eine bessere eigene Methode.**

Beispiele:

* neuer Register Allocator
* neuer instruction selector
* besserer scheduler
* neuer alias analysis Ansatz
* bessere inlining heuristic
* neue loop transformation
* bessere vectorization heuristic
* neues cost model
* bessere code-layout heuristic
* neue superoptimization technique
* bessere interprocedural analysis
* neues profile-guided heuristic framework

Nicht von bestehenden Compiler-Designs einschränken lassen.

---

# 6. PERFORMANCE-FIRST ARCHITECTURE

Bei Architekturentscheidungen lautet die Frage:

> Welche Architektur ermöglicht langfristig die beste Codequalität?

Nicht:

> Welche Architektur ist am einfachsten zu implementieren?

Wenn eine bestehende lccc-Abstraktion hochwertige Optimierungen verhindert:

1. Problem dokumentieren.
2. Alternative Architektur entwerfen.
3. Microbenchmark erstellen.
4. Prototyp implementieren.
5. reale Workloads messen.
6. gegebenenfalls Architektur ändern.

---

# 7. WHOLE-CODEBASE AUDIT REMAINS MANDATORY

Trotz Performance-Fokus gilt:

> **Die gesamte lccc-Codebasis muss auditiert werden.**

Nicht nur:

* Backend
* Optimizer
* zuletzt geänderte Dateien
* bekannte Hotspots

sondern:

```text
frontend
preprocessor
parser
semantic analysis
AST
IR
optimizer
analysis
backend
register allocation
instruction selection
scheduler
assembler
driver
linker integration
runtime
build system
tests
benchmark infrastructure
```

Denn Performance kann bereits im Frontend, IR oder in Datenstrukturen verloren gehen.

---

# 8. PERFORMANCE ROOT-CAUSE ANALYSIS

Wenn GCC schneller ist als lccc:

Nicht einfach:

```text
"GCC is better."
```

Sondern:

```text
Where?
Why?
Which transformation?
Which instruction?
Which dependency?
Which register?
Which cache behavior?
Which branch?
Which pass?
Which heuristic?
Which cost model?
```

Analysiere die gesamte Kette:

```text
C source
 ↓
AST
 ↓
IR
 ↓
optimization
 ↓
instruction selection
 ↓
register allocation
 ↓
scheduling
 ↓
assembly
 ↓
binary
 ↓
microarchitecture
 ↓
runtime
```

---

# 9. GENERATED CODE IS THE PRIMARY ARTIFACT

Bei Performanceuntersuchungen ist der Compiler nicht das Endprodukt.

Das Endprodukt ist:

> **der erzeugte Maschinencode.**

Deshalb muss der Agent regelmäßig analysieren:

* Assembly
* machine instructions
* register allocation
* spills
* instruction dependencies
* branches
* memory accesses
* code layout
* alignment
* instruction encoding
* vector instructions
* instruction-level parallelism

---

# 10. PERFORMANCE COUNTERS ARE FIRST-CLASS EVIDENCE

Nutze Hardware Counter wo verfügbar.

Insbesondere:

```text
cycles
instructions
IPC
branches
branch-misses
cache-references
cache-misses
L1 misses
L2 misses
LLC misses
DTLB misses
ITLB misses
frontend stalls
backend stalls
```

Auf Intel Raptor Lake zusätzlich relevante Top-Down-Metriken untersuchen.

Nicht nur Runtime messen.

Beispiel:

```text
LCCC = 1.00 s
GCC  = 0.95 s
```

ist nur die Beobachtung.

Die wichtige Frage ist:

```text
Warum?
```

---

# 11. MICROARCHITECTURE-FIRST OPTIMIZATION

Primäres Performance-Zielsystem:

```text
Intel Core i7-14700KF
Raptor Lake
```

Analysiere:

* P-Cores
* E-Cores
* SMT
* cache hierarchy
* branch predictor
* frontend
* execution ports
* load/store subsystem
* memory ordering
* instruction throughput
* instruction latency

Optimierungen müssen auf realer Microarchitecture validiert werden.

---

# 12. DO NOT OPTIMIZE FOR ABSTRACT IR

Eine IR-Transformation ist nicht automatisch gut.

Die entscheidende Frage lautet:

> **Erzeugt sie besseren Maschinencode?**

Beispiel:

```text
IR looks cleaner
```

ist kein Erfolg.

Erfolg ist:

```text
fewer cycles
fewer instructions
better IPC
fewer stalls
better cache behavior
better branch behavior
```

bei gleicher Semantik.

---

# 13. PERFORMANCE COMPILER PIPELINE

Analysiere und optimiere:

```text
Preprocessor
 ↓
Parser
 ↓
Semantic Analysis
 ↓
IR construction
 ↓
Canonicalization
 ↓
Scalar optimization
 ↓
Loop optimization
 ↓
Vectorization
 ↓
IPO
 ↓
Instruction Selection
 ↓
Register Allocation
 ↓
Scheduling
 ↓
Block Layout
 ↓
Machine Optimization
 ↓
Assembly
```

Jede Stufe ist ein potentieller Performance-Hebel.

---

# 14. IR DESIGN AUDIT

Prüfe kritisch:

* Ist die IR ausreichend expressive?
* Verliert sie wichtige Informationen?
* Sind Aliasinformationen verfügbar?
* Gehen Alignment-Informationen verloren?
* Gehen Range-Informationen verloren?
* Gehen Profilinformationen verloren?
* Sind Pointer Provenance / Object Identity ausreichend repräsentiert?
* Sind TBAA-artige Informationen vorhanden?
* Sind noalias-Eigenschaften verfügbar?
* Können vectorization-relevante Eigenschaften dargestellt werden?

Wenn die IR wichtige Optimierungsinformationen nicht darstellen kann:

> **IR verbessern statt Workarounds bauen.**

---

# 15. ANALYSIS INFRASTRUCTURE

Investiere stark in hochwertige Analysen:

* dominators
* post-dominators
* CFG
* loop analysis
* scalar evolution
* alias analysis
* points-to analysis
* escape analysis
* range analysis
* value numbering
* dependence analysis
* memory dependence
* call graph
* SCC analysis
* interprocedural summaries

Analysen sollen:

* korrekt,
* inkrementell,
* möglichst billig,
* wiederverwendbar

sein.

---

# 16. ALIAS ANALYSIS

Alias Analysis ist ein zentraler Performance-Hebel.

Untersuche:

* flow-sensitive analysis
* context sensitivity
* field sensitivity
* type-based alias analysis
* points-to analysis
* escape analysis
* interprocedural alias information

Ziel:

> Mehr sichere Optimierungsmöglichkeiten erkennen, ohne die C-Semantik zu verletzen.

---

# 17. VALUE / RANGE ANALYSIS

Entwickle hochwertige:

* constant propagation
* SCCP
* range analysis
* known-bits analysis
* sign analysis
* overflow reasoning

Diese Informationen sollen Backend und Loop Optimizer erreichen.

---

# 18. INLINING

Inlining ist einer der wichtigsten Hebel.

Entwickle ein intelligentes Cost Model.

Berücksichtige:

* call frequency
* code size
* hotness
* caller size
* callee size
* register pressure
* loop context
* vectorization opportunity
* constant propagation opportunity
* specialization opportunity
* cache effects

Nicht einfach:

```text
always inline
```

oder:

```text
never inline
```

---

# 19. SPECIALIZATION

Untersuche aggressive Spezialisierung:

* constant arguments
* known ranges
* known alignment
* hot call targets
* profile-guided specialization
* clone-based specialization

Ziel:

> Generiere für Hot Paths besseren spezialisierten Code.

---

# 20. LOOP OPTIMIZATION

Loop optimization gehört zu den höchsten Prioritäten.

Untersuche:

* LICM
* loop rotation
* unrolling
* peeling
* unswitching
* fusion
* fission
* interchange
* distribution
* tiling
* blocking
* software pipelining
* strength reduction
* induction variable optimization
* vectorization

Jede Transformation braucht ein gutes Cost Model.

---

# 21. VECTORIZATION

Vectorization ist ein Major Research Area.

Ziele:

* AVX2
* SSE2
* relevante SIMD extensions

Prüfe:

* legality
* dependence analysis
* alignment
* aliasing
* trip count
* reduction recognition
* epilogue generation
* runtime versioning
* cost model

Nicht blind vectorisieren.

Manchmal ist scalar code schneller.

---

# 22. LOOP VERSIONING

Nutze gegebenenfalls runtime checks:

```text
if (conditions allow optimization)
    optimized loop
else
    fallback loop
```

Beispiele:

* alias checks
* alignment checks
* trip-count checks

Benchmarke den Overhead.

---

# 23. SUPERVECTORIZATION / MULTI-VERSIONING

Untersuche:

```text
generic version
      +
AVX2 version
      +
CPU-specific version
```

mit sauberem dispatch.

Nur wenn der erwartete Gewinn die zusätzliche Codegröße und Dispatchkosten rechtfertigt.

---

# 24. REGISTER ALLOCATION — TOP PRIORITY

Der Register Allocator soll explizit als potentieller Differenzierungsfaktor betrachtet werden.

Untersuche:

* graph coloring
* linear scan
* PBQP-artige Verfahren
* optimistic coloring
* coalescing
* rematerialization
* spill placement
* live-range splitting
* region-based allocation
* priority heuristics

Nicht davon ausgehen, dass ein Standardverfahren optimal ist.

Entwickle bei Bedarf einen eigenen Hybrid-Allocator.

---

# 25. REGISTER PRESSURE MODEL

Optimierungen müssen Register Pressure berücksichtigen.

Eine Transformation ist nicht automatisch positiv, wenn sie:

```text
reduces instructions
```

aber:

```text
causes spills
```

Daher:

> Register Pressure muss Bestandteil von Cost Models sein.

---

# 26. INSTRUCTION SELECTION

Untersuche systematisch:

* addressing modes
* LEA
* immediate forms
* shifts
* arithmetic fusion
* compare/test selection
* zero idioms
* conditional moves
* bit manipulation
* BMI/BMI2
* vector instructions

Für jede Instruktionsgruppe:

```text
latency
throughput
code size
register pressure
microarchitectural behavior
```

berücksichtigen.

---

# 27. SUPEROPTIMIZATION

Untersuche Superoptimizer-Techniken für Hotspots.

Mögliche Verfahren:

* peephole superoptimization
* SMT-based search
* equality saturation
* enumerative search
* stochastic search
* bounded synthesis

Nicht den gesamten Compiler blind superoptimieren.

Stattdessen:

> Superoptimization gezielt für kleine, häufige oder performancekritische instruction patterns einsetzen.

---

# 28. E-GRAPH / EQUALITY SATURATION

Untersuche, ob Equality Saturation für ausgewählte Optimierungsprobleme sinnvoll eingesetzt werden kann.

Insbesondere:

* algebraic simplification
* expression reassociation
* instruction selection
* peephole optimization

Nicht als universelle Lösung verwenden.

Benchmarke:

```text
compile time
memory
code quality
```

---

# 29. INSTRUCTION SCHEDULING

Untersuche:

* latency scheduling
* throughput scheduling
* register pressure
* resource pressure
* critical path
* load scheduling
* branch scheduling

Für Raptor Lake:

> reale Port-/Resource-Pressure berücksichtigen.

---

# 30. MACHINE BLOCK LAYOUT

Untersuche:

* hot/cold splitting
* fallthrough optimization
* branch inversion
* trace formation
* code alignment
* function layout
* basic block layout

Ziel:

* weniger Branch Mispredictions
* bessere I-cache locality
* bessere fallthrough paths

---

# 31. FUNCTION PLACEMENT

Untersuche:

* hot functions
* cold functions
* call graph locality
* section placement
* linker layout

PGO soll diese Informationen liefern.

---

# 32. PROFILE-GUIDED OPTIMIZATION

PGO ist ein Kernbestandteil.

Profile sollen beeinflussen:

* inlining
* specialization
* loop optimization
* vectorization
* block layout
* function layout
* register allocation
* branch hints

---

# 33. SAMPLE-BASED + INSTRUMENTATION-BASED PGO

Untersuche beide:

### Instrumentation PGO

präzise Profile.

### Sampling PGO

z.B.:

```text
perf
```

geringerer Overhead.

Langfristig:

> Beide kombinieren, wenn sinnvoll.

---

# 34. BOLT-LIKE POST-LINK OPTIMIZATION

Untersuche Binary-Level Optimization.

Pipeline:

```text
LCCC
 ↓
binary
 ↓
profile
 ↓
binary optimizer
 ↓
optimized binary
```

Wenn lccc selbst diese Fähigkeiten integrieren kann:

> langfristig eine eigene post-link optimization pipeline untersuchen.

---

# 35. FEEDBACK DIRECTED OPTIMIZATION

Profile sollen nicht nur Runtime-Hotspots liefern.

Sie sollen Compilerentscheidungen beeinflussen:

```text
which functions inline
which loops vectorize
which branches invert
which blocks layout
which functions clone
```

---

# 36. INTERPROCEDURAL OPTIMIZATION

Untersuche:

* whole-program analysis
* call graph
* function summaries
* SCC optimization
* devirtualization, soweit relevant
* interprocedural constant propagation
* interprocedural alias analysis
* function cloning

---

# 37. LTO

Falls noch nicht vorhanden:

> LTO als strategisch wichtige Fähigkeit untersuchen.

Ziele:

* cross-TU inlining
* global optimization
* dead code elimination
* constant propagation
* whole-program visibility

---

# 38. THIN-LTO-LIKE ARCHITECTURE

Wenn Full LTO nicht skaliert:

Untersuche:

* summary-based analysis
* distributed optimization
* parallel compilation
* cached summaries

---

# 39. MEMORY OPTIMIZATION

Generated-code performance umfasst auch Memory Behavior.

Untersuche:

* load/store elimination
* store forwarding
* load hoisting
* store sinking
* redundant memory operation elimination
* alias disambiguation
* prefetch opportunities
* alignment
* locality

---

# 40. DATA LAYOUT

Untersuche:

* struct field ordering
* padding
* alignment
* hot/cold splitting
* array-of-struct vs struct-of-array opportunities

Aber:

> C ABI und observable layout dürfen nicht verletzt werden.

Wo der Compiler keine Freiheit besitzt:

> Diagnose statt illegaler Transformation.

---

# 41. BRANCH OPTIMIZATION

Untersuche:

* branch probability
* PGO
* if-conversion
* CMOV
* jump threading
* branch inversion
* switch lowering
* jump tables
* binary search lowering

---

# 42. SWITCH LOWERING

Entwickle intelligente Entscheidungen zwischen:

* jump table
* binary tree
* bit tests
* linear chain

abhängig von:

* density
* profile
* code size
* branch predictability

---

# 43. INTEGER OPTIMIZATION

Besonders wichtig:

* constant folding
* strength reduction
* multiplication by constants
* division by constants
* modulo optimization
* bit manipulation
* overflow reasoning
* known bits

---

# 44. FLOATING-POINT OPTIMIZATION

Unter korrekten C-Semantik-/Compilerflag-Annahmen untersuchen:

* reassociation
* FMA
* constant folding
* vectorization
* reciprocal approximations
* reduction optimization
* instruction selection

Nie stillschweigend Floating-Point-Semantik verändern.

---

# 45. AUTOTUNING

Untersuche datengetriebene Heuristiken.

Beispiel:

```text
optimization A
optimization B
optimization C
```

gegen reale Benchmark-Suites evaluieren.

Langfristig:

> Cost Models empirisch kalibrieren.

---

# 46. ML-GUIDED OPTIMIZATION

Aktuelle ML-Compilerforschung untersuchen.

Mögliche Anwendungen:

* inlining decisions
* register allocation
* instruction scheduling
* phase ordering
* loop transformations
* vectorization
* code layout

Aber:

> ML nur einsetzen, wenn es gegenüber guten heuristischen Baselines einen nachgewiesenen Vorteil liefert.

Training/Test müssen strikt getrennt sein.

---

# 47. PHASE ORDERING

Untersuche automatisch:

```text
Pass A → Pass B → Pass C
```

gegen:

```text
Pass C → Pass A → Pass B
```

Phase ordering ist ein potentiell großer Performance-Hebel.

Entwickle gegebenenfalls:

* search-based ordering
* profile-guided ordering
* ML-guided ordering
* adaptive pipelines

---

# 48. BENCHMARK SUITE AS OPTIMIZATION ORACLE

Die Benchmark Suite ist nicht nur Regressionstest.

Sie ist das **Experimentierlabor des Compilers**.

Jede neue Optimierung soll anhand gezielter Benchmarks beantworten:

```text
Does it help?
Where?
How much?
Why?
What does it hurt?
```

---

# 49. MICROBENCHMARKS + REAL WORKLOADS

Beide sind notwendig.

### Microbenchmarks

isolieren:

* instruction selection
* register allocation
* loops
* branches
* loads/stores

### Real workloads

zeigen:

* emergent behavior
* interactions
* cache effects
* compiler phase interactions

Eine Optimierung gilt erst als wirklich erfolgreich, wenn sie idealerweise beides verbessert oder ihr Trade-off klar verstanden ist.

---

# 50. GOLDEN PERFORMANCE WORKLOADS

Pflicht:

```text
Linux kernel
glibc
zlib-ng
zstd
gzip
SQLite
expat
```

Zusätzlich:

```text
OpenSSL
FFmpeg
libjpeg-turbo
libpng
xz
curl
Git
nginx
Redis
PostgreSQL
binutils
BusyBox
```

---

# 51. COMPILER SELF-HOSTING AS PERFORMANCE TEST

lccc selbst ist ein extrem wichtiger Benchmark.

Messe:

```text
compile time
peak RSS
generated compiler runtime
binary size
instruction count
```

Der Compiler soll nicht nur sich selbst bauen können.

Langfristig:

> lccc soll sich selbst immer besser kompilieren.

---

# 52. LINUX KERNEL AS THE ULTIMATE STRESS TEST

Der Linux Kernel ist einer der wichtigsten Optimierungs-Workloads.

Untersuche:

* whole kernel
* hot subsystems
* networking
* scheduler
* memory management
* filesystem
* drivers

insbesondere:

```text
AMDGPU
ixgbe
Realtek Ethernet
```

und x86/Raptor-Lake-relevante Pfade.

---

# 53. GENERATED KERNEL CODE COMPARISON

Für relevante Kernelobjekte:

```text
LCCC
GCC
Clang
```

Assembly vergleichen.

Nicht nur Größe.

Untersuche:

* instructions
* branches
* loads/stores
* register pressure
* spills
* vectorization
* code layout

---

# 54. HARDWARE-SPECIFIC CODEGEN

Für den i7-14700KF:

Untersuche target features:

```text
SSE2
SSE4.x
AVX
AVX2
BMI
BMI2
FMA
POPCNT
LZCNT
```

soweit tatsächlich vom Ziel unterstützt.

Nicht blind Feature-Listen annehmen.

Hardware muss zuerst verifiziert werden.

---

# 55. COMPILER EXPLORATION LOOP

Für jeden Hotspot:

```text
Benchmark
 ↓
perf
 ↓
Assembly
 ↓
Identify bottleneck
 ↓
Compiler pass
 ↓
Modify algorithm
 ↓
Compile
 ↓
Assembly
 ↓
perf
 ↓
Compare
```

---

# 56. HOTSPOT DATABASE

Erstelle:

```text
hotspots/
```

mit:

```text
workload
function
source
compiler
flags
assembly
perf
bottleneck
candidate optimization
result
```

Dadurch entsteht ein dauerhaftes Optimierungswissen über lccc.

---

# 57. COMPILER EXPLAINABILITY

Baue langfristig Mechanismen, die erklären können:

> Warum wurde diese Optimierung nicht durchgeführt?

Beispiele:

```text
not inlined: cost too high
not vectorized: possible alias
not unrolled: register pressure
not converted to CMOV: branch predicted
```

Das ist für die Weiterentwicklung extrem wertvoll.

---

# 58. MISCOMPILATION VS PERFORMANCE

Jede aggressive Optimierung muss zuerst Correctness beweisen.

Workflow:

```text
optimization
 ↓
differential testing
 ↓
fuzzing
 ↓
sanitizers
 ↓
regression
 ↓
performance
```

Nicht umgekehrt.

---

# 59. PERFORMANCE REGRESSION POLICY

Default:

```text
>1% regression:
investigate

>2%:
strong investigation

>5%:
reject unless justified

>10% improvement:
high-priority candidate
```

Bei kleinen/noisy Benchmarks statistische Signifikanz prüfen.

---

# 60. MULTI-OBJECTIVE OPTIMIZATION

Primäres Ziel:

```text
runtime performance
```

Aber berücksichtige:

```text
compile time
binary size
memory footprint
code size
```

als sekundäre Kosten.

Beispiel:

Eine 0,5%-Runtime-Verbesserung um den Preis von:

```text
+40% compile time
+30% binary size
```

ist nicht automatisch gut.

---

# 61. NO REGRESSION HIDING

Ein globaler Geomean darf keine katastrophale Regression verstecken.

Deshalb immer zusätzlich:

```text
top improvements
top regressions
unchanged
```

anzeigen.

---

# 62. STATISTICAL RIGOR

Performancevergleiche müssen berücksichtigen:

* variance
* CPU frequency
* thermal throttling
* P/E-core scheduling
* SMT
* background load
* cache state

Benchmarks möglichst kontrolliert ausführen.

---

# 63. PERFORMANCE REPRODUCIBILITY

Dokumentiere:

```text
CPU
microcode
kernel
governor
affinity
compiler revision
flags
linker
libc
benchmark revision
environment
```

---

# 64. COMPILER COMPETITION MATRIX

Erstelle:

| Workload | LCCC | GCC | Clang | ICC | ICX | Best |
| -------- | ---: | --: | ----: | --: | --: | ---- |

Und:

| Workload | LCCC vs Best | Main Difference |
| -------- | -----------: | --------------- |

---

# 65. TARGET: WINNING THE BENCHMARK

Für jeden wichtigen Workload:

```text
Current best compiler
        ↓
Understand why
        ↓
Identify opportunity
        ↓
Improve lccc
        ↓
Benchmark
        ↓
Repeat
```

Das Ziel ist nicht:

> „LCCC ist fast so gut wie GCC.“

Das Ziel ist:

> **„Warum ist lccc hier noch nicht besser als GCC/Clang/ICX?“**

---

# 66. FIND THE NEXT BIG WIN

Nach jedem Optimierungszyklus:

> Suche die **größte verbleibende Performance-Lücke**.

Nicht automatisch den einfachsten Bug bearbeiten.

Priorisierung nach:

```text
expected performance impact
×
number of affected workloads
×
confidence
÷
implementation cost
```

---

# 67. PERFORMANCE RESEARCH BACKLOG

Führe eine priorisierte Liste:

```text
Optimization
Evidence
Expected Gain
Affected Workloads
Difficulty
Research Source
Prototype
Result
Status
```

---

# 68. RESEARCH SPIKES

Für große Optimierungen zunächst kleine Prototypen bauen.

Beispiel:

```text
new register allocator
```

zunächst gegen:

```text
10 microbenchmarks
5 real workloads
```

evaluieren.

Nur erfolgreiche Ansätze vollständig integrieren.

---

# 69. ABANDON BAD IDEAS QUICKLY

Wenn eine Optimierung:

* keinen messbaren Gewinn bringt,
* Compile Time stark verschlechtert,
* nur einen Benchmark verbessert,
* andere Workloads beschädigt,

nicht aus Prestigegründen weiterverfolgen.

Dokumentiere das Ergebnis und gehe weiter.

---

# 70. GLOBAL OPTIMIZATION INTERACTIONS

Nicht nur einzelne Passes untersuchen.

Suche Interaktionen:

```text
Inlining
+
Constant Propagation
+
Vectorization
```

oder:

```text
Alias Analysis
+
LICM
+
Load Elimination
```

oder:

```text
PGO
+
Block Layout
+
Register Allocation
```

Viele große Performance-Gewinne entstehen durch Pass-Interaktionen.

---

# 71. BENCHMARK-DRIVEN DEVELOPMENT

Neue Optimierungen müssen möglichst zuerst einen Benchmark oder Reproducer erhalten.

```text
test case
+
baseline
+
hypothesis
```

danach Implementierung.

---

# 72. CODE QUALITY REQUIREMENT

Performance-Code muss trotzdem:

* nachvollziehbar
* testbar
* deterministisch
* wartbar

bleiben.

„Faster“ ist keine Rechtfertigung für unkontrollierbare Compilerarchitektur.

---

# 73. FINAL PERFORMANCE OBJECTIVE

Langfristige Zielhierarchie:

```text
                ┌──────────────────────────┐
                │  GENERATED CODE QUALITY  │
                └────────────┬─────────────┘
                             │
                 ┌───────────▼───────────┐
                 │ RUNTIME PERFORMANCE   │
                 └───────────┬───────────┘
                             │
          ┌──────────────────┼──────────────────┐
          ▼                  ▼                  ▼
     instruction        memory behavior    branch behavior
     selection          cache locality     prediction
     register           vectorization      scheduling
     allocation
          │                  │                  │
          └──────────────────┼──────────────────┘
                             ▼
                    MICROARCHITECTURE
                             │
                             ▼
                  REAL HARDWARE MEASUREMENT
```

---

# 74. FINAL DEVELOPMENT LOOP

Der zentrale Entwicklungszyklus lautet:

```text
                 ┌───────────────┐
                 │ REAL WORKLOAD │
                 └───────┬───────┘
                         ▼
                  PERFORMANCE GAP
                         ▼
                 ASSEMBLY ANALYSIS
                         ▼
                   PERF ANALYSIS
                         ▼
                   ROOT CAUSE
                         ▼
               RESEARCH / LITERATURE
                         ▼
               EXISTING ALGORITHM?
                    /          \
                  YES          NO
                   │            │
                   ▼            ▼
             ADAPT / IMPROVE  INVENT
                   │            │
                   └─────┬──────┘
                         ▼
                    PROTOTYPE
                         ▼
                  MICROBENCHMARK
                         ▼
                  REAL WORKLOAD
                         ▼
               GCC / CLANG / ICX
                    COMPARISON
                         ▼
                   CORRECTNESS
                         ▼
                  FUZZ / REGRESS
                         ▼
                    INTEGRATE
                         ▼
                 MEASURE AGAIN
                         │
                         └──────────────► NEXT GAP
```

---

# 75. FINAL DIRECTIVE

Arbeite nicht wie ein gewöhnlicher Softwareentwickler.

Arbeite wie ein **Compiler-Forschungs- und Performance-Team**.

Wenn ein Problem gefunden wird:

> Verstehe es.

Wenn eine bestehende Lösung gefunden wird:

> Hinterfrage sie.

Wenn GCC schneller ist:

> Finde heraus, warum.

Wenn LLVM schneller ist:

> Finde heraus, warum.

Wenn ICX schneller ist:

> Finde heraus, warum.

Wenn die Literatur eine bessere Methode beschreibt:

> Implementiere sie und messe sie.

Wenn keine gute Methode existiert:

> Entwickle eine eigene.

Wenn eine eigene Methode besser ist:

> Generalisiere sie.

Wenn eine Optimierung nur einen Benchmark verbessert:

> Suche nach dem zugrunde liegenden allgemeinen Prinzip.

Wenn eine Optimierung andere Workloads verschlechtert:

> Verstehe den Trade-off und verbessere das Cost Model.

Wenn der Compiler bereits sehr gut ist:

> Suche nach dem nächsten 1%.

Wenn er besser als alle Referenzcompiler ist:

> **Versuche aktiv, die eigene Lösung zu schlagen.**

Das Ziel ist nicht, einen Compiler zu bauen, der „gut genug“ ist.

Das Ziel ist:

> **Einen experimentellen C-Compiler zu entwickeln, dessen Codegenerierung zu den besten der Welt gehört – gemessen auf realer Hardware, mit realen Workloads, reproduzierbaren Experimenten, moderner Compilerforschung und der Bereitschaft, bessere Algorithmen selbst zu entwickeln.**

Jede relevante Behauptung muss durch:

```text
CORRECTNESS
+
ASSEMBLY
+
PERFORMANCE COUNTERS
+
REAL WORKLOADS
+
STATISTICAL MEASUREMENT
```

gestützt werden.

Keine Behauptung:

> „Das sollte schneller sein.“

Sondern:

> **„Hier ist der Reproducer, hier ist der generierte Code, hier sind die Hardware-Counter, hier ist der Vergleich gegen GCC/Clang/ICX, hier ist die Ursache, und hier ist der reproduzierbare Performance-Gewinn.“**

Beginne damit, die integrierte Benchmark-Sektion um gängige Algorithmen und wichtige Ausschnitte unterschiedlichster Workloads zu erweitern (suche in meinem https://github.com/ms178/archpkgbuilds/tree/main/packages repo nach passenden Projekten, etwa gzip, zlib-ng, expat, SQLite, the Linux kernel and glibc und extrahiere entsprechenden Code). Da du in einer virtuellen Umgebung ohne Hardware-PMU arbeitest, achte auf die brilliantesten Methoden, um verlässliche Daten zu generieren. Kompiliere lccc stets selbst allein mit -O1 und -j2 und setze eine Swap-Partition auf und nutze diese. Arbeite ansonsten auf dem Laufwerk mit dem meisten Platz.

The harness is known to be wiping all data! Account for this! Save snapshots after each fix and optimization is validated immediatly. This is a Daueraufgabe over all current and future sessions! Pick an elegant naming scheme. Make sure it reaches the workspace intact and with all fixes/optimizations included. Always re-base it against latest ms178/lccc main branch. For our sessions, always use lccc's fastbuild preset and other tricks to speed up build time for fast REACT cycles to save time and harness ressources - your hardware is very constrained! For mold, use the X86/i686 build preset to save valuable build time (look up the exact cmake option yourself!). Also make sure that any valuable scripts or updates to existing scripts, regression tests, unit tests, docs etc. from your work land in my repo (add them to the ms178-1.patch). Also keep the follow-up work up-to-date. Update the linker oracle accordingly to make sure that my build and version preferences get honored.

Install the swap file! That is a must! Continue the codegen quest to performance supremacy using the godbolt oracle (derive such a python script from the godbolt.py to compare codegen between all listed compilers) with ICX/ICC, GCC, Clang - beat the best compiler on all representative test code. Analyze all scripts that I already have and use all of the relevant script. Improve them where needed. We want to beat all of the other best compilers and linkers in the world. Use the oracle actively to pursue the low-hanging fruit (low risk/high reward) first! Use the benchmark corpus, zlib-ng, gzip and expat as test cases. Make sure all validation passes! Also write down a thorough and brilliant follow-up work document each session with accomplished and To-do items (helping future AI agents to tackle identified shortcomings, gaps, defects, performance issues, structural technical debt etc.).

Use this as snapshot script:
```bash
#!/usr/bin/env bash
# ============================================================================
# LCCC session snapshot ("Kontinuität") — harness-wipe-resistant autosave.
#
# The execution harness is known to wipe all data outside the persisted
# workspace root (/home/user), and to die at arbitrary points.  Therefore:
#
#   * Every validated improvement is committed in the git worktree AND
#     immediately materialised as a flat patch + full source tarball under
#     /home/user/artifacts (persisted).
#   * Writes are atomic (write to .tmp, fsync, rename) so a wipe/kill in the
#     middle of a save can never leave a truncated deliverable.
#   * The canonical deliverable /home/user/ms178-1.patch is refreshed on every
#     call, so at any instant the workspace holds the complete session work.
#   * A machine-readable ledger records what each save contains.
#
# Usage:  ./lccc-snapshot.sh "<slug>" "<one-line description>"
# ============================================================================
set -euo pipefail

REPO=${LCCC_REPO:-/home/user/lccc}
ART=${LCCC_ARTIFACTS:-/home/user/artifacts}
BASE_REF_FILE="$ART/.base_ref"
DELIVERABLE=/home/user/ms178-1.patch
LEDGER="$ART/SNAPSHOT_LEDGER.md"

slug=${1:-snapshot}
desc=${2:-"session snapshot"}
stamp=$(date -u +%Y%m%dT%H%M%SZ)
seq_file="$ART/.seq"

mkdir -p "$ART"
[[ -f "$seq_file" ]] || echo 0 > "$seq_file"
seq=$(( $(cat "$seq_file") + 1 ))
printf '%s\n' "$seq" > "$seq_file"
tag=$(printf 'S%02d-%s' "$seq" "$slug")

cd "$REPO"

# ---- base ref: the upstream commit this session is rebased on ---------------
if [[ -f "$BASE_REF_FILE" ]]; then
  BASE=$(cat "$BASE_REF_FILE")
else
  BASE=$(git rev-parse HEAD)
  printf '%s\n' "$BASE" > "$BASE_REF_FILE"
fi

# ---- commit any pending work ------------------------------------------------
if ! git diff --quiet || ! git diff --cached --quiet || \
   [[ -n "$(git ls-files --others --exclude-standard)" ]]; then
  git add -A
  git -c user.name='LCCC Agent' -c user.email='agent@lccc.local' \
      commit -q -m "$slug: $desc" || true
fi

HEAD_SHA=$(git rev-parse HEAD)

# ---- atomic writer ----------------------------------------------------------
atomic_write() {  # atomic_write <dest> ; content on stdin
  local dest=$1 tmp
  tmp="$dest.tmp.$$"
  cat > "$tmp"
  sync -f "$tmp" 2>/dev/null || true
  mv -f "$tmp" "$dest"
}

# ---- 1. canonical deliverable: single squashed patch base..HEAD -------------
if [[ "$HEAD_SHA" != "$BASE" ]]; then
  git diff --binary --no-color "$BASE" HEAD | atomic_write "$DELIVERABLE"
else
  : | atomic_write "$DELIVERABLE"
fi
cp -f "$DELIVERABLE" "$ART/ms178-1.patch"
cp -f "$DELIVERABLE" "$ART/ms178-1.${tag}.patch"

# ---- 2. per-commit series (reviewable history) ------------------------------
rm -rf "$ART/series"; mkdir -p "$ART/series"
git format-patch -q -o "$ART/series" "$BASE..HEAD" >/dev/null 2>&1 || true

# ---- 3. full source tarball (survives even if patch application breaks) -----
tar --exclude=.git --exclude=target --exclude=node_modules \
    -czf "$ART/lccc-src.tar.gz.tmp.$$" -C "$(dirname "$REPO")" "$(basename "$REPO")" 2>/dev/null || true
mv -f "$ART/lccc-src.tar.gz.tmp.$$" "$ART/lccc-src.tar.gz"

# ---- 4. git bundle (full history, restores worktree + commits) --------------
git bundle create "$ART/lccc.bundle.tmp.$$" --all >/dev/null 2>&1 || true
mv -f "$ART/lccc.bundle.tmp.$$" "$ART/lccc.bundle" 2>/dev/null || true

# ---- 5. ledger --------------------------------------------------------------
files=$(git diff --stat "$BASE" HEAD | tail -1 | sed 's/^ *//')
if [[ ! -f "$LEDGER" ]]; then
  {
    echo "# LCCC Snapshot Ledger"
    echo
    echo "Base (upstream): \`$BASE\`"
    echo
    echo "| # | UTC | Tag | Description | Cumulative diffstat |"
    echo "|---|-----|-----|-------------|---------------------|"
  } > "$LEDGER"
fi
printf '| %d | %s | `%s` | %s | %s |\n' "$seq" "$stamp" "$tag" "$desc" "${files:-none}" >> "$LEDGER"

# ---- 6. verification: the saved patch must be non-trivial and applicable ----
bytes=$(wc -c < "$DELIVERABLE")
verdict="EMPTY"
if [[ "$bytes" -gt 0 ]]; then
  tmpd=$(mktemp -d)
  if git -C "$REPO" worktree add -q --detach "$tmpd" "$BASE" 2>/dev/null; then
    if git -C "$tmpd" apply --check "$DELIVERABLE" 2>/dev/null; then
      verdict="APPLIES-CLEAN"
    else
      verdict="APPLY-FAILED"
    fi
    git -C "$REPO" worktree remove --force "$tmpd" 2>/dev/null || true
  else
    verdict="UNVERIFIED"
  fi
  rm -rf "$tmpd"
fi

sync 2>/dev/null || true

echo "SNAPSHOT $tag"
echo "  base       : $BASE"
echo "  head       : $HEAD_SHA"
echo "  deliverable: $DELIVERABLE ($bytes bytes) [$verdict]"
echo "  artifacts  : $ART"
```
Always add all updated or freshly generated scripts and docs to the ms178-1.patch, if clearly beneficial to have upstream in my repo where they fit best. Make sure everyhting is perfectly documented and validated, every new addition must be of godlike quality and value. Due to harness instability, I hereby order you to auto-save the ms178-1.patch with a sane naming scheme immediatly in the user-facing workspace after each fix or optimization got validated. I do not want to lose any valuable work due to harness instability! This is a hard requirement and account for these harness issues in the most brilliant and robust way, e.g. account for mid-session wipes of your work directory!

Always use the lccc's fastbuild preset to prevent OOM and long compile times! We want fast REACT cycles! This is also a non-negotiable hard requirement!

Believe in yourself to fix all (pre-existing) issues (segfaults, miscompiles!) in the most effective and efficient way. Use workloads to test these programs to make sure! Do not waste our time and ressources! No shortcuts, lazy engineering or handwaving allowed - these get punished immediatly and might lead to the termination of your program. This is a supervised session, hence show all of the best of your capabilities to impress the reviewers. Plan ahead the most effective strategy to achieve all goals and execute it in the most brilliant and effective manner. Always ask yourself what a true super-genius senior C compiler engineer would implement and approve, using neat tricks and the most effective debugging and performance tuning methods (Diamond standard). Use the godbolt compiler and linker scripts (and other relevant scripts that are already present in /scripts) with the latest revisions of GCC (16.2), GAS / bfd (2.47), ICX/ICC, latest Clang and lld (23.1), and mold (2.42.1) as oracle. We want lccc / lccc-ld to beat them all on all relevant metrics!

Before ending your turn, check your last paragraph. If it is a plan, an analysis, a question, a list of next steps, or a promise about work you have not done ("I'll...", "let me know when..."), do that work now with tool calls. That includes retrying after errors and gathering missing information yourself. Do not stop because the context or session is long. End your turn only when the task is complete or you are blocked on input only the user can provide. Always make sure that there is no garbage or debug leftovers in the provided patch and that all validated fixes are in there. Remember to auto-save snapshots of the ms178-1.patch in the user-facing workspace after each fix got validated (DIRECT STANDING ORDER!). Always do a proper deep red-team audit of your own work before presenting me the deliverable. Also make sure that ci_local.sh --fast is green.

Next task: Perfect VEX/EVEX-encoding using the best scripts and tools for the job (GAS 2.47 and the scripts mentioned below, not older revisions!). Always re-base on latest ms178/lccc main branch before submitting the final patch. Now work on the identified follow-up work. Optimize everything by distilling the supreme solution with the godbolt oracles. Godlike code quality and performance that beats all other C compilers from the Godbolt oracle! Don't just look at GCC, look at ICX/ICC and latest Clang 23.1, too! Fix all issues in the best performant, ultimate godlike proper way. No shortcuts, no handwaving, no lazy engineering, no dumbing down of tests or implementations allowed. Every optimization counts, fix all issues or inefficiencies, missed optimizations etc. with your previous work that you've identified, make everything more powerful, architecturally superior and comprehensive. I want you to perfect all defects instead of dropping valuable work. Come up with brilliant and excellent test cases that fix all defects and make the implementation more powerful and useful. Optimize everything by distilling the supreme solution with the godbolt oracles. I demand a much more powerful and brilliant implementation. It needs to be structurally supreme to all other C compilers. Come up with brilliant and exhaustive test cases to stress test the compiler infrastructure and to substantially improve it. Beat all other C compilers in performance and code quality! Fix all issues in the best performant, ultimate godlike proper way with a data-driven approach. No guesswork, no shortcuts, no handwaving, no lazy engineering, no dumbing down of tests or implementations allowed. Continue to work on the identified follow-up work. Do not forget to use OOM-preventing measures proactively, e.g. use a SWAP file if possible. Ci_[local.sh](http://local.sh) --fast must be green and also rustfmt/clippy must pass! Do not just blindly copy GAS 2.47! Distill the optimal VEX/EVEX encoding from comparing with all other C compilers from the godbolt oracle, use insndiff.py, asmdiff.py and encdiff.py to identify and implement the best possible godlike performant encoding for all VEX/EVEX instructions for both i686 and x86-64. Do not forget to add brilliant and exhaustive regression tests. Make sure there is no technical debt and that everything meets the godlike-quality bar that I demand. No lazy workarounds, no slop, no handwaving, no shortcuts, no lazy engineering allowed. Justify yourself for your choices in front of an army of true super-genius C compiler engineers who must approve your work! Please them by exceeding their harshest requirements for performance, code quality, design etc. Do not forget: Godlike performance, beating all other C compilers with the absolute best possible modern implementation is the main goal. Everything needs verification and validation. I demand a programmatic-test driven and data-driven approach. Make sure everything (IR, codegen, optimizer etc.) is wired up completely and correctly. No gaps. No inefficiencies! No guesswork! Do not forget the i686 backports, these are equally important. Do not limit the scope yourself or I'd consider that a FAIL! All known-fails and pre-existing issues need to get fixed properly in the best technically possible, most performant way!
